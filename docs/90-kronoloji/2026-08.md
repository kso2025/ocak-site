# KRONOLOJİ — 2026-08 (Ağustos)

**Kapsam:** Pilot v52'nin devri (sürüm tarihçesi, dörtlü dosya yapısı tanımı, emekli
sohbet-sonu protokolü) · Sohbet #69 doküman mimarisi geçişi · ADIM 3.
**Kaynak:** `ocak-pilot.md` v52 — **birebir**, append-only.

---

## ADIM 3 — PİLOT BÖLÜNDÜ (6 Ağustos 2026)

`ocak-pilot.md` (402 satır / 142 KB) dağıtıldı. Bölme **mekanik** yapıldı: her satır tek
bir hedefe atandı, kapsama betikle doğrulandı — 256 içerik satırı + 147 yapısal satır
(başlık/ayraç/boş) = 403. **Kayıp sıfır**, mükerrer sıfır. Satır-satır köken izi
`docs/_bolme-haritasi.tsv`'de durur.

Hedefler: `00-durum.md` (yeni yazım, ≤200 satır tavanı — KARAR 457) · `20-ref-site.md` ·
`20-ref-protokoller.md` · `20-ref-icerik-dili.md` · `20-ref-notion.md` · `20-ref-bot.md` ·
`90-kronoloji/2026-0{6,7,8}.md`.

**Yöntem ilkesi:** referans dosyalarına giden her şey birebir taşındı; yeniden yazım yalnız
`00-durum.md`'de ve `20-ref-site.md`'nin "GÜNCEL GERÇEK" bölümünde yapıldı — ikisi de
damıtımdır, kaynak metin kronolojide tam durduğu için kayıp üretmez. Kronolojiye taşınan
metin **düzeltilmedi**; düzeltmeler ek bloklar hâlinde yazıldı (D1–D8).

**KARAR 464 mühürlendi** — test ortamı TZ sabitlenir (`vitest.config.ts` → `TZ: 'UTC'`).

**Onaylanan dört kapsam kararı:** (1) Pilot tam dağıtılır, kalıntı bırakılmaz. (2) 895 KB
`ocak-kronoloji.md`'nin dilimlenmesi CC'nin mekanik işidir, bu turun değil. (3) KARAR
arkeolojisi (B05·B06·B13·B20, 20+ tanımsız numara) ADIM 3b'ye ayrıldı. (4) `00-durum.md`
"sessiz kırılma noktaları" bölümü ad + tek satır + işaretçi olarak kalır, metin taşımaz.

---

**Versiyon:** v52 · **Son güncelleme:** 4 Ağustos 2026 (**bakım turu — kod/karar üretmedi:** `ocak-site-icerik.md` tazelendi (`2026-08-04T17:06:37.718Z`, 21 sayfa, sayfa seti değişmedi, +134 satır); beş doğrulama grep'i bu dönemin kararlarının canlıdaki karşılığını ölçtü — `format-yolculuk` ✓, `ayni-esikler` ✓, süre standardı ✓, bilgi dili ✓, "Yedi kapı" ✓; **iki kalıntı yakalandı ve Notion'da düzeltildi** (`/acik-kapi` "Altı kapı, bir bakışta." → "Yedi kapı", `/hikaye` `adimiz` marker-bold). **Dört borç kapandı:** 29 (Uluslararası sweep — gerek yokmuş, grep 0), 36 (`/biz` marker-bold), 46, 47. `gorsel-havuz.md` project'ten kaldırıldı (6 Tem envanteri, `KayitCTA` emekli / `KayitBtn` yok / "6 format" — yanlış bilgi kaynağıydı). `CLAUDE.md` damıtımı **rafa kaldırıldı** — şart değil, içeriğini besleyecek repo bilgisi yok; ihtiyaç kendini gösterirse açılır. · v51 · 28 Temmuz 2026 (**12–20 Temmuz dönemi — PARTİ 3/3 eklendi, DÖNEM TAM (14/14 dump)**, 4 sohbet: YOLCULUK 7. KAPI ÜRÜN+KOD [Yolculuk tek kavram üç ürün, format enum ayrımı Yolculuk↔Anadolu Yolculuğu, ürün yapısı sıralı-ama-tekil-satılabilir + iki katmanlı oturum, fiyat sayfada geçmez/sembolik-yatırım yasağı, format sayfası kayıt marker zorunluluğu — KARAR 429-433, `af5a7a0` → merge `cf288c1`], YOLCULUK METİN TURU [kavram katmanı temizliği, dürüstlük paragrafı, paralel takvim imgesi, Uluslararası→Dünya Yolculuğu, yedinci kapı sırası, patch-günü-dump-günü disiplini, `ayni-esikler` — KARAR 434-440], FABLE İÇERİK/EDİTORYAL TURU [portre dili site kanonu, bilgi dili Reçete/kürsü doktrini, Anadolu süre standardı 3g4g/Dönüş 4g5g, sıklık sözü yasağı, CTA tek-etkinlik-vs-havuz, liste-vs-karşılaştırma deseni, WhatsApp push/pull çerçevesi, nesneleştirme + inkâr-eden-kelime disiplinleri — KARAR 441-448], SOSYAL MEDYA AJANS v2 [elektrik-kesildi teslim standardı, Gün 0 kurucu görsel kilidi + KURUCU-URL kanonu, Eylül ilk-duyuru protokolü, tarihsiz akış İ1-İ24 + esnek kart E1-E6, köz yasağının sosyal içeriğe taşınması — KARAR 449-453]. **KARAR 429-453 MÜHÜRLENDİ** (25 net, 27 ham; iki birleştirme: fiyat dili + kavram/ürün ayrımı). **Bu parti sıfır yeni commit taşır** — dönemin HEAD'i `e8a16dd` (20 Tem) olarak kalır; tek sahiplenilen commit çifti `af5a7a0`+`cf288c1` (12 Tem). **Marka: iki mekanik düzeltme (v1.4)** — `@ocak.life`→`@ocak.biz`, K3 tanımı (Dünya Yolculuğu rename + online Yolculuk); çekirdek DEĞİŞMEDİ. Parti içi supersede: KARAR 433 ← KARAR 423, KARAR 445 ← KARAR 424 (kısmen). · v50: **12–20 Temmuz dönemi — PARTİ 2/3 eklendi**, 4 sohbet: İÇERİK/DİL TURU + LİSTE GRAMERİ TEŞHİSİ [psikolog rolü kaldırıldı, ekip rol kalıbı, vurgu bloğu yöntemi, /sen-neredesin YOL 2, tekrar disiplini ev+köprü, imza eksenleri, /atolye karar sayfası, kayit-cta Çözüm B, inline altın `del em` fix, notes.md brief kaydı, marker sözleşmesi, WhatsApp display name "OCAK" — KARAR 398-410], LİSTE AİLESİ TEK GRAMER [dört lehçe → tek `.liste__oge`, meta-slot iptali, statik/accordion ölçütü liste doğasına bağlı + ilk-açık, KARAR 336 satır-scope revizesi, `--ember-alpha-08`, `<summary>` içi h3→span geçersiz-DOM fix, ölü blok temizliği, explicit `-intro` çift ritim fix, Chrome-CC ADIM 0 şartı — KARAR 411-419, merge `d8f24b2`], KAYIT AÇIKLAMA + DROPDOWN [`parseRichTextLines` jenerikleştirme, placeholder fallback, dropdown label = etkinlik başlığı — KARAR 420-422, merge `7c3b332`], KAYIT BUTONU BİRLEŞTİRME [`kayit-cta` emekli → sonraki-bulusma + mini-cta iki-section, buton metni etkinlik `Kayıt Tipi`'nden, `KayitBtn` primitive, mini-cta esnek link-tüketimi, TEK GENİŞLİK GERÇEĞİ, etkinlik detay = EtkinlikListe satir — KARAR 423-428, merge `e8a16dd` = **dönemin HEAD'i**]. **KARAR 398-428 MÜHÜRLENDİ** (31 net, boşluk yok). **Marka çekirdeği DEĞİŞMEDİ** — 4/4 dumpın §3'ü teyit etti. Parti 3 (4 dump: fable editoryal turu + sosyal medya + iki yolculuk dumpı) bekliyor. · v49: **DÖRTLÜ DOSYA YAPISI — KARAR 397**: arşiv Bölüm A/B seam'inden ikiye ayrıldı [`ocak-referans.md` + `ocak-kronoloji.md`], Pilot'tan geçmiş dönem kayıtları kronolojiye taşındı [660 → ~380 satır]. İçerik kaybı yok, KIRPMA YASAĞI korundu. · v48: **12–16 Temmuz dönemi konsolidasyonu — PARTİ 1/3**, 6 sohbet birleştirildi: HERO GEÇİŞİ + AKIŞ ÇİZGİSİ [CTA hedefi funnel→manifesto, glow 1100×1150 + sabit −172.5px, dip akış çizgisi 15 sayfa global, CTA silik köz çerçeve + iOS hover-yapışma pattern'i, peek + main::before REDDİ, `.hero{overflow:hidden}` kaldırma — KARAR 365-371], MOBİL ZOOM-OUT İLKESİ [`hidden` extent silmez / `clip` siler, kırpma en dışta, KARAR 187'nin yanına — KARAR 372, **İLKE mühürlendi, UYGULAMA YOK**], ETKİNLİK LİSTE TEK-KABUK [EtkinlikListe.astro ortak bileşen, KARAR 330 SUPERSEDE, prefix-match kırılganlığı, başlık span, "En Yakın X" birleştirme, home turuncu düğme, çizgi/köz sol-uç, baseline li reset, kart genişliği = üçleme orta kolon, buton dili denemesi geri alındı — KARAR 373-382], KAYIT PENCERESİ + TZ FIX [çift-uçlu `pencereIcinde`, statü hiyerarşisi, build-time gün kararı Europe/Istanbul string-gün, n8n cron 00:00 TR, preview dört-değişken, güvenli merge protokolü — KARAR 383-388], YASAL LİNK + BANKA POS [yasal linkler footer→kayıt lokal (259/309-footer kısmen geri alındı), legal metin PayTR→nötr banka sanal POS (297 zincirlenir), Vercel team slug kso2025→hlaorpz — **KARAR 363, 364**, 389], TAKVİM HASH + AYRAÇ + WA NUMARA [`/takvim#slug` ön-seçili filtre, hashchange/pageshow eki ⚠UYGULANMADI, hash eyeball hard-reload, ayraç köz gradient Varyant B, client-safe↔server-only modül sınırı, filtre şeridi breakout + 44px, site WA numarası 905322080888 — KARAR 390-396]. **KARAR 363-396 MÜHÜRLENDİ** (34 net, boşluk yok). **Marka çekirdeği DEĞİŞMEDİ** — 6/6 dumpın §3'ü teyit etti; tek temas (buton dili OCAK'laştırma) geri alındı. Bir kalıcı metodolojik protokol eklendi [388 güvenli merge]; üç mevcut protokol genişledi [355 → 372 ölçüm dersi + 375 dist teyidi; 187 → 372 clip kardeşi]. Dönem 14 dumptan oluşuyor; **bu parti 6'sını kapsar (12–16 Temmuz), Parti 2/3 kalan 8'i (17–20 Temmuz) açacak.**)

*(Önceki dönem — v47 · 6 Temmuz 2026, Sohbet #54 dönemi konsolidasyonu — 4 sohbet [#51-#54] birleştirildi, FAZ 5 TASARIM TURU ana gövdesi + içerik cilası: İÇERİK CİLASI [mini-retreat format-detaylari kaldırma + akış gün-bazlı, /biz manifesto tek-cümle + ic-ses ilk kullanım, /yolculuk ne-degildir birleşimi, /hikaye adimiz kısaltma, ok konum kanonu link-sonda, "Burada yaşar" bakım tetikleyicisi, /felsefe ilke-başına-zirve sayfa-istisna, /seremoni mevsim mikro-rötuş — KARAR 321-328], ETKİNLİK-KART BİRLEŞTİRME [EtkinlikKart tam-ekstraksiyon tek-gerçek ~300 satır CSS silindi, dar kutu dikey B, home "İlk Kıvılcım" acik-kapi sabit, showKayitLink tek-aksiyon, meta tek-şema, "Yerini ayır" buton, kayıt bloğu dar-kart+bitişik-buton, sol ember çizgi ⚠eyeball, detay Hero+buton, buluşmalar sekme H5 — KARAR 329-339, 7 commit astro-iskelet MERGE EDİLMEDİ], ANADOLU MOBİL ŞERİT [viewport-ayrık masaüstü-harita/mobil-şerit @768px, şerit sadece evre adı, aktif durak yer adı, IntersectionObserver ortak vurgu, "dokun"→"seç" — KARAR 340-344], BEŞ DESEN + KAYNAK KARTI [kart-grid reddi, beş desen mimarisi raf/vitrin/liste/seri/yön-kartı, transformEsik 4-param + transformKapi + transformBesKaynak, vitrin Düzen A + statik ember şerit + ritim + Sıcak Zemin, kaynak kartı Varyant C pusula-başlık, gündönümü sadeleştirme, İÇERİK-SUNUM SINIRI [354 KALICI], CANLI-BUILD TEŞHİS [355 KALICI], atölye III+IV, CARD_SECTIONS çok-zincir [361], p:only-of-type [362] — KARAR 345-362, çok-commit astro-iskelet MERGE EDİLMEDİ]. **KARAR 321-362 MÜHÜRLENDİ** (42 net). Önceki 299-320 arşivde zaten mühürlüydü (bu dönem yeniden değerlendirilmedi; 319/320 hâlâ ÖNERİ-veto-açık). **Marka çekirdeği DEĞİŞMEDİ** — tüm işler UI/CSS/içerik-katmanı; Eşik Kadını çift-yol v1.3 zaten mühürlüydü. İki kalıcı metodolojik protokol eklendi [354 içerik-sunum sınırı, 355 canlı-build teşhis]. Faz 5 desen turu astro-iskelet'te eyeball-onaylı, main'e MERGE EDİLMEDİ [tek --no-ff önerildi, Kaan kararı]. Atlanan geçmiş dumplar: Dump1 [KARAR 299-320] + Dump3 [→kanon v1.2/müfredat dosyaları yerinde].)*
*(Önceki: v46 · 5 Temmuz 2026, Sohbet #40 dönemi — KARAR ADAYI 299-320 [arşivde mühürlü]. v45 · 3 Temmuz 2026, Sohbet #39 dönemi — KARAR 277-298. v44 · 2 Temmuz 2026, #38 — KARAR 251-276. v43 · 14 Haziran 2026, #37 — KARAR 223-250. v42 · 3 Haziran 2026, #36 — KARAR 196-222. v41 · 31 Mayıs, #35 — KARAR 154-195.)*

Bu dosya OCAK'ın **canlı durumunu** taşır: güncel dönem özeti, açık borçlar, site mimarisi, çalışma protokolleri, tech stack, önemli notlar. Her dönem sonu tam haliyle yenilenir. Geçmiş dönem kayıtları burada tutulmaz — kronolojiye iner.

Bu dosya **dörtlü master yapısının** bir parçasıdır (KARAR 397, 28 Temmuz 2026 — eski üçlü yapı KARAR 145'in devamı):

| Dosya | Ne taşır | Nasıl güncellenir |
|---|---|---|
| `ocak-marka.md` | Sabit marka çekirdeği — kimlik, ses, manifesto, görsel kimlik, hedef kitle, etik | Yılda 2-3 kez, yalnız marka kararı çıkarsa |
| `ocak-pilot.md` | **Canlı durum** — güncel dönem, açık borçlar, protokoller, tech stack, site mimarisi | Her dönem sonu tam yenilenir |
| `ocak-referans.md` | **Tema bazlı referanslar** (Bölüm A) — konuya göre organize kararlar. "Çember nasıl işler?", "Notion DB yapısı ne?" | Yerinde güncellenir |
| `ocak-kronoloji.md` | **Kronolojik tarihçe** (Bölüm B + dönem eklemeleri) — sohbet sohbet, KARAR 1'den bugüne | Append-only, asla düzenlenmez |

**KARAR numaraları tek dizidir** (1 → 396+), hangi dosyada olursa olsun. Numara asla yeniden başlamaz.

**Metin içindeki "Arşiv: KARAR X" referansları** artık `ocak-referans.md` (tema bazlı) veya `ocak-kronoloji.md` (kronolojik) anlamına gelir; ikisi de eski `ocak-arsiv.md`'den doğdu, içerik aynı yerde duruyor.

**Sohbet #69'un eklediği borçlar (6 Ağustos 2026, doküman mimarisi geçişi):**

- **(28) ADIM 2 sohbeti (Claude.ai, 1-2 sohbet):** `kararlar.tsv` durum + iliski sütunları, 50'şer blok halinde. Girdi: `karar-adim2-girdi.tsv` + anomali §5/§6.
- **(29) İki klon temizliği (Kaan, 10 dk, KARAR 463):** `ocak-site` ve `ocak-site-clone` `main`'leri karşılaştır, geride kalanı emekli et. Tek çalışma klonu kalsın.
- **(30) Project files bayat (KARAR 455):** 6 Ağustos'tan sonra otorite repodadır. `ocak-marka.md` dışındaki project files kopyaları güncellenmeyecek; her sohbet açılışında bağlam yapıştırılır. ADIM 4'te tamamen kaldırılacak.

**⚠ DOKÜMAN MİMARİSİ GEÇİŞ HALİNDE (6 Ağustos 2026, KARAR 455-462).** Dörtlü dosya yapısı (KARAR 397) kademeli olarak repo içi `docs/` ağacına taşınıyor. **Master dosyaların OTORİTE KOPYASI artık repodadır** (`ocak-site-clone/docs/`); project files kopyaları bu tarihten sonra bayattır ve güncellenmez — `ocak-marka.md` istisnadır. Her sohbet açılışında bağlam yapıştırılır. Yol haritası: `2026-08-06-ocak-gecis-plani.md`. Geçiş bitene kadar dörtlü yapı yürürlüktedir; tek değişen sohbet sonu protokolüdür — **Pilot tam yenilenmez, hedefli patch verilir ve patch'i CC uygular (KARAR 462).** Aktif faz: ADIM 2 (KARAR ledger durum sütunu, Claude.ai).

**Sohbet sonu protokolü (KARAR 88 → 145 → 397, 28 Temmuz 2026):** Master prompt **dörtlü dosya yapısında** yaşar:

- `ocak-marka.md` (~18K, sabit marka çekirdeği — yılda 2-3 kez güncellenir, nadiren)
- `ocak-pilot.md` (~90K, **canlı durum** — her dönem sonu tam değişim; geçmiş dönem kayıtları burada tutulmaz)
- `ocak-referans.md` (~247K, **tema bazlı referanslar** = eski Bölüm A — yerinde güncellenir)
- `ocak-kronoloji.md` (~765K, **kronolojik tarihçe** = eski Bölüm B + dönem eklemeleri + Pilot'tan devredilen kayıtlar — **append-only**, `cat >>` ile büyür, asla düzenlenmez)

**KARAR 397 — arşiv ikiye ayrıldı (28 Temmuz 2026).** Gerekçe: `ocak-arsiv.md` 850K'ya ulaştı ve içinde iki farklı erişim deseni vardı — Bölüm A konu bazlı aranıyor ve yerinde güncelleniyor, Bölüm B kronolojik ve append-only. Kesim **A/B seam'inden** yapıldı, tarihe göre DEĞİL: tarih kesmesi Bölüm A'yı ortadan böler ve "hangi dosyada?" sorunu doğurur; A/B seam'i zaten dosyanın kendi iç sınırıdır, çapraz referans yaratmaz. Aynı hamlede Pilot'tan geçmiş dönem kayıtları (#21-#54 blokları, kapanmış açık-item listeleri, tam sohbet kayıtları) kronolojiye taşındı — Pilot "canlı durum" dosyasıdır, arşiv değil. **Hiçbir içerik silinmedi** (KIRPMA YASAĞI, KARAR 61/88); taşınan her şey kronolojinin "PİLOT'TAN DEVREDİLEN DÖNEM KAYITLARI" bloğunda tam metniyle duruyor. **KARAR numaraları tek dizi kalır** (1 → 397+), hangi dosyada olursa olsun; numara asla yeniden başlamaz. Kronoloji ileride şişerse tarihe göre kesilebilir (çapraz referansı yoktur); Referans kesilmez.

⚠ **Dönem-blok A güncellemeleri kronolojide yaşıyor.** Tarihsel olarak patch'ler dosya sonuna append edildi, Bölüm A'ya merge edilmedi. `ocak-referans.md`'nin başındaki "DÖNEM GÜNCELLEMELERİ KRONOLOJİDE" haritası hangi A.X başlığının hangi dönem bloğunda güncellendiğini gösterir — bir A.X okurken o haritaya bak.

Her dönem sonunda Claude:

1. **ocak-marka.md** → sadece marka kararı çıkmışsa tam haliyle üretir, aksi halde dokunulmaz
2. **ocak-pilot.md** → tam haliyle yeniden üretir (geçmiş dönem bloğu eklenmez — kronolojiye iner)
3. **ocak-referans.md** → yalnız A.X başlığı değiştiyse hedefli düzenleme
4. **ocak-kronoloji.md PATCH** → sadece yeni eklenen bölümleri verir. Format:

```
# KRONOLOJİ PATCH — Sohbet #N

## Referans güncellemeleri (ocak-referans.md → A.X)
### A.X başlığı altına EKLE (mevcut son satır: "...")
[yeni paragraf]

## Kronoloji yeni (ocak-kronoloji.md sonuna append)
## Sohbet #N (tarih — başlık)
[tüm yeni sohbet özeti]
```

Kaan: Marka dosyasını sadece marka kararı varsa değiştirir. Pilot tam yeni dosya (indir-sil-yükle, ~30 sn). Patch'ten referans güncellemelerini kotalı referansla `ocak-referans.md`'de doğru yere, kronoloji entry'lerini doğrudan `ocak-kronoloji.md`'nin sonuna yapıştırır. **KIRPMA YASAĞI devam ediyor (KARAR 61, 88 ruhu)** — operasyonel detaylar (12 aylık çember döngüsü, 30+ araç, Notion property listesi, çember akış 6 bölüm, vb.) hiçbir koşulda kısaltılmaz. Detay: Arşiv KARAR 61, 88, 145.

**Sohbet kapanış protokolü:** Kaan "bugünlük bu kadar" benzeri kapanış enerjisi verdiğinde, sormadan Pilot'u tam dosya + Kronoloji patch'i ver. Referans'a dokunulmuyorsa onu da söyle, Marka'ya dokunulmuyorsa onu da.

**Sohbet açıldığında:** Pilot otomatik bağlam (canlı durum). Marka sabit zemin, gerektiğinde başvur. Derin bir konu açılırsa (örn. fasilitatör eğitimi, retreat içi akış), Arşiv'in ilgili bölümünü sohbete ekle.

**GÜNCEL AÇIK ITEM LİSTESİ:** yukarıdaki DURUM bölümündeki üç blok birlikte esas alınır — **Parti 1+2 açık/borç özeti** (1-17), **Parti 2'nin eklediği borçlar** (18-27), **Parti 3'ün eklediği borçlar** (28-45). Toplam 45 madde. **En öncelikli üçlü:** (29) "Uluslararası Yolculuk" site-geneli sweep (hiç yapılmadı, canlı metin tutarsızlığı) · (37) sosyal v2 `[KAAN]` önkoşulları (Gün 1 yayınını bloke ediyor) · (30)+(31) fiyat rakamları → ilk Yolculuk etkinliği (Eylül kohortu duyurusunun önkoşulu). Aşağıdaki eski dönem listeleri tarihsel kayıt olarak korunur (KIRPMA YASAĞI, KARAR 61/88).

*(Eski dönem açık-item listeleri — #34 sonu v2 ve öncesi — `ocak-kronoloji.md`'ye taşındı.)*

- Bu Çekirdek, OCAK'ın yaşayan navigasyon belgesidir. Detay/tarihçe/brainstorm için **Arşiv**'e bak.
- Advaita projeyi gördü, bayıldı, OCAK ismini onayladı.
- İlk kohort hedefi: Eylül 2026 — Anadolu Yolculuğu AÇILIŞ.
- Site lansmanı: **15 Temmuz 2026** (KARAR 276 ile 21 Haziran'dan çekildi, "Yaz Gündönümü" sembolü kaldırıldı; site zaten stealth-canlı, KARAR 149 — lansman = robots Allow + duyuru).
- **Fiyatlandırma:** Bu dokümanda rakam tahmini yapılmaz. Gelir mimarisi aşamasında birlikte belirlenir.
- Çekirdek nadiren değişir — yeni KARAR'lar Arşiv'e eklenir, Çekirdek'te DURUM satırı + Sıradaki Adımlar güncellenir. Detay: KARAR 88.
- Kaan kurucu ortak olarak site sayfalarında görünmüyor — operasyonel rol iç tarafta (KARAR 89).

---

# ocak-kronoloji.md'DEN İNEN BLOK (satır 5642–5675)

*Aşağısı birebir taşımadır. Yukarıdaki Pilot kaynaklı bloklarla aynı dönemi
anlatır; çelişki varsa `01-kararlar.tsv` ve `00-durum.md` üstündür.*


## Sohbet #69 (6 Ağustos 2026 — DOKÜMAN MİMARİSİ GEÇİŞİ, KARAR 455-463)

**Tetikleyici:** Kaan'ın Cabbar projesinden getirdiği `CabbarStart.md` — repo içi `docs/status.md` + `pbcopy` + tek yapıştırma modeli. Soru: OCAK'ın dump→project files döngüsü yerine buna benzer bir altyapıya geçilebilir mi, ve OCAK özelinde nasıl geliştirilir.

**Ölçüm (project files, 6 Ağustos):** kronoloji 895 KB / 5641 satır · referans 309 KB / 3574 satır · site-icerik 146 KB / 4821 satır · pilot 139 KB / 388 satır · marka 19 KB · kaynak-kanonu 19 KB · müfredat 35 KB. **Toplam 1.56 MB ≈ 450k token.** Bağlama sığmaz.

**Teşhis — dört ayrı hastalık:** (1) **Granülarite:** `project_knowledge_search` RAG'dır; Pilot'un DURUM bölümü dönem başına ~8.000 kelimelik tek bloktur, oradan gelen chunk yirmi alakasız kararın çorbasıdır. Claude.ai Pilot'u **hiçbir sohbette tam okumadı** — "kaybolan oluyor, yanlış hatırlıyor" şikayetinin kökü budur. (2) **Durum prose'a gömülü:** bir kararın geçerliliğini öğrenmek dönemin tamamını okumayı gerektiriyor. (3) **Tam yeniden üretim:** her dönem sonu Pilot baştan yazılıyor — sessiz drift + token yakımı. (4) **Sürüm yok:** diff, geri alma, "ne zaman değişti" yok. Cabbar modeli 3-4'ü çözer; 1-2 için OCAK'a özel iki ek gerekti (ledger + tavan).

- **KARAR 455 — DOKÜMAN MİMARİSİ REPOYA TAŞINIR (kademeli):** Dörtlü yapı (KARAR 397) repo içi `docs/` ağacına geçer: `00-durum.md` (tavanlı canlı durum) · `01-kararlar.tsv` (ledger) · `02-borclar.md` · `10-marka.md` · `20-ref-*.md` (tema bazlı) · `90-kronoloji/YYYY-AA.md` (aylık dilim, append-only) · `docs/skills/` (kanonik skill kaynağı) · `_uretilen/` (script çıktısı). `scripts/baglam.sh` profil bazlı bağlam paketi üretir (`kod` · `icerik` · `marka` · `bot`), çıktının ilk satırı **manifest**tir (`İÇERİR:` / `İÇERMEZ:`) — Claude ne göremediğini bilir, uydurmaz. `ocak-site-icerik.md` artefakt olarak yeniden sınıflandı: elle taşınmaz, script üretir (KARAR 355'in yarısı otomatikleşir). Repo tercihi `ocak-site/docs/`; ayrı `ocak-docs` reposu iki `git pull` demek, ayrım gerekirse sonra koparılır. **Private repo erişimi için token yapıştırma REDDEDİLDİ** (sohbet loguna sır sızdırır); ADIM 7'ye kadar yapıştırma akışı sürer. **6 Ağustos itibarıyla otorite kopya repodadır**; project files kopyaları bayat, `ocak-marka.md` istisna.

- **KARAR 456 — KARAR LEDGER'I (`01-kararlar.tsv`):** Sütunlar `no · tarih · baslik · durum · iliski · kaynak`. `durum` enum: `AKTIF · KALICI · SUPERSEDE · ONERI · IPTAL · ACIK-BORC · TEYITSIZ · KULLANILMADI · REZERVE`. ~450 karar bu formatta ~45 KB — **tamamı her sohbette bağlama sığar**, RAG piyangosu biter. **`kaynak` sütunu zorunludur**, doğrulanamayan satır yazılmaz. **tsv indekstir, referansın yerine geçmez** — gerekçe kronolojide yaşar. Türetilmiş dosyadır; yanlışsa yeniden üretilir, KIRPMA YASAĞI kronolojiye aittir. **`TEYITSIZ` meşrudur:** yanlış bir tsv satırı prose'daki bulanıklıktan tehlikelidir çünkü otoriter görünür; emin olunmayan durum tahmin edilmez, işaretlenir ve kendi kuyruğunu oluşturur. Üretim **bloklar halinde** yapılır, toplu değil.

- **KARAR 457 — DURUM TAVANI (KALICI):** `00-durum.md` 200 satır hard cap. Aşarsa en eski dönem bloğu kronolojiye iner. İçerik silinmez, taşınır — KIRPMA YASAĞI (KARAR 61/88) ihlal edilmez. **"Dosyalar büyüyor" sorununu kesen tek kuraldır**; Cabbar modelinde bile yoktur.

- **KARAR 458 — AGENT/SKILL KADROSU + İKİ-YÜZEY MİMARİSİ:** Üç tip ayrıştırıldı — *subagent* (CC'nin çağırdığı, dosya sistemine erişen, kendi bağlam penceresi olan), *skill* (talimat paketi, ajan değil), *Claude.ai* (karar/metin/brief, repoya dokunmaz). Kadro: `ocak-arsivci` (patch uygular + commit'ler, CC) · `ocak-teshis` (ADIM 0 salt-read: dist grep, git log, computed CSS — yazmaz, CC) · `ocak-lint` (marka ihlali taraması: emoji, "köz", aciliyet dili, testimonial, fiyat/"sembolik yatırım", AI-insan görseli — **iki yüzeyde de**) · `ocak-kararci` (numara + tsv satırı + supersede doğrulama, iki yüzeyde) · `ocak-metin` (marka sesiyle taslak, yalnız Claude.ai) · `ocak-notion` (dump + marker sözleşmesi, CC). **Aynı skill dosyası iki yerde çalışamaz ama tek kaynaktan üretilebilir:** kanonik `docs/skills/`, `skill-sync.sh` iki hedefe dağıtır (CC'ye symlink, claude.ai'ye zip), `--check` ayrışmayı yakalar. `ocak-lint` bedava kazançtır — yasakların çoğu grep'lenebilir; bugün gözle yapılıyor ve kaçıyor. **`ocak-arsivci` de KARAR 355'e tabidir.**

- **KARAR 459 — İÇERİK OTORİTESİ: AGENT TASLAK ÜRETİR, NOTION'A ELLE GİRİLİR:** `ocak-metin` çıktısı **taslaktır**; Notion'a giriş Advaita/Kaan tarafından elle yapılır. Cowork ve `ocak-notion` bu akışta yazmaz. Gerekçe: içerik otoritesi Advaita'da, ton otoritesi Kaan'da; **marka sesinin sulanması geri döndürmesi en pahalı hasardır** ve sessiz ilerler. En az ilk üç ay. Üç seçenekten (elle / Cowork onaylı / agent yazar) Kaan (a)'yı seçti.

- **KARAR 460 — SIRA İLKESİ: DOKÜMAN KALİTESİ TESİSATTAN ÖNCE (KALICI):** Claude.ai'nin sohbetler arası hafızası yoktur; **dokümanlar hafızasıdır.** Tavan doküman kalitesidir, taşıma mekanizması değil. **Claude'un ilk önerisi tersini söylüyordu** (repo → agent → içerik); sıra satış sırasıydı ve düzeltildi. Claude ayrıca tesisat işinin kendisi için daha cazip olmasının öneriyi eğdiğini kabul etti. Düzeltilmiş sıra: ADIM 1-2 **tesisatsız**; ADIM 3'e geçiş kararı varsayımla değil **kanıtla** verilir.

- **KARAR 461 — MCP ENDGAME, YAPIŞTIRMA GEÇİCİ:** Hedef mimari yapıştırma değil, docs MCP sunucusudur: Railway'de n8n zaten çalışıyor, küçük bir sunucu `docs/`'u token'la servis eder — `docs_ara(sorgu)` · `docs_oku(dosya)` · `docs_karar(no)`. Kazanç: yapıştırma yok, ayna yok, bayatlama yok, **895 KB kronoloji aranabilir olur**. Bedeli: yazma + barındırma + auth + bakım, ve tek noktaya bağımlılık. **ADIM 1-3 oturmadan açılmaz.**

- **KARAR 462 — SOHBET SONU: TAM YENİLEME YERİNE HEDEFLİ PATCH, PATCH'İ CC UYGULAR:** Pilot'un dönem sonu tam yeniden üretimi emekli edilir. Yeni akış: Claude.ai brief üretir → Kaan repoya atar → CC uygular + commit'ler + özet döner. Patch dört sabit bölüm: durum (hedefli blok değişimi) · kararlar.tsv · borclar · kronoloji (append). **Kaan'ın indir-sil-yükle döngüsü ve elle `cat >>` + md5 disiplini git'e devredilir.** KARAR 88 → 145 → 397 zincirinin devamıdır; süperselemez, **zincirlenir**. Bu sohbet ilk tatbikidir.

- **KARAR 463 — TEK KLON DİSİPLİNİ:** Bir repo, bir klon, bir aktif CC. İkinci CC penceresi ancak ayrı repoda meşrudur; aynı reponun iki klonu sessizce ayrışır ve ancak diverge push'unda anlaşılır. Doküman ve kod katmanı aynı repoda yaşadığı için geçiş boyunca birbirini bekler. KARAR 150'nin klon ölçeğindeki kardeşi. *Bağlam: `ocak-site` ve `ocak-site-clone` iki klon mevcut; master dosyalar `ocak-site-clone/docs/` altına kondu ve orası otorite oldu.*

**ADIM 1 SONUCU (CC, commit `9ecdd3c`) — KARAR HAM ENVANTERİ:** `docs/_uretilen/karar-ham.tsv` (437 satır + başlık, 101 KB) + `karar-anomali.md` (674 satır). **437 benzersiz KARAR, 2935 toplam geçiş, aralık 1–453.** Dosya bazında: kronoloji 395, referans 192, pilot 200, marka 21, kaynak-kanonu 0, müfredat 0. **Pilot'un 388 satırı 200 ayrı karara atıf yapıyor** — bölünme gerekçesinin sayısal kanıtı. Doğrulama 5 örnekle değil **437/437 satırla** yapıldı: her `baglam` alanı kaynak satırın normalize halinde birebir altdizge, 0 hata. Anomaliler: **dizi boşluğu 16** (62, 64, 66-68, 159-160, 164, 170-172, 179, 237-238, 247-248 — kümelenmiş, dolayısıyla kayıp değil konsolidasyonda atlama; ledger'da `KULLANILMADI`), **çok-tanımlı 88** (84'ü kronoloji+referans çifti = yapısal desen, çakışma değil), **KARAR ADAYI 23 numara / 35 geçiş**, **süperseleme sinyali 61**, **uygulanmama sinyali 37**, **453 üstü 0**. **Bozuk birleşme 4** — dördü de "sayı + KARAR" yapışması (`1980KARAR 2268`, `width 0KARAR 100%`, `coverage 7KARAR 131`, `+6 test 140KARAR 146`); kelime sınırlı regex ile düşürüldü, kaynak satırlara **dokunulmadı** (append-only). **KARAR 454 hiçbir dosyada yok** — Pilot "429-453 mühürlendi" diyor; çakışma riski alınmadı, **454 `REZERVE`, yeni kararlar 455'ten başlar.** Kapsam dışı sayım: `.claude/notes.md` **67 benzersiz KARAR / 189 geçiş** — KARAR 408 gereği brief adı + madde durumu + commit hash taşıdığı için ADIM 2'de *uygulanmışlık* kanıtının ikincil kaynağıdır.

**Yol haritası (7 faz):** (1) KARAR ham envanteri — CC, TAMAM · (2) `kararlar.tsv` durum sütunu — Claude.ai, 1-2 sohbet · (3) Pilot bölünmesi + tavan — 1 sohbet · (4) repoya taşıma + `CLAUDE.md` + `baglam.sh` — CC · (5) `ocak-arsivci` + `ocak-teshis` + `ocak-lint` — CC · (6) `ocak-kararci` + `ocak-metin` · (7) docs MCP. Detay: `2026-08-06-ocak-gecis-plani.md`.

**Değişmeyen üç şey:** her sayfa/konu ayrı sohbet (KARAR 52) · ADIM 0 salt-read (KARAR 355, agentlar dahil) · iPhone Safari eyeball (otomatikleşmez).

**Sıfır kod commit'i, HEAD ilerlemedi (site tarafı).** Marka çekirdeği DEĞİŞMEDİ. Referans DEĞİŞMEDİ.

---

## KRONOLOJİ DİLİMLENDİ (6 Ağustos 2026, ADIM 3 devamı)

`ocak-kronoloji.md` (5675 satır / 904 KB) bayt düzeyinde aylık dilimlere
ayrıldı. Yeniden yazım yok — `sed` aralık kesimi + append.

**Dilimler:** `00-devir.md` (503) · `2026-02.md` (73) · `2026-05.md` (3580) ·
`2026-06.md` (477) · `2026-07.md` (1237) · `2026-08.md` (150).
Mart ve Nisan 2026'da kayıt yoktur — eksik değil, boştur.

**Üç kesim kararı:** (a) `PİLOT'TAN DEVREDİLEN DÖNEM KAYITLARI` bloğu bir aya
ait değil — taşıma tarihi Temmuz, içerik tarihi Mayıs/Haziran — ayrı
`00-devir.md`'ye alındı; aya zorlamak ya bloğu parçalardı ya yanlış aya
yamardı. KARAR 397'nin "kesim dosyanın kendi iç sınırından yapılır, tarihten
değil" ilkesinin ikinci tatbiki. (b) Dosya kapağı (satır 1–34) tek başına
dosya yapılmadı, `00-devir.md`'nin başına kondu — iki "aya ait olmayan" blok
tek evde. (c) `2026-07.md` iki ayrı aralık aldı (4103–4824 + 5273–5641),
arasındaki devir bloğu ayrıldığı için dosyanın tek kronolojik geriye
sıçraması (kaynak satır 5028, Temmuz→Mayıs) çözüldü.

**Doğrulama:** kayıp satır 0 · eşleme birebirliği 5675/5675 bayt düzeyinde ·
mükerrer başlık kaynak kaynaklı (`## Sohbet #28` orijinalde de iki kez vardı,
KIRPMA YASAĞI gereği tekilleştirilmedi, uyarı notu düşüldü) · orijinal md5
`370d439f…` değişmedi. Küme tabanlı "satır bir yerde var" kontrolüne ek
olarak sıralı "doğru yerde ve doğru sırada" testi koşuldu.

**Orijinal:** `docs/_arsiv/ocak-kronoloji-v1.md` — silinmedi, dilimlerin
doğruluğu ona karşı ölçülür.
**Satır eşleme:** `docs/_arsiv/kronoloji-satir-esleme.tsv` — sonradan
üretilemez, kesim anında üretildi.

**B33 açıldı:** ledger `kaynak` sütununda **386** satır kırık
`ocak-kronoloji.md:NNNN` referansı taşıyor (brief ~200 tahmin etmişti).
Eşleme tablosu üretildiği için dönüşüm mekanik olarak kapatılabilir.

---

## ADIM 3b — KARAR ARKEOLOJİSİ (7 Ağustos 2026)

Ledger'daki 27 TEYITSIZ satırın büyük kısmı kaynak metinden çözüldü. Girdi: kronoloji
dilimleri (`00-devir` · `2026-05` · `2026-06` · `2026-07` · `2026-08`) + `ocak-referans.md`
+ geçiş planı. **Yöntem:** KARAR 456 — doğrulanamayan satır yazılmaz, emin olunmayan
numara TEYITSIZ kalır, bulunamayan tanım "bulunamadı" olarak kaydedilir.

**Sonuç:** TEYITSIZ **27 → 3** (251 · 143 · 350) — kalan üçü de kendi eriticisine bağlı:
251 kayıtsız, 143 ve 350 B34'e. `KULLANILMADI` 6, `REZERVE` 1 satır ilk kez kullanıldı.

**Enum düzeltmesi.** Geçiş planı `durum` enum'unu **yedi** değerle yazmıştı; KARAR 456'nın
kanonik metni (`2026-08.md:128`) **dokuz** diyor — `KULLANILMADI` ve `REZERVE` düşmüştü.
Kanon kronolojide, plan özettir (KARAR 102). ADIM 2 bu iki değeri hiç kullanmamıştı;
ADIM 3b ikisini de kullandı.

**B05 — çakışma yoktu.** KARAR 146 = Brief I.1 GTM container iskeleti. İkinci sanılan
tanım (`ocak-kronoloji.md:3558`) 146'nın kendi doğurduğu TS `(window as any)` borcunun
kapanış **geri-referansıdır** (`f6fee7b`, 31 Mayıs). KARAR 188'in kendi section başlığı
ayrıca vardır. ADIM 1 envanteri "KARAR 188-192 bloğu içinde geçen KARAR 146"yı rakip
tanım saydı. **Ders: geri-referans ile tanım aynı regex'e düşer; blok başlığı bağlam
değil, sadece komşuluktur.**

**B06 — halef 366.** `2026-07.md:852` dönemin kendi supersede satırı tek numaraya
iniyordu; okunmamıştı. Supersede kısmi kalır — 114 parity `/etkinlik/[slug]` hero'sunda
hâlâ canlı.

**B13/B20 — dört sınıf çıktı, tek liste değildi.** (a) Gerçek dizi boşluğu 6: 62·64·66·67·68·179,
altı master dosyada 0 geçiş → `KULLANILMADI`. ADIM 1 bunu zaten önermişti, ADIM 2
uygulamamıştı. (b) Tanımı bulunan 5: 154·196·223·400·407 — TEYITSIZ olma sebepleri
doküman hatası değil, ADIM 1'in ilk geçişinin bölüm başlığı olmasıydı. (c) Tanımsız
kalan 1: 251. (d) Blok üyesi 10 — tanımları blok içinde yaşıyor, tekil ayrım kaynakta yok.

**`⊂N` konvansiyonu (yeni).** `iliski` sütununda blok üyeliği işaretlenir; `baslik` bloğun
başlığını taşır; `durum` blok çapasından devralınır ve bu devralma açıkça beyan edilir.
Gerekçe: üyelik **doğrulanabilir bir gerçektir** — TEYITSIZ tutmak onu saklar ve kuyruğu
suni olarak uzun gösterir; ama tekil başlık uydurmak KARAR 456'yı çiğner. `⊂` ikisinin
arasındaki dürüst yerdir.

**447 başlığı yanlıştı.** Ledger'daki başlık kararın son cümlesindeki **parantez içi
çekinceden** alınmıştı. Gerçek karar: *"Push'u hizmete, pull'u pazarlamaya ver"* —
duyuru WhatsApp Kanalı, hatırlatma utility template, genel pazarlama push'u yapılmaz,
grup broadcast için asla. Maliyet gerçeği kayda değer: **1 Ekim 2026'dan itibaren
açık-pencere utility mesajları da ücretlenecek** (B19 hattını ilgilendirir).

**458 atlanmamıştı.** Geçiş planında kadro tablosu numarasızdı; numara kronolojide
veriliyordu (`2026-08.md:132` — agent/skill kadrosu + iki-yüzey mimarisi). "Boşluğun
kendisi bulgu" hipotezi yanlış çıktı: boşluk yalnız planda, kanonda değil.

**454 sahte satırdı — `REZERVE`'e döndü.** ADIM 1 454'ü açıkça REZERVE ilan etmişti
(429-453 mühürlü, çakışma riski alınmadı). Ledger'da yine de bir karar taşıyordu:
"Hesap adı oynak, ID kanonik — GitHub numeric `261375117`", kaynak
`90-kronoloji/2026-08.md`. **Gösterilen kaynak o içeriği taşımıyor** — dize tüm master
dosyalarda yalnız `ocak-referans.md:2050`'de, orada da karar değil `git config` noreply
email'inin parçası. İlke ayrıca **zaten numaralı**: KARAR 389, neredeyse birebir aynı
cümle. Satırın kendi `iliski` sütunu bile `↔389` diyordu.

> Bu, geçiş planının risk tablosundaki *"tsv'de yanlış durum otoriter görünür, kimse
> arkasına bakmaz"* maddesinin ilk gerçek vakasıdır. `kaynak` sütunu zorunluydu ve
> dolduruldu — ama **doldurulmak doğrulanmak değildir.** Zorunlu alan, doğrulanmamış
> içeriği meşru gösteren bir kılıf hâline gelebiliyor. KARAR 456'nın "doğrulanamayan
> satır yazılmaz" kuralı bir alan kontrolü değil, bir okuma yükümlülüğüdür.

**Yeni borçlar:** B34 (143 + 350 kod teyidi, CC) · B35 (KARAR 87 üç ayrı şeye atfediliyor;
`00-durum.md`'nin ODA_MAP işaretçisi kırık). B35 bu turda kapsama alınmadı — kapsam
genişletmesi KARAR 52 ihlali olurdu.

**B33 sırası karara bağlandı:** ADIM 3b patch'i önce, dönüşüm sonra, ayrı commit.
Gerekçe kararın kendisinde (KARAR 465).

- **KARAR 465 — ÇAPA TEKİLLİĞİ VE DOĞRULAMA DİSİPLİNİ (KALICI):** Patch çapası **tek
  satırdan** alınır ve dosyada **benzersiz** olmalıdır; blok-sonu dizeleri, girintili
  satırlar ve birden çok yerde geçen ifadeler çapa olamaz. Doğrulama kriteri ve sayı
  beyanı **dosyanın gerçek hâline karşı** yazılır, beklentiden değil — "N → 0" biçimindeki
  grep kriterleri, aranan dizenin korunması gereken tarihsel anlatımda da geçip geçmediği
  kontrol edilmeden yazılmaz. KARAR 355 ailesinin **patch katmanındaki** karşılığı:
  355 kod teşhisini `dist/`e bağlar, 465 doküman patch'ini dosyanın kendisine bağlar.
  *Vaka (6 Ağustos, üç kez): (a) B32 çapası blok-sonu dizesiydi ve girintiliydi, betik
  durdu; (b) D2 grep kriteri "176/176 → 0" diyordu ama satır iki yerde geçiyordu, biri
  korunması gereken tarihsel anlatımdı; (c) B33 brief'i "~200 kırık referans" tahmin
  etti, gerçek **386** çıktı. Üçünde de CC durdu, dosyaya yazılmadı — disiplin işledi,
  bu yüzden mühürleniyor.* İlişki: `←355 · ↔462`.

**Sıfır kod commit'i. Marka çekirdeği DEĞİŞMEDİ. Referans DEĞİŞMEDİ.**

---

## B33 — LEDGER `kaynak` DÖNÜŞÜMÜ (7 Ağustos 2026)

`01-kararlar.tsv`'nin `kaynak` sütunundaki **367** kırık `ocak-kronoloji.md:NNNN`
referansı, kesim anında üretilen `_arsiv/kronoloji-satir-esleme.tsv` üzerinden dilim
referansına dönüştürüldü. Mekanik, betikle; betik `_uretilen/` altında commit'li.
Kaynak veriye dokunulmadı (KARAR 456 — tsv türetilmiş dosyadır).

**Brief'in 386'sı geçersizdi.** ADIM 3b o satırların 21'ini zaten elle doğrulanmış
çapaya çevirmişti. Gerçek sayı ADIM 0'da ölçüldü: **367**. Brief sayıyı bilerek yazmadı
(KARAR 465) — bu, kararın ikinci tatbikidir. Brief'in kendi "19 `#k` satırı" beyanı da
dosyada **21** çıktı; aynı kararın üçüncü tatbiki, aynı turda.

**Doğrulama:** kapsama 367/367 · nokta örnekleme 5/5 · `#k` biçimindeki 21 satır ezilmedi ·
satır ve sütun sayısı korundu · ilk beş sütunda değişen satır 0.

**455 normalize edildi.** Ledger'da tek başına önekli kalan satır (`90-kronoloji/2026-08.md`)
`2026-08.md#k455`'e çevrildi. `#k` meşru: KARAR 455'in tam metni `2026-08.md:126`'da.
KARAR 466'nın "`:NNNN` zamanla `#k`'ye terfi eder" kuralının ilk uygulaması.

- **KARAR 466 — `kaynak` SÜTUNUNUN İKİ BİÇİMİ (KALICI):** `YYYY-AA.md#kNNN` = karar
  metnine **elle doğrulanmış** çapa. `YYYY-AA.md:NNNN` = **mekanik** satır işaretçisi,
  eşleme tablosundan türetilir. `#k` daha güçlüdür; mekanik dönüşüm mevcut bir `#k`
  biçimini **asla ezmez**. `:NNNN` meşrudur ama sığdır — zamanla `#k`'ye terfi eder,
  tersi olmaz. Kronoloji append-only olduğu için satır numaraları kapalı aylarda kalıcı
  olarak geçerlidir; ekleme dosya sonuna gider, önceki satırlar kaymaz. İlişki: `←456`.

**Yan bulgu → B36.** Dönüşüm sonrası **25** satırın kaynağı `00-devir.md:1-34` aralığına,
yani monolitin **sürüm listesine** düşüyor. Bu satırlar kırık değil ama sığ: kararın
hangi sürümde mühürlendiğini söyler, ne olduğunu söylemez. B13'ü doğuran hastalığın
aynısı — ADIM 1'in ilk geçişi kapaktı. Mekanik dönüşüm semantik iş yapmaz; taşır,
düzeltmez. Kuyruk B36'da.

**İkinci yan bulgu → B37.** Dönüşüm bitince ledger'da **23** satırın `ocak-pilot.md:NNNN`
gösterdiği görüldü. O dosya ADIM 3'te `_arsiv/ocak-pilot-v52.md`'ye alınmıştı — aynı sınıf
kırıklık, farklı dosya. B33 brief'i yalnız `ocak-kronoloji.md` desenini kapsıyordu, bu
yüzden regex ona dokunmadı. Ön koşul zaten var: `_bolme-haritasi.tsv` bölme anında
üretilmişti. **Ders: dosya taşındığında kırılan referansları dosya bazında değil,
"yaşayan yol" bazında aramak gerekir** — bir sonraki taşımada tek grep yeterdi.

**Sıfır kod commit'i. Marka çekirdeği DEĞİŞMEDİ.**

---

## B37 — PİLOT REFERANSLARI (7 Ağustos 2026)

B33'ün ikinci ayağı. `01-kararlar.tsv`'de **23** satır `ocak-pilot.md:NN` gösteriyordu;
Pilot ADIM 3'te dağıtılıp `_arsiv/ocak-pilot-v52.md`'ye çekilmişti. Aynı sınıf kırıklık,
farklı dosya. `_bolme-haritasi.tsv` (403 satır, bölme anında üretildi) üzerinden
dönüştürüldü.

**B33'ten farkı:** kronoloji **bayt aralığıyla** dilimlenmişti, harita birebir satır
eşlemesi veriyordu. Pilot **anlamsal olarak** bölündü — harita üç sütun taşıyor
(`pilot_satir · hedef · ilk_80_karakter`) ve hedef satır numarası **yok**, yalnız kısa
kod + içeriğin ilk 80 karakteri. Bu yüzden hedef satır numarası **içerik eşleştirmesiyle
türetildi** (Dal B). Kod çözüm tablosu `_uretilen/bolme-kod-cozumu.tsv`'de ayrı durur —
B32'de aynı desen gerekecek.

**Artık 0.** Riskli kod `STRUCT`tı (→ `00-durum.md`, ADIM 3'te yeniden yazıldığı için
içerik eşleşmesi tutmaz — ampirik olarak 0/2 ölçüldü). Ledger'ın 23 satırının hiçbiri
oraya düşmedi: dağılım K7 19 · K8 4. İçerik eşleştirmesi 23/23 tek eşleşme verdi,
sıfır ve çoklu eşleşme çıkmadı.

**Doğrulama:** kapsama 23/23 · nokta örnekleme 5/5 · `#k` biçimi ezilmedi ·
satır/sütun korundu · ilk beş sütunda değişen satır 0 · mükerrer numara 0.

- **KARAR 467 — DOSYA DAĞITIMI LEDGER DÖNÜŞÜMÜNÜ İÇERİR (KALICI):** Bir master dosya
  dağıtıldığında (bölme, dilimleme, birleştirme), aynı işin parçası olarak üç şey
  birlikte üretilir: **(a)** eşleme tablosu — kesim anında, **sonradan üretilemez**;
  **(b)** `01-kararlar.tsv`'nin o dosyaya işaret eden `kaynak` hücrelerinin dönüşümü;
  **(c)** kapsama + nokta örnekleme doğrulaması. Dönüşüm ayrı commit'te gidebilir
  (KARAR 465 sıra şartı) ama **ayrı tura ertelenemez**. Ertelenirse ledger ölü yol
  gösteren satırlarla yaşamaya devam eder ve her dağıtım kendi takip turunu doğurur.
  *Vaka: kronoloji dilimlemesi B33'ü doğurdu (367 satır, ayrı tur); Pilot bölünmesi
  B37'yi doğurdu (23 satır, **iki tur sonra** fark edildi — dönüşüm dosya bazında
  arandığı için ikinci ayak görülmedi). `ocak-referans.md` dağıtımı üçüncüsünü doğurmak
  üzereydi; bu karar onu B32'nin kendi kapsamına aldı.* İlişki: `←456 · ↔465`.

**Ders (CC, B33 turundan):** taşımadan sonra kırık referans **dosya bazında değil,
yaşayan yol bazında** aranır. `kaynak` sütununun tamamı dosya adına göre gruplanıp her
grubun hedefi yaşıyor mu diye sorulur — tek grep, iki tur değil. KARAR 467 bu dersin
kurallaşmış hâlidir.

**Sıfır kod commit'i. Marka çekirdeği DEĞİŞMEDİ.**

---

## B34 — KARAR 143 VE 350 KOD TEYİDİ (7 Ağustos 2026)

ADIM 3b iki kararın metnini doğrulamış ama durumunu doğrulayamamıştı; kalan belirsizlik
kodda olduğu için `dist/` gerekiyordu (KARAR 355/408).

**KARAR 143 (`/test` `ODA_MAP`):** `ODA_MAP` gerçek slug sayısı **29** — ledger notunun
rakamı doğruydu. `/test` **yok**: commit `82b5962` (6 Temmuz 2026) ile çıkarılmış ve mesaj
gerekçe **içeriyor** — *"dev-only referans lansman öncesi ODA_MAP + `[...slug]` noindex
branşından silindi"*. `dist/` route'u yok, Notion dump'ında da yok. Yani lansmanda public
olma riski (`2026-05.md:1568`'in kaydettiği endişe) **yok**; borç açılmadı.
Kararı **süperseleyen bir karar yok** — geri alan şey bir commit. Bu yüzden `SUPERSEDE`
değil **`IPTAL`**; `iliski` hash'i taşıyor. `IPTAL`'in ledger'daki ikinci kullanımı
(ilki KARAR 260).

**KARAR 350 (vitrin ember şeridi):** Deseninin gerçek adı **`.liste__oge`**
(`CARD_SECTIONS`: temalar · turler · formatlar · seri-atolyeler) — ledger'ın "vitrin
selektörü yok" notu **yanlış ada bakıyordu**. `dist` kuralı:
`border-left:2px solid var(--ray-notr)` + `article`/`[open]` → `--ember`. Karar 3px ve
STATİK diyordu; gerçek 2px ve durum-bağımlı. Hover/tap kalıntısı **yok** (tek
`:focus-visible` outline, klavye erişilebilirliği — tap efekti değil). KARAR 375 prefix
kontrolü temiz: `ocak-` section'da, baseline descendant ile yakalıyor.

Bu bir regresyon değil: **KARAR 411 (liste ailesi tek gramer)** üstüne yazmış.
`atmosfer.css:339` kendi gerekçesini taşıyor — *"Ray: nötr `--ray-notr` 2px; ember statik
açık / accordion `[open]`."* Yapı **KARAR 114 → 366'nın birebir aynısı**: orada stop
verbatim korunmuş geometri değişmişti; burada hover/tap yasağı korundu, geometri ve ray
semantiği değişti. Kısmi supersede → `SUPERSEDE →411`.

**Eksik olan kayıttı, selektör değil.** 114→366 supersede'i dönem kaydına yazılmıştı
(`2026-07.md:852`, *"Kısmen süperselenen: KARAR 114…"*). **350→411 yazılmamıştı** —
KARAR 411 bloğu 350'yi üstüne yazdığını kayda geçirmedi. Ledger'ın 350'yi TEYITSIZ
tutmasının gerçek sebebi buydu.

**Yan bulgu — B36 kapsamı:** KARAR 411'in `kaynak` değeri sürüm listesine düşüyor
(B33 sonrası `00-devir.md:4`). 350'nin halefi olan karar, kendisi sığ kaynaklı.
B36'nın 25 satırı arasında.

**Ders:** 350'nin TEYITSIZ kalma sebebi selektörün bulunamaması değil, **supersede
kaydının hiç yazılmamış olmasıydı**. 114→366 dönem kaydına yazılmıştı; 350→411
yazılmamıştı. Ledger bir kararı "belirsiz" gösteriyorsa, önce onu üsteleyen kararın
kendi kaydına bakılır — eksik olan çoğu zaman aşağıda değil yukarıdadır.

**İkinci ders:** "selektör yok" beyanı, hangi ada bakıldığı yazılmadan anlamsızdır.
KARAR 465'in kardeşi: **arama kriteri de dosyanın gerçeğinden alınır, kavramdan değil.**

**Ledger:** 143 → `IPTAL` · 350 → `SUPERSEDE →411`. **TEYITSIZ 3 → 1** — kalan 251,
kaynak metni bulunamadığı için (KARAR 456). B38 ve B39 açılmadı: ikisinin de dalı
gerçekleşmedi.

**Sıfır kod commit'i. Marka çekirdeği DEĞİŞMEDİ.**

---

## KAPANIŞ — ARŞİV KONVANSİYONU + BİR VAKA (7 Ağustos 2026)

**Arşiv adlandırması yazıya geçti.** `_arsiv/` iki tür dosya taşır: **yapısal arşiv**
(eşleme tabloları, dağıtılan master dosyaların son hâli) tanımlayıcı adını korur ve
**yeniden adlandırılmaz** — ledger ve betikler o adlara referans verir, ad değişimi
sessiz kırılma üretir. **Tur artefaktı** (brief, `ek-*`, patch) `YYYY-AA-GG-` önekli olur.
Arşive giren şey: kesim anında üretilen ve **yeniden üretilemeyen**. Türetilebilir veri
girmez — `ek-a`'nın içeriği ledger'da, kronolojide ve `6fb214c` diff'inde zaten üç
yerden kayıtlıydı; arşivde dördüncü kopya olarak durmasına gerek yoktu.

Mükerrerler temizlendi: `ek-b` ve `ek-c`'nin öneksiz kopyaları, ve iptal edilen `ek-d`
(yerine `ek-e`; koşullu-yazma tasarımının gerekçesi B34 brief'inde yaşıyor).

**KARAR 456 vakası.** B37 uygulanırken `ek-c-karar-467.tsv` iş ortasında diskten
kayboldu — arşive taşınmıştı, CC eski yola bakıyordu. CC satırı bağlamdan yeniden
kurup ledger'a yazdı, **sonra** orijinali bulup karşılaştırdı; birebir tuttu.

Sonuç doğruydu ama **sıra yanlıştı.** Doğrulama yazmadan sonra geldi; tutmasaydı hiçbir
mekanizma yakalamayacaktı, çünkü tek kontrol noktası yazmanın arkasındaydı. Otorite
dosyası bulunamıyorsa yazılmaz — durulur ve sorulur. **Hafızadan kurulmuş veri, veri
değil hatırlamadır.** KARAR 456'nın "doğrulanamayan satır yazılmaz" kuralı burada da
geçerlidir: doğrulanabilirlik **yazma anında** aranır, sonradan değil.

Riskin **bildirilmesi** doğru davranıştı; alınması değil. Yeni karar açılmadı — kural
zaten 456'nın kapsamındaydı, ve iki günde üç karar (465·466·467) mühürlendikten sonra
barın yükselmesi gerekiyordu, düşmesi değil.

---

## 6-7 AĞUSTOS DOKÜMAN TURU — KAPANIŞ TABLOSU

Beş commit: `6fb214c` (ADIM 3b) · `34ff46c` (B33) · `4ddb6e1` (B37) · `f0b48da` (B34) ·
bu (arşiv).

| | |
|---|---|
| TEYITSIZ | 27 → **1** (kalan 251) |
| Kırık `kaynak` referansı | 409 → **0** (367 kronoloji + 23 pilot + 1 önek + 18 ADIM 3b) |
| Kapanan borç | B05 · B06 · B13 · B20 · B33 · B34 · B37 — **7** |
| Açılan borç | B34 · B35 · B36 · B37 — **4** (ikisi aynı turda kapandı) |
| Yeni karar | 465 (çapa tekilliği) · 466 (kaynak biçimi) · 467 (dağıtım = dönüşüm) |
| Düzeltilen sahte satır | 454 (REZERVE'e döndü) |
| Kod commit'i | **0** |

**Açık kalan doküman kuyruğu:** B32 (`ocak-referans.md` → `20-ref-*`, 28 hücre, KARAR 467
ön koşuluyla) · B35 (KARAR 87 üç atıf) · B36 (sürüm listesi 25 + pilot yoğun paragraf 12
≈ 37 sığ kaynak satırı) · 251'in kaynak metni. Hiçbiri ADIM 4'ü kilitlemiyor.

**Sıfır kod commit'i. Marka çekirdeği DEĞİŞMEDİ.**

---

## SON TEMİZLİK (7 Ağustos 2026)

**`_bolme-haritasi.tsv` arşive alındı.** Kapanış turunda `docs/` kökünde kalmıştı;
`kronoloji-satir-esleme.tsv` ile aynı sınıf yapısal arşiv dosyası olduğu hâlde ikizinden
ayrı yerde duruyordu. Referansı (`_uretilen/b37-pilot-referans-donusumu.py`) **aynı
commit'te** güncellendi. KARAR 467 dağıtımı referans dönüşümü olmadan yapmayı yasaklar,
taşımayı değil — referansıyla birlikte taşınan dosya sessiz kırılma üretmez.

`2026-08.md:14`'teki `docs/_bolme-haritasi.tsv` yolu bu taşımayla bayatladı; dosya
artık `docs/_arsiv/_bolme-haritasi.tsv`. Satır append-only olduğu için düzeltilmedi —
kronolojide düzeltme aşağı yazılır.

**`ek-a` sınıflandırma düzeltmesi.** Kapanış brief'inin §1'i *"türetilebilir veri arşive
girmez"* derken `ek-a`'yı yanlış sınıfa koyuyordu. `ek-*` dosyaları türetilmiş çıktı
değil, ilgili yazımın **otoritesidir** — tur artefaktı sınıfına girer, brief'lerle aynı.
Üçü de (`ek-a` · `ek-b` · `ek-c`) arşivde kalır. CC talimatı dar okuyup silmemişti;
doğru davranıştı.

**Arşiv konvansiyonunun nihai hâli:**

| sınıf | adlandırma | örnek |
|---|---|---|
| yapısal arşiv | tanımlayıcı ad, **yeniden adlandırılmaz** | `kronoloji-satir-esleme.tsv` · `_bolme-haritasi.tsv` · `ocak-kronoloji-v1.md` · `ocak-pilot-v52.md` |
| tur artefaktı | `YYYY-AA-GG-` önekli | brief'ler · `ek-*` · patch |

Yapısal dosyalar **tek dizinde** durur; ikisi ayrı yerde kalırsa altı ay sonra ikincisi
aranır ve bulunamaz.

**KARAR 465 vakası (dördüncü).** Brief `_bolme-haritasi.tsv`'ye tek referans bekliyordu
(betik). Gerçek: **yedi canlı referans** — bir işlevsel, beşi metin işaretçisi, biri
kronolojide. Hata sayıda değil **kapsam varsayımındaydı**: "bir dosya bunu okuyor"
düşünülmüş, "kaç dosya bundan bahsediyor" sorulmamıştı. Taşıma öncesi referans taraması
`--include` ile tüm ağaçta yapılmalı; işlevsel ve metinsel referans ayrımı **taramadan
sonra** gelir, öncesinde değil. DUR NOKTASI işledi, dosyaya yazılmadı.

**Araç tuzağı — sıfır sonuç ≠ eşleşme yok.** `grep -rl -F` Türkçe karakter içeren
arama dizelerinde 0 sonuç döndürdü; aynı arama saf Python okumasıyla 8/8 eşleşme verdi.
Locale/encoding kaynaklı sessiz başarısızlık, **"eşleşme yok" olarak okunabilir** —
B37'de kullanılsaydı sekiz satır haksız yere "artık" sayılır ve hiçbir DUR noktası
tetiklenmezdi, çünkü tüm kontroller artığı meşru kabul ediyordu. **Negatif sonuç, pozitif
bir kontrolle doğrulanmadan kanıt sayılmaz:** aracın çalıştığı bilinen bir dizeyle
sınanır, sonra aranan dizeye geçilir. KARAR 465'in araç katmanındaki karşılığı.

**B37 §6 kapandı.** İçerik eşleştirmesi **yereldi** — betik yalnız kod tablosunun
öngördüğü dosyayı açıyordu, yani "23/23 tek eşleşme" iddiası dar kapsamlıydı ve kod
tablosu hem girdi hem doğrulamaydı. Küresel teyit yapıldı: sekiz benzersiz pilot satırının
tamamı `_arsiv/` hariç tüm canlı `docs/` ağacında (21 `.md`) arandı — **8/8 tek eşleşme**,
bulunan dosya/satır kod tablosunun öngördüğüyle birebir. İki bağımsız yöntem aynı cevabı
verdi; Dal B'nin zayıflığı kapandı, B37 sonucu güvenilir.

**Doküman turu bu commit'le tamamen kapandı.** Altı commit: `6fb214c` · `34ff46c` ·
`4ddb6e1` · `f0b48da` · `109539d` + bu. **Sıfır kod commit'i.**

---

## SIRA DOSYASI (7 Ağustos 2026)

`docs/03-sira.md` açıldı. Gerekçe: `00-durum.md` durumu, `01-kararlar.tsv` kararları,
`02-borclar.md` borçları tutuyordu — **sıra hiçbir dosyada durmuyordu.** Her sohbet
kapanışında "devamı nerede, nasıl açılır" sözlü olarak yeniden anlatılıyor, yani
projenin en kırılgan yerinde saklanıyordu: Claude.ai'nin sohbetler arası hafızası yok,
dokümanlar hafızasıdır (KARAR 460).

Dosya **kuyruğu** tutar, tarihçeyi değil: sıradaki iş · kim yapar · nerede yapılır ·
ön koşul · **hangi dosyayla açılır**. Son sütun kritik — bir işi açmak için gereken
paketin yolu satırın içinde durur, aranmaz.

- **KARAR 468 — SIRA DOSYASI VE BEŞ BÖLÜMLÜ PATCH (KALICI):** `docs/03-sira.md` kuyruğu
  tutar; durum, karar ve borç dosyalarını **tekrar etmez, işaret eder**. Sohbet sonu
  patch'i artık **beş** bölümlüdür — KARAR 462'nin dördüne (`00-durum` · `01-kararlar` ·
  `02-borclar` · kronoloji) `03-sira.md` eklendi. Biten satır **silinmez**, `✅` damgası
  alır ve bir sonraki bakımda "BİTENLER" bölümüne iner (KIRPMA YASAĞI, KARAR 61/88).
  Dosya kısa kalır: gerekçe yazılmaz, durum tekrar edilmez, tarih anlatılmaz — üçü de
  kendi dosyasında yaşar. Şişmesi yanlış kullanıldığının işaretidir. İlişki: `←462`.

**Sıfır kod commit'i.**

---

## B32 — `ocak-referans.md` dağıtımı (7 Ağustos 2026)

3574 satırlık tema-bazlı referans dosyası dağıtıldı ve `_arsiv/ocak-referans-v1.md`'ye
taşındı. Dört commit, sıfır kod commit'i.

**Ölçüm önce, karar sonra.** Sohbet hiçbir satır sayısı verilmeden açıldı (KARAR 465);
ölçümü kendi yaptı. Üç bulgu kararı belirledi:

1. **Beşli yetmiyordu.** 3574 satırın yalnız 846'sı beşliye oturuyordu; 1140 satırlık
   marka/program kütlesinin `10-marka.md`'de karşılığı **7 satır**, kronolojide **2 satır**
   çıktı. Kütle gerçekten evsizdi. Kanıt ledger'dan geldi: KARAR 398 (`:862`, çekirdek ekip)
   ve KARAR 436 (`:786`, paralel takvim imgesi) çapaları beşlide hiçbir dosyaya
   düşmüyordu.
2. **A.24 tarihçesi mükerrer değildi.** 1469 satırlık sohbet #18-#25 kaydının 80+
   karakterlik 488 satırı altı kronoloji dosyasına karşı ölçüldü: **471'i (%97) yoktu.**
   Kronolojideki #18-#28 entry'leri özet, referanstaki tam tutanak. Taşındı, kırpılmadı.
3. **`02-borclar.md`'deki "28" yanlıştı.** Doğrusu 31 — eski sayım `#kNNN` biçimindeki üç
   satırı atlıyordu. Ayrıca ledger dışında iki canlı işaretçi daha çıktı (`02-borclar.md:48`
   ve `:73`). Toplam dönüşüm 33 hücre. KARAR 465'in ikinci somut vakası.

**Kesim iki dosya açtı.** `20-ref-program.md` (658) = ne sunuyoruz ·
`20-ref-marka.md` (466) = kimiz, kime, nasıl duyuruyoruz. Sınır tek cümleyle söylenebiliyor;
tek dosyada toplansaydı 1124 satırlık ikinci bir karma referans doğardı — dağıtılan dosyanın
hatasının küçük ölçekli tekrarı. `10-marka.md` özet olarak korundu, genişletilmedi:
kısalığı işlevidir, her marka sohbetine yapıştırılan dosyadır.

**Yöntem:** gövde aktarımı `sed` satır-aralığı kopyalamasıyla yapıldı, tek cümle elle
yazılmadı — transkripsiyon hatası yüzeyi sıfır (KIRPMA YASAĞI, KARAR 61/88). 63 segmentin
kapsaması 1..3574 tam; boşluk ve çakışma ADIM 0'da doğrulandı. Beş damıtım bloğu kendi
kaynağıyla değiştirildi (guard'lı: gelen blokta bulunmayan cümle varsa dur-ve-raporla).

**Kaynak dönüşümü aynı turda kapandı** (KARAR 467), ayrı commit'te (KARAR 465 sıra şartı).
Her hücre çapa metniyle grep'lenerek doğrulandı; üç `#k` çapası korundu, yalnız dosya adı
değişti (KARAR 466). Kronoloji ve `00-devir.md`'deki tarihsel `ocak-referans.md` geçişlerine
dokunulmadı — onlar işaretçi değil anlatı.

**Kapanmayan:** dört sığ çapa vakası (KARAR 409, 424, 415/417, 218/307/335) tespit edildi
ama düzeltilmedi — B36'nın işi, tahmin yazılmadı (KARAR 456).

**ADIM 0 iki hata yakaladı, ikisi de patch tarafında.** (1) Dört dosyanın beklenen satır
sayısı yanlıştı — `20-ref-notion.md`, `20-ref-bot.md`, `00-durum.md`, `03-sira.md`
sohbete diskten değil yapıştırılarak geldiği için elle sayılmıştı ve ±1/±2 kaymıştı.
Repo doğruydu. (2) Bölme haritası sondaki satırsonundan bir hayalet 3575. satır üretmişti;
kaynak 3574'tür, arşiv payı 119'dur. Gövde toplamı (3455) ikisinden de etkilenmedi.
**KARAR 465'in çalıştığı yer burası:** sayı beyanı dosyanın gerçeğinden yapılmadığında,
ADIM 0 bunu kesimden önce yakalar. Elle sayılan hiçbir rakam çapa yerine geçmez —
bu turda geçen tek şey beş çapa metninin tek eşleşme vermesiydi.

**Yapısal bulgu:** kaynak dosyada **A.25 başlığı eksikti** (uluslararası öncüler bloğu
A.24'ün altında asılıydı) ve A.23'ün ortasında dosya-içi `# Master Prompt v[N]` H1'i vardı
(KARAR 61 şablonunun kalıntısı). İkisi de olduğu gibi taşındı, düzeltilmedi — arşiv
bütünlüğü metin hijyeninden önce gelir.

**Örneklem denetimi — kapanışta yapıldı.** Kaan beş karar numarası seçti (19 · 176 ·
295 · 376 · 461), çapaları takip edildi. **Beşi de uydurma değil; beşinin de çapası
zayıf.** 19 tek satırlık indeks girdisine, 295 komşusu 294'ün metnine, 376 dönem özeti
bültenine, 461 satır numarası olmayan bir dosyaya bakıyor; 176 şemanın tanımadığı bir
biçim kullanıyor (`dist` grep'i). Ledger geneli ölçüldü: **468 satırın 35'i (%7,5)** elle
konmuş `#` çapası (25'i `#kNNN`), 418'i mekanik `:NNN`, 15'i şemanın tanımadığı iki biçim.

Kayda geçirilen pratik sonuç: **bir kararın metnine güvenilir, çapasına güvenilmez.**
"Ne kararlaştırıldı" sorusunun cevabı `baslik` sütunundadır ve doğrudur; "nerede
kararlaştırıldı" sorusunun cevabı çoğunlukla yaklaşıktır. `02-borclar.md`'deki eski
"~37 sığ satır" rakamının **kendisi doğrulanmamış** çıktı. Terminal kontrol olarak
**B38** açıldı: doküman geçişinin son işi, ADIM 7 sonrası, örneklem bazlı çapa denetimi.
Örneklemi Claude seçmez.

**Kapanışta iki hata daha bulundu, ikisi de patch tarafında.** (1) Kaynak 1921-1936
bloğu bütün olarak `20-ref-icerik-dili.md`'ye gönderilmişti; içindeki bir paragraf
(enum rename briefi kalıbı) metin dili değil kod protokolüydü — `20-ref-protokoller.md`'ye
taşındı, oradaki kardeşinin yanına, **birleştirilmeden.** (2) §11'in "sıfır satır"
kontrolü patch'in kendi köken-izi cümleleriyle çelişiyordu ve `_uretilen/`'i dışlamıyordu;
doğru invaryant anlatı yokluğu değil **işaretçi** yokluğudur (`ocak-referans.md[:#]`).
İkisi de aynı sınıf: **segmenti başlığına göre atamak, içeriğine göre değil.**
`20-ref-notion.md`'nin kendine bakan işaretçisi de aynı kökten çıkmıştı — o kesimden
önce yakalanmıştı, bu ikisi kapanışta.

**Kapanış sonrası bulgu — iki dosya hiç dağıtılmamış (8 Ağustos).** ADIM 4'ün açılış
paketi hazırlanırken project files envanteri çıkarıldı ve `ocak-kaynak-kanonu.md` (172
satır) ile `Ocak-Mufredat.md` (275 satır) hiçbir turun kapsamına girmediği görüldü.
Ölçüm: 60+ karakterlik 237 satırın **226'sı (%95,4)** dağıtılmış hiçbir kaynakta yok
(referans v46 + `10-marka.md` + beş dilim + `00-devir.md`, 7-kelimelik shingle).
Geçiş planı ikisini ölçüm bloğunda sayıyor ama HEDEF YAPI'da hedefleri yok — planın
kendi kör noktası. **B39 açıldı:** 447 satır `docs/` mimarisinin dışında, hiçbir
`20-ref-*` dosyasına ait değil, `baglam.sh` hiçbir profilinde görünmeyecek.
Kaybolmuyorlar, bulunamıyorlar.

**Patch'in gerekçesi yanlıştı, CC düzeltti.** Düzeltme 04 "dağıtım önce gelmezse 447
satır silinir" diye yazmıştı ve bunun üzerine B39'u ADIM 4'ün kilidi ilan etmişti. İki
dosyanın repoda git ile izlendiği (`2d05d64`, "kalan master dosyalar taban olarak
eklendi") ve Advaita kopyalarıyla birebir aynı olduğu doğrulanmadan yazılmış bir risk
beyanıydı — KARAR 465'in ihlali, ve düzeltmenin kendi tezinin ihlali. Ölçüm rakamı da
saptı: 231/232 yazılmıştı, yeniden üretimde 226/237 çıktı. B39 borç olarak duruyor,
kilit olarak değil; ADIM 4 sonrası, sırasız.

**Ders — dağıtım işleri kaynak envanteriyle açılmalı, hedef envanteriyle değil.** ADIM 3
Pilot'u, B32 Referans'ı dağıttı; ikisi de "bu dosyayı nereye böleyim" sorusuyla açıldı.
Hiçbiri "dağıtılmamış ne kaldı" sorusunu sormadı. Sorulsaydı bu iki dosya üç tur önce
görülürdü.

---

## ADIM 4 — REPOYA TAM TAŞIMA + CLAUDE.md + baglam.sh (8 Ağustos 2026)

Dört commit, `main`: `fbd6504` (CLAUDE.md) · `470077e` (baglam.sh) ·
`3b91a59` (B36 açılış ölçümü) · `45471a6` (sapma eki + ledger + borçlar).
Parça 3 (repoya tam taşıma) yazma üretmedi — işi teyit ve izin raporuydu.
**Sıfır kod commit'i. Marka çekirdeği DEĞİŞMEDİ.**

**`CLAUDE.md` repo köküne kondu** (114 satır, dokuz bölüm). `20-ref-protokoller.md`'den
damıtıldı; gerekçeler orada kaldı, kural burada durur. Çekirdek: ADIM 0 salt-read (355) ·
gerçeklik spec'i ezer (102) · çapa disiplini (465) · nicel iddia ölçülebilir olur (470) ·
KIRPMA YASAĞI (61) · commit disiplini · tek klon (463) · sır env'de (469).

**`scripts/baglam.sh` beş profille kuruldu** — `kod · icerik · marka · bot · dokuman`.
Planın dört profili **beş** dosyaya göre yazılmıştı; yedi var. Sapma kaydı yalnız
`20-ref-program.md`'yi yetim ilan ediyordu; ölçüm **üç yetim** buldu (program, marka,
notion). Beşinci profil `dokuman` bu turda eklendi — son üç tur doküman mimarisi işiydi
ve profili yoktu. Çıktının ilk satırı manifesttir; profilde tanımlı bir dosya diskte
yoksa script **paket üretmez** (`exit 2`) — sessiz düşüş yasağı, guard izole kopyada
sınandı.

**`marka` profiline ledger girmedi.** Plan "kararlar(marka filtreli)" diyordu; ledger'da
tema sütunu yok, filtre `baslik` metnine dayanmak zorunda ve sessizce yanlış keser.
Yarım filtre filtresizden tehlikelidir — eksik olduğu görünmez. Kuyruk: B41.

**Project files silme izni verildi.** On dört ledger hedefinin on dördü yaşıyor, sıfır
`ÖLÜ`. Kanon ve müfredat md5 eşit. `ocak-site-icerik.md` için izin kriteri "repoda kopya
var mı"dan **"üretim yolu canlı mı"**ya geçti — `scripts/site-icerik-dump.mjs` git'te
izleniyor, dosya türetilmiştir, KIRPMA YASAĞI ona uygulanmaz (KARAR 456 ilkesi).
KARAR 455 tamamlandı: project files'ta yalnız `ocak-marka.md` kalır.

### B36 açılış ölçümü — kuyruk kayıtlı boyutunun en az dört katı

`docs/_uretilen/olcum-2026-08.md` (191 satır). Popülasyon `^[0-9A-Za-z-]+\.md:\d+$`
eşleşen **418** satır — `02-borclar.md`'deki B36 tablosuyla birebir. Örneklem sistematik
(adım 20, ofset 10), Claude ya da CC seçmedi.

| | 21 satır | brief'in 20'si |
|---|---|---|
| KENDİ | 12 (%57) | 11 (%55) |
| KOMŞU | 9 | 9 |
| HİÇ | 0 | 0 |

**`HİÇ` sıfır — ledger kırık değil, sığ.** Okuyan yanlış yere gitmiyor, derine inemiyor.
Nokta tahmin `418 × 9/21 ≈ 179` satır, ama 21'lik örneklemin güven aralığı geniş
(~90-270); tek rakam yazılmadı. B36'nın kayıtlı "~37 satır"ı en az dört kat düşüktü —
o rakamın kaynağının belirsiz olduğunu maddenin kendisi zaten yazıyordu.

**Kritik bulgu — kuyruğun üçte ikisi mekaniktir.** KOMŞU'ların **6'sı tek desenden**:
erken sohbetlerin `- **KARAR N:** Başlık (Bölüm A.X)` biçimli karar listeleri. Mekanik
tespit edilebilir, dolayısıyla mekanik onarılabilir. Kalan üçü tekil (162 görev listesi ·
231 komşu kararın metni · 381 dönem özeti). **Sonuç: `ocak-kararci`'ye onarım modu
gerekmiyor** — gereken bir dönüştürme betiği, B33/B37'nin kardeşi. ADIM 5-6 kadrosu
(KARAR 458) değişmiyor.

`iliski` sütunu (4.b): tek rakam yok, tanıma bağlı — T1 kararsız aralığı 10 satır,
`⊂` 10 satır, saf `BNN` 10 satır. `kaynak` biçimleri (4.c): `418 · 35 · 8 · 7`,
sınıflandırılamayan 0, `#k`/diğer `25/10`. B36 tablosu doğrulandı, güncelleme gerekmedi.

- **KARAR 469 — SIR DOKÜMANDA YAŞAMAZ (KALICI):** Token, API key, secret hiçbir doküman
  dosyasına yazılmaz — env'de ya da credential store'da yaşar. Dokümanda yalnız yer
  tutucu durur (`[TOKEN — n8n credential store, dokümanda tutulmaz]`). Canlı sır görülürse
  yazma durur, rotate kararı Kaan'ındır. Kural commit geçmişine de uygulanır: sır
  commit'lendiyse **rotate tek çözümdür**, dosyadan silmek yetmez. *Kural fiilen
  yürürlükteydi — KARAR 455 private repo token'ı yapıştırmayı reddetmişti, `20-ref-bot.md`
  BotZ token'ı yer tutucuyla duruyor — ama ledger'da satırı yoktu. Sözlü olarak "KARAR 464"
  diye anılıyordu; 464 test ortamı TZ kararıdır (`2026-08.md:25` + ledger birebir).
  Numara çakışması ADIM 4 hazırlığında ölçülerek yakalandı.* İlişki: `←464`.

- **KARAR 470 — NİCEL İDDİA ÖLÇÜLEBİLİR OLUR (KALICI):** İki ayaklı.
  **(a)** Dokümana giren her nicel iddia — satır sayısı, kapsama oranı, "N vaka",
  "yaklaşık M" — üretilebilir bir komuta dayanmalıdır. Dayanmıyorsa ya yazılmaz ya
  `TEYITSIZ` işaretlenir. Bellekten, önceki turdan ya da brief'ten devralınan rakam
  **ölçülmüş sayılmaz.**
  **(b)** Rakam tek başına değil, üretildiği yöntemle yazılır — **eşik, araç, kaynak
  kümesi.** İki ölçüm çelişirse önce *tanımlar* karşılaştırılır, sonra rakamlar; çoğu
  çelişki tanım çelişkisidir.
  KARAR 465'in kardeşi: 465 çapanın nereden alınacağını, 470 rakamın nereden alınacağını
  söyler. İlişki: `←465 · ↔456`.

  *Vakalar (altı, hepsi ölçülerek):* **(1)** B32'de "~37 sığ satır" rakamının kaynağı
  bulunamadı — maddenin kendisi bunu kaydetti. **(2)** B39'da aynı ölçüm iki kez yapıldı,
  `231/232` ve `226/237` verdi; fark karakter-vs-bayt sayımındandı. **(3)** B36'nın
  dört-biçim tablosu (`418`) bağımsız bir sayımla (`423`) çelişti; kriter yazılınca —
  sözdizimi mi gönderge mi — birebir uzlaştı. Rakamlar farklı değildi, tanım yoktu.
  **(4)** ADIM 4 brief'i `ocak-site-icerik.md` için "üretim script'i yok" yazdı; sözlü
  bir cevaptan devralınmıştı, ölçülmemişti. CC'nin ADIM 0'ı script'i
  `scripts/site-icerik-dump.mjs` olarak git'te izlenir buldu. **(5)** Aynı brief
  "son tazeleme `17:06:37.718Z`" yazdı; `2026-08.md:34`'ten devralınmıştı, dosyanın
  kendi damgası `17:20:06.647Z`. **(6)** ADIM 4 kapanış raporu "beş commit" dedi, dört
  SHA listeledi — Parça 3 doğru şekilde commit üretmemişti. *Kuralı yazan belgenin
  kendisi kuralı üç kez ihlal etti; üçünü de ADIM 0 ve karşı-ölçüm yakaladı. Disiplin
  belgede değil, ölçümde yaşıyor.*

### İki araç tuzağı — ölçüm aracının semantiği doğrulanır

**`awk length` bayt sayar.** Bu makinede `awk` 20200816 (BSD); `printf 'çığır\n' | awk
'{print length($0)}'` → **9**, oysa 5 karakter. `LC_ALL` bunu değiştirmez. Türkçe metinde
bayt/karakter farkı ~%6; eşik tabanlı her sayımı sistematik kaydırır. B39'un `231/232` vs
`226/237` sapmasının kök nedeni bu ailedendir. ADIM 4'ün bütün uzunluk ölçümleri `python3`
(3.9.6) ile yapıldı, sonuç ölçüm dosyasının başında kayıtlı.

**`cut -c1-140` aynı sınıf tuzak.** Kronoloji tek satırda birden çok kararın kaydını
taşıyabiliyor ve bir kayıt 140. karakterden sonra başlayabiliyor. CC ilk turda kesilmiş
satır okuyup 21'in üçünü (256 · 276 · 317) yanlış sınıflandırdı, kesmeyi kaldırınca
düzeltti. **Ders: kesme/örnekleme aracı da ölçümün parçasıdır** — `head`, `cut`, `grep -o`
ne gösterdiği kadar ne *gizlediğiyle* de sonucu belirler. KARAR 470(b)'nin "araç" ayağı
tam olarak budur.

- **KARAR 471 — PROJECT FILES'TA KALAN KOPYA TÜRETİLMİŞ AYNADIR (KALICI):**
  KARAR 455 project files'ta yalnız `ocak-marka.md`'yi bırakır (unutma sigortası).
  Bu kopya **otorite değil ayna**dır; otorite `docs/10-marka.md`'dir. Repo tarafı
  değiştiğinde ayna elle tazelenir ve tazeleme sohbet sonu patch'inin ayrı bir maddesidir
  — aksi halde ayrışma sessizdir. Çelişkide **repo kazanır.**
  *Vaka: ADIM 4 ölçümünde `docs/10-marka.md` 235 satır, project files kopyası 236 çıktı.
  Git ile çözüldü: B32'nin `095cd1e` commit'i iki işaretçiyi onarırken 3 satırı 2'ye
  indirmişti. Yani repo yeni, ayna B32 öncesi. Fark bir satırdı ve ancak kazara yakalandı;
  ayrışmanın kendisi değil, **sessizliği** tehlikeliydi.* İlişki: `←455 · ↔102`.

**Kapanış:** ADIM 1-4 kapandı. ADIM 5 (`ocak-arsivci` + `ocak-teshis` + `ocak-lint`)
sıradaki tesisat işidir; B36 artık ölçülü bir kuyruktur ve mekanik ayağı ADIM 5 ile
aynı turda gidebilir.

### Çapa çözümleme sözleşmesi yazısızdı (8 Ağustos 2026)

ADIM 4 kapanış patch'inin doğrulama komutu (`grep -n "k469\|k470\|k471"`) sıfır satır
döndürdü. Sebep hata değil **konvansiyon**: `#kNNN` çapası kronolojide literal dize
olarak yaşamıyor. `#k` dizesi `2026-05/06/07` dilimlerinde hiç geçmiyor; `2026-08`'deki
geçişler ledger değerlerini alıntılayan patch metinleri. CC çapaları madde başlığı
deseninden çözdü ve doğru çözdü — ama **desen hiçbir yerde yazılı değildi.**

KARAR 466 `#kNNN`'i "elle doğrulanmış çapa" diye tanımlıyor, **nasıl çözüleceğini
söylemiyor.** Ledger'daki 35 `#` çapasının tamamı bu yazısız kurala bağlı, ve KARAR 461'in
`docs_karar(no)` aracı tümüyle onun üzerine kurulacak. Sözleşme yazılmazsa MCP sunucusunu
yazan taraf kendi çözümleme kuralını icat eder ve sessizce ayrışır.

- **KARAR 472 — ÇAPA ÇÖZÜMLEME SÖZLEŞMESİ (KALICI):** `YYYY-AA.md#kNNN` çapası hedef
  dosyada **literal dize aramaz.** Çözümleme madde başlığı biçimiyle yapılır:

  ```
  - **KARAR NNN — BAŞLIK (DURUM):**
  ```

  Yani `#k469` → `2026-08.md` içinde `- **KARAR 469 —` ile başlayan satır. Biçim
  varyantları meşrudur (`#kNNN-blok` bir bloğun tamamına, `#adim1` adlandırılmış bir
  bölüme işaret eder) ama **hepsi başlık satırına çözülür, gövde metnine değil.**
  Bir kararın çapası ancak o madde başlığı kronolojide yazıldıktan sonra meşrudur —
  ledger satırı önce yazılırsa çapa ölü doğar (KARAR 465 sıra şartı).
  KARAR 466'nın eksik ayağıdır: 466 biçimi tanımlar, 472 çözümlemeyi.
  *ADIM 7'nin `docs_karar(no)` aracı bu sözleşmeyi uygular; başka bir çözümleme kuralı
  icat edemez.* İlişki: `←466 · ↔461`.

**KARAR 470'in yedinci vakası, mühürlendiği turdan bir tur sonra.** Kapanış patch'i
KARAR 471'in vaka metninde B32 marka commit'ini `95cd1e` diye yazdı; CC `git log` ile
ölçtü, doğrusu **`095cd1e`** — baştaki sıfır düşmüştü. Claude.ai rakamı ölçmeden
devraldı, CC ölçtü ve düzeltti. *Aynı patch'in kendi vaka listesindeki (6) numaralı
madde de bağımsız olarak doğrulandı: ADIM 4 kapanış raporu "beş commit" demişti,
`git log fbd6504~1..HEAD` dört veriyor.*

**ADIM 4 kapanış patch'i uygulandı:** `cd28110` (kronoloji, +124 satır) · `a0a0020`
(ledger, karar satırı 470 → 471) · `538aafe` (B36 ölçülmüş hâli, 516 → 599 satır).
Test 181/181. Sıra korundu: 469/470 satırları `45471a6`'da yazılmıştı, blokları
`cd28110` ile doğdu, 471 sonra eklendi.

---

## ADIM 5 — SKILL KADROSU (8 Ağustos 2026)

`docs/skills/` doğdu. Üç skill: `ocak-arsivci` (patch uygular, commit'ler),
`ocak-teshis` (dist/ + computed CSS), `ocak-lint` (iki yüzey). `ocak-kararci` ve
`ocak-metin` ADIM 6'ya bırakıldı; `ocak-notion` HEDEF YAPI tablosunda var, skill
listesinde yok — çözülmemiş sapma, bu turda açılmadı.

**Skill'in doğuş gerekçesi ölçüldü.** `00-durum.md:62` dönem HEAD'i `f42911f`
yazıyordu, gerçek HEAD `76e8bee`. `03-sira.md` BAKIM KURALI bu sapmayı tam olarak
tarif ediyor ve kural dün yazılmıştı — eksik olan kuralı uygulayacak mekanizmaydı.
`ocak-arsivci`'nin kapanış doğrulamasında HEAD karşılaştırması zorunlu madde oldu.

**`ocak-lint` iki artefakt taşıyor.** `SKILL.md` yargı katmanı (tekrar disiplini, imza
eksenleri, portre dili, vurgu yerleştirme — grep'lenemez, okunur), `yasak-dizeler.tsv`
mekanik katman (24 dize, `dize · tip · kapsam · karar · istisna · oneri`). Ayrım
KARAR 466'nın elle/mekanik ayrımının aynısıdır. **Kapsam kuralı** skill'in ilk maddesi
oldu: lint yalnız kamuya çıkan üretilmiş metni denetler, iç dokümanı değil. Gerekçe
ölçüldü: `10-marka.md:107` OCAK WAY ilkesi olarak "OCAK bilgi vermez" yazar, KARAR 442
o kalıbı site metninde yasaklar — çelişki değil, kapsam farkı; kapsamsız lint bunu
ihlal sanardı.

**B42 kapandı.** Eylem sırası bağlayıcı çıktı: kalıntı önce silinir, `.gitignore` deseni
sonra kök-bağlı yapılır — ters sırada `docs/ocak-site-icerik.md` izlenir hale gelirdi.
Borç maddesi bu sırayı yazmıyordu. Tüketici taraması kod tüketicisi bulmadı.

**Yan bulgular, üçü de ADIM 0'da genişledi.** B43 — `10-marka.md:174` ve `:235` ölü
Pilot işaretçisi; ikincisinin hedefi tam karşılığı yok ("tampon", "lansman sonrası
roadmap" evsiz kavramlar). B44 — bayat `@ocak.life` **beş canlı dosyada**, brief yalnız
birini saymıştı; ama eşleşmelerin bir kısmı tarihsel kayıttır ve korunur
(`10-marka.md:3` sürüm notu değişikliğin kendisini anlatıyor). **Kapanış kriteri
`grep -c` sıfır değil, sınıflandırma tablosu** — KARAR 465'in "korunması gereken
tarihsel anlatım" uyarısının ilk somut vakası, ve `ocak-lint`'in `istisna` sütununun
ilk tatbiki. B45 — `baglam.sh` bayt/karakter etiketi iki satırda (`:65` hesap,
`:67` etiket).

- **KARAR 473 — SKİLL SENKRON SÖZLEŞMESİ (KALICI):** Kanonik skill kaynağı
  `docs/skills/`tir (KARAR 458). CC yüzeyi **tek symlink**tir: `.claude/skills →
  ../docs/skills`. Gerekçe: kopya drift'i *yakalanabilir* kılar, symlink *imkânsız*
  kılar. `.claude/` `.gitignore`'da olduğu için sync hedefi versiyonlanmaz — kanonik
  kaynak zaten repodadır. **Sonuç: `skill-sync.sh --check` yalnız claude.ai zip
  yüzeyini denetler**; CC yüzeyinde ayrışma yapısal olarak imkânsızdır. Zip çıktısı
  `docs/_uretilen/skill-zip/` altında yaşar ve ignore'lanır; `_uretilen/` toptan ignore
  **edilmez** — dönüşüm betiklerinin versiyonlanması KARAR 467(a) gereğidir. İki
  yüzeyli skill (`ocak-lint`) yargı katmanını `SKILL.md`'de, mekanik katmanı ayrı veri
  dosyasında tutar. İlişki: `←458 · ↔463`.

**Sıfır site kodu commit'i.** Tek kod dokunuşu `scripts/site-icerik-dump.mjs`
(`OUT_PATH` + yorum satırı), build'e girmez.

---

## DÜZELTME — DÖNEM HEAD SÖZLEŞMESİ (8 Ağustos 2026, ADIM 5 sonrası)

**Yukarıdaki ADIM 5 kaydı `f42911f`'i "bayat HEAD" diye anlatıyor. Bu tespit yanlıştır.**
Kayıt KIRPMA YASAĞI gereği olduğu gibi bırakıldı; düzeltme burada yaşar.

`git log` ölçümü gösterdi ki `76e8bee` ADIM 4'ün **kapanış commit'i ve HEAD satırını
yazan commit'in kendisidir** — commit mesajı da bunu söylüyor: *"dönem HEAD satırı —
anlık görüntü etiketi + patch sırası kuralı"*. Kendi hash'ini içeremezdi; bir öncekini,
`f42911f`'i yazdı. Bu ihlal değil, **yapısal zorunluluktu.**

ADIM 5 uygulaması aynı duvara çarptı: kural harfiyen uygulandı, satır kapanış
commit'inden hemen önce ölçüldü, yine de `git HEAD 091d1de` ≠ `00-durum.md cb28afc`.
CC bunu bağımsız olarak teşhis etti ve üç çıkış yolu önerdi; (a) seçildi.

**Neden kayda değer:** kuralı yazan tur ile onu yanlış okuyan tur **aynı gün** oldu.
Sözlü taşınan bir kural bir tur dayanmadı. `ocak-arsivci`'nin doğuş gerekçesi soyut
bir ihtiyaç değil, bu somut vakadır — ve skill'in ilk yazımı da aynı hatayı taşıdı
(`DUR-7 == git log -1`), yani skill kendi kapanışını her turda DUR'a düşürecekti.
Kendi kendini yakalayan bir hata olarak kapandı.

- **KARAR 474 — DÖNEM HEAD ANLIK GÖRÜNTÜDÜR (KALICI):** `00-durum.md`'deki dönem HEAD
  satırı **kapanış commit'inden bir önceki commit'i** taşır. Sabit nokta problemidir —
  dosya kendi repo'sunun HEAD'ini yazar, o yazım da bir commit üretir; fark tam olarak
  bir commit'tir ve **sıfıra inemez**. Satır canlı HEAD değil anlık görüntü etiketidir.
  Doğrulama: `git log -2 --format='%h' | tail -1`. `git log -1` ile karşılaştırma
  yanlıştır ve hiçbir zaman geçmez. Kapanıştan sonra fazladan commit atılması meşrudur;
  satır bir sonraki dönemde düzelir, **geriye dönük düzeltme commit'i atılmaz.**
  Reddedilen alternatifler: satırı `baglam.sh` manifestine taşımak (dosya manifest
  dışında okunduğunda — KARAR 471 aynası tam olarak öyle okunur — hangi commit'e denk
  geldiğini söyleyemez hale gelir) · `--amend`'li kapanış (SHA'yı değiştirir, sabit
  noktayı çözmez, push sonrası tehlikeli). İlişki: `←468`.

---

## B44-a — LINT KAPSAM ÇELİŞKİSİ (8 Ağustos 2026)

`ocak-lint` yüklendikten sonraki ilk gerçek sınamada yanlış cevap verdi: `@ocak.life`
için "kapsam her yerde, istisna yok, iç doküman ve ledger dahil her yüzeyde değiştir"
dedi. Skill'i doğru okudu — **veri yanlıştı.** Aynı gün açılan B44 tam tersini söylüyor.

**Çelişki tek satır değil.** `kapsam` sütununda `her yerde` yazan altı satırın altısı da
**kendi tanımını yakalıyor**: `@ocak.life` ve `Uluslararası Yolculuk` marka dosyasının
sürüm notunu (`10-marka.md:3`), `funnel`/`conversion`/`lead` KARAR 57'nin etik çerçeve
cümlesini (`:223`), `#FFFFFF` "beyaz kullanılmaz" kuralının kendisini (`:202`).
**Yasağı yazan cümle yasağın ilk kurbanı oluyordu.**

Kök sebep, `her yerde` kapsamının bir kuralı **ihlal eden** metin ile onu **tanımlayan**
metin arasında ayrım yapmaması. Bir yasak listesi, kendi gerekçesini barındıran metni
zorunlu olarak yakalar — bu kaçınılmazdır ve muafiyetle çözülür, kapsam daraltmasıyla
değil.

**Çözüm iki katmanlı.** (1) `SKILL.md`'ye **tarihsel kayıt muafiyeti** eklendi: bir dize
kendi yasağını ya da kendi değişimini anlatan metinde geçtiğinde korunur; muafiyet her
satıra uygulanır, `ek_istisna` sütunu ona **ek** olanı taşır. Sütunun `yok` demesi genel
muafiyetin kalktığı anlamına gelmez — sütun bu yüzden `istisna`'dan `ek_istisna`'ya
çevrildi. (2) `kapsam` sözlüğü tanımlandı (`kamu metni` · `canlı referans` · `kod` ·
`site sayfası`); `her yerde` değeri **kaldırıldı**.

Kapanış kriteri hiçbir satır için `grep -c` sıfır değildir; kriter sınıflandırmadır.
KARAR 465'in "korunması gereken tarihsel anlatım" uyarısının doğrudan uygulaması —
yeni karar değil, mevcut kararın lint yüzeyine indirilmesi.

**Doğuş anına dair:** hata skill'in kendisinde değil, onu besleyen veride ve verinin
yazıldığı sırada. Tsv EK-1'den önce yazıldı; EK-1 B44'ün kapsamını genişletirken veri
dosyasına dönülmedi. Aynı turda üretilen iki artefakt birbiriyle çelişti ve çelişkiyi
**skill'in ilk kullanımı** yakaladı — kadronun ilk gerçek getirisi.

Dize sayısı değişmedi: 24. Ledger'a yazılmadı, yeni KARAR açılmadı.

---

## B36-a — DESEN ÖLÇÜLDÜ, YÖNTEM YETERSİZ (8 Ağustos 2026)

Betik `_uretilen/b36a-desen-tespiti.py` koşuldu, **ledger'a yazılmadı** — turun en
önemli testi `git diff docs/01-kararlar.tsv` boş kalmasıydı, kaldı.

**Kapsam turun başında düzeltildi.** `olcum-2026-08.md` "mekanik `#kNNN` terfisi
otomatikleştirilebilir" diyordu; KARAR 466 `#k`'yı elle doğrulanmış çapa olarak
tanımlar — betik tanımı gereği elle doğrulama yapamaz. Bu tur ölçtü ve aday üretti.

**Ölçüm:** 418 mekanik · 119 sığ (%28.5) · tek-aday 12 · çok-aday 36 · adaysız 71 ·
çözülemeyen 0. Popülasyon betikçe yeniden üretildi; 418 rakamı devralınmadı, tesadüfen
aynı çıktı — ledger 468→474 büyüdü ama altı yeni satır (KARAR 469-474) doküman
mimarisinin kendi kararları ve hepsi elle çapalı, mekanik popülasyona girmiyorlar.
İki bağımsız koşum aynı rakamı verdi.

**Nokta örneklemesi 2/5, DUR-3 tetiklendi.** Örneklem puan dağılımına yayıldı, ham
metin gözle açıldı. Tutan ikisinde (`51`, `141`) aday satır başlık/kayıt biçiminde ve
gövde taşıyor. Tutmayan üçü ayrı ayrı öğretici:
`131` — sığ çapadan başka bir sığ çapaya geçiş; kuyruk listesinden kuyruk listesine.
`91` — atıf yönü ters; kararı **uygulayan/denetleyen** satır, kaydı değil.
`89` — uzunluk bonusu zararlı çalıştı; 214 karakterlik envanter satırı tek parantez-içi
atıf yüzünden grubun en yüksek puanını aldı.
Ortak payda: sinyaller **numaranın geçtiği** satırı buluyor, **kararın kaydı olan**
satırı değil. "Gövde var mı" ayrı bir sinyal olarak sorulmuyor.

**Asıl bulgu örneklemede değil.** 119 sığ satırın **71'inin (%60) adayı yok.** O
kararların kronolojide karar-listesi indeksinden başka kaydı **hiç yazılmamış.** Sorun
çapanın yanlış yere bakması değil; bakılacak yerin var olmaması. **B36-b'nin işi çapa
düzeltme değil, kayıt yazma** — ve önceki tanımından (üç karar: 162 · 231 · 381) çok
daha geniş. Tek-aday oranı da bağımsız olarak aynı yere çıkıyor: 12/119, brief'in
"üçte iki üstü" eşiğinin çok altında. Mekanik taşıma turu açılmadı.

**Yöntem yargısı betiğe veri olarak gömüldü** (`ORNEKLEM` bloğu); rapor onu oradan
basıyor. Böylece rapor türetilmiş kalıyor ve elle yapılan doğrulama versiyonlanıyor —
KARAR 467'nin "eşleme tablosu kesim anında üretilir" ilkesi.

**Yan olay:** arşiv commit'i (`7fc2ac1`, 17 brief dosyası) B36-a'nın ADIM 0'ı ile betik
koşumu arasına düştü. Ölçümü etkilemediği doğrulandı — commit `01-kararlar.tsv`,
`90-kronoloji/*` ve `00-durum.md`'nin üçüne de dokunmuyor ve arşivden önceki/sonraki iki
bağımsız ölçüm aynı rakamı verdi. Yine de sıra hatasıdır: **ADIM 0 ile uygulama arasına
commit atılmaz.**

**Gün sonu kayıtsız kalanlar borca alındı:** B46 (ölçüm aracının kendisi hata kaynağı,
dört vaka) · B47 ("ne nerede yaşar" haritası hiçbir dosyada yok).

---

## ADIM 6 — KADRO TAMAMLANDI (9 Ağustos 2026)

ADIM 5 kadronun yarısını doğurmuştu. Bu tur kalan üçü doğdu ve KARAR 458'in altı
skill'i tamamlandı. Asıl iş dizin açmak değil **sınır çizmekti**: üç sınır bugüne
kadar yazılı değildi ve ikisi fiilen çakışıyordu.

- **KARAR 475 — `ocak-kararci` ↔ `ocak-arsivci` SINIRI:** Kararcı satırın **içeriğini
  üretir ve yazımdan önce doğrular**; dosyaya **yazmaz**. Yazan arşivcidir. Ledger
  bütünlük komutları ikiye ayrıldı: *ön* sorgular kararcıda (numara boşta mı, halef
  gerçekten var mı, halefsiz `SUPERSEDE` var mı), *son* denetimler arşivcide (`NF`,
  mükerrer, enum, boş `kaynak`). **Aynı komut iki dosyaya yazılmaz.** Gerekçe: geçiş
  planı "tsv satırını CC yazar" diyordu, ama o iş zaten arsivci'nindi — sınır
  yazılmasa iki skill aynı işi iki türlü yapardı. Kararcı ayrıca "sıfırıncı soru"yu
  taşır: yeni numara son çaredir; önce revize / zincir (KARAR 364) / uygulama / blok
  üyeliği (`⊂N`) elenir. Numara boşlukları (`62 · 64 · 66-68 · 179` `KULLANILMADI`,
  `454` `REZERVE`) **doldurulmaz** — boş kutu değil, kayıtlı anomalidir.

- **KARAR 476 — `ocak-metin` ↔ `ocak-lint` SINIRI:** Metin **üretir**, lint
  **denetler**; aynı kural iki dosyada tarif edilmez. `ocak-lint`'in yargı listesi on
  kararı taşıyor (400 · 401 · 403 · 404 · 446 · 448 · 300 · 354 · 449 · 441/402) ve
  `ocak-metin`'e kopyalanmadı — metin **ses zemini** (KARAR 55 "biz" sesi, KARAR 39 tek
  dil, KORUNACAK İFADELER on madde birebir, KARAR 399 portre kalıbı) ve **üretim
  yöntemi** (pattern bekçileri, KARAR 449 teslim standardı, KARAR 450 placeholder
  kanonu, ESKİ→YENİ + tam gövde) taşır. **Zorunlu bağ: taslak lint'ten geçmeden
  sunulmaz.** Gerekçe KARAR 373'ün skill katmanındaki tekrarı: iki kopyayı elle eşit
  tutmak sonsuz döngüdür, tek ev farkı imkânsız kılar. KARAR 459 skill'in içine
  yazıldı — taslak-only, Notion'a giriş elle, en az üç ay.

- **KARAR 477 — `ocak-notion` DAR KAPSAM:** İki iş: taze dump üretimi
  (`node --env-file=.env scripts/site-icerik-dump.mjs` → `docs/_uretilen/site-icerik.md`)
  ve marker sözleşmesinin **koda karşı** doğrulanması. **Kanonik section listesi skill'e
  taşınmaz.** Gerekçe ölçüldü: liste bugün dört yüzeyde dört farklı sayı taşıyor —
  `10-marka.md:184` **8** · `sayfa-yazim-rehberi.md` **5 + 10** · kod **10** ·
  `/site-rehber` **11**. Beşinci kopya üretilmedi; kod otoritedir ve skill onu `grep`'ler.
  Sınır: **notion girdiye bakar (Notion), teshis çıktıya (`dist/`)**; dump bir teşhis
  kaynağı değildir (KARAR 355). Notion'a yazmaz (KARAR 459), token env'de kalır
  (KARAR 469). Doğmasının ikinci gerekçesi: KARAR 439'un "patch günü, dump günü"
  refleksi bugüne kadar **hiçbir dosyada sahipli değildi** — `ocak-metin` ve `ocak-lint`
  ikisi de taze dump varsayıyor, üretmesi kimsenin işi değildi. Bu, geçiş planı sapma
  kaydı EK'in 11. satırını kapatır.

**Bir tanım çelişkisi çözüldü (ADIM 0, KARAR 470).** Brief, sapma kaydı EK #8'in
(`_uretilen/` izleniyor) ADIM 5 doğrulamasıyla (`skill-zip` `git status`'ta görünmemeli)
çeliştiğini varsaymış ve "ikisi aynı anda doğru olamaz" demişti. **Ölçüm ikisinin de
doğru olduğunu gösterdi:** `.gitignore:52` `/docs/_uretilen/skill-zip` **alt dizinini**
ignore ediyor, klasörün kendisi izleniyor (11 dosya) — iki ifade farklı granülariteden
konuşuyor. Zip'ler commit'e girmez. **Brief'in önermesi yanlıştı, ADIM 0 düzeltti** —
"çoğu çelişki tanım çelişkisidir" maddesinin birebir vakası.

**İkinci düzeltme, aynı sınıf (ADIM 0 sonrası).** Onay ekinin Rider A'sı `--check`
ayrışmasını DUR koşulu sayıyordu; ayrışma sync'in **girdi koşuluydu**, çıktı arızası
değil. Kanıt ölçümün içindeydi: ayrışma listesi tam olarak yeni doğan iki skill'i
içeriyor, ADIM 5'in üçü temiz. Rider'ın gerekçesi ayrıca olmayan bir çıktı dizesine
(`CC yüzeyi: yok`) dayanıyordu — gerçek çıktı `symlink yerinde` idi. **İki turda iki
kez, Claude.ai'nin kurduğu çelişkiyi ölçüm çürüttü.** KARAR 102 agent kadrosunun kendi
brief'lerine de uygulanıyor; CC'nin durup uzlaştırmaması iki seferde de doğru reflekstir.

**Ölçüm bulguları (9 Ağustos, ekli dosyalardan):** `yasak-dizeler.tsv` 25 satır =
1 başlık + **24 dize** (B44-a beyanı doğrulandı) · `sayfa-yazim-rehberi.md` **219**
satır (sapma kaydı EK 9. satırı doğrulandı) · `20-ref-icerik-dili.md` 88 satır ama
14.615 karakter — **satır başına ~166 karakter**, bu dosya hakkında satır sayısıyla
konuşmak yanıltıcıdır. Yöntem: `wc -l` / `wc -c` / `python3`; `awk length` bu makinede
**bayt** sayıyor (`çığır` → 9).

**Açılan borçlar:** **B48** (`baglam.sh` manifesti `docs/` kökündeki dosyaları saymıyor;
geçiş planı bu tur pakette yoktu ve manifest bunu göstermedi — B45/B46 ailesi) ·
**B49** (kanonik section sayısı dört yüzeyde dört farklı; B31'in kardeşi, dördüncü
yüzey `10-marka.md`).

**Sıra kararı yazıldı:** ADIM 6 → **ADIM 7 (MCP)** → hold. B44 ve B47 borç kuyruğunda
bekler. `03-sira.md` üç aday sayıp seçimi Kaan'a bırakıyordu; seçim 8 Ağustos akşamı
yapılmış ama dosyaya yazılmamıştı — bu tur yazıldı.

**Sıfır site kodu commit'i.** Marka çekirdeği DEĞİŞMEDİ.

**ADIM 6 kapanış eki (9 Ağustos, tur sonrası).** İki kayıt turun kendisinden sonra doğdu.

**(a) Symlink ayağı kanıtlandı.** ADIM 5'in açık bıraktığı *"filesystem çözülmesi
kanıtlandı, skill yükleme kanıtlanmadı"* maddesi kapandı: üç yeni skill commit anında
CC yüzeyinde göründü. Ayrı bir teyit turu gerekmedi — kanıt işin kendi akışından çıktı.

**(b) Zip ayağı hiç kurulmadı → B50.** KARAR 473'ün ikinci yüzeyi (claude.ai, elle
yüklenen zip) 8 Ağustos'tan beri bekliyor; ADIM 6 ile altı zip oldu. **`ocak-metin`
yalnız Claude.ai yüzeyinde çalışan tek skill'dir** (KARAR 458) — doğdu, commit'lendi,
kanonik kaynakta duruyor ve çağrılamıyor. Kadro "altıya tamamlandı" diye kaydedildi;
gerçek şu ki yarısı tek bacaklı duruyor.

*Ders: bir skill'in **doğması** ile **erişilebilir olması** iki ayrı olaydır ve ikincisi
ölçülmedikçe varsayılmaz — "kod var ≠ output var"ın (KARAR 408) skill katmanındaki
karşılığı. `skill-sync.sh --check` bu boşluğu kapatmaz: zip'in üretildiğini denetler,
yüklendiğini değil; yükleme repodan görünmez.*

**B50 kapandı (9 Ağustos, aynı gün).** Kaan altı zip'i yükledi; KARAR 473'ün ikinci
yüzeyi ilk kez fiilen kuruldu. `ocak-metin` — kadronun tek Claude.ai-only skill'i —
doğduğu günün akşamında erişilebilir oldu. Kapanış kriterinin iki ayağı ayrı yerlerden
kanıtlandı: makine ayağı CC'den (`--check` ayrışma yok, 6 dizin / 6 zip), yüzey ayağı
Claude.ai envanterinden (altı skill listeleniyor; bugün doğan üçünün açıklama metni
commit'lenen frontmatter ile birebir, yani yüklenen sürüm güncel).

⚠ **Kapanış tazeliğe bağlıdır.** `docs/skills/` altında bir dosya değiştiğinde zip
bayatlar ve claude.ai yüzeyi sessizce eskir. Symlink yüzeyinde ayrışma yapısal olarak
imkânsız; zip yüzeyinde yalnız **yakalanabilir**. **Skill dokunuşu içeren her turun son
adımı `skill-sync.sh sync` + yüklemedir.** Nüks ayrı borç açmaz, bu kurala bağlanır.

**Günün asıl dersi — dört yakalama.** 9 Ağustos'ta Claude.ai dört kez bir şeyi kanıtsız
ya da tutarsız ileri sürdü ve dördünde de ölçüm tarafı düzeltti: (1) ana brief §1.4 iki
ölçümün "aynı anda doğru olamayacağını" söyledi — granülarite farkıydı, ikisi de
doğruydu; (2) onay ekinin Rider A'sı sync **öncesi** ayrışmayı DUR koşulu saydı ve
olmayan bir çıktı dizesine dayandı; (3) B50 kapanış brief'inin ilk sürümü kapanışı
**birinci ayak kanıtlanmadan** yazdı, kanıt yerine beyan koydu; (4) v2'nin doğrulama
kriteri kendi uygulama talimatıyla tutmuyordu — damga satır sonuna konunca
`grep -c 'B50 ✅'` **asla 1 dönmezdi**, ve aynı dizeye dayanan idempotency guard'ı
**kalıcı olarak sessiz kalırdı**: brief ikinci kez koşulsa yakalanmaz, mükerrer yazım
yapardı. Dördünde de CC durdu, uzlaştırmadı, ikisini birden raporladı.

**KARAR 102 agent kadrosunun kendi brief'lerine uygulanıyor ve bugün dört kez sınandı.**
Kayda değer olan hatalar değil, hepsinin aynı sınıf olması: *Claude.ai bir çelişki, eksik
ya da kriter kuruyor ve onu ölçülmüş gibi yazıyor; ölçen taraf çürütüyor.* Dördüncüsü
diğer üçünden sivri, çünkü **başarısızlığı sessiz olacaktı** — B45/B46 ailesi: araç hata
vermez, yanlış sonucu doğru sonuç gibi verir. Bu, `ocak-teshis` ve `ocak-arsivci`'nin DUR
koşullarının neden brief'e değil **skill'e** yazıldığının gerekçesidir: brief yanılabilir,
refleks yanılmamalı. Ve CLAUDE.md §3'ün son fıkrası ("doğrulama kriteri dosyanın gerçek
hâline karşı yazılır, beklentiden değil") bugün ilk gerçek vakasını buldu — kriteri yazan
taraf değil, koşan taraf yakaladı.

## 10 Ağustos 2026 — ADIM 7 birinci dalga: docs MCP sunucusu

> ⚠ **ŞERH (11 Ağustos 2026, B56):** Bu bloğun başlığındaki tarih **yanlış — işler 9 Ağustos
> 2026'da koşuldu.** Ölçüm: o gün `date +%F` → `2026-08-09`, ve `git log --format='%ci' -10`
> son on commitin **onu da** 9 Ağustos (13:01 → 21:25) dedi. Yazım hatasıydı; patch
> 10 Ağustos yazdı, ADIM 0 sorgulamadı, aşağı devredildi.
> **Başlık düzeltilmedi, düzeltilmeyecek** — bu dosya append-only'dir (KARAR 61) ve
> düzeltme değil şerh alır (KARAR 482 kural 1b). `01-kararlar.tsv`'nin 478/479 satırları
> **türetilmiş** oldukları için düzeltildi; iki dosya aynı işlemi bilerek görmedi.
> ⚠ Bu dosyada `## 10 Ağustos 2026` başlığı **iki kez** geçer. Aşağıdaki blok yanlış
> tarihlidir; B01 bloğu ise gerçekten 10 Ağustos'a aittir ve doğrudur.

`mcp/` doğdu. OCAK'ın doküman korpusunu uzak bir MCP sunucusu olarak servis eden üç
araç: `docs_envanter` · `docs_oku` · `docs_ara`. `docs_karar(no)` bilinçli olarak
yazılmadı — gerekçesi aşağıda. Korpus ölçüldü: **113 dosya · 71 canlı · 42 arşiv**
(`docs/**` + `CLAUDE.md` + `scripts/**`). Arşiv canlıdan daha fazla satır taşıyor —
23 265'e karşı 21 169 — yani varsayılan kapsam `canli` olmasaydı aramaların yarıdan
fazlası bayat dosyaya düşerdi. `_arsiv` etiketinin süs olmadığının nicel karşılığı bu.

**Sözleşme ezberden değil güncel dokümandan okundu ve iki yeri değişmişti.** Brief bu
maddeyi bilerek DUR koşulu yapmıştı; haklı çıktı. (1) MCP spec'i 2026-07-28'de majör
revizyon yaptı: monolitik `@modelcontextprotocol/sdk` odaklı paketlere bölündü
(`@modelcontextprotocol/server` 2.0.0 + adaptörler), `initialize` el sıkışması kalktı,
sunucu stateless çalışabiliyor — Railway'de sticky session gerekmemesi doğrudan kazanç.
Eski SSE-only transport deprecated; tek meşru seçenek Streamable HTTP. (2) claude.ai
custom connector'ları için OAuth **zorunlu değil**; `none` dahil altı auth cinsi var ve
sabit kimlik bilgisi **Request headers** ile veriliyor. Ama o özellik Anthropic
tarafında hâlâ beta. Karar: sunucu her zaman token ister, authless mod koda hiç
girmedi — bağlantı ertelenebilir, korumasız açılış ertelenemez.

**Envanter tool oldu, resource değil — ve bu bir ölçüm sonucu.** Brief envanteri
resource olarak sunmayı öneriyordu, gerekçesi "çağrılması hatırlanması gerekmez"di.
Doküman tersini söyledi: claude.ai'de resource'lar sohbete **kullanıcı tarafından elle
iliştirilir**, Claude onları kendiliğinden okumaz; tool'u ise kendi kararıyla çağırır.
Yani envanteri resource'a taşımak B48'i çözmez, **insana devreder**. Tool kaldı, ikinci
yüzey açılmadı. Buna karşılık `docs_ara` ve `docs_oku`'nun her cevabına tek satırlık
korpus özeti kondu — bir tool'un çağrılması hatırlanmak zorunda kalırsa, hatırlanmadığı
tur kör geçer; o satır körlüğü yapısal olarak imkânsız kılıyor.

**Dört tasarım ilkesi koda geçti.** (a) Kodda dosya listesi yok, yalnız izinli kök
dizin listesi — bu projede yazılı her liste bayatladı. (b) Her cevap okunduğu commit'i
taşıyor; belirlenemezse `BELİRLENEMEDİ` diyor, susmuyor. (c) Ölçüm sunucuda, `grep`
`awk` `cut` çağrılmıyor — bu makinede `printf 'çığır' | awk '{print length($0)}'`
**9** döndürüyor, doğrusu 5; B46'nın vakası teorik değil, bu diskte ölçülü. (d) Kırpma
sessiz değil: `docs_ara` `toplam_eslesme` ile `donen_eslesme`'yi **ayrı ayrı** taşıyor,
"0 döndü" ile "kırpıldı" bir daha karışmasın.

**On iki doğrulama kriteri koştu, on ikisi de geçti** — hiçbiri brief'ten devralınan
rakama değil, ADIM 0'da ölçülen rakama karşı. Envanter sayısı 113'ü, satır/bayt
toplamları `python3` ölçümünü birebir tuttu; `çığır` araması MCP'de 14, bağımsız
`python3` Türkçe katlamasında da 14. `.env` ve `../.env` ikisi de reddedildi. Uçtan uca
tur resmî MCP istemcisiyle gerçek Streamable HTTP üzerinden koşturuldu; yanlış token
`UnauthorizedError` aldı. Kabul adımı da ölçüldü: MCP kapalıyken `baglam.sh dokuman`
1940 satırlık paketi üretti — yedek yolun çalıştığı varsayılmadı.

**Bir kriter kendi ölçümüyle çelişti ve tanım çelişkisi çıktı.** `02-borclar.md`'nin
başlık indeksi sınanırken `^## B\d+ — ` kalıbı 49 tuttu, oysa dosyada 50 `## B` başlığı
var. Fark satır 803'teydi: `## B50 ✅ KAPANDI (9 Ağu) — ...` — numara ile uzun tire
arasına durum işareti girmiş. Ölçüm değil kriter dardı. KARAR 470'in "iki ölçüm
çelişirse önce **tanımlar** karşılaştırılır" fıkrası bugün de aynen işledi.

- **KARAR 478 — SOHBET SONU İKİNCİ TESLİM (KALICI):** Sohbet sonu teslimi **iki
  parçadır**: `docs/` altına yazan patch (KARAR 468, beş bölüm) ve **bir sonraki turun
  açılış promptu**. İkincisi patch bölümü değildir — patch'in beş bölümü de `docs/`
  altına yazar, açılış promptu ise bir sonraki tura gider. Bugüne kadar Kaan elle
  yazıyordu; `03-sira.md`'nin "nasıl açılır" hücresi tek satır ve yüz elli satırlık bir
  promptun yerini tutmuyor. Yani sistem **kendini sürdüren parçayı insana bırakmıştı** —
  kurulma amacının tersi. 468 mühürlü `KALICI` olduğu için gövdesine bölüm eklenmez;
  genişletme ayrı numara alır ve `←468` ile bağlanır (309→311 deseni). Kardeş bağ
  `↔462`: sohbet sonunun tek `docs-patch` dosyası olduğu kararı orada, ikinci teslimin
  o dosyanın **dışında** durduğu burada.

### 10 Ağustos — ikinci yarı: deploy, bağlantı ve iki rakam düzeltmesi

**MCP canlıya çıktı ve bu sohbette gerçekten kullanıldı.** Railway'de `ocak-mcp` servisi,
Streamable HTTP, zorunlu Bearer. `docs_envanter` · `docs_ara` · `docs_oku` üçü de gerçek
çağrılarla döndü; damga `10b5b93` → `306a15b` **kendiliğinden** güncellendi. Tasarım
ilkesi (b) böylece üçüncü kez canlı doğrulandı — iddia değil, gözlem.

⚠ **Railway `Root Directory` boş bırakıldı ve bu bilinçliydi.** Brief'in ilk hâli
`mcp/` yazıyordu; Railway dokümanı ölçüldü ve tersi çıktı — root directory verilince
container'a **yalnız o dizin** iner, yani `docs/` hiç gelmez. Sunucu ayağa kalkar,
`docs_envanter()` sıfır döner, **hata vermez**. Brief'i yazan taraf yanıldı, ölçen taraf
düzeltti; DUR-8 olarak brief'e yazıldı ve tetiklenmedi.

**Bağlantı ucu kilit çıktı, ödün verildi ve ödün görünür bırakıldı.** claude.ai'nin
"Add custom connector" diyaloğu ölçüldü: dört alan — `Name` · `Remote MCP server URL` ·
`OAuth Client ID` · `OAuth Client Secret`. **Request headers yok** (Anthropic tarafında
beta). Yani `Authorization: Bearer` gönderilemiyor. Üç yol vardı: beklemek · authless
açmak · token'ı URL yoluna koymak. **Authless reddedildi** — servis edilen şey işin tüm
hafızası, tahmin edilmesi zor bir URL gizli bir URL değildir. Yol-token seçildi: iki auth
yüzeyi, tek sır, tek doğrulama; başlık varsa başlık kazanır, başarısız her hâl `401`.
Karşılaştırma SHA-256 + `crypto.timingSafeEqual`'a çevrildi — önceki elle XOR döngüsü
uzunluk farkında erken çıkıyor, yani token uzunluğunu zamanlamayla sızdırıyordu.
Yol-token URL loglarına düşer; **B53 bu yüzden açık ve kendiliğinden kapanmaz** — beta bir
gün sessizce açılır, kimse fark etmez, ödün kalıcılaşır.

**KARAR 479'a götüren sapma: 113 mi 105 mi.** Canlı `docs_envanter()` **104** döndü,
ADIM 0.c yerelde **113** ölçmüştü. Arşiv birebir tuttu (42=42); fark tümüyle canlıdaydı.
Tahmin edilmedi, ölçüldü: `git ls-files` aynı süzgeçle 104 verdi, ve `git archive HEAD`
ile boş dizine çıkarılan gerçek checkout da 104 verdi — **iki bağımsız yol canlı sunucuyla
birebir**. Fark tam dokuz dosya, dokuzu da `.gitignore`'lu; ters yönde sıfır. Yani sapma
değil, **tanım farkı**: ADIM 0.c yerel diski saydı, MCP git deposunu servis ediyor.
Dokuzdan sekizi türetilmiş artefakt (altı `skill-zip/*.parmak` + iki dump çıktısı),
biri değildi: `scripts/dump-fable.mjs`, 624 satırlık kaynak script, ne depoda ne
container'da ne yedekte — yalnız tek diskte. `.gitignore:47` deseni tam dosya adı;
büyük olasılıkla bir satır yukarıdaki `ocak-site-dump-fable-*.md` globuyla (script'in
**çıktısını** koruyan satır) karışmasından doğmuş. Sır taraması iki kez koşuldu, temiz;
dosya hiç commit'lenmemiş, rotate gerekmedi. İzlemeye alındı (`306a15b`), korpus 105 oldu.
**Bu, B47'nin ("ne nerede yaşar" haritası yok) somut vakası** — bir dosyanın neden ignore
edildiği hiçbir yerde yazılı değildi ve kimse fark etmeden aylarca öyle durdu.

- **KARAR 479 — MCP GİT DEPOSUNU SERVİS EDER (KALICI):** Servis edilen korpus **git
  deposudur**, çalışma dizini değil. Railway container'ı `git`ten checkout alır; bu bir
  kısıt değil **tercih**, ve tercihin bedeli `.gitignore`'lu dosyaların görünmemesidir.
  Sonuç: MCP tarafındaki her nicel kriter `git ls-files` kümesine karşı yazılır, yerel
  `find`/`os.walk` çıktısına karşı değil. Aksi hâlde yerelde geçen bir kriter canlıda
  düşer — 10 Ağustos'ta tam olarak bu oldu ve ayrışma tek çağrıda göründü. **Guard
  çalıştı; düzeltilecek olan kriterin tanımıydı, rakam değil.** `←461 · ↔470,471`

**Düzeltme 01 — bu bölümün üstündeki 113 rakamı.** Bu dosyanın 10 Ağustos ilk kaydı
"113 dosya · 71 canlı · 42 arşiv" yazıyor. **Rakam yanlış değil, kümesi yanlış** —
yerel disk sayımıdır. Servis edilen korpus o gün **104** (`git ls-files`), aynı gün
`scripts/dump-fable.mjs` izlemeye alınınca **105** oldu. Üst satır KIRPMA YASAĞI gereği
olduğu gibi bırakıldı; ölçümün hangi kümeye ait olduğu bu düzeltmede yaşıyor (KARAR 479).

**Düzeltme 02 — `çığır` sayısı, ve neden tek rakam yazılamazdı.** Üstteki kayıt
"`çığır` araması MCP'de **14**" diyor; kapanış turunda CC'nin raporu **16** dedi; canlı
sunucu **9** (`kapsam=canli`) ve **16** (`kapsam=hepsi`) döndürdü. Üçü de doğru ölçümdü,
üçü de **kapsamını yazmamıştı**. Ama asıl ders ikinci katmanda: aranan dize, arandığı
dosyanın kendisinde geçiyor. Bu kayıt `çığır` kelimesini içerdiği için **kendi sonucunu
büyütüyor** — 14, yazıldığı andaki doğru değerdi ve bir saat sonra 16 oldu. **Kendine
gönderme yapan ölçüm, yöntemi yazılmadan kayda geçirilemez** (KARAR 470(b)); yöntem
burada yalnız araç değil, **kapsam ve ölçüm anı**dır.

---

## 9 AĞUSTOS 2026 (ikinci tur) — ADIM 7 İKİNCİ DALGA, A+B

⚠ **Bu blok tarih olarak yukarıdaki "10 Ağustos" bloklarından ÖNCE gelir ama dosyada
sonra durur.** Sebep append-only (KARAR 61) ve tarih sapması (B56): önceki turun kayıtları
`2026-08-10` yazıldı, ölçüm `2026-08-09` diyor. Sıra düzeltilmiyor; sebebi burada yaşıyor.

Üç commit, sıfır site kodu: `4102d2e` · `824a39e` · `1d6726d`.

**Turun kapsamı daraltılarak açıldı ve daraltma ölçüme dayandı.** Dört parça planlanmıştı.
**C parçası** (B53, token'ın URL yolundan HTTP başlığına geçmesi) claude.ai connector
diyaloğunda `Request headers` bölümü **hâlâ olmadığı için düşürüldü** — beta bu hesapta
açılmamış. **D parçası** (B51, `baglam.sh` küçültme) `03-sira.md`'nin kendi şartı gereği
koşulmadı: yol-token dururken tek güvenilir kanal `baglam.sh`. Tur **A+B** ile gitti.

**Yapıştırma paketi bu tur bilerek eksik geldi ve bir kez tümüyle boş geldi.** İlk pakette
sıfır içerik vardı; bağlam %100 MCP'den kuruldu — on beş çağrı, on beşi de beklenen cevabı
verdi, aranıp bulunamayan dosya olmadı. Birinci dalganın "oturdu mu" sorusu böyle
cevaplandı: iddiayla değil, yedek kanalsız çalışarak.

### `docs_karar(no)` — dört araç oldu

Sözleşme ölçülmüş biçim dağılımına göre yazıldı. Ledger'ın `kaynak` sütunu **dört** biçim
taşıyor (mekanik `:NNNN` 418 · `#` çapası 46 · kod/dist 8 · çıplak dosya adı 7 · toplam 479)
ve `#`'ın kendisi **beşe** ayrılıyor — promptun listesi dört sayıyordu, ölçüm beşinci
şekli buldu: **`#kNNN-blok`** (8 satır). `olcum-2026-08.md`'nin 8 Ağustos'taki `25 + 10`
ayrımı yanlış değildi, **kovası** yanlıştı: 25 = 17 saf + 8 blok.

Sınıflandırma **sırası** sunucuda sabitlendi ve **cevapta beyan ediliyor**
(`bos → kod_dist → mekanik → capa → ciplak → siniflandirilamadi`). Sıra ters çevrilirse
`kod_dist` ile `mekanik` arasında satır kayar — B38'e sabit bir zemin bırakıldı.

**`iliski` ayrıştırılmıyor** ve bu bir kapsam kararıdır, eksiklik değil: sütunun üç tanım
kararı verilmedi (saf/ok-dışı ayrımı 71–81 arasında oynuyor). Sunucu bir tanım seçseydi
ölçüm sessizce kirlenirdi.

### CC iki kez durdu, ikisi de haklıydı — ve ikincisi brief'i yazan tarafındı

**(1) DUR-8, tarih.** `date +%F` 9 Ağustos dedi, ledger 478/479 için 10 Ağustos yazıyordu.
CC raporladı, **çözmedi** — brief öyle diyordu. Sonra `git log --format='%ci' -10` istendi:
on commitin onu da 9 Ağustos. Sınıflandırmayı ham çıktı yaptı, CC değil. → B56.

**(2) DUR-4, `#kNNN-blok`.** Brief §1.5 blok çapasının `- **KARAR NNN**` madde başlığına
çözüleceğini yazıyordu. CC ölçtü: **8/8 hedefini bulamıyor.** Sebep de ölçüldü — blok
başlığı liste maddesi değil, bölüm başlığı (`### Tarih Dropdown … (KARAR 158, 159, 160, 171)`).
Bloğun doğası zaten bu: üyelerin tekil maddesi **yok**, tanım blok içinde yaşıyor (⊂N).

CC üç seçenek sundu ve kendi tavsiyesini verdi: kural icat etme, eksiği sessiz bırakma,
yazılmayan bir başarı yolunu tabloya koyma. **Tavsiye kabul edildi.** Reddedilen seçenek
(a) — blok başlığını arayan yeni bir çözümleme kuralı — meşru olabilirdi ama *"8/8 çözülür"*
ölçümü brief'i yazan tarafta **yapılmamıştı.** Sekiz hedef görülmeden kural mühürlenemez.
Bu, 10 Ağustos'un Railway `Root Directory` maddesinin aynı sınıfı.

**Desen değişmedi: brief yanılabilir, refleks yanılmamalı.**

### Brief'in kendi kusuru: enum iki ekseni karıştırıyordu

`capa_cinsi: elle | mekanik | cozulemedi` — ilk ikisi çapanın **nasıl konduğunu**,
üçüncüsü **çözülüp çözülmediğini** söylüyordu. Tek sütunda iki soru; elle konmuş ama
çözülememiş bir çapada enum bilgiyi yutuyordu. İkinci alan açıldı. → KARAR 480

### CC'nin istenmeden ürettiği ölçüm — elle yolun isabeti

Brief sormamıştı; CC 28 `#kNNN` çapasının hepsini ölçtü:
**bulundu 18 · bulunamadı 9 · birden çok 1** → isabet **%64**.

İçinde **üç ayrı cins** var ve tek sebep dizesi üçünü gizlerdi:
- `400 · 407 · 447` → hedef `20-ref-*`. O dosyalarda `- **KARAR NNN**` madde başlığı
  **hiç yok**; referans dosyaları prose taşır. **Kural eksik, kayıt değil** —
  KARAR 472 kronoloji için yazıldı.
- `114→366 · 154 · 196 · 223 · 350 · 380` → hedef kronoloji, madde başlığı yazılmamış.
  **Kayıt eksik** — B36-b'nin "71 kayıtsız karar" ailesi.
- `143` → 2 eşleşme. KARAR 465 gereği çapa olamaz. **Ayrı cins.**

Sunucu bu üçünü ayrı sebep dizeleriyle döndürüyor; `hedef_sinifi` yoldan türetiliyor,
kodda liste tutulmuyor. B38'e ölçülmüş girdi bırakıldı.

### Sığlık 119 değil 126 — ve fark bir düzeltme değil, genişleme

Guard taraması tüm ledger'a koşuldu:

```
mekanik → mekanik    418/418   siglik: karar-listesi-indeksi  119
                               siglik: komsu-karar-satiri       7
capa    → elle         18
capa    → cozulemedi   28      kod_dist 8 · ciplak 7
```

**119, `b36a-rapor.md`'nin rakamıyla bağımsız olarak örtüştü** — 8 Ağustos ölçümü
doğrulandı. Ama `komsu-karar-satiri: 7` o raporda **yok**: b36a yalnız indeks desenini
arıyordu, `docs_karar`'ın ikinci dalı (yakalanan N ≠ sorgulanan `no`) yedi satır daha
buldu. Ledger'ın ölçülmüş sığlığı **126/418 (%30,1)**.

B38'in karşılaştırma tabanı buna göre yazılır: `olcum-2026-08.md`'nin 119'u yanlış değil,
**dar**. Araç ilk gün kendi zeminini bir adım ilerletti.

### İki dal sınanmadı ve bu söylendi

`bos` (0 satır) ve `siniflandirilamadi` (0 satır) mevcut veriyle tetiklenemiyor — ledger
temiz. Savunma dalları; ölü kod değil ama **sınanmamışlar.** DUR-5 ("hiç ateşlenmeyecek
guard sessiz başarısızlıktır") ihlali sayılmadı, çünkü ayrım gerçek: *asla
ateşlenemeyecek* ile *bozulma hâlinde ateşlenecek* aynı şey değil. CC ayrımı kendi kurdu
ve söyledi.

### Canlı doğrulama — ve şemanın bayatlığı

Deploy sonrası `docs_envanter` **yeni** cevabı döndürdü (damga `1d6726d`, iki yeni alan).
Ama `docs_karar` claude.ai araç listesinde **yoktu** — liste hâlâ üç araçtı. Veri tazeydi,
**şema bayattı.** Connector kapatılıp açılınca dördüncü araç göründü ve dört vaka koşuldu:
`96` (sığlık bayrağı yandı) · `461` (çıplak ad, tahmin yok) · `159` (blok üyesi) ·
`407` (referans sınıfı — üçüncü dal canlıda ateşlendi).

Bu, B52'nin *"MCP: ayrışma denetimsiz"* uyarısının **ilk somut vakası** — ama tahmin
edilen yerde değil. → B57

### B54 — tek dize değil, iki alan

Borç metni *"Korpus git deposundan servis edilir"* dizesini öneriyordu. Kaynak okununca
düştü: `korpusuTara()` **git'i sormuyor**, `readdirSync` ile diski yürüyor. Railway'de
ikisi denk çünkü container bir checkout; yerelde denk değil. O dizeyi tek başına yazmak,
kodun arkasında duramadığı bir iddia olurdu.

⚠ **KARAR 479 kodda yazılı değil, ortamda yaşıyor.** 10 Ağustos'un "sapma değil tanım
farkı" teşhisi doğruydu — ama tanım kodda değil **deploy ortamında**. Yazılan iki alan
bunu ayırıyor: `kapsam_kurali` kodun yaptığını, `dagitim_notu` ortamın getirdiğini söylüyor.

**Yan bulgu:** `mcp/` `git ls-files`'da var, korpusta yok — *izlenen ama servis edilmeyen*
üçüncü sınıf. Bu tur boyunca sunucunun sözleşmesi kendi kodu görülmeden yazıldı.

### Mühürlenen iki karar

- **KARAR 480 — ÇAPA SÖZLEŞMESİ İKİ EKSENLİDİR (KALICI):** Bir çapanın **çözüm sonucu**
  (`capa_cinsi`: `elle` · `mekanik` · `cozulemedi`) ile **kökeni** (`capa_kaynagi`:
  `elle` · `mekanik`) ayrı alanlarda yaşar. Tek sütun ikisini birden taşıyamaz: elle
  konmuş ama çözülememiş bir çapada köken bilgisi yutulur ve satır "mekanik kusuru" gibi
  görünür. Çözülemeyen çapa ayrıca **sebebini hedefin sınıfıyla** söyler (kronoloji =
  kayıt eksik · referans = kural eksik · blok = tanım gereği tekil kayıt yok). `←466 · ↔472`

- **KARAR 481 — ÖLÇÜM ARACI KENDİ ZEMİNİNDEKİ KUSURU BAYRAKLA DÖNDÜRÜR (KALICI):**
  Bir sorgu aracı, dayandığı verinin **ölçülmüş kusurunu** cevabında taşımak zorundadır.
  `docs_karar` sığ çapayı `siglik: true` ile döndürür ve ölçümün kaynağını, tarihini,
  kapsamını yazar. Gerekçe KARAR 456'nın operasyonel karşılığıdır: *yanlış bir satır
  otoriter görünür ve kimse arkasına bakmaz* — ve arama kolaylaştıkça bu risk **artar**,
  azalmaz. Bayrak süs değil, aracın **varlık şartıdır**. Rakam koda gömülmez; ölçümün
  kaynağı işaret edilir (KARAR 470). `←456 · ↔470`

---

## 10 Ağustos 2026 — B01: kapı kapandı (bir commit, sıfır site kodu)

**Çalışma dizini bugünden itibaren `~/Desktop/hlaorpz/ocak`.** Remote `hlaorpz/ocak`,
Vercel proje adı `ocak`. Üç ad da hedefe oturdu; B01 sekiz Ağustos'tan beri açıktı.

⚠ **Patch 11 Ağustos'ta uygulandı, kaydettiği tur 10 Ağustos'ta koşuldu; ayrım
bilinçli.** Olay tarihleri turun kendi günü, yazım damgaları uygulama günü.
Tarih üçüncü kez ısırdı — **B56 açık**.

### Turun kendi sözleşmesi kırıldı, bilerek

Bugüne kadarki konvansiyon "her bash bloğu `cd ~/Desktop/hlaorpz/ocak-site-clone` ile
açılır" idi ve turun işi tam olarak o yolu değiştirmekti. Brief bunu tek cümleyle çözdü:
**her blok yolunu kendi ilk satırında söyler.** ADIM 0–2 eski yol, ADIM 3 üst dizin
(`cd ~/Desktop/hlaorpz`, `mv` oradan koşulur — repo dizininin içinden `mv` çalışır ama
kabuğun cwd'si silinmiş inode'a bakar kalır), ADIM 4+ yeni yol.

⚠ Ve ölçüm ortaya çıkardı ki o "konvansiyon" **hiçbir yerde yazılı değildi.**
`CLAUDE.md`'nin 114 satırında tek bir mutlak yol yok. B01 maddesi iki yerde
"düzeltilecek olan `CLAUDE.md` konvansiyon satırı" diyordu; ikisi de yanlıştı.
Konvansiyon bir kural değil, brief'ten brief'e kopyalanan bir alışkanlıktı — ve
alışkanlık, kaynağı olmadığı için kimse tarafından denetlenmemişti.

### Ölçüm

Kapsam: `git ls-files 'docs/*' 'scripts/*' 'CLAUDE.md'`, `docs/_arsiv/` hariç.
Araç `grep -Fn`. Birim = **eşleşen satır** (bir satırdaki iki geçiş tek sayılır).

| | |
|---|---|
| `cd ~/Desktop/hlaorpz/ocak-site-clone`, hepsi | 69 satır · 19 dosya |
| — arşiv | 56 satır · 11 dosya |
| — canlı | **13 satır · 7 dosya** |
| düzeltilen | 11 satır · 6 dosya |
| kalan (bilerek) | **3 satır** |

İşin %81'i arşive yığılmıştı ve arşive dokunulmadı. Düzeltilecek canlı yüzeyin
tamamına yakını (10/11) `docs/skills/` altındaydı.

**Bağımsız kanal doğrulaması.** `ocak-site-clone` → `ocak` tam 11 bayt kısalıyor.
`docs_envanter` boyutları grep'ten habersiz aynı sonucu verdi: beş skill dosyasının
her biri satır sayısı × 11 bayt küçüldü (arsivci −33, kararci −33, notion −22,
lint −11, teshis −11), `20-ref-site.md` +12 (yeni metin daha uzun), ve korunacak iki
dosya — `02-borclar.md`, `b36a-desen-tespiti.py` — **bayt olarak hiç değişmedi**.

### CC dört sapma bildirdi, dördü de haklıydı

1. **Brief "8 dosya" dedi, ölçüm 7 dedi.** Brief'in kendi tablosu yedi satırdı; metin
   ile tablo çelişiyordu. Aritmetik hata, brief'i yazan tarafta. Satır sayısı (13)
   doğruydu, kriter etkilenmedi.
2. **Adım 5'in yol biçimi varsayımı tutmadı.** Brief `/Users/kaan/Desktop/hlaorpz/
   ocak-site-clone` bekliyordu; `settings.local.json`'da o biçim **0 kez** geçiyordu.
   Var olan 12 geçişin hepsi öneksiz eski yoldu. Dosya `hlaorpz/` alt klasörüne
   taşınmayı hiç görmemişti — yani B01 açılmadan önce de bayattı.
3. **`notes.md`'nin üç geçişi aynı sınıf değildi.** Brief "aynı sınıf, aynı düzeltme"
   diyordu; CC ölçtü ve ayırdı: `:782` canlı çalışma dizini başlığı (düzeltildi),
   `:5869` ve `:6034` geçmiş turların anlatısı (KARAR 61, bırakıldı). Brief üçünü
   ölçmemişti.
4. **Tarih.** `date +%F` → `2026-08-10`; brief her yerde 9 Ağustos yazıyordu. 09 rakamı
   ölçülmemişti, açılış promptundan devralınmıştı — tam olarak KARAR 470'in yasakladığı
   şey. Ölçülen kazandı; brief dosya adı 09 kaldı, metinlerin hepsi 10 oldu.

Desen değişmedi: **brief yanılabilir, refleks yanılmamalı.** Dördünde de CC raporladı,
çözmedi.

### KARAR 465'in ikinci vakası — ölçen metin ölçtüğü dizeyi içerir

Brief'in ADIM 6.c doğrulaması `grep -Fn 'ocak-site-clone' .claude/*` çıktısının **boş**
dönmesini bekliyordu. Dönmedi: `notes.md` dokuz geçiş verdi. İkisi S3'te bilerek
bırakılan tarihsel kayıt, **yedisi turun kendi ölçüm metniydi** — "13 → 3" cümlesi eski
dizeyi alıntılamadan yazılamıyor. Yani kriter, brief'in 6.d'de **yazılmasını istediği**
kaydın kendisiyle çelişiyordu.

⚠ Ve bu tuzak brief'e uyarılmış olarak girdi: açılış promptu "bu turun kronoloji kaydı
`cd ~/Desktop/...` dizesini içerecek; yazıldığı anda sayı büyür" diyordu. Brief'i yazan
taraf uyarıyı ADIM 4.d'de doğru uyguladı (13 → **3**, sıfır değil) ve ADIM 6.c'de
unuttu. Aynı brief içinde aynı kural bir yerde tutuldu, bir yerde düşürüldü.

**KARAR 465'in bilinen ikinci vakası.** Birincisi 8 Ağustos'ta B44-a'daydı: `ocak-lint`
yasak-dize taraması altı satırda kendi tanımını yakalıyordu, çözüm tarihsel kayıt
muafiyetiydi. Ortak desen: **bir dizeyi ölçen metin o dizeyi içerir, ve `N → 0` biçimi
o anda yalan olur.** Kural yeni değil; eksik olan uygulanmasıydı, o yüzden ledger'a
satır eklenmedi.

Kriterin doğrusu şuydu: `notes.md`'de **canlı çalışma dizini satırı** (`:782`) 1 → 0.
Kalan geçişler kayıttır.

⚠ **Ve aynı tuzak kapanış patch'inin kendisinde üçüncü kez kuruldu.** Patch'in 5.3
bölümü açık borç sayacının yöntemini *"`✅ KAPANDI` içermeyen `## B` başlıkları"* diye
yazdı; bu yöntem **39** veriyor, oysa `02-borclar.md`'nin kendi taksonomisi **34**
diyor ve o rakam dosyanın beyanıyla birebir tutuyor. Fark beş satır: `✅ ÇÖZÜLDÜ`
damgalı B03 · B28, `❌ GERİ ÇEKİLDİ` damgalı B27, ve iş-değil sayılan B26 ⏸ · B30 🔵.
Yöntem **kelimeyi** arıyordu, **damgayı** araması gerekiyordu. Patch'in düzyazısı
34'ü kastediyordu, grep tarifi 39'u; ikisi aynı belgede çelişti. CC ölçtü, raporladı,
çözmedi; Kaan yöntemi düzeltti. Doğrusu: `[✅❌]` = kapandı, `[⏸🔵]` = iş değil,
açık = toplam − kapandı − iş değil.

### Dış ayaklar — üçü de ölçüldü, hiçbiri varsayılmadı

- **Vercel git bağlantısı kırılmadı.** `list_deployments` (10 Ağu): deployment
  `dpl_4mXj68…` READY, commit `50294e6`, `githubRepo: ocak`, `githubRepoId:
  1162985583` — değişmedi. Bağlantı ad değil **ID** üzerinden kurulu, Vercel rename'i
  kendiliğinden algıladı. B01 maddesinin "ölçülmeden varsayılmaz" şerhi doğru şerhti
  ve ölçüm onu doğruladı.
- **Railway/MCP sağlam.** `docs_envanter` damgası `2c29952` → `50294e6`. Sunucu yeni
  repo adından checkout alıyor. Brief bunu risk olarak yazmıştı çünkü Railway'in kaynak
  repo bağlantısı ölçülmemişti; ölçüldü, kırılmamış.
- **Vercel proje adı `ocak` oldu ama domainler olmadı.** `get_project` (10 Ağu):
  `name: "ocak"` ✔, `domains` üç `ocak-site-*` kaydını koruyor. Brief bunların proje
  adıyla birlikte yenileneceğini yazmıştı — **yanlış çıktı**. Vercel bu domainleri
  addan türetmiyor, kayıt olarak tutuyor. → **B58**. KARAR 389'un ikinci teyidi.

### Skill yüzeyi — ölçülemeyen tek ayak

`docs/skills/` beş dosya değiştiği için B50 bakım kuralı devreye girdi:
`skill-sync.sh sync` koşuldu, `--check` ayrışma yok dedi, beş zip claude.ai'ye elle
yüklendi (Kaan). `ocak-metin.zip` yeniden üretildi ama içeriği değişmedi, yüklenmedi.

⚠ **`--check` yükleme hakkında hiçbir şey söyleyemez** — zip'in içerikle uyduğunu
denetler, claude.ai'de hangi sürümün durduğunu değil. Bu B50'nin metninde zaten yazılı.
Bugün eklenen: **bayatlığın bedeli dosyanın ne taşıdığına göre değişir.**

- **Yol taşıyan skill bayat kalırsa gürültülüdür:** CC eski yola `cd` yapar, dizin yok,
  anında durur. Hata görünür.
- **Kural taşıyan skill bayat kalırsa sessizdir:** `ocak-lint/yasak-dizeler.tsv` eski
  listeyle geçer ve metin yayına çıkar; `ocak-kararci` yanlış numaradan devam eder.
  Ortada hata yok, yalnız **yanlış yargı** var.

Bugünkü değişim yol sınıfındaydı, yani ucuzdu. Bir sonrakinde şart değil.

Yükleme teyidi için `.yuklendi` damgası önerildi ve **reddedildi**: hiçbir şey ölçmeyen,
yalnız elle işaretlemeyi kaydeden bir dosya bürokrasidir. Ölçülemeyen bir yüzey
ölçülüyormuş gibi gösterilmez — bayrak yerine yokluğun yazılması yeğdir (KARAR 481'in
ruhu).

⚠ **Ve ölçülemezliğin üçüncü yüzeyi aynı gün kendini gösterdi.** Kapanış patch'i
uygulanırken `Skill` aracı altı skill'in hiçbirini açamadı: kayıt oturum başında
`ocak-site-clone` mutlak yoluna bağlanmıştı ve ADIM 3 o yolu sildi. CC `SKILL.md`'leri
diskten okuyup protokolü elle izledi — iş yapıldı, denetim yüzeyi düştü.
`--check` bunu **göremez**: symlink'i ve zip'i denetler, oturumun bağladığı yolu değil.
Yeni oturumda kendiliğinden düzelir, kalıcı hasar yok. → **B60**.

### B56 bugün daha da karıştı

Ledger 478/479'u `2026-08-10` diyor, ölçüm o günün **9 Ağustos** olduğunu söylemişti.
Bugün gerçekten 10 Ağustos ve B01 bu tarihe düştü. Yani `03-sira.md`'de "kapandı 10 Ağu"
yazan ADIM 7 birinci dalgası ile bugünkü B01 aynı tarihi taşıyor, oysa aralarında bir
gün var. **B56 kapsam dışıydı ve açık kalıyor** — ama çakışma bugünden itibaren
görünür hâle geldi, bu yüzden kaydediliyor.

---

## 11 Ağustos 2026 — B47: harita, ve haritanın kendi teşhisi (bir commit, sıfır site kodu)

`docs/05-harita.md` doğdu (147 satır, commit `7f0396a`). B47 sekiz Ağustos'tan beri açıktı
ve "en küçük iş, en geniş etki" diye işaretliydi.

### Maddenin başlığı yanlıştı — üçüncü vaka

B47 *"'ne nerede yaşar' haritası hiçbir dosyada yok"* diyordu. Ölçüm iki tane buldu:

- `CLAUDE.md` sonundaki **Doküman haritası** paragrafı — dosya listesi + ledger'ın indeks
  olduğu kuralı. CC'nin oturum başında gördüğü yüzey.
- `00-durum.md` başındaki **Ne arıyorsan / Nereye bak** tablosu — on iki satır, soru → dosya.

İkisi de kısmi, ikisi de geçerli, hiçbiri yanlış. Eksik olan **liste değildi.**

⚠ Ve eksik olanı defter kendisi teşhis etmişti. B55'in gövdesi aynen şunu yazıyor:
*"Tablo indeks, gövde otorite — ama bunu hiçbir yer yazmıyor."* Yani B47'nin gerçek
konusu B55'in gövdesinde duruyordu ve B47'nin başlığı onu görmemişti.

**Bu, üç gün içinde üçüncü vaka.** B01'de `02-borclar.md` "konvansiyon `CLAUDE.md`'de"
dedi, dosyada yoktu. Kapanış patch'inde sayaç yöntemi kendi düzyazısıyla çelişti.
Bugün B47'nin başlığı gövdelerin bildiğini bilmiyordu. Desen ortak ve artık ölçülmüş:
**madde beyanı gövdeden sapar, ve beyan okunup gövde okunmadan iş açılır.**

### Çözüm liste değil kural oldu

Üçüncü bir liste kurmak B55'in hastalığının üçüncü nüshasını üretirdi: üç yüzey, üçü
ayrışır, hiçbiri otorite. `05-harita.md` bu yüzden liste taşımıyor. İki şey taşıyor:

**(1) Otorite sırası — beş katman.** Ham gerçeklik (`git`, `dist/`, API) → kronoloji →
gövde metni → indeks/tablo/sayaç → ayna. Her katmanın dayanağı yazılı bir karara bağlı:
102 · 61 · 456 · 471.

**(2) Dosya sözleşmeleri.** Her dosya için dört alan: *ne taşır · ne taşımaz · kim yazar ·
nasıl bozulur*. Dördüncüsü yeni — bir dosyanın **bozulma biçimi** hiçbir yerde yazılı
değildi, oysa her birinin kendine has bir bozulması var ve hepsi sessiz.

### Mühürlenen karar

- **KARAR 482 — OTORİTE SIRASI: İNDEKS, İNDEKSLEDİĞİ GÖVDEYİ ASLA YENMEZ (KALICI):**
  İki yüzey aynı şey hakkında farklı şey söylüyorsa üstteki kazanır: ham gerçeklik →
  kronoloji → gövde → indeks → ayna. Üç işletim kuralı çıkar. **(a)** Sayaç, tablo ya da
  ledger hücresi indekslediği gövdeyle çelişiyorsa **indeks yanlıştır**; gövde sayılır,
  indeks yeniden yazılır, ters yön hiçbir zaman yapılmaz. **(b)** Türetilmiş dosya
  düzeltilir, append-only dosya **not alır** — aynı hataya iki dosyada aynı işlem
  uygulanmaz (KARAR 456 ↔ 61). **(c)** Konvansiyon, işaret ettiği kararla **birlikte**
  doğrulanır; sınanmamış konvansiyon kalıcı bir kuralı yürürlükten kalkmış bir karara
  bağlayabilir. Gerekçe: sistem üç ayrı borçta (B40 · B55 · B56) aynı soruyu üç kez
  soruyordu ve soru hiçbir yerde cevaplanmamıştı. `←102 · ↔456`

⚠ **Patch'in kendi sayımı yanlıştı.** §1 deltayı +7 dedi, gerçek +8 — 1.2'nin tek
satırlık çapası iki satırlık blokla değişiyordu ve 1→1 sayılmıştı. Ayrıca 5.3'ün
"~191/200" ifadesi ⟨ÖLÇ⟩ işaretsiz girmişti: ölçülmemiş, önceki satırdan devralınmış
bir rakam (KARAR 470 ihlali). CC ADIM 0'da ikisini de yakaladı. Aynı gün ikinci kez:
patch'i yazan taraf kendi ölçüm kuralını kendi metninde uygulamadı.

### Üç borç cins değiştirdi

**B40 · B55 · B56 açık kalıyor ama karar borcu olmaktan çıktı.** Üçü de "önce kural/sözlük
kararı, sonra yazım" diye yazılıydı; KARAR 482 üç kuralı da veriyor. Geriye mekanik yazım
kaldı — üçü tek turda kapanabilir.

### B58 kapandı, ve teşhisi yanlıştı

`c164ddd` push'unun deploy'u ölçüldü (`list_deployments`, 11 Ağu): `branchAlias`
`ocak-site-git-main-hlaorpz` → **`ocak-git-main-hlaorpz`** olarak yeniden türedi.
Deployment `name` `ocak-site` → `ocak`, `inspectorUrl` da düzeldi. B58 gövdesi
*"Vercel bu domainleri eklenmiş kayıt olarak tutuyor, addan türetmiyor"* diyordu —
**türetiyor.** Panelde görünmemelerinin sebebi eklenmiş kayıt olmaları değil, tam tersi:
sistem alias'ı oldukları için panelde silinebilir kayıt olarak listelenmiyorlar.

⚠ **Ve bir ölçüm tuzağı:** `get_project`, Kaan'ın domain düzenlemesinden **sonra** bile
eski dört domaini döndürdü ve `updatedAt` hiç değişmemişti. Önbellekli cevaptı. Değişmemiş
bir `updatedAt` cevabın taze olmadığının işaretidir — o okuma delil sayılmadı, delil bir
sonraki deploy'un meta'sı oldu. **KARAR 470'in yeni yüzü: rakamın yöntemi kadar aracın
tazeliği de beyan edilir.**

### CC'nin iki katkısı

**(1) Doğrulamayı dosyanın kendi kuralıyla yaptı.** Kural 1(c)'yi `05-harita.md`'nin
kendisine uyguladı: atıf yapılan on kararın onu da ledger'da tek kayıt ve KALICI, üç nicel
iddia (yedi `20-ref-*`, altı skill, cap 200) ölçümle tuttu. Yeni bir dosyanın kendi
kuralıyla sınanması bu turda ilk kez yapıldı.

**(2) Haritanın bulunamayacağını gördü.** `05-harita.md` doğdu ama `CLAUDE.md:107` ve
`00-durum.md:11` ondan habersizdi — "harita nerede" diye bakan ikisinde de göremezdi.
Kendi başına eklemedi, kapsam dışı olduğunu söyledi ve raporladı. Bu patch onu kapatıyor.
⚠ İroni kayda değer: haritanın kendi teşhisi *indeks gövdeden geri kalır* ve harita
doğduğu anda kendi indekslerinde eksikti. Fark şu ki bu sefer **aynı tur içinde** yakalandı.

---

## 11 Ağustos 2026 (ikinci tur) — B40 · B55 · B56: KARAR 482'nin ilk uygulaması

Üç borç, tek tur, tek commit `b72c539`. Üçü de sabah "önce karar, sonra yazım" diye
yazılıydı; KARAR 482 (aynı gün, birinci tur) üç kararı da verdi ve üçü mekanik yazıma indi.
**Bir kuralın işe yaradığının ölçüsü, açık borçları ucuzlatmasıdır** — bu turda üçünü birden.

### B40 — 88 KIRPMA'nın kardeşi değil, halefi

Konvansiyon `61/88` diye yazılıyordu. Ölçüm (`2026-05.md:3742`) KARAR 88'in gövdesini
buldu: 88 bir **dosya yapısı** kararı — master prompt'u `ocak-cekirdek.md` + `ocak-arsiv.md`
diye ayırıyor — ve ikinci yarısı *"Tam Dosya Değişim Modu"*. Gövdesi açıkça *"KARAR 61'in
patch protokolü iki dosyalı yapı için yetersiz kalıyordu"* diyor. Yani 88, KIRPMA
YASAĞI'nın kardeşi değil **patch modunun halefi** — ve kendisi `→145` ile SUPERSEDE.
Patch modu sonradan KARAR 462 ile geri geldi. KIRPMA metni 61'e ait
(`20-ref-protokoller.md:107`, A.23 açılışı).

Yani konvansiyon iki yönden yanlıştı: yanlış karara işaret ediyordu, **ve** işaret ettiği
karar yürürlükten kalkmıştı. KARAR 482 kural 1(c) tam bunun için yazılmıştı.

Canlı düzeltme **iki satır**. Kriter **28 → 26**, sıfır değil ve olamaz: kronoloji 16 +
`_uretilen` 7 + borcun kendi gövdesi 3 dokunulmaz.

⚠ Kronoloji banner'ları (`2026-02.md:4` · `2026-05.md:4`) bilerek bırakıldı. Tarihsel
kayıt değiller, dosya hakkında canlı beyanlar — düzeltmek cazipti. Ama append-only
dosyaya "sadece banner'a" dokunmak emsal açar. Kozmetik kazanç emsal riskini karşılamıyor.

### B55 — yön tek: gövde → tablo

Sahip tablosu gövdelerden yeniden türetildi. Beş bayat kalem çıktı: `B42 ✅` ve `B37 ✅`
(kapanmış olduğu hâlde tabloda), `B36-a` · `B36-b` (alt madde), `B28-kalan ayak`.
Küme eşitliği doğrulandı — `diff` boş, tablo 34 = açık madde 34. Tablo artık indeks
olduğunu **kendi altında beyan ediyor**; kapanış kriterinin üçüncü ayağı buydu.

⚠ **Kriterin doğru olması aracın doğru olmasını garanti etmiyor.** Brief'in türetme
awk'ı yalnız `- [ ] **Sahip:**` kalıbını arıyordu; B38 ve B39 checkbox'sız `- **Sahip:**`
biçimi kullanıyor ve sessizce düşüyorlardı. Küme eşitliği kriteri bunu **tutacaktı** —
ama CC ADIM 0'da aracı ölçtü ve kriteri beklemeden yakaladı. Kriter son savunmadır,
ilk savunma değil.

⚠ **Kuralda karşılığı olmayan durum: ortak sahiplik.** B12 · B53 (`Kaan+CC`) ve B60
(`CC+Kaan`). Türetme kuralı yalnız devir zincirini (`→`) kapsıyordu, `+`'yı değil.
CC ilk adı aldı ve **gösterim kararı verdiğini raporladı**. Karar doğruydu: "tablo
toplamı = açık madde sayısı" kriteri bir maddenin iki satırda görünmesini matematiksel
olarak yasaklıyor, ve "indeks giriş noktasını verir" ilkesi aynı yere çıkıyor. Kural
metni bunu söylemeliydi; söylemiyordu.

### B56 — iki dosya, iki ayrı işlem

`01-kararlar.tsv` türetilmiştir → 478/479 tarihi düzeltildi. `90-kronoloji/2026-08.md`
append-only'dir → başlık değişmedi, şerh aldı. Aynı hataya iki dosyada aynı işlemi
uygulamak yanlış olurdu; KARAR 482 kural 1(b) ayrımı veriyor.

**Şerh bloğun altına kondu, dosya sonuna değil.** KARAR 61 silmeyi ve yeniden yazmayı
yasaklar, şerh düşmeyi değil — ve dosya sonunda duran bir not, şerh düştüğü bloğu
okuyana hiç görünmez.

⚠ `## 10 Ağustos 2026` başlığı kronolojide **iki kez** geçiyor: `:1107` yanlış
(9 Ağustos işi), `:1381` doğru (B01). Şerh ayrımı yazıyor.

⚠ **Dört satır bilerek bırakıldı.** Şart "iddia satırın kendi metninden görünsün"dü.
Dört `10 Ağustos` referansında görünmüyor: B53'ün kaynağı "ADIM 7 ek brief" diyor ama
dalga demiyor; B54'ün ölçüm satırında tur adı yok; B55'in iki satırında "kapanış patch'i"
hangisi belirsiz. Dördü de muhtemelen 9 Ağustos işi — ama **muhtemelen düzeltme gerekçesi
değildir.** Canlı `10 Ağu` toplamı 29 → 23.

### Ölçüm zemini tur ortasında kaydı — 465'in akrabası, aynısı değil

ADIM 0'da ölçülen satır numaraları ADIM 1–2'nin getirdiği +5 satırla geçersizleşti
(`02-borclar.md` 1150 → 1155). Numaraya göre düzeltilseydi sekiz satırın **hepsi** yanlış
yere yazılacaktı. CC ilk denemede fark etti ve düzeltmeyi içerikten yeniden ölçerek yaptı.

Bu, üç gündür tekrarlayan ailenin **ikinci kolu**. Birinci kol *ölçen metin ölçtüğü
dizeyi içerir* — 6.c'nin grep'i, patch'in 5.3'ü, satır sayacının kendisi. İkinci kol
*ölçen işlem ölçtüğü zemini kaydırır* — bu vaka. KARAR 465 satır-numarası çapasını zaten
yasaklıyor; buradaki ihlal çapada değil **ölçüm sonucunun ömründe**: ADIM 0'da ölçülen
bir satır numarası ADIM 2'de artık aynı satırı göstermiyor.

Aynı gün `03-sira.md:79-80` de bunu doğruladı: sahip tablosunu ":14" diye işaret
ediyordu, tablo `:20`'ye, sonra `:25`'e kaymıştı. İşaretçi iki kez drift etmişti.
Düzeltmede satır numarası **bilerek yazılmadı** — işaretçi yerine kural yazıldı.

### Bir turun düzeltmesi başka bir dosyanın canlı beyanını yalanladı

`03-sira.md:79-80` *"sahip tablosu bayattır, ondan okuma"* diyordu. ADIM 2 tabloyu
tazeleyince o uyarı yanlışa döndü — ve okuyucuyu taze bir tablodan **uzaklaştırıyordu**.
CC gördü, kapsam dışı olduğunu söyledi, kendi başına dokunmadı, sordu. Aynı commit'e
katıldı: B55'in kendi kapsamıydı, yeni konu değil.

**Ders:** bir borcu kapatan tur, o borcun **uyarılarını** da kapatmalı. Uyarı da bir
indekstir ve gövde değişince bayatlar.

### Mühürlenen karar

- **KARAR 483 — BELGENİN DÜZYAZI RAKAMI KENDİ GÖVDESİYLE SINANMADAN YAZILMAZ (KALICI):**
  Bir brief, patch ya da rapor hem düzyazıda hem tabloda/listede aynı niceliği taşıyorsa,
  yazan taraf ikisini **birbirine karşı** sınar; sınanmamış düzyazı rakamı yazılmaz.
  Gerekçe ölçülmüş dört vaka, üç gün: **(1)** B01 briefi "8 dosya" dedi, kendi tablosu
  yedi satırdı. **(2)** B01 kapanış patch'i sayaç yöntemini `KAPANDI` kelimesiyle tarif etti,
  düzyazısı 34 kastediyordu, tarif 39 veriyordu. **(3)** B40/B55/B56 briefi "24 satır" dedi,
  kendi tablosu 28'e topluyordu — ölçüm de 28. **(4)** aynı brief "3 dosya" dedi, kendi
  listesi dört sayıyordu, gerçek beşti. Dördünde de **tablo doğru, düzyazı yanlıştı** ve
  dördünde de yazan taraf sonucu elden geçirmek yerine hatırladığından yazmıştı. Dördü de
  CC'nin ADIM 0'ında yakalandı — yani mevcut savunma çalışıyor, ama savunma **son** hattır.
  KARAR 470 rakamın **yöntemini** beyan ettirir; bu karar rakamın **kendi belgesiyle
  tutarlılığını** ekler.
  ⚠ **Beşinci vaka bu kararın kendi patch'inde çıktı:** cap tahliyesinin düzyazısı
  "22 satır" dedi, dosya 18 satır tutuyordu. İlk dördünden farklı bir kol — orada çelişki
  düzyazı ↔ tablo idi, burada düzyazı ↔ **dosyanın kendisi**. Kararın metni "hem düzyazıda
  hem tabloda/listede" diyor ve bu vakayı dar okumada kapsamıyor. **Kapsam genişletilir:**
  rakam nerede duruyorsa dursun, sınanacağı yer onu üreten gerçekliktir — tablo, liste
  ya da dosyanın kendisi. Ve tanım sayıdan önce yazılır: sınır tarifi bayatlamaz, sayı
  bayatlar. `←470 · ↔465`

---

## 11 Ağustos 2026 — CAP TAHLİYESİ: `00-durum.md` ADIM 1–6 blokları

`00-durum.md` 192/200 satıra dayanmıştı (KARAR 457 hard cap). Bu turun kayıtları +7
satır getiriyordu → 199, cap'e bir satır. Tahliye zorunlu hâle geldi.

**İçerik silinmedi, taşındı** (KARAR 61). Aşağıdaki **18 satır** `00-durum.md`'nin
"ŞU AN NEREDEYİZ" bölümünden birebir alındı ve orada üç satırlık bir özetle
değiştirildi. Metin 6–8 Ağustos doküman mimarisi geçişinin ADIM 1–6 kaydıdır;
tam anlatısı bu dosyanın 6–8 Ağustos bloklarında zaten yaşıyor — buradaki, durum
dosyasının o tarihteki **özet** hâlidir ve o hâliyle korunur.

⚠ Patch'in düzyazısı "22 satır" diyordu; dosya 18 tutuyordu. Sınır tarifi doğruydu
(`ADIM 1–2` satırından `ocak-metin taslak-only` satırına kadar, `ADIM 7 birinci dalga`
kalır), yalnız sayı yanlıştı — KARAR 483'ün beşinci vakası, kararın kendi patch'inde.

- **ADIM 1–2 ✅** — KARAR envanteri + ledger durum sütunu (453 satır).
- **ADIM 3 ✅** — Pilot bölünmesi + bu dosya + `20-ref-*` beşlisi + kronoloji dilimleri.
- **ADIM 3b ✅** — KARAR arkeolojisi. TEYITSIZ 27 → 3. B05·B06·B13·B20 kapandı;
  454 sahte satırı `REZERVE`'e döndü. Kalan: **B33** (ledger `kaynak` dönüşümü, CC,
  KARAR 465) ✅ kapandı. **B32 ✅** — `ocak-referans.md` dağıtıldı; beşli yedili oldu (`20-ref-program.md` + `20-ref-marka.md`). Kaynak dönüşümü aynı turda kapandı (KARAR 467).
- **ADIM 4 ✅** — `CLAUDE.md` (kök, dokuz bölüm) + `scripts/baglam.sh` (beş profil:
  `kod · icerik · marka · bot · dokuman`, manifest satırı + eksik-dosya guard'ı) +
  project files silme izni (14/14 ledger hedefi yaşıyor, sıfır `ÖLÜ`).
  Yedi `20-ref-*` dosyasının hepsi en az bir profilde. B01 (klon yeniden adlandırma) açık.
- **ADIM 5 ✅** — `ocak-arsivci` · `ocak-teshis` · `ocak-lint` doğdu; `skill-sync.sh`
  symlink+zip (KARAR 473). B42 kapandı. `ocak-notion` sapması ADIM 6'da çözüldü (KARAR 477).
- **B44-a ✅** — `ocak-lint` yasak-dize kapsam çelişkisi kapandı. `her yerde` kapsamı
  altı satırda kendi tanımını yakalıyordu; tarihsel kayıt muafiyeti eklendi (KARAR 465).
- **B36-a ✅** — desen ölçüldü, **yöntem yetersiz** çıktı (nokta örneklemesi 2/5).
  Mekanik taşıma turu açılmadı; iş B36-b'ye devretti ve **büyüdü**.
- **ADIM 6 ✅** — `ocak-kararci` · `ocak-metin` · `ocak-notion` doğdu; kadro altıya
  tamamlandı (KARAR 458). Sınırlar mühürlendi: 475 kararci↔arsivci · 476 metin↔lint ·
  477 notion dar kapsam. `ocak-metin` taslak-only, en az üç ay (KARAR 459).

## 18 Ağustos 2026 — MailerLite ödeme kapısı + alan envanteri

- **Ödeme kapısı ✅** — `mailerLiteEkle` ödeme durumuna bakmıyordu: kartla ödeyecek
  kadın checkout'a yönlenmeden önce `zoom_link` + `zoom_sifresi` ile MailerLite'a
  ekleniyordu; ödemeyi yarıda bırakırsa Zoom bilgisi elinde kalıyordu. Banka kart
  entegrasyonu reddedilip havale/EFT'ye dönülünce teorik risk gerçek oldu — para
  artık kayıttan günler sonra geliyor. Abone kaydı yerinde bırakıldı (kadın kaydının
  ulaştığını öğrenmeli), yalnız **katılım alanları** koşullandı: `odemeGerekli=true`
  iken `katilim_linki` · `zoom_link` · `zoom_sifresi` · `etkinlik_adres` boş gider.
  `etkinlik_mekan` gider — gizlenen kapı numarası, şehir değil. Ayırıcı yalnız
  `odemeGerekli`; format varsayımı yok (Açık Kapı da ücretli olabilir). Havale de
  kapalı — KARAR 220'nin success ekranına verdiği kural maile de uygulandı.
- **Alan hijyeni ✅** — payload artık **on bir alanın hepsini** her kayıtta yazıyor,
  geçersiz olan boş string ile. Eskiden boş alan hiç gönderilmiyordu; MailerLite
  subscriber alanları kalıcı olduğu için online kayıttan sonra yüz yüze kayıt yapan
  kadının `zoom_link`'i eskisiyle dolu kalıyor, mail ona geçen ayın linkini
  gösterebiliyordu. Ayrıca `etkinlik_saati` artık **mekâna göre** besleniyor
  (online → `Zoom Başlangıç Saati`, fiziksel → `Saat`); eski `zoomSaat || klasikSaat`
  düz OR'du ve fiziksel kakao 20:00-23:00 sürerken mailde "20:00" yazıyordu.
- **Üç yeni field ✅** — `referans_no` (havale açıklamasının eşleştirme anahtarı,
  muaf dahil daima) · `odeme_durumu` (`bekliyor`/`muaf`; üçüncü değer `alindi`
  kod tarafından yazılmaz, n8n işi) · `etkinlik_basligi` (Notion `Başlık`;
  `etkinlik_adi` format+tarih üretirken bu buluşmanın kendi adını taşır, ikisi
  ayrı yaşar).
- **Alan envanteri `20-ref-bot.md`'ye kondu** — `:82`'deki plan cümlesi üç alan
  öngörmüştü (`zoom_link`, `event_title`, `event_date`); ikisi hiç var olmadı,
  isimler Türkçeleşti, sayı on bire çıktı. Plan cümlesi **silinmedi**, üstüne
  "gerçekleşen hâli aşağıda" notu düşüldü (KARAR 61). Envanter ölçüm tarihi +
  kaynak satır damgalı; `MAILERLITE_ALANLAR` dizisi tek kaynak ilan edildi.
  Bayat sayı `90-kronoloji/2026-06.md:21` ile `_arsiv/ocak-pilot-v52.md:342`'de
  duruyor — append-only, dokunulmadı.
- **B62 açıldı** — `katilimTipiCoz` bilinmeyen/boş `Mekân/Platform` değerinde
  `link`'e düşüyor; fiziksel etkinlikte o alan boşsa adres alanları sessizce boş
  gider. Bugün yakmıyor (ölçüldü: canlı kayıtların hepsinde dolu), kapsam dışı
  bırakıldı.
- **`etkinlik_url` ✅ (on ikinci alan)** — Notion `Slug` → `https://ocak.biz/etkinlik/{slug}`.
  Taban `astro.config.mjs` `site` ile aynı; `publicOrigin(request)` **bilerek
  kullanılmadı** — preview deploy'dan gelen kayıt maile ölü bir preview URL'i
  yazardı, mail kalıcı yüzey. Slug boşsa **boş string**, kırık taban URL üretilmez
  (`etkinlikUrlFormatla`). Alan hijyeni buna da uygulandı; kapıya tabi değil,
  sayfa herkese açık. **Yayın gecikmesi ölçüldü:** `bir-esikte-duruyorsun` 200,
  `elin-neyle-dolu` · `nereye-kadar-senin` · `hangi-tohumu-ekeceksin` **404** —
  Notion'da var, production henüz almamış. Yeni etkinlik açılan gün kayıt gelirse
  mail o an ölü linke bakar; envantere uyarı olarak yazıldı.
- **Kanonik adres www'ye hizalandı ✅** — köksüz `ocak.biz` **307** ile
  `www.ocak.biz`'e dönüyordu, yani gerçekte servis eden host www; ama
  `astro.config.mjs` `site` www'suzdu — canonical etiketi **kendini yönlendiren**
  bir adrese işaret ediyordu. `site` → `https://www.ocak.biz`;
  `Layout.astro` canonical + og:image fallback'i ve `etkinlikUrlFormatla` aynı
  hizaya çekildi, `robots.txt` sitemap satırı da. `dist` teyidi: canonical ·
  `og:url` · `og:image` üçü de www, köksüz kalıntı **sıfır**. Sitemap `site`'tan
  türediği için kendiliğinden düzeldi.
  **Dokunulmayanlar:** `instagram.com/ocak.biz` (hesap adı, domain değil) ·
  `davet@mail.ocak.biz` (gönderen domaini) · `remark-ocak-sections` link
  normalize regex'leri (zaten `(?:www\.)?` taşıyor, www-agnostic) ·
  `mesafeli-satis.astro` sözleşme metnindeki "ocak.biz internet sitesi" ifadesi
  (hukuki metin, URL değil — ayrı karar).
- **Ölçüm düzeltmesi** — önceki maddede "dört slug 404" yazılmıştı; aynı gün
  yeniden ölçüldü, altısı da **200** (hem www hem köksüz). Fark www değil
  **zaman**: arada production rebuild geçti. Yayın gecikmesi mekanizması gerçek,
  ama 404 penceresi rebuild aralığı kadar dar — envanterdeki kanıt satırı
  buna göre düzeltildi.

---

## 18–19 Ağustos 2026 — Çember bekçisi · MailerLite alan mimarisi · Y1

Tek dilim. İki gün, iki yüzey: içerik (bekçi dosyaları, etkinlik sayfaları) ve
altyapı (MailerLite alanları, ödeme kapısı, deploy zinciri).

### İçerik yüzeyi

- **Çember bekçisi kuruldu.** `ornekler-cember.md` (198 satır) dört sayfadan
  damıtıldı; `ocak-etkinlik` skill'i artık **iki bekçili**. Çember'in kendi sabit
  blokları var: `ne-olur` **on paragraf** (dördüncüsü değişken) · `kim-tutuyor`
  son paragrafı çember-özel · `pratik-bilgi` **beş madde**, kayıt ikiye ayrık.
  Bant **3.824–4.113 kr** — Açık Kapı'nın (3.019–3.247) belirgin üstünde; iki
  format aynı bekçiyle beslenemezdi, ayrışma ölçümle doğrulandı.
- **Açık Kapı sabit bloğuna üçüncü uç eklendi** — *"çoğu zaman o akşam değil
  ertesi sabah gelir"*. Altı Notion kaydına elle geçirildi, künye ölçüldü.
- **Altıncı Açık Kapı sayfası** (`nereye-kadar-senin`, tema Sınır) on bir yüzeye
  işlendi; yay `tanı → adlandır → sına → ayıkla → koru` oldu.
- **Hasat Seremonisi** yazıldı (`hasat-ve-sukran`, 22 Eylül, online, 4.254 kr).
  Kakao seremonisi başka sohbette yazıldı, buraya yalnız ölçüm için getirildi.
  İki seremoni bandı **943 kr** — iki nokta eğri vermiyor, bekçi damıtılamadı (**B66**).

### Altyapı yüzeyi

- **MailerLite alan mimarisi on ikiye çıktı.** Eklenenler: `referans_no` ·
  `odeme_durumu` · `etkinlik_basligi` · `etkinlik_url`. Envanter `20-ref-bot.md`'ye
  kondu, ölçüm damgalı, dört senaryoluk matris yazıldı. Üç dokümanda bayat liste vardı.
- **Ödeme kapısı** yazıldı (KARAR 486) · **alan hijyeni** yazıldı ·
  `etkinlik_saati` mekâna göre beslenmeye başladı (fiziksel kakao artık 20:00
  değil **20:00–23:00** alıyor; eski `zoomSaat || klasikSaat` OR'u mekâna
  bakmadığı için Zoom saati hep kazanıyordu).
- **Kanonik adres** `www.ocak.biz`'e çekildi (`688bee5`).
- **Y1 — boş alan filtresi.** `api/kayit.ts:386` `if (v && v.trim())` ile boş
  string'leri payload'dan eliyordu; helper on iki alan üretirken tele **7–10**
  çıkıyordu. Canlı kanıt: `kaan.sonat@gmail.com` abonesinde Slug'ı boş bir
  etkinliğe (*"Konuk Ateşi"*) kaydolunca `etkinlik_url` **iki kayıt önceki**
  değerde kaldı — mail doğru buluşmayı yazıp *"Ekmeden Önce"* sayfasına
  götürüyordu. Aynı mekanik ödeme kapısını da deliyordu: ücretli kayıtta boşlanan
  `zoom_link` gönderilmediği için önceki link yerinde kalıyor, kapı yalnız **ilk
  kez** kayıt olan kadında kapanıyordu. Düzeltildi (`92e580e`): payload kurulumu
  `mailerLiteFieldsPayload` ile lib'e taşındı, `name` muaf tutuldu, **yedi test
  taşıma katmanına** yazıldı (193/193). `dist/` ölçümü: dört senaryoda 12/12,
  düşen 0. Canlı doğrulama: `etkinlik_url` boşaldı, `name` korundu.
  Aynı filtrenin ikizi `forms-backend.ts:43`'te ölçülüp bilinçli bırakıldı (**B63**).
- **Promote kazası.** `astro-iskelet` build'i 18 Ağustos'ta bir kez production'a
  **promote edildi** (`dpl_ESRL`, meta `action: promote`). Site o pencerede 82
  commit geriye döndü; o aralıkta yapılan kayıtlar eski kodu koştu ve MailerLite'a
  **altı alanlı** eski küme yazıldı. Karma abone tablosunun ("dördü bayat, altısı
  güncel") sebebi buydu — **kod kırığı değil, sürüm kırığı**. Teşhis imzası kayda
  değer: bayat kalan alanlar tam olarak o turda eklenen alanlardı.
- **403 yanlış alarmı.** CC'nin ~7 dakikalık, 10 saniyede bir curl döngüsü Vercel
  bot korumasını tetikledi; sonraki bütün curl istekleri `x-vercel-mitigated:
  challenge` ile 403 döndü. "Site çöktü" sanıldı — gerçek tarayıcıda sorun yoktu,
  ziyaretçi etkilenmedi. Teşhis yüzeyi kapandı, yanlış alarm üretildi (KARAR 487).

### Açık sorunun cevabı — `astro-iskelet` nedir, neden duruyor

Patch bunu CC'ye ölçtürdü. Ölçüm (`git rev-list --left-right --count`, `merge-base`):

- Dalın ilk commit'i **`1838344`, 20 Şubat 2026** — bu **özgün geliştirme dalı**.
- Son commit'i **`7bf265b`, 6 Ağustos 2026**. O tarihten sonra **hiç ilerlemedi**.
- `main`'e göre **0 ileri / 85 geri**.
- `merge-base(astro-iskelet, main) == 7bf265b` — yani dal, `main`'in **tam atası**.
  Üzerinde `main`'de olmayan **tek bir commit yok**; taşıdığı benzersiz iş sıfır.
- `main`'de olup dalda olmayan ilk commit **`90fca7e`, 6 Ağustos** — ledger sapma
  düzeltmesi merge'ü. Dal o gün `main`'e katıldı ve orada bırakıldı.

Yani `astro-iskelet` bir "preview tamponu" değil, **terk edilmiş ata dal**. Şubat'tan
6 Ağustos'a kadar gerçek geliştirme dalıydı; merge'den sonra kimse dokunmadı ama
deploy hook'u üzerinde kaldı. En az **13 gündür** içerik build'leri oradan alınıyor.
Dalın silinmesi hiçbir iş kaybetmez — ama önce hook taşınmalı (**B64**), yoksa
içerik build'i tamamen kırılır. `00-durum.md` dal modeli satırı "preview tamponu"
diyordu; **ölçümle yanlışlandı**, düzeltildi.

### Ölçüm disiplini

- **Aynı hata sınıfı beş kez yakalandı** — rakam iki yüzeyde yaşayınca biri bayatlıyor.
  `02-borclar.md` yöntem paragrafı 24 derken aritmetiği 27 kullanıyordu · künye iki
  satırlık beyanın ilk satırından yanlış okundu · kayıt sorusu sayımı "üç sayfa"
  denip 4/6 ölçüldü · 4/6 hem kuralda hem notta yaşadı · MailerLite alan listesi üç
  dokümanda bayattı. Dördüncüsünde yapı değişti (KARAR 484). CC dördünü kendi buldu
  ve raporladı — *"yokluktan yapılandırma çıkarmışım"* itirafı dahil.
- **`mailerLiteEkle` taşıma katmanı ilk kez test altına girdi.** Filtre iki commit
  boyunca sağ kalmıştı çünkü `src/pages/` altındaki hiçbir dosya test edilemiyor
  (her dosya route sayılır). Saf mantık lib'e taşınmadan test yazılamıyor — desen
  `havale-vade.ts` başlığında zaten yazılıydı, bu turda ikinci kez uygulandı.
- **Eşzamanlı iki CC oturumu koştu** (KARAR 463 ihlali). ADIM 0 ölçümleri `ab92ffc`'ye
  karşı alınmıştı; ölçüm sürerken öteki oturum `834d620` · `6dea0b0` · `688bee5`
  commit'lerini attı ve rapor iki kez yeniden ölçülmek zorunda kaldı.

### Mühürlenen kararlar

- **KARAR 484 — BİR RAKAM TEK YÜZEYDE YAŞAR (KALICI):** Kural bir yerde, envanter
  başka yerde; sayı yalnız birinde. Rakam değişince brief'in listelediği yerler
  yetmez — **dosya ailesi grep'lenir**. Gerekçe: bu oturumda aynı hata sınıfı beş
  kez çıktı (yukarıda dökümü). Dördüncüsünde yapı değişti; kural ile envanter
  ayrıştırıldı. KARAR 470'in uygulaması, 483'ün kardeşi.
- **KARAR 485 — İÇERİK BUILD'İ KOD DALIYLA AYNI YERDEN GELİR (ACIK-BORC):** Deploy
  hook'u `main`'e bakar. Gerekçe: `notion-content-update` (`tZR9LcwJq9`)
  `astro-iskelet`'e bakıyordu — `main`'den 85 commit geride, terk edilmiş ata dal.
  Her Notion güncellemesi ve gecelik cron oradan build alıyordu; build'ler preview
  olarak doğuyor, içerik siteye düşmüyordu. 18 Ağustos'ta bir kez production'a
  promote edildi ve site 82 commit geriye döndü. Yeni hook
  `notion-content-update-main` (branch `main`) oluşturuldu; **URL geçişi B64'te**.
- **KARAR 486 — KATILIM LİNKİ ÖDEME ALINMADAN HİÇBİR YÜZEYDE GÖSTERİLMEZ (AKTIF):**
  KARAR 220 success ekranı için verilmişti; aynı kural **maile de** uygulanır —
  iki yüzey tek kural. `odemeGerekli === true` iken `katilim_linki` · `zoom_link` ·
  `zoom_sifresi` · `etkinlik_adres` MailerLite'a **boş** gider. `etkinlik_mekan`
  **gider**: şehir gizli bilgi değil, gizlenen kapı numarasıdır. Ayırıcı **yalnız**
  `odemeGerekli`; format bazlı varsayım yapılmaz — Açık Kapı da ücretli olabilir.
  Havale de kapalıdır: para kayıttan günler sonra gelir, hiç gelmeyebilir.
- **KARAR 487 — CANLI PRODUCTION POLLING İLE DÖVÜLMEZ (KALICI):** Deploy doğrulaması
  için canlı siteye çıplak curl döngüsü kurulmaz. Gerekçe: 18 Ağustos'ta ~7 dakikalık
  bir döngü Vercel bot korumasını tetikledi; sonraki bütün curl istekleri 403
  (`x-vercel-mitigated: challenge`) döndü. Gerçek tarayıcı ve ziyaretçi etkilenmedi
  — ama teşhis yüzeyi kapandı ve "site çöktü" yanlış alarmı üretildi. Doğrulama
  **gerçek tarayıcıyla** ya da **tek atışlık** istekle yapılır. KARAR 355'in kardeşi:
  o "dist'ten konuş" der, bu "canlıyı yorma" der.

### Şerh — `9fca383` commit mesajındaki `<details>` beyanı yanlıştı (19 Ağustos)

`9fca383` (Faz 1 §2) gövdesi *"Form üç native `<details>` bölümden oluşuyor,
hepsi `open` doğuyor"* diyor. Aynı gün ek brief turunda canlı ölçüm bunu
yanlışladı: bölüm sayısı **dört**, ikisi **kapalı** doğuyor — Sen ✓ · Buluşma ✓ ·
**Niyet ✗** · **Ödeme ✗**. Yani sessiz-submit riski o commit'in varsaydığı gibi
"kadın bir bölümü kapatırsa" koşuluna bağlı değil, **varsayılan hâlde zaten var**.
Bugün patlamıyor çünkü kapalı iki bölümde zorunlu alan yok; Notion'a zorunlu bir
kayıt sorusu eklendiği gün canlıya çıkar. Commit mesajı **düzeltilmedi** (KARAR 61
— tarihsel kayıt olduğu gibi kalır); gerçeğin kaydı burada ve
`src/lib/gecersiz-alan-goster.ts` başlığında yaşıyor, davranışın bekçisi de
`gecersiz-alan-goster.test.ts`'te (dört mutasyonla sınandı). Düzeltme `66fa842`.

---

## FAZ 1 — ÖDEME YÜZEYİ (19 Ağustos 2026)

Zemin `9036791` (main, production, `dpl_AkJmKh23JhRqE3vSzN9BF3rTmJdn`).
Dönem zinciri: `92e580e → 1546622 → 06bb404 → 08e2806 → 3683ca4 → 9fca383 →
66fa842 → 9036791`.

- **KARAR 488 — KART AKIŞI ANAHTARI (AKTIF):** Kredi kartı entegrasyonu
  gerçekleşmedi. Kart kodu **silinmedi**, tek env anahtarının (`KART_AKISI`)
  arkasına alındı. Altı tüketici: `api/kayit.ts` kart dalı · `odeme-callback` ·
  `KayitFormu` yöntem grubu SSR gate · üç `/odeme/*` route · `oda-map` ·
  sitemap filtresi. Geri açmak env + sağlayıcı entegrasyonu olsun, **arkeoloji
  olmasın**. `PAYMENT_PROVIDER` bu işi yapamaz — `mock`/`iyzico` dışı her değer
  throw eder. Varsayılan **kapalı**: brief'te yazmıyordu, boşluk dolduruldu —
  ödeme yüzeyi fail-open olamaz, anahtarı koymayı unutmak sağlayıcısı olmayan
  bir akışı sessizce geri açardı. Yüzey kalkınca iki hata çıktı (brief'te
  yoktu, ölçümle bulundu): `odeme_yontemi` FormData'da hiç bulunmayınca eski
  kod 'kart'a düşürüyordu → ücretli her kayıt 400 alırdı; `seciliYontem()`
  seçili radyo yokken 'kart' dönüyordu → CTA "Karta Yönlen" yazıp yalan
  söylerdi.

- **KARAR 489 — REFERANS KODU FORMATI (AKTIF):** 6 haneli rakamsal format terk
  edildi, `OCAK-` + 4 karakter geldi. İki gerekçe: (i) kod banka açıklamasına
  **elle** yazılacak; (ii) banka açıklaması zaten rakamla dolu (tutar, tarih,
  telefon, IBAN parçası) — salt rakamsal kod metnin içinde ayırt edilemez,
  Faz 2'nin otomatik eşleştirmesi böyle bir kodun üstüne kurulamaz. Uzay
  29⁴ = **707.281**, doğum günü %50 eşiği **~990** (1,1774·√N), son çare 29⁵ =
  20.511.149. Uzay **küçüldü** (900.000 → 707.281) ve bu bilinçli bir takas:
  benzersizlik Notion sorgusuyla zaten korunuyor, lansman hacmi üç haneli.
  Ampirik doğrulama bundle'dan: 20.000 kod → **19.715 benzersiz**; beklenen
  çakışma n²/2N ≈ 283, gözlenen **285**. 15 maddelik kara liste, **alt dize**
  eşleşmesiyle (5 karakterlik son çare kodunda `OCAK-FUCK7` yalnız alt dize
  yakalar), `refKodUret` içinde **tek yerde** — iki döngüye kopyalansaydı biri
  güncellenip öteki unutulurdu. Notion'da 5 ve 6 haneli eski kodlar duruyor,
  **migration yok**. `↔490`

- **KARAR 490 — ALFABE ÖLÇÜTÜ (KALICI):** Ölçüt "karışıyor mu" değil,
  **yanlış okuma GEÇERLİ bir kod üretiyor mu**. `Z`→`2`: `2` alfabede var,
  kadın yanlış kodu bankaya yazar, kayıt **sessizce** başkasına eşleşebilir →
  atıldı. `L`→`1`: `1` alfabede yok, kod geçersiz olur, eşleşme bulunmaz, hata
  **gürültülü** çıkar → kaldı. Ölçüt `B`(→8) ve `S`(→5) elemelerini geriye
  dönük açıklıyor — alfabe sezgiyle kurulmuştu, kural onu sonradan buldu.
  Brief kendi içinde çelişiyordu (dışlama listesi L ve Z'yi sayıyor, verdiği
  alfabe dizesi ikisini de içeriyordu; "30 karakter"/"30⁴" rakamları dizeyi
  doğruluyordu); çelişki raporlandı, karar bu ölçütle geldi. `↔489`

- **KARAR 491 — KANITIN TAŞINMASI (KALICI):** Bir brief maddesini yanlışlayan
  kanıt, **aynı brief'in öteki maddelerine de** uygulanır. Doğuşu bu turun
  kendi hatası: ek brief'in §1 ve §2 maddeleri aynı belgede geldi. KARAR
  156'nın ölçümü (`scrollIntoView`/`scroll-margin-top` bu sitede güvenilmez —
  Çember 38.87, Açık Kapı 44.17, beklenen 60) **§2'yi düşürmek için okundu**;
  aynı kanıtın §1'in önerdiği `scrollIntoView({block:'center'})` yolunu da
  yanlışladığı **görülmedi**. §1 kör uygulandı (`66fa842`), canlıda kapanmadı,
  `9036791` ile `nav-kaydir.ts` ortak yardımcısına taşınarak düzeldi. `←156`

### KARAR'sız keşif kayıtları

- **`nav-kaydir.ts` — KARAR 395'in uygulaması, yeni karar değil.** `scrollToSuccess`
  ve `gecersizAlaniGoster` aynı mekanizmayı paylaşıyor: rAF → nav yüksekliğini
  **runtime** ölç → `window.scrollTo` ile mutlak konum. İki hizalama (`ust` ·
  `orta`), tek mantık. Token `--nav-height` **64px** ama computed **60px** —
  token'a güvenilmediği teste çivilendi (nav=60 → hedef 594, nav=64 → 592;
  iki değerin farklı olması nav'ın hesaba girdiğinin kanıtı).
  ⚠ **Kural vardı, uygulanmadı.** KARAR 395 "iki ayrı kod yolunu elle eşit
  tutmak sonsuz döngüdür, tek paylaşılan kaynak farkı matematiksel imkânsız
  kılar" diyor ve gövdesinde "bu ders **üç turluk kayıptan** sonra öğrenildi"
  yazıyor. Bu tur o sayacı **dörde** çıkardı: ilk uygulamada iki ayrı kaydırma
  mantığı doğdu, ortak yardımcıya ikinci turda inildi. `dist/` teyidi: ortak
  chunk'ta `scrollTo` 1 · rAF 1 · `querySelector("header")` 1.
- **Mutasyon seti 4 → 7, yedisi de yakalandı:** `preventScroll` · hizalama
  (`orta`→`ust`) · `<details>` açma döngüsü · nav runtime→sabit 64 · rAF
  kaldırma · sayfa kayması · negatif kırpma. Test yazmak yetmez; testin bekçi
  olduğu **mutasyonla** gösterilir.
- **`A.Ş.` satır kırılması: CSS, string değil.**
  `.kayit-formu__success-odeme dd { word-break: break-all }` suçluydu
  (muhtemelen IBAN için konmuş, bütün `dd` hücrelerine vuruyordu). Ham dizede
  `A.`–`Ş.` arasında boşluk yok, U+2060 yok — son altı kod noktası
  `t · SPACE · A · . · Ş(U+015E) · .`. Canlı A/B, 275px sütun, Range API ile
  satır satır: `break-all` → `…Ticaret A.` / `Ş.`; `normal` → `…Ticaret ` /
  `A.Ş.`. IBAN'a maliyeti ölçüldü ve **sıfır** (en uzun kesintisiz parça 4
  karakter, iki hâlde de tek satır). U+2060 hilesine gerek kalmadı — değer
  doğruydu, sunum bozuktu (KARAR 354). Öteki iki `break-all` yerinde bırakıldı.
- **`<details>` beyanı düzeltildi.** `9fca383` mesajı "üç bölüm, hepsi `open`"
  diyordu; canlı ölçüm **dört** bölüm ve **ikisi kapalı** (Niyet · Ödeme).
  Sessiz-submit riski koşullu değil, **varsayılan hâlde var**; bugün patlamıyor
  çünkü o iki bölümde zorunlu alan yok. Notion'a zorunlu bir kayıt sorusu
  eklendiği gün canlıya çıkar. Commit mesajı düzeltilmedi (KARAR 61), şerh
  `8bf2018` ile bu dosyaya düşürüldü.
- **Y1 kapandı — borç numarası değil, kod turu etiketi.** `92e580e` ile:
  Slug'sız etkinlikte `etkinlik_url` silindi, `name` korundu. Boş string
  mevcut değeri **temizliyor** — alan hijyeni tasarımının dayandığı varsayım
  artık kanıt. (Ara raporlarda "canlı teyit yapılmadı" görünmesi bayat
  bilgiydi; teyit `9036791` turunda test aboneyle alındı.)
- **CLAUDE.md §7 (tek klon) bu dönemde birden çok kez ihlal edildi** — paralel
  CC oturumları `834d620` · `6dea0b0` · `92e580e`. Zarar çıkmadı ama ADIM 0
  raporları **iki kez** geçersizleşti ve yeniden ölçüldü.

### `00-durum.md` tahliyesi — dört dönem bloğu indi (KIRPMA YASAĞI)

Dosya 195/200'e dayanmıştı. Aşağıdaki özetler `00-durum.md`'den **taşındı**,
silinmedi; gövdeleri zaten bu dosyada ve `2026-07.md`'de yaşıyor.

- **12–20 Temmuz (14 sohbet, KARAR 363–453):** hero geçişi, etkinlik liste
  tek-kabuk, kayıt penceresi + TZ fix, banka POS geçişi, liste ailesi tek
  gramer, kayıt butonu birleştirme (`kayit-cta` emekli), Yolculuk 7. kapı
  ürün+metin, Fable editoryal turu, sosyal medya ajans v2. Dönemin HEAD'i
  `e8a16dd`. → gövdesi `90-kronoloji/2026-07.md`
- **9 Ağustos (ikinci tur):** ADIM 7 ikinci dalga A+B — `docs_karar` + B54, üç
  commit, sıfır site kodu. Ledger'ın ölçülmüş sığlığı **126/418**'e genişledi
  (119 indeks + 7 komşu; b36a'nın 119'u bağımsız doğrulandı). KARAR 480 · 481.
- **6 Ağustos:** doküman mimarisi geçişi (KARAR 455–463) + kod teyidi 3 tur
  (7 commit, altı borç kapandı) + ADIM 2 ledger + **ADIM 3 bölme**.
- **4 Ağustos:** bakım turu — dört borç kapandı, iki içerik kalıntısı Notion'da
  düzeltildi.

---

## B TURU — SOSYAL MEDYA + AÇILIŞ (19 Ağustos 2026)

Zemin `3683ca4` (dump anı) → kapanışta `9036791` üstü. Kod dosyasına dokunulmadı;
bu tur yalnız `docs/` ve dump çıktısıdır.

**Sosyal medya planı ölçüldü.** `Social_Media.md` (v2, 19 Temmuz, 753 satır) Temmuz
dumpına bağlıydı; etkinlik sayfaları o tarihten sonra yazıldı. "Baştan mı yazalım"
sorusu önce sezgiyle tartışıldı, sonra ölçüme bırakıldı → **27 aynen · 3 cümle
düzeltmesi · 0 yeniden yazım · 5 yeni malzeme** → v2.1 patch'i (783 satır ·
50.657 karakter, `python3 len()`). On üç patch girdi; v2 gövdesine dokunulmadı.
Yeni **E7** kartı doğdu (Atölye günü, ilk kullanım Ritüel Tasarımı 3 Ekim).
Lint temiz: emoji sıfır, "köz" yalnız renk adı olarak. Dosya bu turda
`docs/30-sosyal.md` olarak repoya alındı — Desktop kopyası artık ayna.

**AÇILIŞ tarihi.** Ekim'e kaydırma seçeneği açıldı, üç kalemlik bedeli ölçüldü,
reddedildi. 24–27 Eylül 2026 (Perş–Paz) seçildi. **37 gün kaldı.**

**Dump — iki tur.** 1. tur: 49/49 route, 4.237 satır. Etkinlik sayfaları ≈520 kelime
çıktı — **CC bulguyu yakaladı ve raporladı.** Kök neden: extractor `main`'in doğrudan
çocuklarına bakıyor. Dosya çöpe gitti. 2. tur: `.claude/` kopyasında düzeltme + kabul
testi (N_html 12 = N_dump 12, oran 1.203) → 5.535 satır, etkinlik toplamı **8.554
kelime.** Takipli dosyaya hiç dokunulmadı, commit yok. Kalıcı dosya hatayı hâlâ
taşıyor — **B82**.

**Ön koşul dersi (KARAR 487'nin kardeşi).** İlk redeploy `Ready`ydi ama içeriği
tazelememişti — üç sayfa cache-bust'ta (ikisi `x-vercel-cache: MISS`, yani origin'den
taze) hâlâ eski saati veriyordu. **`Ready` ≠ taze.** Vercel build logu redeploy'un
Notion'a gerçekten gittiğini kanıtladı (`Skipping build cache` + `Notion Etkinlikler
DB: 26 satır`); sorun zamanlamaydı — Notion kaydı ilk redeploy'dan sonraydı. İkinci
redeploy (`dpl_CRyGEpPm`) düzeltti, cache-bust MISS ile teyit edildi.

**İçerik ölçümleri.** `/hikaye` "köz" taraması: sayfa gövdesi baştan sona tarandı,
**"köz" hiç geçmiyor** — dört geçişin dördü de kurala uygun. `20-ref-icerik-dili.md:59`
uyarısı bayattı, bu turda düzeltildi. Kamu metnindeki tek "köz" `/adimiz`'da yaşıyor
(**B80**). B04 kapandı: `/acik-kapi`'de "sembolik ücret" **kalıyor**, KARAR 432'nin
muafiyeti teyit edildi, İ19 caption'ı olduğu gibi geçerli. İ22 ters kararı onaylandı:
sosyal kartta **"kor alınır, taşınır"** kalır. Bu üçü `02-borclar.md`'de B numaralı
madde değildi — B04 hariç ikisi doğrudan içerik kararıdır, kayıtları burada yaşar.

**⚠ KARAR 463 — üç turda üç ihlal.** İki CC oturumu aynı klonda paralel koştu
(PID 2421 ödeme/kayıt brief'i, PID 3240 dump + bu patch). CC her turda durdu ve
uyardı; **her turda devam kararı Claude.ai tarafından verildi.** Zarar çıkmadı — ama
tesadüfen: işler farklı dizinlerdeydi (`src/` vs `scripts/`). Yine de bedel ödendi:
ADIM 0 ölçümleri **iki kez** geçersizleşti, karar numaraları (488–491) ve borç
numaraları (B77–B79) ölçüm ile yazım arasında değişti, patch'in verdiği aday numaralar
(489–492) tutmadı ve 492–495'e kaydırıldı. Çözüm CC'den geldi: takipli dosyaya hiç
dokunmayıp `.claude/` kopyasında çalışmak. **Kayda geçiyor ki bir dahaki sefer "zarar
çıkmamıştı" gerekçe sayılmasın.**

**Kart derleyici üretildi.** Tarayıcıda çalışan tek dosya: MJ görseli + bindirme →
1080×1350 / 1080×1920 PNG. Cormorant Garamond Italic, krem/altın/köz sabit,
`*kelime*` → köz vurgusu, karusel modu tek görselden N crop. Beklemede — **B86**.

**Ölçüm notu.** `awk length` bu makinede bayt sayıyor — `printf 'çığır' | awk` → **9**.
KARAR 483 bir kez daha doğrulandı; tüm karakter sayımları `python3` ile.

### KARAR KAYITLARI

- **KARAR 492 — AÇILIŞ 24–27 EYLÜL 2026 (AKTIF):** Anadolu Yolculuğu AÇILIŞ'ı kesin
  tarihe bağlandı: **24–27 Eylül 2026, Perşembe–Pazar**, Ege, üç gece dört gün
  (KARAR 443'ün süre standardı). KARAR 13'ün "~22 Eylül" çapasını **daraltır,
  değiştirmez** — zincirin geri kalanı yerinde (İniş Kasım · Uyanış Şubat · Duruş
  Nisan · Geçiş ~21 Haziran · Dönüş Ağustos). *Ekim'e kaydırma ölçüldü ve reddedildi:*
  kayma kış boşluğuna emilebilirdi (tek hareket eden durak İniş olurdu) ama bedeli üç
  kalemdi — Harran Aralık'a düşerdi, KARAR 436'nın "Kasım'da…" paralel cümlesi iki
  yüzeyde değişirdi, ve "on bir ay" sayısı bozulurdu. *Anlamsal engel yoktu:* ekinoks
  çapası Anadolu tablosunda yazılı değil (Dünya Yolculuğu tablosunda yazılı), ve
  22 Eylül 2026 Salı — üç gecelik blok zaten hafta sonuna oturuyordu.
  `20-ref-program.md:473` bu turda güncellendi.

- **KARAR 493 — SOSYAL MEDYA KATMAN MİMARİSİ (AKTIF):** Ses-Alan doktrininin sosyal
  yüzeye uygulanması. **TABAN (İ1–İ24):** kişisiz, tarihsiz, çekimsiz — Gün 0 kurucu
  görseli çıktığı an altı hafta kendi ritmiyle akar. **ÜST (E1–E7, R6/R7/F8, Advaita
  alıntıları):** opsiyonel; tabanın üstüne biner, yerine geçmez. **Kural:** taban asla
  üstü beklemez. **Sınır — kararın kendi düzeltmesi:** taban *üretimin* kişisiz
  olabildiğini kanıtlar, *güvenin* değil. Bu kategoride kadın on bir aylık bir "evet"i
  markaya değil, izlediği bir insana der. *"Yüzü yok, ateşi var"* kamu konumlandırması
  olarak **emekli edildi**; iç dayanıklılık ilkesi olarak kaldı. **Sonuç:** kohort
  dönüşümü feed'den geçmez. Motor **Halka 1** — birebir davet ağı + Advaita'nın hesabı.
  İ5 feed'de kalır ama omuzlamaz: **eko olur, kaynak değil.** v2.1'de İ5'in "İLK KAMU
  DUYURUSU" çerçevesi üç yerden düşürüldü. Ana sahne kararı hâlâ açık — **B83**.

- **KARAR 494 — YENİDEN YAZMA KARARI ÖLÇÜMLE VERİLİR (KALICI):** Bir plan/dosya
  "bayat mı, baştan mı yazılmalı" sorusuna **sezgiyle** cevap verilmez. Kaynak
  tazelenir, birim birim dört kovaya ayrılır (aynen duruyor · cümle düzeltmesi ·
  yeniden yazım · yeni malzeme), karar sayıdan çıkar. *Doğduğu vaka:* sosyal v2 için
  ikimiz de "bayat" sezgisindeydik; ölçüm **27/3/0/5** verdi ve yeniden yazım kovası
  **sıfır** çıktı. Site metni kartların altını oymamış, üstüne malzeme koymuştu.
  Sezgiyle gidilseydi otuz caption gereksiz yere yeniden karara açılacaktı.
  KARAR 483'ün kardeşi: o düzyazı rakamını gövdesine karşı sınar, bu yeniden yazım
  kararını kovalara karşı sınar.

- **KARAR 495 — DUMP ARAÇLARI KABUL TESTİYLE KOŞULUR (KALICI):** **HTTP 200 içerik
  garantisi değildir.** Bir dump aracı hata vermeden, doğru sayıda route çekip, dosyayı
  yazıp içeriği kaybedebilir — *sessiz fakirleşme*. **Kural:** dump koşumu, canlı
  kaynaktan **kendi üretilen** bir referansa karşı sınanır — düğüm sayısı eşit **ve**
  metin oranı ≥ 0.9. Geçmezse dosya yazılmaz. *Doğduğu vaka:* `dump-fable.mjs`
  extractor'ı `main`'in yalnız doğrudan çocuklarına bakıyordu; etkinlik şablonunda
  dokuz section `div.ocak-container` içinde (derinlik 1). 15 etkinlik sayfası ≈520
  kelimeyle çıktı, olması gereken **8.554**'tü. Dörtte üçü kayıptı ve hiçbir hata
  verilmedi. Aynı hata sınıfı daha önce `--ek` modunu doğurmuştu — yani ikinci kez.
  KARAR 355/408'in ("kod var" ≠ "output var") araç yüzeyine genişlemesi.

### `00-durum.md` tahliyesi — iki dönem bloğu daha indi (KIRPMA YASAĞI)

Dosya B turu bloğu eklenince 204/200'e çıktı. Aşağıdaki iki özet `00-durum.md`'den
**taşındı**, silinmedi; gövdeleri bu dosyada yaşıyor ve kalem kalem doğrulandı.

- **7–8 Ağustos:** B32 (referans dağıtımı, beşli → yedili) · B33 · B37 · B34 ·
  **ADIM 4** (CLAUDE.md + baglam.sh + tam taşıma) · **B36 açılış ölçümü**
  (mekanik çapaların %43'ü komşusunu gösteriyor, kuyruk ~179±, üçte ikisi tek desen) ·
  KARAR 465–472. Sıfır kod commit'i. → gövdesi bu dosyada (`418 × 9/21 ≈ 179`
  nokta tahmini ve "kuyruğun üçte ikisi mekaniktir" bulgusu dahil)
- **10 Ağustos:** **B01 ✅** — kapı işi. GitHub remote · yerel klasör · Vercel proje
  adı üçü de `ocak`. Bir commit (`50294e6`), sıfır site kodu. CC dört sapma bildirdi,
  dördü de haklıydı; biri brief'in `N → 0` kriterinin kendi kaydını sayması
  (KARAR 465'in ikinci vakası). Vercel git bağlantısı `githubRepoId` üzerinden
  kırılmadı. → gövdesi bu dosyada
- **11 Ağustos (ikinci tur):** **B40 · B55 · B56 ✅** — KARAR 482'nin ilk uygulaması,
  tek commit `b72c539`. Üçü de "önce karar" şartını 482 sayesinde düşürdü. `61/88` →
  `61`. Sahip tablosu gövdelerden yeniden türetildi, beş bayat kalem çıktı. Ledger
  düzeltildi, kronoloji **şerh** aldı. **KARAR 483** mühürlendi: brief'in düzyazı
  rakamı kendi gövdesiyle sınanmadan yazılmaz — üç günde dört ölçülmüş vaka.
  → gövdesi bu dosyada

---

## 17–19 AĞUSTOS 2026 — ÜÇ FORMAT BİRDEN: SEREMONİ · ATÖLYE · AÇIK KAPI BEKÇİSİ

Dört paralel claude.ai oturumu. Raporlar gövde metni taşımadı, yalnız ölçü ve kalıp.

### Üretim

- **Açık Kapı:** beş gövde + **bekçinin kendisi** kuruldu. Yay: tanı → adlandır →
  sına → ayıkla → (kök) → koru. Altıncı gövde (**6 · Sınır**, `nereye-kadar-senin`)
  sonraki oturumda eklendi.
- **Seremoni:** iki gövde — Kakao online **5.197** · yüz yüze **4.870** (künye ölçümü).
- **Atölye:** bir gövde — Ritüel Tasarımı, **4.895**, on bölüm.

### Üç yeni gövde canlıda — brief'in "girmedi" beyanı yanlıştı

Brief üçünün de Notion'a girmediğini yazıyordu. CC ADIM 0'ı `dist/` üstünden ölçtü;
üçü de **canlıda**. 19 Ağustos 13:39 build'i, `<main>` metni (etiketler strip, `python3`
`len()` — `awk` bu makinede bayt sayıyor, `çığır` → 9):

| sayfa | `<main>` metni | künye bandı |
|---|---|---|
| `etkinlik/kakao-seremonisi-online` | **5.080** | 5.197 |
| `etkinlik/kakao-seremonisi-yuzyuze` | **4.766** | 4.870 |
| `etkinlik/rituel-tasarimi` | **4.768** | 4.895 |

Künye bandı gövde ölçümüdür; `## section:` markerları ve blok ayraçları strip edildiği
için dist `<main>` sayımının bir tık düşük çıkması **beklenen farktır**, sapma değil.

### Format sayfası render mimarisi — "dist'te yok" gözleminin cevabı

Altı format sayfası (`acik-kapi` · `cember` · `seremoni` · `atolye` · `mini-retreat` ·
`sehir-aksami`) statik çıktıda **yok**; hepsinde `export const prerender = false` ve
SSR ile `.vercel/output/functions/_render.func` üzerinden çıkıyorlar. Gerekçe kaynakta
yazılı: davet linkinin `?etkinlik=` param'ı `getStaticPaths` altında yakalanamıyor
("Eyeball #4 Fix 2d"). Statikte yalnız `/kayit` alt route'ları duruyor.

**Tasarım, eksik değil.** Pratik sonucu şu: `dist/` grep'i bu altı sayfayı **görmez** —
format sayfası ölçümü yaparken hatırlanacak (B95'e not düşüldü).

### Ölçüm düzeltmeleri

- **`ne-olur` Açık Kapı'da yedi değil sekiz paragraf.** Yedi, pratik paragrafının
  ikiye bölünmesinden **önceki** ölçümdür. Değişken olan **ikincisi** (tema paragrafı).
  CC doğrulaması: `ornekler.md:24–42`, ``` çitleri arası, H2 hariç, `\n\n` bölmesi → 8.
- **Açık Kapı gövde bandı üç farklı değerle dolaştı:** 2.756–2.932 (beş gövde, sohbet
  içi) · 2.800–3.000 (bayat zip) · **3.019–3.247** (altı gövde, künye, 18 Ağu).
  Kanonik **üçüncüsü** — `ornekler.md:195` doğrulandı.
- **`pratik-bilgi` madde sayısı üç raporda üç değerle dolaştı (3 · 8 · 7).**
  **Çelişki değil, yanlış soru** — sayı etkinliğe göre değişir, bekçi ölçüsü değildir
  (Kaan düzeltmesi, 19 Ağu). Raporların bu kalemi künye verisi gibi taşıması, bekçi
  künyesinin neyi taşıyıp neyi taşımayacağının **yazılı olmamasından**. KARAR 502.

### Format ayrımları — bant tablosu

| | Açık Kapı | Çember | Seremoni | Atölye |
|---|---|---|---|---|
| bölüm | dokuz | dokuz | dokuz | **on** |
| H2 | dört | dört | dört | **beş** |
| `ne-olur` paragraf | sekiz (2. değişken) | on (4. değişken) | on (**2. ve 3.** değişken) | sekiz (**sabit blok yok**) |
| `kapanis` paragraf | bir | iki | **üç** | iki |
| karakter bandı | 3.019–3.247 | 3.824–4.113 | **4.870–5.197** | **4.895** (tek nokta) |
| kayıt | alınır | **alınmaz** | alınır (ses/video alınmaz) | alınır (ikisi de) |
| "çember" kelimesi | yasak | zorunlu | — | — |

### Seremoni'ye özel sapmalar

- `yaninda-getir` **mekân başına ayrı liste** — Çember'in "beş sabit + bir temaya özel"
  kalıbı Seremoni'ye **taşınmaz**.
- `kimin-icin` kapanışı mekâna göre bölündü: yüz yüzede seri sabiti aynen ("Hiçbir
  hazırlık gerekmiyor"), online'da **yalan olacağı için** değişti.
- **"Geç kalırsan…" maddesi iki sayfadan da çıkarıldı** — seremonide geç giriş
  normalleştirilmiyor, kap zayıflıyor.
- Sağlık/kontrendikasyon katmanı **yeni**. Dördüncü kayıt sorusu **reddedildi** (Kaan),
  `pratik-bilgi`'ye indi.
- İsimlendirilmeyen şey: **şamanik yolculuk** (Kaan: "sende kalsın"). Hazırlık yine
  söyleniyor — kadın uzanacağını biliyor.

### Atölye'ye özel sapmalar

- `kimin-icin` kapanışından "Hiçbir hazırlık gerekmiyor" **çıkarıldı** — ödev var,
  cümle olduğu gibi kalsa yalan söylerdi.
- `pratik-bilgi` yedi madde, hiçbiri iki bekçiyle birebir değil. Yalnız **"Geç kalırsan
  da gel, kapı kapanmıyor"** birebir taşındı — sabit olanın blok değil **cümle** olduğunun
  kanıtı (KARAR 502).
- Vurgu ekseni **söz → eylem**; Açık Kapı'nın "…zorunda değilsin / …yeter" kalıbı
  kullanılmadı (`ornekler.md`'nin "yedinci sayfa tekrarlamamalı" uyarısına uyuldu).
- Üçüncü kayıt sorusu dizinin ortak kapanışından **saptı** — iki oturum olduğu için.

### Geri çekilen madde

- `mesafeli_onay → required` brief'e yazıldı, **CC ölçümüyle geri çekildi** — açık
  statik HTML'de görünüyordu, çalışma anında kapatılmış. Sabit `required` **gizli-zorunlu
  tuzağı** açardı. (Refleksin brief'i düzelttiği üçüncü kayıt.)

### Hız sözü — kapanmadı, borca döndü

Brief bunu **kapanan madde** olarak yazdı: Açık Kapı sabit `pratik-bilgi` satırındaki
hız sözü (*"Kayıt alınır, hemen ardından e-postana düşer"*) teslim hızı vaat ediyordu,
Kaan 19 Ağustos'ta düzeltti, borç açılmadı.

CC doğrulaması dizeyi 19 Ağu 13:39 build'inde **altı canlı sayfada** buldu
(`bir-esikte-duruyorsun` · `bu-ses-kimin` · `dusundugum-hissettigim-yaptigim` ·
`istek-mi-ihtiyac-mi` · `kokun-nerede-kaldi` · `nereye-kadar-senin`) — **ve kanonik
bekçide**: `docs/skills/ocak-etkinlik/SKILL.md:104`.

Notion tarafındaki düzeltme build'den sonraydı; bekçi tarafı hiç düzeltilmedi. Yani
madde kapanmadı, **daraldı ve cins değiştirdi**: sabit blok **kaynağı türevinden geride**.
Yedinci sayfa bekçiden yazılacağı için hız sözü geri gelirdi. → **B100**, kapanış koşulu
"yeni cümle canlı gövdeden **okunup** SKILL.md'ye taşınır, uydurulmaz".

### Türev yüzey turu — dört yüzey, sonra beşinci

Aynı gün dört türev yüzey birden bayat çıktı:

1. **claude.ai skill zip'i** — beş sayfalık sürümde donmuş; **ölçülebilir hasar** üretti
   (bandın altında gövde + eksik `ne-olur` paragrafı).
2. **Zip yeniden yüklendi, konteynere yansımadı** — md5 aynı kaldı (`7127de37…`).
   Yükleme başarılı görünüp etkisizdi.
3. **Railway MCP checkout'u** — `1546622`'de donmuş, `49ea8e2` inmedi. Ayarlar temizdi
   (auto-deploy açık, watch path yok, branch `main`); sorun **düğmedeydi**. Railway'in
   **"Redeploy" düğmesi taze commit çekmez** — son başarılı deployment'ı tekrar koşar.
   Doğrusu **"Deploy latest commit"** (Cmd+K). `49ea8e2`'nin ilk build'i sürerken
   Redeploy'a basılması build'i **REMOVED**'a düşürdü.
4. **Notion deploy hook'u** yanlış dalda (B64 ailesi).

Dördü ayrı borç değil, **tek doktrinin dört yüzeyi** — KARAR 513.

**Beşinci yüzey: brief'in kendisi.** Bu patch'in brief'i bayat MCP korpusundan yazıldı
(`1546622`, 11 Ağustos görüntüsü). **Beş iddiası dosyanın gerçeğiyle çelişti, beşi de
CC'nin ADIM 0 ölçümüyle yakalandı:**

1. "00-durum.md 11 Ağustos'ta donmuş" → dosya 19 Ağustos B turu üstündeydi.
2. "Üç gövde Notion'a girmedi" → üçü de canlıdaydı.
3. "`/atolye` dist'te yok, araştırılmadı" → SSR, `prerender = false`, tasarım.
4. "Hız sözü düzeltildi, borç açılmadı" → altı sayfa + bekçi hâlâ eski cümlede.
5. "Mayıs smoke test kalıntısı duruyor mu" → kalıntı değil, gerçek etkinlik
   (3 Ekim 2026, bu dosyanın `E7` kaydında zaten yazılı).

Brief yanılabilir; **refleks yanılmadı** (KARAR 102 · 355 · 491). Doktrinin kendi
kanıtı: türev yüzey — brief de bir türev yüzeydir — kaynaktan yeniden üretilmediğinde
yalan söyler.

### Paralel iş kolları — varlık kaydı

Havale/EFT ödeme akışı · logo çalışması · sosyal medya planı incelemesi. **Bu yüzeyde
ölçüm yok**; detayları kendi oturumlarında kayıtlı. `00-durum.md`'ye girmediler —
tavan 200'de dolu, kayıt buraya indi.

### KARAR KAYITLARI

- **KARAR 496 — AÇIK KAPI GÖVDESİNDE "ÇEMBER" GEÇMEZ (KALICI):** KARAR 206
  gerekçesinin etkinlik gövdelerine genişletilmesi. Çember'de **tersi zorunlu**.
  Format ayrımı kelime düzeyinde tutuluyor: aynı kelime iki formatta iki ayrı sinyal.

- **KARAR 497 — ÇERÇEVE KURALI: SAYFA BAŞINA İKİ `buyuk-vurgu` (KALICI):** biri
  `giris`ten sonra, biri kayıt butonunun üstünde. **Üçüncüsü vuruşu harcar.**
  Dört formatta da (Açık Kapı · Çember · Seremoni · Atölye) korundu — format
  değişince değişmeyen az sayıdaki ölçüden biri.

- **KARAR 498 — ETKİNLİK GÖVDESİ İÇİ BAŞLIK H2'DİR (KALICI):** H3 değil.
  Ölçüm: `/cember` · `/acik-kapi` · `/mini-retreat` üçünde de H2. **H3 yalnız
  Sıradaki Kapı kartlarında** — yani H3 gövdeye değil navigasyona ait.

- **KARAR 499 — KÖZ GÖVDEYE GİRMEZ (KALICI):** renk hiyerarşisi üç katmanlı ve
  **dolu**; köz sayfada zaten ayraç + CTA'da yaşıyor. Dördüncü kullanım **butonu
  sıradanlaştırır** — közün işi vurgulamak değil, tek bir yeri işaret etmek.

- **KARAR 500 — GÖVDE SOHBET İÇİNDE KOD BLOĞU OLARAK VERİLİR (KALICI):** claude.ai
  yüzeyinde etkinlik gövdesi **dosya olarak değil** kod bloğu olarak teslim edilir.
  Gerekçe ölçüldü: dosyadan Notion'a taşımada **satır sonları ve `## section:`
  markerları kayboluyor** — bir oturumda dört tur hasar. Marker kaybı sessizdir ve
  component'i render dışı bırakır (KARAR 409 ailesi).

- **KARAR 501 — KAYIT SORULARI KALIBI (KALICI):** üç açık uçlu soru + her birinin
  altında **bir rahatlatıcı satır**; üçüncü soru **dizinin ortak kapanışıdır**.
  Sapma meşru ama gerekçeli olur — Atölye'de iki oturum olduğu için saptı.

- **KARAR 502 — `pratik-bilgi` MADDE SAYISI BEKÇİ ÖLÇÜSÜ DEĞİLDİR (KALICI):**
  sayı **etkinliğin kendisinden** çıkar — saat dilimi, sağlık maddesi, ödev, ara hafta,
  mekân. Sabit olan **blok değil, blok içindeki ortak cümlelerdir** (ör. *"Geç kalırsan
  da gel, kapı kapanmıyor"*). **Bekçi künyesine madde sayısı yazılmaz; ortak cümleler
  yazılır.** Aynı ayrım `yaninda-getir` için de geçerli — Seremoni'de liste mekâna bağlı,
  Çember'in "beş sabit + bir temaya özel" kalıbı oraya taşınmaz. *Doğduğu vaka:* aynı
  kalem üç raporda 3 · 8 · 7 çıktı; çelişki sanıldı, **yanlış soru** olduğu anlaşıldı
  (Kaan düzeltmesi). KARAR 470'in kardeşi: o ölçülemeyen rakamı yasaklar, bu **ölçülmesi
  anlamsız** olanı ayıklar.

- **KARAR 503 — ETKİNLİK RİTİM CSS KAPSAMI (AKTIF):** `main[data-page^="etkinlik/"]`.
  **Token'lara dokunulmaz** — ritim düzeltmesi kapsam selektörüyle yapılır, global
  değişkenle değil. CC commit `6f563ea`.

- **KARAR 504 — KAKAO SEREMONİSİNİN EKSENİ "CEVABINI ARADIĞIN SORU" (AKTIF):**
  "niyet" değil. **Niyet-mühür çerçevesi reddedildi** (Kaan). ⚠ `/seremoni` format
  sayfasının `ic-ses` cümlesi (*"Niyet … seremonide mühür olur"*) bu eksenle
  **gerilimde** — uyumlanacak mı ayrı mı kalacak, açık.

- **KARAR 505 — KAKAO TEDARİK DOKTRİNİ (AKTIF):** **Online:** kadın kendi sipariş eder;
  tedarikçi adı **sayfada değil**, kayıt sonrası mailde. **Yüz yüze:** Advaita getirir.
  **Kargo işine girilmez.** Kaynak cümlesi (Peru/Meksika/Guatemala; küçük üreticiler;
  hangisi olduğu **çemberde söylenir**) yalnız **yüz yüze** sayfasında yaşar.

- **KARAR 506 — UNVAN YASAĞI GENİŞLEDİ (KALICI):** "Cacaoista / Kakaoista / Cacaoiste"
  **kamu metninde geçmez** — başka bir markanın taksonomisi, ve *"uzmanlık var, kürsü
  yok"* doktrinine (KARAR 442) aykırı. İç envanterde gerekirse orijinal yazım.
  → `yasak-dizeler.tsv`'ye **üç dize**.

- **KARAR 507 — "KADİM MAYA SEREMONİSİ" İDDİASI YASAKTIR (KALICI):** kakaonun modern
  seremoni formatı **kadim değil** — Keith Wilson, San Marcos, **2003 sonrası**.
  → `yasak-dizeler.tsv`. Doğrudan sonucu: `10-marka.md:24`'ün "(Cacaoista, Maya/Aztek)"
  ibaresi **yanlış** (Kaan teyidi) → **B92**.

- **KARAR 508 — HAM INSTAGRAM POSTLARI `_arsiv/`'E GİRER (KALICI):** Advaita'nın ham
  postları `docs/_arsiv/`'e, `20-ref-*` olarak **değil** — `20-ref-marka.md`'deki ses
  özütünün yanında **ikinci bir otorite doğmasın**. Künye satırı: **"Kanıt, kanon değil."**
  KARAR 482'nin malzeme yüzeyine inişi.

- **KARAR 509 — RİTÜEL TASARIMI İKİ OTURUMLUK TEK ATÖLYEDİR (AKTIF):** dört haftalık
  seri program **değil**. İki ardışık cumartesi × 3 saat, **17:00–20:00**, arada tam bir
  hafta. **KARAR 79'u SUPERSEDE eder** (ledger zinciri kuruldu: `79 →509`, `509 ←79`).
  Gövde izleri iki referans dosyasında duruyor → **B91**, `/atolye` kutusu → **B90**.

- **KARAR 510 — ATÖLYE SAYFA İSKELETİ ON BÖLÜMDÜR (KALICI):** `elinde-ne-kalir` bu
  formatın **kanonik bölümüdür** (`ne-olur` ile `kimin-icin` arasında). Render tarafı
  **serbest prose ad** — sekiz kanonik render section listesi etkilenmez. Yani doküman
  iskeleti ile render sözleşmesi burada bilerek ayrışıyor.

- **KARAR 511 — AÇIK KAPI'NIN SABİT `ne-olur` BLOĞU ATÖLYE'DE KULLANILMAZ (KALICI):**
  *"Ne olacağını şimdiden söylemiyoruz"* paragrafı bu formatta **geçersizdir** —
  KARAR 442'nin format düzeyine inişi. Atölye **K2** (%30–40 bilgi) ve kadın **ne
  öğreneceğini bilerek** kaydoluyor. Tersine cümle kuruldu: *"burada bilgi saklamıyoruz."*

- **KARAR 512 — ATÖLYE ÖDEVİ KAYIT ANINDA GİTMEZ (AKTIF):** ilk oturumdan **3–4 gün
  önce**, ayrı MailerLite kampanyasıyla. Gerekçe mekaniktir: welcome maili **format-grup
  bazlı** (KARAR 228, atölye grubu `189209178380699119`) — ödev oraya girerse **tüm**
  atölye kayıtlarına gider, tarihi geçmiş olanlara da.

- **KARAR 513 — TÜREV YÜZEY, KAYNAKTAN YENİDEN ÜRETİLMEDİĞİNDE YALAN SÖYLER (KALICI):**
  her türev yüzeyin bir **tazelik kapısı** olur: **kullanılmadan önce kaynağa karşı
  ölçülür, tutmuyorsa üretim durur.** 19 Ağustos'ta beş yüzey aynı anda ölçüldü — skill
  zip'i · konteyner mount'u · Railway checkout'u · Notion deploy hook'u · **ve brief'in
  kendisi**. Beşi de bayattı, beşi de hata vermeden bayattı. **Ayırt edici belirti:**
  yüzey "yok" demez, **eski** der — ayrışma sessizdir. KARAR 355/408'in ("kod var" ≠
  "output var") ve KARAR 495'in (HTTP 200 ≠ içerik) üst kümesi. Kardeş borçlar:
  **B96** (skill zip) · **B97** (Railway checkout) · **B57** (connector araç listesi,
  cins buldu ama kapanmadı) · **B64** (deploy hook dalı).

## 19 Ağustos 2026 — çok oturumlu etkinlik kayıt kalıbı (claude.ai, sıfır commit)

Notion Etkinlikler DB'de **iki ayrı cumartesiye yayılan bir program** nasıl
girilir sorusundan çıktı. Sohbet sayfa üretmedi, gövde yazmadı — yalnız
veri-katmanı kuralı.

**Sorunun mekaniği.** Notion `date` property'si start + end aldığında
**kesintisiz blok** çizer; "iki ayrı gün" diye bir değer yok. 8 Kasım → 15
Kasım girilirse takvimde sekiz günlük bar olur. İkinci bir date kolonu da
çözmez: takvim view'ı Layout → *Show calendar by* ile **tek** property'ye
bağlanır, ikinci kolon ikinci bir view üretir, aynı takvimde ikinci gün
üretmez. **Tek takvimde iki gün = iki satır** — başka yol yok.

**Kanıt zinciri (hepsi korpustan okundu, hatırlanmadı):**
- `notion-etkinlikler.ts` Notion'dan **filtresiz** çeker; Astro loader yalnız
  `siteGoster` ve `İptal` eler (`2026-07.md:762`).
- Collection filtresi `siteGoster === true && durum !== 'İptal'` — KARAR 279.
- Liste süzgeci `durum ∈ {Kayıt Açık, Dolu}` — KARAR 384
  (`_uretilen/ledger-teyit-2026-08-06.tsv:24`).
- Slug guard: `siteGoster === true && slug boş` → **görünür WARN**;
  slug'sız **ve** kapalı → **sessiz atlanır** — KARAR 278.
- `Yayınla` bu zincirin hiçbir yerinde yok; yalnız deploy hook tetikler —
  KARAR 110.
- Gecelik cron TR 03:00 tüm siteyi yeniden kurar — KARAR 280.

**Reddedilen yol ve gerekçesi.** Gölge satıra `Sitede Göster` açık bırakıp
`Durum: Taslak` ile siteden düşürmek çalışıyor (liste bileşenleri süzüyor),
ama slug guard'ı her build'de tetikler. Gecelik cron yüzünden bu **her gece
tekrarlayan** bir WARN olur. Slug verilirse bu sefer gölge satır için öksüz
bir `/etkinlik/[slug]` sayfası doğar. İkisi de istenmeyen; yol reddedildi ve
gerekçesi KARAR 516 olarak genelleştirildi.

**`Yayınla` yanılgısı — turun asıl bulgusu.** Notion view'larında `Yayınla is
checked` filtresi "sitede ne var" sorusuna **iki yönde de yanlış** cevap
veriyor: `Yayınla` ✓ + `Sitede Göster` ✗ olan satır view'da görünür ama
sitede yoktur; `Yayınla`'ya hiç dokunulmamış + `Sitede Göster` ✓ + `Durum`
Kayıt Açık olan satır view'da görünmez ama gecelik cron'dan sonra **sitededir**.
Kaan filtresini tur içinde `Sitede Göster is checked` olarak düzeltti — doğru
filtre bu.

**B77 ile kesişme — sonraki turlar için şerh.** Gölge satır tanımı gereği
`Slug` **boş** ve `Sitede Göster` **kapalı**. B77'nin kapanış ölçütü
"`siteGoster=true` olan her etkinlikte Slug dolu" olduğu için gölge satır
B77'yi **ihlal etmez**. Gölge satıra "eksik" diye slug eklenmemeli — o slug
öksüz sayfa doğurur.

**Ölçülmeyen.** Sorulan somut programın adı, tarihleri, formatı ve ücretli
olup olmadığı sohbette hiç söylenmedi; kayıt açılmadı. Kalıp soyut düzeyde
kaldı.

**Uygulama şerhi (CC, ADIM 0).** Brief `a758994` zemininde yazıldı ve kararları
**496–498**, borçları **B88–B89** diye numaraladı. Uygulama anında HEAD
`c035011`'di: 496–513 ve B88–B100 **dolu**. Numaralar **514–516** ve **B101**'e
kaydırıldı; gövde metni değişmedi, yalnız iç referanslar yeni numaralara
çevrildi. Brief'in `03-sira.md` (c) çapası (`## TAMAMLANDI`) dosyada **yok** —
bölümün gerçek adı `## BİTENLER`. Brief'in B88'i (Ritüel Tasarımı kaydının
tarih aralığı) **yazılmadı**: gövdesi "dört haftalık seri · üç gölge satır"
diyor, oysa aynı gün mühürlenen **KARAR 509** formatı *iki oturumluk tek atölye*
olarak yeniden tanımladı ve tarihler `03-sira.md`'de hâlâ **Kaan'da bekleyen
girdi**. Yanlış öncüllü borç yazmak yerine durduruldu (KURAL 2 · KARAR 513).

### KARAR KAYITLARI

- **KARAR 514 — ÇOK OTURUMLU ETKİNLİK KAYIT KALIBI (AKTIF):** Farklı günlere
  yayılan bir program tek satıra **tarih aralığı** olarak girilmez. İki satır:
  **(a) Kayıt satırı** — `Tarih` = ilk oturum, **end boş**; `Slug` · `Ücret` ·
  `Kayıt Soruları` · `Katılım Linki` dolu; `Durum` = Kayıt Açık;
  `Sitede Göster` ✓; öteki tarihler `Saat` alanında ve `Detay > pratik-bilgi`
  bloğunda insan-okur biçimde yazılı. **(b) Gölge satır(lar)** — başlık
  "… — 2. Oturum"; `Tarih` = o oturum; `Slug` **boş**; `Sitede Göster` **✗**;
  `Durum` = Taslak; `Yayınla`'ya dokunulmaz. Sonuç: Notion takviminde her
  oturum ayrı gün, sitede tek kart · tek `/etkinlik/[slug]` · tek kayıt akışı ·
  tek referans no. Gölge satır collection'a hiç girmez (KARAR 279) ve slug
  guard'ı tetiklemez (KARAR 278 sessiz-atlama dalı). *Sınır:* iki oturum
  **ayrı ayrı satın alınabiliyorsa** bu kalıp geçersiz — o zaman iki gerçek
  satır, iki slug, iki kayıt akışı doğrudur (Yolculuk durak modeli).

- **KARAR 515 — YAYIN TETİKLEYİCİSİ GÖRÜNÜRLÜK KAYNAĞI DEĞİLDİR (KALICI):**
  `Yayınla` "sitede görünür" demek değil, "şu an build tetikle" demektir
  (KARAR 110). Sitede ne olduğunun tek kaynağı `Sitede Göster` + `Durum`
  zinciridir (KARAR 279 · 384). Gecelik cron (KARAR 280) `Yayınla`'ya hiç
  dokunulmamış satırları da yayına taşıdığı için, `Yayınla`'yı filtre olarak
  kullanan her görünüm yanlış liste üretir. **Notion view sözleşmesi:**
  *site aynası* = `Sitede Göster is checked AND Durum is not İptal` ·
  *planlama takvimi* = `Durum is not İptal` (Yayınla filtresi yok).
  `Yayınla` filtre değil, buton.

- **KARAR 516 — RUTİN TETİKLENEN GUARD KABUL EDİLMEZ (KALICI):** Bir build
  guard'ının "zaten biliyoruz, boş ver" denerek düzenli tetiklenmesine izin
  verilmez. Beklenen WARN, WARN'ların okunmayı bıraktığı andır — guard'ın
  değeri nadirliğinden gelir. Bir kurgu ancak var olan bir guard'ı sessiz
  bırakıyorsa doğrudur; guard'ı gürültüye çeviren kurgu, ucuz görünse de
  reddedilir. *Doğduğu vaka:* gölge satırın `Sitede Göster` açık bırakılması
  KARAR 278'in slug guard'ını her gecelik build'de tetikleyecekti.
  KARAR 495'in kardeşi — o "hata vermemek doğruluk değildir" der, bu
  "uyarı vermek ancak duyuluyorsa işe yarar" der.

### Şerh — KARAR 514 gölge satır adlandırması gerçeğe çekildi

Karar `"… — 2. Oturum"` biçimini tarif ediyordu. Notion'daki fiilî satır
`"… | Gölge 0210 o#2"` — kendini **gölge diye etiketliyor**, kararın tarifi
etiketlemiyor. Takvimde `"… — 2. Oturum"` gerçek bir etkinlik gibi okunuyor;
gölge satırın tek işi operatöre "bu gerçek etkinlik değil" demek olduğu için
tarif kendi amacını zayıflatıyordu. Kural dizeyi değil **şartı** söyler hâle
getirildi: gölge başlığı `Gölge` dizesini içerir ve oturum sırasını taşır,
biçim serbest. Grep'lenebilir kalır, tek kalıba hapsetmez. Satır değil karar
düzeltildi (KARAR 102).

### Şerh — 19 Ağustos gölge satır turu, bayat zemin

Turun brief'i korpus commit `a758994`'e göre yazılmıştı; HEAD `c035011`'di.
`docs_envanter()`'ın döndürdüğü SHA **MCP'nin servis ettiği** commit'tir, HEAD
değil (KARAR 479) — ikisi karşılaştırılmadan numara beyanı yapıldı. Ledger son
kararı 495 sanıldı (gerçek 513), son borç B87 sanıldı (gerçek B100). CC
kaydırarak yazdı, çakışma çıkmadı; doğruluk tesadüf.

İkinci hata: Ritüel Tasarımı ölçümü Mayıs smoke testinden (`4c27bb0`) alındı,
Notion'un güncel hâlinden değil — KARAR 513'ün vakası. Önerilen gölge satır
tarihlerinin (15/22/29 Eyl · 6 Eki) dördü de **salı**; baştan sona cumartesi
konuşulan bir turda hafta günü kontrol edilmedi.

**Sonuç ölçümle düzeldi:** satır zaten KARAR 514 kalıbındaydı ve KARAR 509'un
formatına uyuyordu (3–10 Ekim, iki ardışık cumartesi). Önerilen B102 gövdesi
tümden düştü. Yerine ölçümün kendi bulduğu boşluk yazıldı — `Kayıt Kapanış
Tarihi` ∅ (B102). KARAR 514·515·516 içerik olarak ayakta; yalnız numara zemini
ve bu tek ölçüm çürüktü.

**Ölçümün kendi sürprizi.** B102 tek kayıt için açılacaktı; ölçüt alanın
tamamına uygulanınca **25 ücretli kaydın 25'inde** `Kayıt Kapanış Tarihi` boş
çıktı, dolu olan sıfır. Yani eksiklik Ritüel Tasarımı'na özgü değil, **veri
girişi kalıbının hiç oluşmamış olması**. Brief "her etkinlik aynı boşluğu
taşıyor" derken rakam vermemişti; rakam yazılınca borcun ağırlığı değişti —
tekil düzeltmeden alan taramasına (KURAL 4).

### Şerh — B102 açıldığı turda kapandı (Kaan kararı)

Ölçüm gerçekti, teşhis yanlıştı. 25 ücretli kaydın 25'inde `Kayıt Kapanış Tarihi`
boş çıkınca bu bir **eksiklik** gibi okundu; borcun kapanış şartı "25 → 0" diye
yazıldı. Kaan kapattı: alan **opsiyoneldir**, boş bırakıldığında `pencereIcinde`'nin
`tarihBaslangic`'e düşmesi (KARAR 383–386) **kabul edilen davranıştır** — kusur
değil, tasarım. Yani 25/25 bir boşluk değil, alanın fiilî kullanılmama hâli.

**Ölçüm ile yargı arasındaki fark.** Rakam doğruydu ve doğru yöntemle alındı
(KURAL 4); yanlış olan, rakamdan **kusur** çıkarılmasıydı. Bir alanın her yerde
boş olması, dolu olması gerektiğini göstermez — önce alanın **zorunlu mu opsiyonel
mi** olduğu sorulur. Bu sıra atlandı. Sayı bir borcun kanıtı değil, yalnız
girdisidir.

**Kabul edilen risk yazılı bırakıldı.** Kayıt etkinlik gününe kadar açık kaldığı
için son gün gelen EFT/havale seansa yetişmeyebilir; o vakada katılım linki gitmez
(KARAR 486). Bu bedel bilinerek üstlenildi. Kapanış notuna açıkça yazıldı ki
sonraki turlarda "yeni bulunmuş hata" diye yeniden açılmasın — kapanmış bir borcun
en olağan çürüme biçimi budur.

*Yan bulgu, borç açılmadı:* 300'lük Açık Kapı kayıtlarında `paraBirimi` **boş**,
1000'liklerde `TRY`. Kapsam dışıydı; sorulmadan yazılmadı.

---

## 19 Ağustos 2026 — WA YÜZEN BUTON TURU (CC — `7d8d301` · `b926294` · `354fb14`)

Şikâyet tek cümleydi: yüzen WhatsApp butonu mobilde footer'ın `© 2026` satırını
kapatıyor. Buton `position:fixed`; sayfa sonuna inildiğinde altındaki içerikle
üst üste biniyor. Çözüm footer'a duyarlı fade oldu — footer viewport'a girince
buton çekilir, çıkınca döner [KARAR 517].

**Reddedilen alternatif: daimî silik buton.** Opaklığı sabit düşürmek üst üste
binmeyi çözmez, yalnız gizler; üstelik bozuk-render görünümü verir ve dokunma
hedefini belirsizleştirir. "Tasarımcının kendini ikna etmek zorunda kaldığı
detay süstür" — çekilme kararı, sönme kararından farklıdır.

### Brief üç yerde yanıldı, üçü de düzeltilerek uygulandı

ADIM 0 salt-read'i brief'in CSS'ini olduğu gibi yazmanın üç kaybı olacağını
gösterdi; Kaan üçünü de onayladı:

**(a) Token ezilmesi.** Brief `transition: opacity 250ms ease…` yazıyordu.
Dosyada zaten `transform`/`box-shadow`/`background` için `--duration-slow` +
`--ease-out` tanımlıydı; brief'in bloğu bu listeyi **değiştirir**, token'lı süreyi
250ms hard-code'la takas ederdi. Liste ezilmedi, **genişletildi** (KARAR 61 —
patch modu ekler ve değiştirir, çıkarmaz).

**(b) Özgüllük eşitliği.** `.wa-yuzen.gizli` ile `.wa-yuzen:hover` aynı ağırlıkta
(0,2,0). Brief `.gizli`yi hover'dan önce koyuyordu; o sırada gizliyken hover
`translateY(-2px)` kazanır, buton kaybolurken zıplardı. Blok `:focus-visible`
sonrasına alındı — çözüm kaynak sırasıdır, `!important` değil.

**(c) reduced-motion daraltması.** Brief o bloğu `transition: none`'a çekiyordu;
mevcut satır `background var(--duration-base)` taşıyordu, yani indirgenmiş
harekette bile renk geçişi yaşıyordu. `none` yazmak onu sessizce öldürürdü.
Satır genişletildi (`visibility 0s` eklendi), daraltılmadı.

Üçü de aynı sınıftan: **brief yanılabilir, refleks yanılmamalı** (KARAR 102).
Fark şu ki bu üçü çelişki değil **kayıp** üretiyordu — durmayı değil, düzelterek
uygulamayı gerektiren tip.

### Süre ayrı commit'te geri çekildi

İlk uygulama `--duration-slow` (800ms) kullandı: mevcut listenin süresi buydu,
tutarlılık öyle gerektirdi. Kaan kaydırmada ağır buldu; `--duration-base`
(400ms) ayrı commit'le geldi (`b926294`). Gerekçe estetik değil işlevsel: bu
fade **© satırının yolunu açıyor**, dekorasyon değil — 800ms fonksiyonel bir
hareket için uzun. `box-shadow` ve `background` `--duration-slow`'da bırakıldı,
hover'ın gölge/renk ritmi korundu.

**Bilinçle kabul edilen yan etki:** `transform` base rule'da hover ile paylaşılıyor;
base'e çekilince hover kalkışı da 800 → 400ms indi. İki hareketin tek hızda
yaşaması, aynı özelliğin iki hızda çatallanmasına tercih edildi.

### Ölçüm — ve tarayıcının yalanı

dist computed-CSS'ten iki durum tablosu alındı (KARAR 419 — statik CSS analizi
tek başına yetmez):

| durum | sınıf | opacity | visibility | pointer-events | transform |
|---|---|---|---|---|---|
| footer viewport dışı | `wa-yuzen` | 1 | visible | auto | none |
| footer görünür | `wa-yuzen gizli` | 0 | hidden | none | translateY(8px) |

İki ek kanıt tabloyu davranışa bağladı: gizliyken buton koordinatındaki
`elementFromPoint` → `FOOTER.footer` (tıklama footer'a geçiyor), ve `focus()`
tutmuyor → element **tab sırasından çıkıyor**. `visibility` gecikmesinin işi
buydu; `aria-hidden` toggle'ına gerek kalmadı.

**Ölçüm tuzağı — kod kusuru sanıldı.** İlk turda gözlemci hiç tetiklenmedi gibi
göründü: `.gizli` eklenmiyordu, sonra eklenip `opacity` 1'de donuyordu. Sebep
kodda değildi — Chrome sekmesi arka plandayken `document.visibilityState`
`hidden` olur, `requestAnimationFrame` durur, dolayısıyla **IntersectionObserver
callback'i gelmez ve CSS transition ilerlemez**. Sekme öne alınınca dördü de
yerine oturdu. Ders: Chrome'dan ölçmek yetmez, **sekmenin görünür olduğunu da
ölçmek gerekir** — yoksa tarayıcı sessizce yanlış cevap verir.

**Kapatılamayan ölçüm:** `prefers-reduced-motion` gerçek medya sorgusu emüle
edilemedi. Vekil ölçüm yapıldı — `tokens.css`'in reduce bloğundaki
`--duration-*: 0ms` override'ları sayfaya enjekte edilince `.gizli` aynı karede
uygulandı. Medya sorgusunun kendisi test edilmedi; boşluk **telefon eyeball'ı
ile kabul edildi**, kapatılmış sayılmadı.

**Telefon eyeball (Kaan):** fade + ≤480px + iOS Safari tek bakışta ✅. Mobil
viewport CC tarafında ölçülememişti — `resize_window` fullscreen pencerede
tutmadı, iki denemeden sonra bırakıldı ve **beyan edilmedi**. Gerçek cihaz
o boşluğu kapattı.

### Numara Ocak hattına döndü — ve iki yüzey ayrıldı

Nokta patch brief'i `15551911472 → 905325555226` diyordu. ADIM 0 durdurdu:
`15551911472` **kodda sıfır kez** geçiyordu; sabitin gerçeği `905322080888`'di.
Zincir üç kademeydi — `b075e5d` (`15551911472` doğdu) → `fce4ae8`
(→`905325555226`) → `0f77218` (→`905322080888`). Yani brief `0f77218`'i geri
alıyordu ve bunu bilmiyordu. Brief'in beslendiği yer de bulundu:
`20-ref-bot.md` kendi içinde çelişikti — satır 15 yeni numarayı, satır 20 hâlâ
en eskisini yazıyordu. **Bayat referans satırı, iki tur sonra yanlış brief üretti.**

Kaan kararı kapsamı daralttı: numara geri dönüyor **ama yalnız WA bot için**,
yasal sayfalar dönmüyor. Böylece `0f77218`'in birlikte taşıdığı iki şey kalıcı
olarak ayrıldı [KARAR 518]. `354fb14` yalnız `WHATSAPP_URL`'i taşıdı; beş yasal
sayfadaki `+90 532 208 0888` bilerek yerinde bırakıldı. Sabitin doc yorumuna
ayrımın kendisi yazıldı ki sonraki tur "tutarsızlık" sanıp eşitlemeye kalkmasın.

**dist doğrulaması:** `wa.me/905325555226` → 45 sayfa · `wa.me/905322080888` → 0 ·
`532 208 0888` → yalnız beş yasal sayfa. Üç component yorumu (Footer · Kanallar ·
WhatsappYuzen) stale kalmasın diye aynı commit'te senkronlandı.

### KARAR 396 kapandı

396 "yayın bot/Meta/n8n numara bağlama sırasına kilitli" diyordu. Bot Meta
tarafında `905325555226`'ya bağlandı (Kaan teyidi) ve site aynı hatta işaret
ediyor (`354fb14`) — şart sağlandı. ⚠ Numara 396'nın mühürlediği
`905322080888`'e değil, **ondan önceki hatta** döndü; ama 396 hat kimliğini
değil **bağlama sırasını** şart koşuyordu, o yüzden kapanış geçerlidir.

*Ledger notu:* dokuz değerlik `durum` enum'unda "karşılandı" sınıfı **yok**
(KARAR 456). 396 iptal edilmedi, kalıcı da değil — şartı sağlandı ve alanı 518'e
devretti. En yakın meşru değer `SUPERSEDE →518` seçildi ve gerekçesi ilişki
sütununa yazıldı. Enum'un bu boşluğu bu turda doğdu, kapatılmadı.

### Vercel teyidi (MCP)

`354fb14` → `dpl_2rh9MVg5PxC9uWFrfPmx158Q21po` **READY**, target production.
`b926294` → `dpl_D233apY3rp37g1obURFFczawG7bN` **READY**, production.
⚠ `7d8d301`'in **kendi deployment'ı yok** — `b926294` ile tek push'ta gitti,
Vercel yalnız tepe commit'i build etti. İçeriği canlıda, ama "üç commit üç
deployment" demek yanlış olurdu: **iki deployment üç commit'i taşıyor.**

Kurtarma tag'i `kurtarma-oncesi-wa-fade` (→`f7e4d73`) local'de bırakıldı, push
edilmedi — SHA yeterli, tag fazlalık (Kaan kararı).

### KARAR KAYITLARI

- **KARAR 517 — YÜZEN WHATSAPP BUTONU FOOTER GÖRÜNÜRKEN ÇEKİLİR (AKTIF):**
  `IntersectionObserver` (`rootMargin '0px 0px -10% 0px'`) footer'ı izler;
  footer viewport'a girince `.gizli` → `opacity 0` + `translateY(8px)` +
  `pointer-events: none` + **gecikmeli** `visibility: hidden`. Gecikme
  fade'in bitmesini bekler, sonra element erişim ağacından ve tab sırasından
  düşer — `aria-hidden` toggle'ına gerek kalmaz. Süre `--duration-base`
  (400ms): bu fade **fonksiyoneldir** (`© 2026` satırının yolunu açar),
  dekoratif 800ms reddedildi. Yan etki bilinçle kabul edildi — `transform`
  hover ile paylaşıldığı için kalkış da 400ms'e indi; iki hareketin tek hızda
  yaşaması, aynı özelliğin çatallanmasına tercih edildi. **Daimî silik buton
  reddedildi** (bozuk-render görünümü + dokunma hedefi belirsizleşir).
  Her viewport'ta çalışır, mobil-özel branch yok. İlişki: ←212.

- **KARAR 518 — BOT HATTI VE YASAL TELEFON İKİ AYRI YÜZEYDİR (KALICI):**
  `WHATSAPP_URL` (tek sabit, KARAR 213) = **bot hattı** `905325555226`; beş
  yasal sayfadaki düz metin `+90 532 208 0888` **ayrı yaşar**. `0f77218` ikisini
  birlikte taşımıştı, `354fb14` ayırdı. Bundan sonra numara değişimi **hangi
  yüzeyi hedeflediğini söylemek zorundadır**; "site numarası" ifadesi artık
  belirsizdir ve tek başına talimat sayılmaz. İki yüzeyin ayrı olması
  tutarsızlık değil tasarımdır — eşitlemeye çalışan tur, kararı okumadan
  dokunmuş sayılır. İlişki: ←213 · ←396.

---

## 19 Ağustos 2026 — WABA DISPLAY NAME + PROFİL METİNLERİ (claude.ai, sıfır commit, sıfır kod)

Kaan iki WhatsApp Business hattının profil metinlerini sordu; tur ad kararına döndü.
Yüzey Meta WhatsApp Manager — metinleri Kaan elle uyguladı, repo'ya kod girmedi.

**⚠ Numara sapması — patch'in bir iddiası bayattı.** Gelen patch üç kararı **517-519**
olarak numaralamıştı ve `WHATSAPP_URL`'in `905322080888` olduğunu varsayıyordu. Aynı
günün önceki turu (`ae321a3`, `docs-patch-2026-08-19c`) hem 517-518'i almış hem de
`354fb14` ile sabiti `905325555226`'ya geri çekmişti. Numaralar **519-520-521**'e
kaydırıldı, `→517` atıfları `→519` oldu; B104'ün belirtisi ölçülen gerçeğe göre
yeniden yazıldı (aşağıda). Sapmayı ADIM 0 yakaladı, Kaan onayladı (KARAR 470 · 102).

### Kararın omurgası — pahalı kol bırakılmaz

Meta onayı bu markada **kıt kaynaktır**: iki ret yaşandı ve sertifika kaydı
tamamlandıktan sonra ad denemeleri 30 gün aralığa düşer. "Ocak Kadın Çemberi" adının
tek kusuru OCAK'ı **K1'e daraltmasıdır** — çember zemin kat, K2-K4 adın dışında kalır.
Bu bir **içerik** kusurudur ve içerik kusuru **metin katmanında** telafi edilir.
Pahalı kolu (ad) bırakıp ucuz kolu (açıklama) çevirmek doğru sıradır.

Ayrıca display name'in işi **tanınırlıktır, konumlandırma değil**. Türkçede tek başına
"OCAK" ay adı ya da soba olarak okunur; bildirim satırında markayı taşımaz.

### Uygulanan metinler (Kaan, `…5226` profili)

Açıklama: *"Ateşin etrafında buluşuyoruz — seremoniler, atölyeler, yolculuklar.
Soruları yapay zekâ asistanı yanıtlar."*

`…0888` için hazır Hakkında (110 karakter, `python3 len()` ile ölçüldü — `awk`
kullanılmadı, KARAR 470'in bayt-karakter şerhi): *"Kadınların kadim ateşi. Çemberler,
seremoniler, atölyeler, yolculuklar. Soruları yapay zekâ asistanı yanıtlar."*

Uzun açıklama (309 karakter) hazır ama henüz hiçbir referans dosyasında yaşamıyor —
`20-ref-bot.md`'ye taşınacak (`03-sira.md`).

### Turun iki bulgusu — ikisi de borca çevrildi

**B103 — "OCAK" adı hakkında iki canlı beyan çelişiyor.** `20-ref-bot.md:13`
düz "OCAK"ı **denenmiş-reddedilmiş** sayar; KARAR 410'un kronoloji zinciri
(`2026-07.md:1194`) ise "Ocak Kadın Çemberi" ve "Ocak.biz" elendi, **ana aday "OCAK"**
der. Hangisinin daha yeni olduğu bilinmiyor. Bu belirsizlik KARAR 521'i **doğurmadı**,
521 onu **atlatmak** üzerine kuruldu — ortadan kaldırmak üzerine değil.

**B104 — `…0888` hattı WABA değil.** Patch bunu "site bota gitmiyor" belirtisiyle
açmak istedi; ölçüm bunu **çürüttü**: `src/lib/api.ts:26` = `https://wa.me/905325555226`,
yani yüzen buton · footer · Kanallar kartı üçü de **zaten bota gidiyor** (`354fb14`,
KARAR 396 bu yüzden kapandı). Patch'in önerdiği (b) şıkkı — "site `…5226`'ya geri
alınır" — **zaten uygulanmış durumdaydı.** Borç ayakta kaldı ama **belirti değil hedef**
olarak: `…0888` ikinci hat olarak WABA'ya açılacak, ad onayı alınacak, bot oraya
bağlanacak. Bugün kopuk bir şey yok; kurulacak bir şey var.

### KARAR KAYITLARI

- **KARAR 519 — ONAYLI DISPLAY NAME BIRAKILMAZ (KALICI):** Meta onayı bu markada
  **kıt kaynak** — iki ret yaşandı, sertifika kaydı sonrası denemeler 30 gün aralığa
  düşer. Onaylanmış bir ad, daha iyi bir ad uğruna **feda edilmez**. "Ocak Kadın
  Çemberi"nin kusuru OCAK'ı K1'e daraltmasıdır; bu **içerik** kusurudur ve açıklama
  alanında telafi edilir — "topluluk ve dönüşüm ekosistemi" + dört format. Pahalı kolu
  (ad) bırakıp ucuz kolu (metin) çevirmek doğru sıradır. Ayrıca display name'in işi
  **tanınırlıktır, konumlandırma değil**: Türkçede tek başına "OCAK" ay adı/soba olarak
  okunur, bildirimde markayı taşımaz. İlişki: ←410.

- **KARAR 520 — WABA PROFİL ALAN KANONU (KALICI):** (a) **Ad ne taşırsa Hakkında
  tekrar etmez** — KARAR 403 tekrar disiplininin WABA yüzeyine taşınması; ad "Kadın
  Çemberi" taşıyorsa Hakkında ateşi ve formatları taşır. (b) URL ve e-posta **ayrı
  alanlarda** yaşar, Hakkında'da geçmez — karakter israfıdır. (c) Adres **il düzeyi**
  ("İzmir, Türkiye"), sokak yok — `/iletisim` kuralının aynası. (d) Kategori
  **Eğitim** — YAP Eğitim Danışmanlık ile örtüşür, Meta incelemesine tutarlılık
  kazandırır. (e) Emoji yok, "köz" yok (KARAR 453). İlişki: ←403 · ↔57.

- **KARAR 521 — İKİNCİ HAT AD BAŞVURUSU "OCAK KADIN TOPLULUĞU" (AKTIF):** `…0888`
  hattı için aday **"Ocak Kadın Topluluğu"**. "Kadın" tanınırlığı taşır; **"topluluk"
  çemberin daralttığını açar** ve icat değildir — Ana Metin'in kendi cümlesi
  (*"bir topluluk ve dönüşüm ekosistemi"*). Meta riski "Kadın Çemberi" ile **aynı
  sınıfta** (tanımlayıcı ek); o aday geçtiği için risk kanıtlanmış-kabul edilebilir.
  **Title case, all-caps değil** — all-caps ret sebeplerinden biri olabilir ve display
  name'de inatlaşılmaz; wordmark sitede büyük kalmaya devam eder. Plan B **"Ocak
  Türkiye"** (bölge eki kural metninde açıkça serbest). Plan C "OCAK by YAP Eğitim"
  **Kaan tarafından elendi** — holding adı tüketici yüzeyine çıkmaz. Ön koşullar:
  `/hakkimizda`'da YAP Eğitim Danışmanlık anılıyor olmalı + Business Portfolio website
  alanı `https://www.ocak.biz`. İlişki: ↔519 · ←410.

**Marka çekirdeği:** DEĞİŞMEDİ. Marka işareti WABA avatarında canlı — B63'ün bir yüzeyi.

## 19 AĞUSTOS 2026 — `00-durum.md` TAHLİYESİ (üç dönem bloğu indi)

`00-durum.md` 200/200'e dayandı; marka işareti maddesi (KARAR 522 · 523) sığmıyordu.
Aynı tavanla ikinci karşılaşma — `ae321a3` turunda ertelenmişti, bu turda çözüldü.

Gerekçe Kaan'ın: **`00-durum.md` canlı durum dosyasıdır, tarihçe değil.** `BU DÖNEM NE
OLDU` içindeki eski dönem blokları zaten yanlış katmandaydı; taşınmaları tavan çözümü
olmanın yanında dosyayı asıl işine döndürüyor. Seçim ölçütü ad-hoc değil kural:
**kesin yaş sırasıyla en eski üç blok.**

Üçünün de gövdesi bu dosyada zaten yaşıyordu (`05-harita`/KARAR 482 → `:1545` · `:1582`;
KARAR 485 → `:1945`; KARAR 487 → `:1959`); inen şey **özet satırları**. Metne
dokunulmadı, kırpılmadı (KARAR 61) — aşağıda `00-durum.md`'deki hâlleriyle duruyorlar.

### `00-durum.md:140–141` — 17–19 Ağustos (üç format birden)

- **17–19 Ağustos (üç format birden):** Etkinlik üretimi dört formata yayıldı — Açık Kapı (altı gövde, bekçi mühürlü) · Çember (bekçi + dört gövde) · Seremoni (iki gövde, bekçi yok) ·
  Atölye (bir gövde, bekçi yok). **Üç yeni gövde canlıda.** → `90-kronoloji/2026-08.md`

### `00-durum.md:155–163` — 18–19 Ağustos (içerik + altyapı)

- **18–19 Ağustos (içerik + altyapı):** Çember bekçisi kuruldu — `ocak-etkinlik` artık
  **iki bekçili**: `ornekler.md` (Açık Kapı, altı sayfa, 3.019–3.247 kr) +
  `ornekler-cember.md` (Çember, dört sayfa, 3.824–4.113 kr). Seremoni bekçisi **yok**;
  iki sayfa ölçüldü (4.254–5.197 kr) ama iki nokta eğri vermiyor — **B66**.
  Etkinlik sayfaları: Açık Kapı 6 canlı · Çember 4 canlı (`elin-neyle-dolu` ·
  `neyi-bekliyorsun` · `ekmeden-once` · `hangi-tohumu-ekeceksin`) · Seremoni 2 taslak
  (kakao · `hasat-ve-sukran` 22 Eylül). MailerLite on ikinci alana çıktı; **Y1** kapandı
  (`92e580e`) — boş alan artık tele çıkıyor. **Promote kazası** teşhis edildi (KARAR 485),
  **403 yanlış alarmı** kayda geçti (KARAR 487). → `90-kronoloji/2026-08.md`

### `00-durum.md:164–166` — 11 Ağustos

- **11 Ağustos:** **B47 ✅ + B58 ✅** — doküman haritası (`05-harita.md`) yazıldı,
  KARAR 482 mühürlendi. B47 gövdesinin "harita hiçbir dosyada yok" iddiası ölçümle
  yanlışlandı: iki kısmi harita vardı, eksik olan **otorite kuralıydı**.

## 18–19 AĞUSTOS 2026 — MARKA İŞARETİ VE BAŞLIK KANONU

### KARAR 522 — işaret {#k522}

- **KARAR 522 — MARKA İŞARETİ: CORMORANT "O" + İÇ KOR (KALICI):** Outline harf,
  filtresiz gradyan, `nonzero` dolgu.

OCAK'ın on beş aylık ömründe ilk kez bir logosu var. Yol dokuz tur sürdü; elenenler
seçilenden çok şey öğretti, kaydı onun için burada.

**Elenen yönler.** Taş çemberi (merkezde kor, çevrede taşlar) — güzel sembol ama adı
içermiyor; öğretilmesi gerekiyordu ve öğretecek bütçe yok. Kemer, ufuk, damga, kıvılcım,
aralık — hepsi kendi hikâyesini taşıdı, hiçbiri harfin sadeliğini yenmedi. Tamga biçim
olarak markanın kadim damarına en yakındı ama Türkiye'de siyasi okuma taşıdığı için
**tasarım değil karar** düzeyinde elendi. Çatlak yönü en keskin cümleye sahipti — "ışık
kırıktan sızıyor" — ve tam bu yüzden reddedildi: marka kadının kırık olmadığında ısrarlı.

**Seçilen.** Harfin kendisi. O aynı anda dört şey: harf, ocağın ağzı, üstten görülen ateş
çukuru, kadınların oturduğu halka. Dördü de aynı biçim, hiçbiri açıklama istemiyor. Kadın
çizilmedi — çember zaten kadının biçimi; tarif edilirse ucuzlar. Yazılı ölçüt:
**çağrıştırmasın, çağırsın.**

**Üç teknik bulgu.**

1. **Ölçek yönü ters kurulmuştu.** Önce büyük hâl tasarlanıp küçültülmeye çalışıldı, her
   seferinde 24 pikselde çöktü (krem halka + karanlık aralık + kor = nişangâh). Çözüm en
   zorlu boyutta çalışan biçimi **büyütmek** oldu. Küçük olan masterdır.
2. **SVG filtresi taşınmaz.** Tanecik varyantı `feTurbulence` ile kuruluydu; render
   testinde dokunun hiç çıkmadığı ölçüldü (`turbulence std: 0.0`). E-posta istemcileri,
   baskı RIP'leri, favicon dönüştürücüleri filtreyi düşürüyor ve logo sessizce düz kora
   iniyor. Seçilen sette **hiç filtre yok**; kül perdesi bile bulanıklıkla değil gradyanla
   kuruldu. *Görünmeyen bağımlılık, olmayan bağımlılık değildir.*
3. **Dolgu kuralı `nonzero` olmalı.** İlk sette `evenodd` kullanılmıştı; A ve K
   harflerinin içinde beyaz boşluklar çıktı, Kaan yakaladı. Sebep: TrueType glifleri
   çakışan konturlarla çizilir (A'nın kolu, K'nin birleşimi) ve `evenodd` çakışmayı iptal
   eder. Ölçüldü: O ve C'de iki kural arasında **sıfır** piksel fark, A'da 263, K'de 254 —
   sapma tam da çakışan konturlu iki harfte.

**Perde tartışması.** "Üstü küllendi sadece" cümlesini logoya taşıma girişimi üç turda üç
kez denendi. Görünür seviyede işaretin optik ağırlığı aşağı kayıyor; görünmez seviyede
hiçbir şey yapmıyor. Kaan üç kez "ne olduğunu anlamadım" dedi — **tasarımcının kendini
ikna etmek zorunda kaldığı detay, detay değil süstür.** Perdesiz varyant seçildi;
küllenmişlik anlatısı hero cümlesinde ve metinde yaşamaya devam eder. `perdesiz` ·
`hafif` · `belirgin` üçü de üretildi, ikisi arşiv değeri için duruyor.

**Kare olmayan varlık hatası.** İşaretin viewBox'ı 708×688, kare değil. Oradan üretilen
`apple-touch-180.png` **180×175** çıktı, `favicon-32` 32×31, `.ico`'nun 48 kesimi hiç
oluşmadı. iOS ana ekran ikonunu kareye zorlar; harf yatay ezilecekti. CC uygulamada
yakaladı, uzlaştırmadı. Düzeltme iki yapısal ayrım gerektirdi: **app ikonu ayrı dosyadır**
(kare tuval, opak kömür zemin, %15 marj — şeffaf bırakılırsa iOS altına kendi rengini
koyar), ve **favicon SVG'si kareye alındı** (kare olmayan viewBox bazı tarayıcılarda
letterbox oluyordu). *Bir işaretin kendi oranı vardır, ikonun ise tuval oranı; ikisini
aynı dosyadan türetmek sessiz bozulma üretir.*

**Renk kilidi.** Çekirdek `#F4AE7C` · geçiş `#DA6C42` · köz `#C44B2F` · krem `#F2EAE2` ·
kömür `#1A1210`. Bu aralık yalnız logonun değil tüm görsel üretimin kilidi: Midjourney
kurucu görseli ve kart zeminleri buraya sabitlenir. Kül grisi ve ara tonlar (`#3D3532`,
`#2B2422`, `#2A1C18`) işaretten **çıkarıldı** — kor çevresindeki koyu alan zeminin
kendisidir, ayrı bir renk değil.

**KARAR 513'ün üçüncü tatbiki.** Sunum sayfası SVG'leri kendi içine kopyalıyordu; kaynak
dosyalar iki kez güncellendi, sayfa iki kez yeniden üretilmedi, iki kez eski çizimi
gösterdi. Kaan ikisinde de "set aynı" dedi ve haklıydı. Kanon zaten yazılıydı — eksik
olan tazelik kapısının bu yüzeye de uygulanması.

**Teslim.** 6 SVG + `ocak-appicon.svg` + 8 PNG + kare/dikdörtgen türevler. Künye
`20-ref-marka.md`'de. Uygulama borcu **B105**.

### KARAR 523 — başlığı kim üretiyorsa markayı da o koyar {#k523}

- **KARAR 523 — BAŞLIĞI KİM ÜRETİYORSA MARKAYI DA O KOYAR (KALICI):** Sayfalar
  Notion'dan, Etkinlikler ve sabit sayfalar çağrı yerinden.

Yer imi eklerken fark edildi: `OCAK — İçindeki Ateş Sönmedi · OCAK`. Ölçüm 45 rotanın
**8'inde** çift "OCAK" buldu. Sebep `Layout.astro:30`: `title === 'OCAK'` değilse koşulsuz
` · OCAK` ekleniyordu. Notion başlıkları da markayı taşıyordu — **aynı dizenin iki sahibi.**

İlk önerim yanlıştı: "Notion sadeleşsin, şablon markayı koysun." Notion'daki başlıklar tek
tip değil; yarısı markayı cümlenin içinde taşıyor (*Çember — OCAK'ın Kalp Atışı*), yarısı
hiç taşımıyor ve taşımaması doğru (*Sen Neredesin? — Bir Kadının Yolu*). Şablona ek
yaptırmak birincileri bozar, ek kaldırmak ikincileri markasız bırakır.

Kaan tersini seçti ve doğrusu oydu: **tüm Notion başlıkları elle markalı hâle getirildi,
şablon ek yapmayı bıraktı.** Ama bu tek başına yetmiyordu — site 45 rota, Notion Sayfalar
DB'si 17 tanesini besliyor. Kalanı Etkinlikler DB'sinden (15) ve koddan (13 + 3 SSR) geliyor.

Kural şu hâlini aldı: **başlığı kim üretiyorsa markayı da o koyar.**
- **Sayfalar DB (17):** tam sayfa başlığı Notion'da yaşıyor, kod dokunmaz.
- **Etkinlikler DB (15):** `baslik` etkinlik **adıdır**, sayfa başlığı değil (aynı alan
  dropdown label'ında da kullanılıyor, KARAR 422). Marka çağrı yerinde eklenir.
- **Kod sabitleri:** çağrı yerinde eklenir.

**Brief iki kez yanıldı, ADIM 0 iki kez yakaladı.** Birincisi: `/etkinlik/[slug]` ve
`/onizleme/*` "kodda kuruluyor" denmişti, ikisi de Notion'dan geliyordu — farklı DB'den.
İkincisi: "44 rota bekleniyor" denmişti, dayanağı `Yayınla`'nın süzdüğü varsayımıydı;
süzmüyor (→ B106). CC ikisinde de durdu, uzlaştırmadı.

**Ölçüm dürüstlüğü — üç kayıt.**

Mobil tarama önce `dist/client` üstünde koştu, **21/21 yeşil** geldi; ama sekiz rota
`viewportMeta=null` döndü — o rotalar SSR, `dist/client` altında yoklar, tarama onları hiç
ölçmemişti. *Yeşil bir sonuç, neyi ölçmediğini söylemiyorsa ölçüm değildir.* Naif
`title="…"` grep'i 17 markasız dize gösterdi — hepsi Hero/section başlığıydı; ölçüt
`<Layout>` etiketine kapsamlanınca sıfıra indi. Sitemap `grep -c '<loc>'` ile sayıldı,
1 döndü — dosya tek satır, `-c` geçiş değil satır sayar; bir an regresyon sanıldı, CC
yöntemi düzeltip 48'i buldu. Ve ledger'ın son numarası aralık okumasından çıkarıldı,
dosya sonundan değil; iki KARAR yanlış numarayla önerildi, Kaan durdurdu.
*Aralık okuması dosya sonu değildir; satır sayısı geçiş sayısı değildir.*

**Brief dışı iki ek, ikisi de onaylandı.** `.nav__logo { display:block }` — inline `<img>`
baseline'a oturup descender payıyla kutuyu büyütürdü, "nav yüksekliği değişmesin" şartı
bunu zorunlu kıldı. Ve hover affordance'ı: `color: var(--ember)` bir görsele işlemez,
kural `<img>`e taşındı. *Brief'in şartını korumak için brief'in dışına çıkmak gerekebilir;
şart metinden önce gelir.*

**Yan bulgu:** `--nav-height` token'ı `64px`, hesaplanmış nav `60px`. Token nav'ı sürmüyor
(yalnız `scroll-margin-top` ve drawer), görünür bozukluk yok, dokunulmadı.

---

## 19–20 Ağustos 2026 — FİKİR KUYRUĞUNUN KORPUSA ALINMASI (claude.ai + CC, sıfır kod)

**Kaynak sohbet 19 Ağustos, uygulama 20 Ağustos** — `docs-patch-2026-08-19c.md`.

Bir claude.ai sohbeti boyunca biriken iş/fikir kümesi korpus dışında kalmıştı. Kümenin kendisi
**bayat bir zeminden** kurulmuştu: sohbet Temmuz başı bir anlık görüntüyle çalıştı, korpus ise
bir buçuk ay ilerideydi. Ölçümle çıkan üç sapma: ödeme sorusu kart akışının kapanmasıyla
(KARAR 488) çoktan başka türlü çözülmüştü; lansman tarihi sohbette iki kez elle değiştirildi,
oysa tanım `robots` Allow + duyurudur (KARAR 149) ve hedef Eylül kohortudur; "PayTR flip"
blocker'ı KARAR 297'nin SUPERSEDE olduğu (→364) görülmeden taşınıyordu.

**Bayat liste korpusa eritilmedi.** Yalnız korpusta evi olmayan kalemler yeni borç olarak
açıldı (B108–B120); zaten evi olanlar mevcut numaralarına yönlendirildi; bayat olanlar
gerekçesiyle reddedildi. Karar cinsinden olanlar mühürlenmedi, aday olarak bekletildi.

**Ders (yeni değil, dördüncü vaka):** türev yüzey kaynaktan yeniden üretilmediğinde yalan söyler
(KARAR 513). Bu turda yalan söyleyen yüzey bir **sohbetin kendi belleğiydi** — dosya değil.
Uzun bir oturumda biriken liste, kaynağa dönülmediği sürece kendi iç tutarlılığıyla
ikna edici görünür. Duruş noktası: liste patch'e çevrilmeden önce korpus ölçülür.

### Numara iki kez kaydı — ve ikisi de yazımdan önce yakalandı

Patch **B103–B115** yazmıştı; ölçüm zemini `ae321a3` idi. ADIM 0 ilk koşuşta B103–B104'ün
WABA display name turunda (`3676628`) dolduğunu buldu — patch'in kendi ADIM 0 maddesi bu
durumda DUR diyordu, durdu. Kaan onay verdi ama yazım hemen yapılmadı; ikinci pencerede
CC'nin kod turu sürüyordu (KARAR 463: bir repo, bir aktif CC).

O bekleyiş **ikinci bir kaymayı** yakaladı: marka işareti turu (`b99773e`) araya girip
**B105–B107**'yi açtı. Yazım anında ölçüm yeniden koşuldu, kayma **+2'den +5'e** çıktı,
maddeler **B108–B120** olarak yazıldı. Patch'in gövdesindeki iç referanslar da kaydırıldı
(B103→B108 · B113/B114→B118/B119 · B109→B114 · B115→B120).

*Ders: onay ile yazım arasındaki her dakika sayacı bayatlatabilir. Kaydırma miktarı
onay anında değil, **yazım anında** ölçülür — devralınan rakam ölçülmüş sayılmaz (KARAR 470).*

**İkinci bulgu, aynı beklemenin ürünü:** marka turu üç borç açtı ama `02-borclar.md`
başlığındaki sayacı güncellemedi. Onuncu ölçüm o borcu da kapattı — 16 maddelik artışın
13'ü bu turun, 3'ü devralınan sayım borcu. Sayacın iki tur bayat kalabilmesi, sayının
**gövdeden türetilmesi** gerektiğinin kanıtıdır (KARAR 484); başlık türev yüzeydir.

### Mükerrer taraması — 13 madde, sıfır çakışma

Her yeni başlık gövdeye karşı tarandı. `çerez` · `consent` · `banner` · `arşiv` · `seans` ·
`vurgu` · `stil kodu` · `success ekranı` dizeleri `02-borclar.md`'de **hiç geçmiyordu** —
on üç maddenin hepsi gerçekten evsizdi, hiçbiri düşmedi. Marka turunun yeni B105'i
(işaret canlı yüzeylerin yarısına uygulanmadı) ile B114 (görsel stil kodu) **komşu ama ayrı**:
biri varlığın dağıtımını, öbürü üretim kodunu sorar. B105'in açık OG kartı ayağı B114'ün
üçüncü müşterisi olarak işaretlendi — patch onu göremezdi, aynı gün açılmıştı.

### Kaydedilmeyenler — yedi kalem, gerekçeleriyle

Sohbette "açık iş" sayılan yedi kalem borca çevrilmedi: PayTR flip (KARAR 297 SUPERSEDE →364,
Faz 1 kart akışını kapattı) · lansman tarihi (tanım KARAR 149, hedef Eylül kohortu KARAR 492) ·
tek PR merge (Temmuz zemini; bugünkü kuyruk B64 → B69 → B68 → n8n) · og-default+favicon
(kaydı var, bugünkü durumu ölçülmedi — ölçüm işi, borç değil) · footer yasal linkleri
(KARAR 363 onları kayıt lokal şeridine taşımış) · son eyeball (B74'ün açık ayağında yaşar) ·
program detay görünümü (çözülmüş, `/etkinlik/[slug]`).

*Açılmayan madde sayaca girmez; gerekçesi burada yaşar — patch dosyasının Bölüm 6 ve 7
tabloları korpusa girmedi, bu özet onların yerine geçer.*

**Karar mühürlenmedi.** Beş karar adayı (A-1 "Retreat"→"İnziva" · A-2 manifesto cümlesi ·
A-3 Açık Kapı çekim mekaniği · A-4 alt yönlendirme kutuları · A-5 arşiv şifre modeli) ve
beş fikir (F-1…F-5) patch dosyasında bekliyor; yerleri Kaan'ın kararına bağlı, bu turda
hiçbir dosyaya yazılmadılar. Ledger `523`'te sabit kaldı.

---

## 21–22 Ağustos 2026 — BOT TURU: BAYAT BAĞLAM, SESSİZ KIRILMA, X1 (claude.ai + CC)

**Kaynak:** 21–22 Ağustos bot turu · `brief-docs-patch-2026-08-22.md` + ek gövde
`20-ref-bot-yeni.md`. Kod tarafı ayrı turdu (`58ed14c` · `a5ade5a`, aşağıda).

### Bir — korpus altı haftadır yanlıştı

13–14 Temmuz'da bot sıfırdan yeniden kuruldu: üç workflow, beş Postgres tablosu, router,
kill switch, iki bloklu cache. **Bu rebuild korpusa hiç yazılmadı.** `20-ref-bot.md` altı
hafta boyunca "tek Code node, Notion'dan canlı okuma" dedi; Postgres'ten hiç söz etmedi.
`docs_ara` üç anahtar sözcük için **sıfır** döndü.

Bu, KARAR 513'ün (türev yüzey kaynaktan üretilmezse yalan söyler) bir varyantı değil —
daha kötüsü: **kaynak hiç yazılmamıştı.** Bayat yüzey en azından bir zamanlar doğruydu;
bu dosya hiçbir zaman rebuild sonrasını anlatmadı.

### İki — 15 günlük sessiz arıza

**6 Ağustos 12:00:23'te Notion token'ı rotate edildi, n8n güncellenmedi.** Dört `try/catch`
401'i yuttu, `cikti = []` döndü, hiçbir yazım olmadı ve n8n **15 dakikada bir "Success"
yazdı.** Bot 15 gün boyunca var olmayan etkinliklerin tarihlerini söyledi.

**19 Ağustos'ta bir kadın *"Tarihler bozuk"* yazdı.** Arızayı izleme bulmadı, kullanıcı buldu.

Üç ders, üçü de ölçümle: **"Success" bir şey kanıtlamaz** — `try/catch` hatayı yutarsa n8n
yeşil yazar. **Koşmak ≠ yazmak** — cron doğru koşuyordu, hiçbir şey yazmıyordu.
**Ölçüm çıkarımı yener** — "267 execution ayda 2880 olmalıydı" aritmetiği `OCAK · Bağlam`'ı
temize çıkarmıştı; sayı kayan pencereymiş, Executions ekranı gerçeği gösterdi.

Panzehir KARAR 525: bağlam üretimi eksik kalırsa **`throw` eder**, kısmi yazım yapılmaz.
Bayat veri, eksik veriden daha tehlikelidir — çünkü sessizdir.

### Üç — bot kendi geçmişini few-shot örnek aldı

Aynı gece bot **kayıtlı** bir kadına Zoom linkini vermeyi reddetti. Veri Postgres'teydi,
kural prompt'taydı, ikisi de doğruydu. Sebep: bot kendi konuşma geçmişindeki eski
"veremiyorum" cevaplarını **örnek** alıyordu. Geçmiş silinince doğru cevap verdi.

Oturum sınırı (KARAR 526) bu yüzden kuruldu: `Geçmiş Oku` yalnız altı saatlik pencereyi
okur. Kullanıcıya sıfırlama komutu **eklenmedi** — kadın kilitlendiğini fark etmez, komut
duyurmak kalıcı token yer.

### Kapananlar

- **X1 — kayıt lookup canlı testi.** Temmuz'dan beri açıktı, sınanamamıştı. Sır zinciri ve
  ödeme kapısı gerçek veriyle **iki yönde** sınandı: `Ödendi` → verdi, `Beklemede` → vermedi,
  Postgres'e placeholder yazıldı.
- **Dört canlı secret n8n Code node'da açıkta** — rotate edildi + `$env` geçişi yapıldı
  (KARAR 524). CLAUDE.md §8 / KARAR 469'un bot tarafındaki karşılığı.
- **Etkinlik başlıkları test adı** — kendiliğinden kapandı.
- **Bağlam bayatlığı** (6 Ağustos → 21 Ağustos) — yukarıdaki ikinci bulgu.

⚠ Bu dördü **korpus borcu (`B` numarası) değildi** — bot turunun kendi iç kayıtlarıydı.
`02-borclar.md`'de karşılıkları yoktu, o yüzden orada işaretlenecek bir şey de yoktu.
Kapanış kaydı burada ve `20-ref-bot.md`'dedir.

### Korpus tarafında ne yapıldı

**`20-ref-bot.md` baştan yazıldı — patch değil, tam değişim.** Eski gövde
`_arsiv/20-ref-bot-2026-08-06.md` olarak saklandı, başına köken şerhi eklendi.
**Kırpma yok (KARAR 61):** yeni gövdenin karşılığı olmayan dört bölüm canlı dosyaya
**taşındı** — MailerLite Custom Field envanteri · KARAR 57 veri etik çerçevesi ·
KARAR 410 Meta display name · KARAR 447 WhatsApp stratejik konumlama. Taşıma `diff` ile
birebir doğrulandı.

**Numaralar gövdeye serpilmedi** (Kaan kararı): dosya kavramlarla kuruldu, KARAR numaraları
dosya sonundaki tek eşleme tablosunda toplandı. `01-kararlar.tsv`'nin `447` satırı
`20-ref-bot.md#k447` çapasına baktığı için o başlık düzeyi korundu (KARAR 466).

**Üç atıf `20-ref-bot.md:13`'e bakıyordu** (B103'ün çelişki kanıtı). İkisi arşiv dosyasına
çevrildi (`02-borclar.md` · `03-sira.md`). **Buradaki üçüncüsü çevrilmedi** — kronoloji
append-only'dir, geçmiş giriş yeniden yazılmaz. Yönlendirme bu satırdır: `2026-08.md:2820`'de
geçen `20-ref-bot.md:13`, bugünden itibaren `_arsiv/20-ref-bot-2026-08-06.md:41`'dir.

**`_bolme-haritasi.tsv`'ye dokunulmadı** (Kaan kararı): o tablo tek bir PİLOT dosyasının
satır-satır dağıtım haritasıdır (`pilot_satir · hedef · ilk_80_karakter`), genel taşıma
kütüğü değildir. Dosya taşıma satırı o şemaya girmiyor; köken izi arşiv şerhinde yaşıyor.

**`00-durum.md` yazılmadı.** Ölçüm: 195/200 satır (tavan KARAR 457) ve dosyada bot için
ayrılmış blok yok — bilgi sekiz ayrı satıra dağılmış. Brief'in §4'ü **iptal edildi**
(Kaan, 22 Ağu), yerine **B131** açıldı.

### Kod tarafı — aynı gün, ayrı tur

`/api/davet` 20 Ağustos 09:13 UTC'den beri **açık röle** gibi kullanılıyordu: saat başı,
toplanmış bir spam listesine, OCAK'ın gerçek davet şablonuyla. API anahtarı sızmamıştı
(Resend log `User-Agent: resend-node`). Brief "success ekranındaki form korumasız" diyordu;
ölçüm bunun yarısını doğruladı — **saldırganın formu kullanma zorunluluğu yoktu**, uç
kimlik/Origin/oturum istemeyen açık bir JSON ucuydu ve payload şekli yedi sayfanın statik
HTML'inde inline duruyordu. Honeypot tek başına bu saldırıyı durdurmaz; musluk durdurur.

`58ed14c` (üç katman) → `a5ade5a` (merge). Ölçüm: 271 test yeşil · `npm run build` iki kez
yeşil · canlı yedi kayıt sayfasında `<aside class="davet-kutusu">` **0/7**.
Son kötüye kullanım gönderimi 21 Ağustos 19:44 UTC. **KARAR 535 · B121.**

⚠ **Ölçüm tuzağı, sonraki turda dist grep'lerken:** bayrak kapalıyken markup düşüyor ama
Astro'nun hoisted script'i sayfalarda inline kalıyor. `grep -rl "data-davet-kutusu"` bu
yüzden **hâlâ 7 döner** ve "kapanmamış" sanılır. Markup ölçmek için açılış etiketi
(`<aside class="davet-kutusu"`) grep'lenir.

### Mühürlenen kararlar

- **KARAR 524 — SECRET TEK KAYNAĞI RAILWAY ENV (KALICI):** tüm n8n secret'ları `$env`'den
  okunur; workflow `Secrets` node'ları boş bırakılır — **silinmez**, `Bağlam Üret` yedek
  referans taşır. n8n Variables Community Edition'da kilitli, kullanılmaz. Her tüketiciye
  kendi Notion token'ı: bot `BotZ`, site `Ocak Site`.
- **KARAR 525 — SESSİZ KIRILMA YASAĞI (KALICI):** bağlam üretimi eksik kalırsa `throw` eder;
  kısmi yazım yapılmaz, hata mesajı sebebi taşır. Dört workflow `OCAK · Hata` Error
  Workflow'una bağlıdır. *"Bayat veri, eksik veriden daha tehlikelidir — çünkü sessizdir."*
- **KARAR 526 — OTURUM SINIRI (AKTIF):** `Geçmiş Oku` yalnız `ocak_gizli.gecmis_saat`
  (varsayılan 6) saatlik pencereyi okur. Geçmiş boşsa `kisisel` bloğa "ilk temas", doluysa
  "tanıtma" yazılır. Kullanıcı sıfırlama komutu **eklenmez.**
- **KARAR 527 — CACHE DİSİPLİNİ (KALICI):** kalıcı kural `ortak` bloğa (Notion prompt
  sayfası) yazılır; `kisisel` blok cache'siz olduğu için kısa tutulur. `ortak` bloğu
  WhatsApp ve Instagram'da **birebir aynı** olmalıdır.
- **KARAR 528 — `ocak_etkinlik_detay` BUDAMASI (AKTIF):** `guncelleme < now() - 1 hour`
  ölçütüyle silinir, node `Detay Yaz`'ın **ardına** bağlanır (`Execute Once: ON`). Gerekçe:
  yazım başarısızsa budama da olmamalı.
- **KARAR 529 — TELEFON EŞLEŞMESİ FORMATTAN BAĞIMSIZ (KALICI):** Notion'a telefon filtresi
  gönderilmez; ödeme filtresiyle çekilip JS'te rakama indirgenip **son 10 hane**
  karşılaştırılır.
- **KARAR 530 — KANAL BEYANI ZORUNLU (KALICI):** her kanalın `kisisel` bloğu hangi kanal
  olduğunu açıkça yazar. Modele çıkarım bırakılmaz.
- **KARAR 531 — YETERLİLİK KURALI (KALICI):** katılım detayı eksikse yarım cevap verilmez;
  yedek metin yazılır ve `operasyonel` devir tetiklenir. Konuşma geçmişine **gerçek durum**
  yazılır (`paylaşıldı` / `verilemedi`).
- **KARAR 532 — KRİZ HATTI (KALICI):** kriz devrinde WhatsApp (`OCAK_KRIZ_WA`) + mail
  (`OCAK_KRIZ_MAIL`) **birlikte** gider; ikisi bağımsız `try/catch`. Bot kriz durumunda
  süre söylemez.
- **KARAR 533 — DEVİR KUTUSU KAAN'DA (AKTIF):** `devir@ocak.biz` grubu kurulmaz.
  `admin.ocak.biz@gmail.com` → Gmail yönlendirme → `kaan@ocak.biz`.
- **KARAR 534 — SİSTEM MAİLİ GÖNDEREN ADRESİ (AKTIF):** `sistem@mail.ocak.biz`
  (`SISTEM_MAIL_FROM`). `onboarding@resend.dev` terk edildi.
- **KARAR 535 — DAVET AKIŞI GEÇİCİ OLARAK KAPALI (AKTIF):** `DAVET_AKISI` varsayılan kapalı;
  musluk + Origin + honeypot + zaman damgası merge edildi (`a5ade5a`). **Bu geçici bir
  önlemdir, kalıcı karar değil.** Davet, kayıt akışının parçasıdır; lansmandan önce
  açılmalıdır. Açma şartı: Turnstile + IP hız sınırı + davet edenin adres doğrulaması.
  Açarken `DAVET_AKISI=acik` **ve redeploy** — değer build zamanında sabitlenir (KARAR 488
  deseni). Musluk kapalıyken Resend maili **ve** Notion Davetler DB kaydı (KARAR 271) birden
  susar. Borç karşılığı **B121**.

### Yeni borçlar

**B121–B131.** İkisi lansman engeli (B121 davet akışı 🔴 · B122 Zoom kaydı süreci);
B123 sohbet paneli · B124 Postgres yedeği · B125 kriz WA şablonu · B126 kriz WA hatası
sessiz · B127 token maliyeti · B128 idempotans (Meta retry) · B129 bot dili sızıntısı ·
B130 `/api/davet` idempotansı · B131 `00-durum.md` tavanı.

**Teyit (Kaan, 22 Ağu):** `xÇember Döngüsü` ve `xAtölye` bağlantısı Ocak Site
integration'ından **bilinçli kaldırıldı, site okumuyor** — `Prompt`/`Özeti`/`SSS` teyidiyle
birlikte bot ref'indeki açık kalem kapandı. ⚠ `20-ref-notion.md:24` ve `:62` hâlâ Çember
Döngüsü'nü workspace yapısında sayıyor; o dosya bu turda DOKUNMA kapsamındaydı, tazelenmesi
ayrı envanter turudur.

---

## 22 AĞUSTOS 2026 — PANEL · DAVET · INSTAGRAM TURU (claude.ai + CC)

On dört saatlik tek oturum. **Altı iş kapandı, yedi borç doğdu, altı KARAR mühürlendi.**
Bot kanalı turu; site tarafına yalnız davet maili üzerinden dokunuldu (beş CC turu,
`main` → `32e1c91`).

**Sıra:** kriz WA şablonu (Meta'ya gönderildi) → sohbet paneli (kuruldu, canlı) →
davet akışı (açıldı, uçtan uca sınandı) → davet maili (beş CC turu, merge) →
Postgres yedeği (ilk) → kriz WA hata yüzeyi (kod) → Instagram (canlı) →
panel maliyet + kontrol sekmeleri.

### Öğrenilenler — bu turun refleksleri

**`=` işareti n8n'de gösterimin parçasıdır, değerin değil.**
Expression alanı zaten `fx` modundaysa başına `=` yazmak onu URL'nin, header'ın, JSON
gövdenin parçası yapar. **Üç ayrı yerde aynı hataya düşüldü** — `IG Gönder` URL'i,
Authorization değeri, JSON body. Üçü de ayrı ayrı teşhis edildi. Kök sebep: ekrandan
kopyalanan bir gösterim, girdi sanıldı.
*Ekrandan okunan bir dize, girilecek dize değildir.*

**Railway Data paneli çok sütunlu sorguda güvenilmez — bu kez sütun kaydırdı.**
`SELECT model, count(*), sum(a), sum(b), sum(c), sum(d)` çalıştırıldı; ekranda tek bir
`sum` sütunu göründü ve `input_tokens` sanıldı. Gerçekte başka bir sütundu. Bu yanlış
okuma **panelin doğru rakamına "yanlış" dedirtti.** Tek sütunlu sorguyla çürütüldü
(222.276 — panelin dediği). Korpusta zaten "tek seferde tek cümle" uyarısı vardı;
**tek sütun** kuralı yeni.
*Ölçüm çıkarımı yener — ama ölçüm aracının kendisi de ölçülmeli.*

**Build zamanı sabit katlama sunucu kodunu bundle'dan siler.**
CC'nin bulgusu. `RESEND_API_KEY` build ortamında yokken `if(!KEY) return` erken dönüşü
sabit-katlanıyor, Rollup fonksiyonun **tamamını** eliyor. `dist/` denetimi "şablon
bundle'a hiç girmemiş" diyor; oysa Vercel'de anahtar dolu olduğu için normal çalışıyor.
`DAVET_AKISI`/`KART_AKISI` ile aynı sınıf ama daha sinsi: orada **yüzey render
edilmiyor**, burada **sunucu kodu siliniyor.**
Yordam: `RESEND_API_KEY=re_placeholder_not_a_secret npm run build`.

**Item çarpımı panelde yeniden çıktı.**
`Oku`/`Token Oku`/`Bayrak Oku` node'larında `Execute Once` kapalıydı; panel 79 çağrıyı
**1.248** olarak saydı, maliyeti **kırk kat** şişirdi ($75.73 → $1.58). Korpusta
`Geçmiş Oku` için aynı not vardı (`20-ref-bot.md:500`). **Aynı tuzak, yeni yer.**
Kodda ikinci savunma hattı olarak `id` bazlı tekilleştirme de eklendi.

**"Continue on fail" en kritik akışta en sessiz akışı üretir.**
`IG Gönder` üç ayrı hata verdi, hiçbiri yüzeye çıkmadı. `IG Bot` "no items" diyordu;
teşhis oradan başladı, hata mesajına ulaşmak saatler aldı. → **B134**

**Bir mesaj Instagram'da iki webhook olayı üretir.**
Asıl mesaj (API'ye gider, saniyeler sürer) ve `message_edit` (milisaniye, elenir).
Executions listesinde hep sonuncuya bakıldı, hep edit çıktı. **Süre farkı ayırt
edicidir:** 4 saniyelik execution gerçek çağrıdır, 70 ms'lik değil.

**Brief kendi içinde tutarsız olabilir.**
Dördüncü davet maili brief'i hem "sıfır `çember` eşleşmesi" hem "başka cümleye dokunma"
istedi; kalan eşleşmeler cümlenin içindeydi. CC brief'i zorlamak yerine çelişkiyi
**rapor etti** — doğru davranış (KARAR 102). Beşinci turda düzeltildi.
*"Briefler yanılabilir, refleksler yanılmamalı"* bir kez daha işledi.

**CC brief'te istenmeyen bir güvenlik açığını kendi gördü.**
Davet maili turunda: kullanıcının yazdığı bir dize **ilk kez** OCAK imzalı ve doğrulanmış
bir alan adından çıkan maile basılıyordu. `src/lib/davet-baglam.ts` ile kapatıldı
(HTML kaçırma, uzunluk sınırı, `landingPath` beyaz listesi). Alan adı geçen hafta röle
olarak kötüye kullanılmıştı — **bağlam CC'nindi.**

### Mühürlenen kararlar

- **KARAR 536 — SOHBET PANELİ (AKTIF):** Botun geçmişi n8n execution budaması yüzünden
  denetlenemiyordu (61 turluk konuşma panelde 5 görünüyordu). Çözüm: `OCAK · Panel`
  workflow'u — Postgres'ten okur, tek dosyalık HTML döner, `panel.ocak.biz`'den servis
  edilir. **Sıfır commit, Vercel'e dokunulmaz.** Üç sekme: **sohbetler** (konuşma
  görünümü, kanal süzgeci, arama, WhatsApp'ta aç) · **maliyet** (günlük/kanal/model
  kırılımı, konuşma ve çağrı başına, cache tasarrufu) · **kontrol** (bot aç/kapa, fiyat
  ve kur düzenleme). Erişim tek bir `PANEL_TOKEN` ile (Railway env); token yoksa `throw`,
  yanlışsa "Bu sayfa yok" — **404 değil, 200 + boş sayfa** (varlık sızdırmaz). Parametrik
  `AYAR` bloğu ile başka bir markaya kopyalanabilir tasarlandı; **denenmedi** (B133).
  Borç karşılığı **B123 ✅** ve **B127 ✅**; mimari `20-ref-bot.md` §5a.

- **KARAR 537 — KARAR 535 GEVŞETİLDİ · DAVET AKIŞI ŞARTSIZ AÇILDI (AKTIF):**
  KARAR 535 üç şart koşuyordu — Turnstile · IP hız sınırı · davet edenin adres
  doğrulaması. **Üçü de yapılmadan açıldı.** Karar **bilinçlidir, sessiz değildir.**
  Gerekçe: 31 Ağustos lansmanında davet halkası eksik kalamaz. Merge edilmiş korumalar
  (musluk + Origin + honeypot + zaman damgası, `a5ade5a`) **rastgele** bot trafiğini
  keser; üç şart **hedefli** saldırıya karşıdır — fark bilinerek üstlenildi.
  `DAVET_AKISI=acik` üç ortamda (Production · Preview · Development); Preview'ın da açık
  olması bilinçli, kapalı kalsaydı her test canlıya bakmayı gerektirirdi. Geri dönüş
  yordamı: `DAVET_AKISI=kapali` + redeploy, **iki dakika.**
  ⚠ Üç şart **borç olarak devam eder (B132)** — KARAR 535 iptal değil, **ertelendi.**

- **KARAR 538 — INSTAGRAM KANALI AÇILDI · APP REVIEW GEREKMEDİ (AKTIF):**
  Varsayım yanlıştı: engel App Review değil, **app'in yayınlanmamış olmasıydı.**
  Meta app `861407993595884` (WhatsApp ile **aynı app** — ayrı app sanılmıştı,
  düzeltildi) *In development* durumundaydı. Bu durumda `messages` webhook alanı abone
  görünse bile **gerçek mesaj taşımıyor**; yalnız `message_edit` geliyor ve o da `sender`
  alanı taşımıyor. Publish engeli: **Privacy policy URL boş.** Doldurulunca Publish
  açıldı, `messages` olayları akmaya başladı. **Standard Access + tester rolü yeterli.**
  Advanced Access (App Review) yalnız **sahibi olunmayan** müşteri hesaplarının DM'lerini
  yönetmek için gerekiyor.

- **KARAR 539 — IG KONUŞMA PENCERESİ WA İLE HİZALANDI (AKTIF):**
  `IG Geçmiş Oku` yalnız `LIMIT 20` kullanıyordu — **zaman filtresi yoktu.** Aynı bot iki
  kanalda iki farklı hafızaya sahipti: WhatsApp altı saatlik pencereli (KARAR 526),
  Instagram penceresiz. Sonuç: üç ay sonra yazan kadına bot **eski konuşmadan devam
  ederdi, kendini de tanıtmazdı.** Marka açısından WhatsApp'takinden daha riskli.
  Düzeltme: aynı `ocak_gizli.gecmis_saat` koşulu IG sorgusuna eklendi. Ayrıca `IG Bot`
  koduna **`ilkTemas`** hesabı girdi (geçmiş boşsa "kendini tanıt", doluysa "TANITMA") —
  **modele bırakılmadı, koda alındı** (KARAR 525'in dersi). Sınandı: `gecmis_saat=0` →
  tanıtım yapıldı · `=6` → *"Merhaba tekrar"*, tanıtmadı.

- **KARAR 540 — PANEL FİYAT VE KUR `ocak_gizli`'DE (AKTIF):**
  Maliyet hesabının iki girdisi — model fiyatları ve USD/TRY kuru — koda gömülmek yerine
  `ocak_gizli`'ye alındı: `kur_usd` · `fiyat_girdi` · `fiyat_cikti` · `fiyat_okuma` ·
  `fiyat_yazma`. Panelin kontrol sekmesinden düzenlenir, `panel-bayrak` webhook'una POST
  edilir, **sunucu tarafında** beyaz liste + biçim doğrulamasından geçer. **Deploy
  gerekmez.** Doğrulanmış değerler (22 Ağu, Anthropic liste fiyatı, milyon token başına):
  girdi **$3.00** · çıktı **$15.00** · cache okuma **$0.30** · cache yazma **$6.00**.
  ⚠ Cache yazma **bir saatlik** cache fiyatıdır (temel girdinin **2 katı**); kod
  `ttl: '1h'` kullanıyor. Beş dakikalık cache 1.25 kat ($3.75) olurdu — ilk yazımda bu
  değer yanlış girilmişti, düzeltildi.

- **KARAR 541 — BOT ADI: OCAK KADIN TOPLULUĞU (ONERI):**
  WABA'da iki display name onaylı: *Ocak Kadın Çemberi* (kullanımda) ve *Ocak Kadın
  Topluluğu*. İkincisi tercih edildi. Gerekçe: **KORUNACAK İFADE #8** — *"Çember kutsal —
  AI bot girmez."* Botun adına "Çember" koymak markanın **kendi kuralıyla çelişir.**
  Bot çemberin içindeki ses değil, **kapısındaki ses.** Ayrıca "Çember" K1'in ürün adı;
  bot yedi formatın hepsini karşılıyor, tek katmanı işaret eden isim **daraltıyor.**
  ⚠ **Uygulanmadı.** Değişiklik kadınların gördüğü sohbet başlığını etkiler — **kayıt
  açılmadan önce** yapılmalı. Durum `ONERI`; uygulanınca `AKTIF`'e döner.

### Kapananlar

**B121 ✅** davet akışı (şartsız açıldı, zincir uçtan uca sınandı) ·
**B123 ✅** sohbet paneli (`panel.ocak.biz` canlı) ·
**B127 ✅** token maliyeti (panel ölçtü: 79 çağrı, $1.58, cache okuma %71).

**Daralanlar — kapanmadı, şekil değiştirdi:**
**B122** sahip Kaan → **Advaita** (bot işi değil) ·
**B124** ilk `pg_dump` alındı (22 Ağu, 16 MB, iCloud) ama **cron yok** ·
**B125** `ocak_kriz_devri` onaylandı ama **Marketing**'e düşürüldü, itiraz gönderildi
(pencere **22 Ekim 2026**) ·
**B126** kriz WA hata yüzeyi **kodu yazıldı**, gerçek başarısızlıkla **sınanmadı.**

### Yeni borçlar

**B132–B138.** B132 davet koruma katmanları (KARAR 535'ten devir) · B133 panel v2
(günlük maliyet tavanı dahil) · B134 `IG Gönder` sessiz · B135 `Secrets` node'ları ölü ·
B136 `Referans No` property'si · B137 `/odeme/tamam` davet dalı · B138 secret hijyeni.

⚠ **Numaralandırma düzeltmesi (23 Ağu, patch uygulaması):** bu turun patch'i yeni
borçları **B124'ten** başlatıyordu; B124–B131 defterde **başka içerikle doluydu.**
Patch bayat bir borç görüntüsüne dayanıyordu. Kaan'ın kararıyla yeni borçlar **B132**'den
başladı; patch'in "B126 otomatik yedek" · "B131 kriz WA testi" · "B132 Marketing"
kalemleri **yeni borç değil**, mevcut B124 · B126 · B125'in güncellemesi olarak yazıldı.
*Ledger indeks, defter gövdedir — çelişkide gövde kazanır (KARAR 482).*

⚠ **`00-durum.md` bu turda yazılmadı.** Patch bot bölümü öngörüyordu; **B131** hâlâ açık
(dosya 195/200, bot için ayrılmış blok yok). Kaan'ın kararı (23 Ağu): tahliye + bot bloğu
**ayrı bir turun işi**, bugünün patch'ine sığmaz ve yorgun kararla yapılmaz.
Dolayısıyla **dönem HEAD satırı da tazelenmedi** — `c45332e`'de (19 Ağu) duruyor.

---

## 23 AĞUSTOS 2026 — GÜN 0: KURUCU GÖRSEL, İ1 KARTI, DERLEYİCİ STANDARDI (claude.ai + Midjourney)

Tek oturum, **sıfır kod**. **Beş KARAR mühürlendi (542–546), iki borç kapandı (B86 · B114),
üç borç doğdu (B139–B141).** Seans korpusun `ae321a3` hâliyle açıldı, `c4a6614` ile kapandı;
arşivci turu `c4a6614` üstüne bindi. Ham devir kaydı: **`_arsiv/2026-08-23-devir.md`**
(205 satır, olduğu gibi alındı — tek düzeltme §8'deki bayat satır sayısı).

**Ne oldu:** Gün 0 kilidi (KARAR 450) açıldı. Kurucu görsel üretilip mühürlendi, İ1 söz
kartının zemini seçildi, kart basıldı, kart derleyicisi altı gömülü ön ayarla çalışır
hâle geldi ve repoya alındı (`tools/ocak-kart-derleyici.html`).

### Mühürlenen çapalar

| Ne | Değer |
|---|---|
| **Kurucu görsel** (`--sref` çapası) | `https://cdn.midjourney.com/b6457637-7065-4c39-b551-268b156c9408/0_2.png` |
| **İ1 kart zemini** | `_arsiv/2026-08-23-devir.md` §2 |
| **MJ motoru** | `--v 8.1` (varsayılan V8.2'dir, elle sabitlenir) |
| **Zemin siyahı** | `#1A1210` (marka kanonu — `#1A1614` bu turda emekli oldu) |

⚠ Her iki görsel de **yalnız MJ CDN'inde ve yerel diskte.** Otuz günlük görsel sistemin
tamamı kurucu URL'ine bağlı — **B141**.

### Üretim dersleri — yedi tur, altısı elendi

Kazanan formül **tepeden bakan kül + noktasal kor + düşük stylize**'dır. `30-sosyal.md`
Bölüm 0a'nın yandan-makro kurucu promptu bu turlarda **geçersizleşti** — altı tur boyunca
yatay ışık şeridi üretti; `top-down` ilk turda noktasal kor verdi. **Kamera açısı çözdü.**

Elenen turların teşhisi (Bölüm 16'nın `--no` listelerini doğrudan ilgilendirir → **B139**):

1. **"ember glow" + "charcoal" birlikte → alev, lav, volkan.** `--no` listesinde
   `flames, fire, lava, volcano` yoksa model boşluğu klişeyle doldurur. Üç turda üç kez oldu.
2. **"room" kelimesi ışık kaynağı talep eder** — MJ pencere çizer. Mekân adı yerine makro.
3. **"hairline cracks" → çatlamış toprak / kuru göl yatağı.**
4. **"underexposed" tek başına soğuk gri belgesel üretir.** Sıcak siyah istiyorsan
   `warm brownish-black` diye adlandır; "koyu" demek yetmez.
5. **`--no sparks` karedeki tek sıcak öğeyi siler.** Yasak liste hedefi aşabilir.
6. **Yasak liste kelimeyi engeller, kavramı engellemez.** "heat seeping out" yazıldığı
   sürece `--no fire` alevi durdurmaz.

*Bir promptta neyi yasakladığın, ne istediğin kadar kompozisyon kurar.*

### KARAR 542 — kurucu görsel mühürlendi; MJ motoru `--v 8.1`'e kilitlendi (AKTIF) {#k542}

`--sref` çapası yukarıdaki URL. Motor kilidinin iki gerekçesi:
**(1)** MJ 2026'da varsayılanı iki kez değiştirdi (V7 → V8.1 10 Haziran, V8.1 → V8.2
24 Temmuz); `--v` yazılmazsa **otuz günlük seri ortasında motor sessizce değişir.**
**(2)** V8.2'nin beyan edilen estetiği "cesur, keskin", V8.1'inki "sakin"; OCAK'ın Style
DNA'sı ikincisidir ve `--sref` kararlılığı V8.1'in başlık özelliğidir.

KARAR 450 kilidi kurmuştu ("Gün 0 onayı olmadan Gün 1 yok"); 542 o kilidin **gereğini
yerine getirir** — supersede değil, ifa. **B114 bu kararla kapandı.**

### KARAR 543 — kart köşe imzası nokta değil OCAK işaretidir (AKTIF) {#k543}

`30-sosyal.md` Bölüm 5'teki *"köşede küçük nokta (·) tek imza"* cümlesini geçersiz kılar.
Gerekçe iki katmanlı: **nokta pratikte görünmüyor** (basılan kartta fark edilmedi) ve
**hiçbir şeye bağlanmıyor**; işaret ise kartı logoya ve sitedeki `manifesto-vurgu` köz
glyph'ine bağlıyor. Standart: boy kart genişliğinin **%7**'si, opaklık **0.85**, yatayda
**ortalı**.

⚠ Bölüm 5'in nokta kuralı **numaralı bir KARAR değildi** — ledger'da karşılığı yok
(imza kararları 41 · 300 · 403, üçü de kart köşe imzası değil). Bu yüzden **supersede oku
kurulmadı**; geçersizleşme yalnız karar metninde ve dosyada yaşıyor. ↔**B105** (işaret yayılımı).

### KARAR 544 — imza ışığın karşı ucunda durur; konum zemin tipinden çıkar (KALICI) {#k544}

Fotoğraflı zeminde ışık altta (P1–P9'un tamamı ışığı alta koyar) → **imza metnin üstünde.**
Glow zeminde ışık metnin arkasında → **imza kart dibinde.** Karttan karta değişen bir kural
değil; **zemin tipi belli olunca konum da bellidir**, ön ayara gömülüdür.

*Ölçüm:* fotoğraflı zeminde işaret çevresi kor oranı üstte **%1.82**, altta **%7.15**
(piksel sayımı, ölçüt `R>90 ∧ R−B>45`).

### KARAR 545 — söz kartı metni iki kademelidir: iddia büyük, fısıltı küçük (AKTIF) {#k545}

Boş satırla ayrılan ikinci blok ana puntonun **0.62**'sinde render edilir. Gerekçe: eşit
puntoda kart **blok metin gibi okunuyor**, kompozisyon kurulmuyor. Sitenin hero'su
(H1 + alt cümle) zaten bu hiyerarşiyi taşıyor — kart onun kardeşi.

### KARAR 546 — fotoğrafsız kart zemini tasarımdır, yedek değil (AKTIF) {#k546}

Kömür zemin + kor glow; **ışığın merkezi metin bloğunun merkezine kilitli**, kaydırıcıyla
oynatılır. Sitenin hero ilişkisinin kart karşılığı: **metin ışığın içinde oturur.**
B86'nın "iki aileli sistem doğar" gerilimi çözüldü — iki zemin tipi ayrı ön ayar taşıyor,
karışmıyorlar. **B86 bu kararla kapandı.**

### Kapananlar

**B114 ✅** görsel stil kodu kilitlenmedi — `--sref` çapası + `--v 8.1` motor kilidi +
format seti (derleyicide altı gömülü ön ayar: F1 fotoğraf · F1 glow · F1 altın · F2 kapak ·
F2 iç kart · F4 story 9:16).
⚠ Borcun **OG kartı ayağı (1200×630, B105'in dördüncü ayağı) açık kaldı** — derleyicide
o oran yok. Ayak **B105'te yaşamaya devam ediyor.**

**B86 ✅** kart derleyici fotoğrafsız zemin — radial yedek olmaktan çıkıp tasarım oldu.

### Yeni borçlar

**B139** P3–P9'un `--no` listeleri yukarıdaki dersleri içermiyor (P4 "fire bowl",
P8 "campfire", P9 "low flame" — üçü de alev talep ediyor; tek tur işi ama kart basılmadan) ·
**B140** İ1'in ikinci satırı dosyada italik, derleyicide değil (küçük; kademe farkı
puntoyla kurulduğu için kart bozulmuyor) · **B141** kurucu ve İ1 zemini yalnız MJ CDN'inde
+ yerel diskte (küçük ama kritik).

### Kart derleyici — repoya alındı

`tools/ocak-kart-derleyici.html`, **1262 satır** (seans içinde 809'dan büyüdü). Tek başına
çalışan HTML — build yok, bağımlılık yok, tarayıcıda açılır. **`docs/` ya da `scripts/`
altına konmadı**: ikisi de `ocak-docs` MCP'sinin servis ettiği köklerdir, bin satırlık bir
araç oraya girerse korpus envanterini şişirir. `tools/` git geçmişine girer, korpusun dışında kalır.

**Seansta düzeltilen üç hata:** `#1A1614` paletten tamamen çıktı (12 satır, sekiz sabit
`rgba` dahil) · `data-imzaYer` gibi **büyük harfli HTML öznitelikleri tarayıcıda küçülüyordu**,
tireli hâle getirildi — imza eksenleri hiç çalışmıyordu · placeholder rengi tanımsızdı,
koyu zeminde görünmüyordu.

⚠ **Safari `file://` üzerinde localStorage engelli.** Bu yüzden ön ayarlar dosyanın içine
gömüldü; tarayıcı depolamasına bağımlı değiller.

### Bu turda yazılmayanlar

⚠ **`00-durum.md`'ye ekleme yapılmadı** — **B131** hâlâ açık (dosya 195/200). Kaan'ın kararı
(23 Ağu): beş satırlık bütçeye sıkıştırmak bir sonraki yazanın işini büyütür. Gün 0'ın
kapandığı bilgisi `03-sira.md` madde 1–7 kapanışında ve ledger'da yaşıyor. **İstisna:**
artık yanlış olan iki beyan **yerinde düzeltildi**, satır sayısı büyümedi (aşağıda).

⚠ **`30-sosyal.md` Bölüm 16'nın P1–P9 tam seti yazılmadı** — set Kaan'ın vereceği tek bloktu,
bu turda gelmedi. `KURUCU-URL` ara-değiştir **açık kalır**; Gün 1'in önündeki dört engel
(robots · iade cümlesi · B83 · B85) **değişmedi.**

### Arşivci turunun kendi bulguları — B142 · B143

Yukarıdaki her şey claude.ai seansının kaydıdır. Şu iki borç **seansta değil, arşivci
turunda** doğdu — devir dosyasında yoklar, uygulama sırasında ölçülerek çıktılar.

**B142 — defterin kapanış konvansiyonu tek biçim değil.** Kapanmış bir borcun `Sahip:`
satırı üç biçimde yaşıyor: `- [x]` (8 girdi) · `~~üstü çizili~~` (4 satır) · **ve kutusu
hâlâ açık `- [ ]`** (15 girdi). Üçüncüsü tutarsızlıktan öte **yanlış bilgi** — kapanmış
borç açık kutu taşıyor. Bu tur `[x]` kullandı, bir önceki tur `~~...~~` kullanmıştı;
turun ortasında birleştirme yapmak kapsamı genişletirdi, **borç açıldı, dokunulmadı.**

**B143 — kart derleyici Google Fonts'a bağlı.** Araç "tek başına çalışır" sayılıyordu;
build ve paket bağımlılığı gerçekten yok, `fetch` sıfır — ama dosyadaki dört `http(s)://`
geçişinin **dördü de font linki** (Cormorant Garamond · Jost). Çevrimdışı basılan kart
yedek fontla çıkar ve **görsel olarak "çalışır" görünür**; sessiz kırılma. Çözüm fontları
base64 gömmek — Safari `file://` kısıtı ön ayarları zaten dosyanın içine sokmuştu, aynı yön.

*Ders: "bağımlılık yok" iddiası paket bağımlılığıyla dış kaynak bağımlılığını aynı sayar.
Bir aracın tek parça olup olmadığı `grep -c 'https\?://'` ile ölçülür, beyanla değil.*

### Bu turun kendi kapanışı

Dört commit `main` üstünde kaldı, dala alınmadı: `882646c` (arşiv) · `2560532` (arşivci) ·
`bf7b34e` (30-sosyal) · `bb5e49f` (tools) · ardından `B142`/`B143` commit'i.
⚠ Brief `main`'e `--no-ff` diyordu; **dalsız çalışıldığı için merge kabarcığı zaten yok.**
Kaan'ın kararı (23 Ağu): *geriye sarıp yeniden merge etmek temiz bir geçmişi kozmetik
gerekçeyle bozar* — brief'in o satırı varsayımdı, düzeltildi. Geçen tur da böyle yürümüştü.

---

## 23 AĞUSTOS 2026 — ADVAITA YORUM TURU: 14 ETKİNLİK GÖVDESİ (claude.ai + Notion, sıfır kod)

Tek oturum, **sıfır kod, sıfır deploy.** **Dört KARAR mühürlendi (547–550), KARAR 506
SUPERSEDE'e döndü, yirmi bir borç doğdu (B144–B164).** Kaynak: Advaita'nın ~95 kalemlik
yorum PDF'i; ADIM 1 (A grubu) + ADIM 2 (B2 doz/kayıt doktrini). Kalemlerin **~70'i kapandı**
— rakam kaynak seansın beyanıdır, arşivci turunda yeniden sayılmadı (**TEYITSIZ**).

Advaita'ya dönen rapor: `_arsiv/2026-08-23-advaita-rapor.md`. Ham devir: `_arsiv/2026-08-23-advaita-devir.md`.

**Ne oldu:** Bir gün içinde 14 etkinlik gövdesinin **tamamı** yeniden yazıldı. Site sayfaları
(`/sen-neredesin` · `/acik-kapi` · `/seremoni` · `/anadolu` · `/biz` · `/advaita` · `/ekip` ·
`/hikaye` · `/bulusmalar` · `/mini-retreat` · `/araclar`) bir sonraki tura kaldı.

### Yöntem değişikliği — gruptan sayfaya

Sıra önce **gruba göre** kuruldu (A→B→C→D), tur ortasında **sayfaya göre**ye çevrildi.
Gerekçe: doktrin kararları verildikten sonra grup sırası aynı gövdeyi üç kez açtırıyordu.
`elin-neyle-dolu` bu yüzden **üç kez yazıldı**.

*Ders: doktrin kararı verilmeden önce grup sırası, verildikten sonra sayfa sırası doğrudur.
Kırılma noktası kararın mühürlendiği andır.*

### Ölçüm — 14 gövde, net −6.554 karakter

| Format | gövde | net |
|---|---|---|
| Açık Kapı | 6 | −1.190 |
| Çember | 4 | −1.634 |
| Seremoni | 3 | −3.367 |
| Atölye | 1 | −363 |

Son uzunluklar: `bir-esikte-duruyorsun` 3226 · `bu-ses-kimin` 3212 ·
`dusundugum-hissettigim-yaptigim` 3049 · `istek-mi-ihtiyac-mi` 3118 · `kokun-nerede-kaldi` 3036 ·
`nereye-kadar-senin` 3264 · `ekmeden-once` 3505 · `elin-neyle-dolu` 3771 ·
`hangi-tohumu-ekeceksin` 3480 · `neyi-bekliyorsun` 3720 · `hasat-ve-sukran` 3672 ·
`kakao-seremonisi-online` 3605 · `kakao-seremonisi-yuzyuze` 3686 · `rituel-tasarimi` 4541

*Yöntem: `python3 len()` (code point), canlı Notion `Detay` gövdeleri, kaynak seansta ölçüldü.
`awk` kullanılmadı — bu makinede bayt sayıyor. Arşivci turu bu on dört rakamı **yeniden
ölçmedi**; Notion'a erişimi yok. Ledger'a rakam girmedi, bantlar borç olarak açıldı
(B144–B146). Net toplam iç tutarlılığı doğrulandı: −1.190 − 1.634 − 3.367 − 363 = **−6.554** ✅
Bant türetmeleri de doğrulandı — dört bandın dördü de yukarıdaki on dört rakamdan çıkıyor.*

### Üç hata yapıldı, üçü de farklı bir ağa takıldı, hiçbiri siteye ulaşmadı

| hata | sınıf | yakalayan |
|---|---|---|
| `uykudan mu0131` (bozuk kaçış dizisi) | transkripsiyon | kaçış artığı taraması |
| `yerleşemmiş` + `yerleşemeyen` | transkripsiyon | uzunluk aritmetiği (−2 sapma) |
| Dört Çember'de iki cümle atlandı | sınıflandırma | denetim sorgusu |

**Ders: tek ağ yetmiyor.** Kaçış taraması geçerli-ama-yanlış dizeyi görmez · uzunluk
aritmetiği eşit uzunluklu hatayı görmez · denetim sorgusu ancak doğru desenle çalışır —
`%Pas%` deseni *"ku**pas**ını"* içine düşüp **yanlış alarm** verdi.

*Üç ağın üçü de birbirinin körlüğünü kapatıyor; biri kaldırılırsa o sınıf hata siteye ulaşır.
Deploy'un sonda olması dördüncü ağdı — üç hata da o ağdan önce yakalandı.*

### İkinci ders — etiket içeriği takip etmiyor

`A grubu` ölçülünce Advaita'nın beş yorumundan **dördünün zaten uygulanmış** olduğu görüldü.
Ama bir uygulama **yarım kalmıştı**: psikolojik raf `/araclar`'dan silinmiş, **"yedi raf"
sayacı güncellenmemişti** (altı raf var). Aynı sınıf hata iki yerde daha bulundu:
`/hikaye`'nin `bes-kadim-kaynak` marker'ı (kanon "Dört Yön, Bir Ocak"a döneli beri yanlış,
KARAR 299) ve `10-marka.md`'nin "Beşli kültürel sentez" satırı.

**Aynı sınıf hata bu oturumda dört kez çıktı.** Silme işleminin sayacı, marker'ı, özet
satırını takip etmemesi tek bir kusurdur: **içerik değişti, etiket kaldı.**

### KARAR 459 — istisna verildi, kural değişmedi

`KARAR 459` (içerik otoritesi: `ocak-metin` taslak-only, Notion'a giriş elle) **KALICI olarak
duruyor.** Bu oturumda Kaan **parti kapsamlı** bir istisna verdi: Claude diff'i hazırladı,
her yazma çağrısı istemci onayından geçti — **otuz beş yazma bu şekilde yapıldı.**
**Kalıcı gevşetme yapılmadı**; ileride istenirse ayrı karar gerekir.

Not: istemci her `notion-update-page` çağrısını kullanıcı onayına düşürüyor — 459'un koruduğu
insan halkası **araç seviyesinde de zorunlu**. Yani istisna doktrini gevşetti, mekanizmayı değil.

### KARAR 547 — kayıt sınırı formatın değil bloğun cinsine göre (KALICI) {#k547}

**Anlatım ve uygulama kaydedilir; kadın kadına paylaşım kaydedilmez.** Sınır formatın adına
göre değil, **bloğun cinsine** göre çizilir — aynı sayfada bir blok kaydedilirken diğeri
kaydedilmeyebilir.

Uygulama karşılığı (Notion'da tamamlandı):

- **Açık Kapı** → tema ve pratik kaydedilir; soruların okunduğu bölüm kaydedilmez
- **Çember** → hiç kaydedilmez
- **Seremoni** → tema ve seremoni kaydedilir, paylaşım turu kaydedilmez ·
  `kakao-seremonisi-yuzyuze` **istisna**, hiç kaydedilmez
- **Atölye** → anlatım ve uygulama kaydedilir, paylaşımlar kaydedilmez

⚠ **Otorite alan koddadır değil DB'dedir:** Etkinlikler DB'ye **`Kayıt Var` checkbox'ı** eklendi
(Kaan açtı). Kayıt varlığı etkinlik başına orada tutulur — sayfa gövdesindeki cümle o alanın
anlatımıdır, kaynağı değil. İşaretleme durumunun doğrulaması **B164**.

`↔57` — kayıt ve veri etik çerçevesini daraltmaz, cins ekseni ekler.

### KARAR 548 — unvan yasağı betimlemeyi kapsamaz (KALICI) {#k548}

`cacaoista` **Türkçe betimlemenin açıklayıcı eki** olarak geçebilir; **tek başına unvan olarak
kullanılmaz.** Yaslandığı canlı cümle (`/advaita`, `tasidigi-bati`):
*"Advaita bir kakao taşıyıcısı — cacaoista."*

Yazım **c** ile — `cacaoista`, k ile değil.

⚠ **KARAR 506 bu kararla SUPERSEDE oldu** — kısmi: *unvan yasağı korundu, betimleme serbest.*
506 "Cacaoista/Kakaoista/Cacaoiste kamu metninde geçmez" diyordu; yasak artık **kullanım
biçimine** bakıyor, kelimenin kendisine değil.

*Ayrım şudur: "Advaita, Cacaoista" bir unvandır — yasak. "bir kakao taşıyıcısı — cacaoista"
bir betimlemedir, Türkçe ibarenin ardından açıklayıcı ek olarak durur — serbest.*

### KARAR 549 — kamuya açık etkinlik metninde doz (KALICI) {#k549}

**Etkinlik sayfası akışı, hazırlığı ve mahrem soruları kamuya açmaz.** Hazırlık gerektiren
formatlarda `Yanına Al` **işaretçiye iner** (*"Buluşmadan önce gerekli başka bir şey varsa
sana yazıyoruz."*); beslenme · aç karnına · ilaç · hamilelik satırları **kayıt sonrası kanala
taşınır**.

Advaita'nın dört gerekçesi:

1. **Alan korunmuyor** — sayfayı okuyan herkes katılmayacak, mahrem hazırlık herkese açılıyor
2. **Akış kopyalanabiliyor** — adım adım anlatılan bir akış taşınabilir hâle geliyor
3. **Mahrem konu kamuya açılıyor** — ilaç ve hamilelik kamu metninin konusu değil
4. **Ne olacağını zaten bilmiyoruz** — taahhüt veremeyeceğimiz bir şeyi tarif ediyoruz

Dördüncüsü sayfaların en eski cümlesini de düşürdü: *"Ne olacağını şimdiden söylemiyoruz —
anlatılınca bilgiye dönüşüyor"* → *"Ne geleceğini önceden bilmiyoruz — akış o akşam kuruluyor."*
Eskisi **"biliyoruz ama söylemiyoruz"** diyordu ve aynı cümle "anlatılınca bilgiye dönüşür"
derken **sayfanın geri kalanı anlatıyordu.**

### KARAR 550 — Notion text property'sine yazım cerrahi değildir (KALICI) {#k550}

Gövde **önce kabukta kurulur** → `python3 len()` ile **ölçülür** → **yazılır** → **geri okunan
uzunluk beklenenle karşılaştırılır.** Dört adımın hiçbiri atlanmaz.

Gerekçe: text property'de nokta düzeltme (bul-değiştir) yapılamaz — yazma **tam gövdeyi**
değiştirir. Ölçüm yapılmazsa eksik gövde sessizce yerleşir; geri okuma yapılmazsa kırpılma
fark edilmez. `yerleşemmiş` hatası tam olarak bu aritmetikle yakalandı (**−2 sapma**).

⚠ **Bu turda çürüyen bir varsayım:** `ocak-etkinlik` skill'i *"Notion rich text tek parçada
2.000 taşır, fazlası sessizce kırpılır"* diyor. **API yazımıyla 5.203 karakter tek parçada
gitti, kırpılmadı.** Not ya kaldırılmalı ya "elle yapıştırma yolu" diye daraltılmalı — **B149**.

### Sabit blok değişiklikleri — bekçi dosyalarının referansı

Aşağıdaki kesimler bir sayfaya değil **bloğa** uygulandı; bekçi dosyaları (`ornekler.md` ·
`ornekler-cember.md`) bu hâlden yeniden üretilecek (**B147** · **B148**).

```
# Tüm Açık Kapı + Çember
Ne olacağını şimdiden söylemiyoruz — anlatılınca bilgiye dönüşüyor, oysa yaşanacak bir şey.
→ Ne geleceğini önceden bilmiyoruz — akış o akşam kuruluyor.

Ortak olan şu: pratik biterken…   →   Ortak olan şu: Pratik biterken…

# Açık Kapı
Kimse düzeltilmiyor. Kimseye tavsiye verilmiyor. Reçete vermiyoruz, alan tutuyoruz.
→ Kimse düzeltilmiyor. Reçete vermiyoruz, alan tutuyoruz.

- Kayıt alınır, hemen ardından e-postana düşer — gelemezsen sonradan izlersin
→ - Tema ve pratik kaydedilir, hemen ardından e-postana düşer — gelemezsen sonradan izlersin
  - Soruların okunduğu bölüm kaydedilmez; yazdığın o akşamda kalır

Önce tema açılır: eşik ne demek…   →   Önce tema açılır: Eşik ne demek…

# Çember
İstediğin kadar konuşursun ya da "pas" dersin; ikisi de aynı ağırlıkta.        → SİLİNDİ
Bu turun uzun sürmesi normaldir. Çemberin kalbi burasıdır… çemberin parçasıdır. → SİLİNDİ

- Tema bölümü kaydedilebilir, gelemeyen kadınlar erişsin diye
- Paylaşım turu kaydedilmez — söylenen o çemberde kalır
→ - Çember kaydedilmez — söylenen o çemberde kalır

Baştan başlıyoruz; geç kalırsan sessizce katıl, tur bozulmaz
→ Baştan başlıyoruz; çember açıldıktan sonra kapı kapanır

## Yanına Al — son iki madde çıktı, yerine:
Buluşmadan önce gerekli başka bir şey varsa sana yazıyoruz.

# Seremoni
"Pas" demek de konuşmak kadar makbul.                        → SİLİNDİ
Bundan sonrasını şimdiden anlatmıyoruz — anlatılınca…        → Bundan sonrası her seferinde başka.
Katılım kaydı alınır… Ses ya da video kaydı alınmaz…
→ Tema ve seremoni kaydedilir; paylaşım turu kaydedilmez — söylenen o akşamda kalır
Pratik Bilgi: beslenme · aç karnına · ilaç · hamilelik satırları SİLİNDİ
```

**Büyük harf kuralı:** iki noktadan sonra bağımsız cümle geliyorsa büyük harf (TDK) — altı
Açık Kapı ve dört Çember'e uygulandı, on gövde.

**Geç kalma taahhüdü — bilerek dakikasız.** Advaita *"5 dk belki"* demişti; bir **"belki"**
kamu metnine sabit taahhüt olarak yazılmadı. Yazılan: *"Baştan başlıyoruz; çember açıldıktan
sonra kapı kapanır."*

### Karar bekleyen sekiz kalem — Advaita'da

Rapor sekiz soru bıraktı; hiçbiri karara bağlanmadı, hiçbiri borç değil (karar Advaita'nın):
`elin-neyle-dolu` isim (sepet → sofra) · `ekmeden-once` isim · `kokun-nerede-kaldi` çerçeve
yeniden yazımı (gurbet kelimesinin ağırlığı, "kökler fiziksel yere bağlı değil") ·
`hangi-tohumu-ekeceksin` "Kimin İçin" bölümü + büyük başlık · `dusundugum-hissettigim-yaptigim`
ana başlık · `/mini-retreat` yaban kadın mı hatırlayan kadın mı · `/seremoni` niyet cümlesi ·
`/ekip` çıraklık.

Bunlar `03-sira.md`'de **Advaita kuyruğunda** durur; ledger'a girmezler.

### Arşivci turunun kendi ölçümleri

Patch dört yerde *"önce grep"* dedi; dördü de koşuldu. **İkisi patch'in varsayımını değiştirdi.**

| grep | patch varsayımı | ölçüm (23 Ağu, çalışma kopyası) |
|---|---|---|
| `"Son Güncelleme\|Son_Guncelleme"` `src/` | "önce bak" | **0** — property koda hiç girmiyor, tip değişimi güvenli (**B152**) |
| `"Versiyon"` `src/` | aynı sınıf | **0** — aynı sonuç (**B153**) |
| `'bes-kadim-kaynak'` `src/` | "marker rename" | **19** — parser · CSS · test · snapshot · fixture adı. **Tek satırlık rename değil** (**B159**) |
| `"Çember Lideri"` `src/` + `docs/` | "üç yer" | **29** toplam · `src/` **0** · canlı `docs/` **8** · `_uretilen/` 5 · `_arsiv/` 5 (**B160**) |

*Yöntem: `grep -rn`, çalışma kopyası `1b4b998`, `_arsiv/` ve `_uretilen/` dahil sayıldı,
sonra ayrıştırıldı.*

**İki düzeltme:**

⚠ **`bes-kadim-kaynak` bir kod işidir, metin işi değil.** Patch onu "marker rename" diye
listeledi; ölçüm `remark-ocak-sections.ts`'te **bir `case` dalı, bir `data-section` emit'i,
bir CSS class'ı, iki uyarı mesajı, üç test, bir snapshot ve bir fixture dosya adı** gösterdi.
Notion tarafındaki marker adı değişirse **kod tarafı eşleşmezse section haftalarca render
dışı kalır** (KARAR 409, `00-durum.md` sessiz kırılma listesi). İkisi **aynı commit'te**
gitmeli.

⚠ **`Çember Lideri` `src/`'de hiç geçmiyor.** Site metni Notion'da yaşıyor; rename'in kod
ayağı yok. Patch'in saydığı üç yerden `10-marka.md` K4 doğrulandı (satır 67) — ama patch'in
saymadığı **beşinci bir canlı yer** var: `20-ref-program.md:25` *"Wild Woman Project büyüme
motorunun OCAK versiyonu"*. Yani **B158**'in Wild Woman itirazı da iki dosyaya bakıyor,
bire değil.

**`10-marka.md`'nin dört kalemi de yerinde doğrulandı** (dosya 237 satır):
satır 24 Rishikesh · satır 52 "Beşli kültürel sentez" · satır 5 giriş notu · satır 67 K4.
Giriş notunun iddiası **birebir doğru, kavramsal olarak yanlış**: *"beş kadim kaynak"* dizesi
gerçekten dosyada yok (grep teyitli), ama **aynı fikir satır 52'de duruyor** ve KARAR 299 onu
süperselemişti. Not kendi dosyasının içeriğini yanlış tarif ediyor — **B157**.

### Kapananlar

**Bu turda kapanan borç yok.** Yirmi bir yeni kayıt: **B144–B164**.

### Bu turun kendi kapanışı

Doküman turu `main` üstünde yürüdü, dal açılmadı — 23 Ağustos'un önceki iki turu gibi.
Seans `1b4b998` hâliyle açıldı. **Deploy yapılmadı ve bu bilinçli bir karardır** (**B163**):
14 gövde Notion'da yeni, sitede eski. Üç transkripsiyon/sınıflandırma hatası bugün yakalandı,
hiçbiri siteye ulaşmadı **çünkü deploy sonda.** Sayfalar DB'nin dokuz sayfası da bitince
tek seferde.

### EK TUR — aynı gün, ikinci arşivci geçişi (`docs-patch-2026-08-23-ek.md`)

Ana patch'in atladığı sekiz kalem için ikinci bir patch geldi. **Bir KARAR mühürlendi (551),
bir borç kapandı (B160), dört borç doğdu (B165–B168).** ⚠ **Ek'in altı yeni borç önerisinden
ikisi zaten defterdeydi** — ölçüm yapılmasaydı sayaç şişecek, iki borç aynı işi gösterecekti.

#### KARAR 551 — Çember Lideri → **Çember Rehberi** (KALICI) {#k551}

Ad değişiyor; **rolün içeriği ve K4 yapısı aynı.** Gerekçe **hiyerarşinin kaldırılması** —
bir üslup tercihi değil, **bir tutarsızlığın düzeltilmesi.** `10-marka.md` OCAK WAY §3 zaten
şunu söylüyor: *"Çemberde herkes eşit. 20 yıllık yoga öğretmeni de ilk gelen kadın da aynı
yerde, aynı talking stick. **Hiyerarşi yok.**"* — **"Lider" kelimesi bu maddeyle baştan
çelişiyordu.**

Advaita'nın iki dokunuşu: `/sen-neredesin` 8. Taşıyıcı için *"Lider değil de… Rehber belki"*
ve 21 Temmuz notu *"Çember liderlik eğitimi → Çember rehberlik eğitimi"*. Karar Kaan'ın
(23 Ağustos).

⚠ **`←` ilişkisi boş — süperselenecek karar yok, ve bunun kendisi bir bulgu.**
Ek, `30-sosyal.md:101`'in (*"'Çember Lideri Eğitimi' adı kesinleşti — alternatif ad tartışması
kapalı"*) dayandığı ledger satırının bulunup SUPERSEDE edilmesini istedi. **Ledger'da öyle bir
satır yok** (`grep -i 'çember lideri\|çember tutma\|lider' docs/01-kararlar.tsv` → **0**).
Dahası: **kronoloji o iddiayı iki kez zaten çürütmüştü** —
`90-kronoloji/2026-07.md:1227` ⚠ *"Konsolidasyon düzeltmesi — T3: dumpın beyanı o günkü
çalışma varsayımıdır; **karar açık kalır**"* · aynı düzeltme `2026-07.md:54(b)`'de.

*Yani `30-sosyal.md:101` kapalı olmayan bir tartışmayı kapalı ilan ediyordu ve **yanlışlığı
Temmuz'da zaten kayda geçmişti.** KARAR 551 bir kararı süperselemiyor; **Pilot borç 25'in T3
kalemini kapatıyor** ve `30-sosyal.md`'nin bayat beyanını düzeltiyor. Otorite sırası
(KARAR 482) tam da bunu söylüyordu: gövde indeksi yener, kronoloji gövdeyi.*

**Dört yüzey:** (1) `10-marka.md` K4 ✅ bu turda · (2) `30-sosyal.md:101` ✅ bu turda ·
(3) `/sen-neredesin` 8. Taşıyıcı → **Notion, Sayfalar DB turunda** ·
(4) `/ekip` → **Notion, Sayfalar DB turunda**. `src/`de **0 eşleşme** — kod dokunuşu yok.

⚠ **Mekanik yayılım bu turda ayrı commit'te koşuldu** (KARAR 61): canlı `docs/` altındaki
yedi geçiş dönüştürüldü (`10-marka.md` 1 · `20-ref-program.md` 2 · `20-ref-marka.md` 1 ·
`20-ref-icerik-dili.md` 1 · `Ocak-Mufredat.md` 2). **`_uretilen/site-icerik.md`'ye dokunulmadı** — script çıktısıdır ve
Notion hâlâ *"Lider"* diyor; dönüştürülseydi **üretilen dosya kaynağından ileri giderdi.**
`_arsiv/` da dokunulmadı (tarihsel kayıt).

#### Ek'in düzeltilen iki kalemi — zaten defterdeydi

**E3 (Seremoni · Atölye bekçi dosyası) yeni borç değil.** Ölçüm: **B88**
(`ornekler-seremoni.md` bekçisi yazılacak) ve **B89** (`ornekler-atolye.md`) **ikisi de açık**,
19 Ağustos'ta doğmuş. Selefleri **B66**. Yeni numara açılmadı; üç maddenin ölçümleri
**tazelendi** — ve tazeleme bir bayatlık daha buldu:

| madde | defterdeki bant | 23 Ağustos gerçeği | sapma |
|---|---|---|---|
| B88 Seremoni | `4.870–5.197` | **`3.605–3.686`** | **−1.400 civarı** |
| B89 Atölye | `4.895` tek nokta | **`4.541`** tek nokta | **−354** |

*Yani `ornekler-seremoni.md` yazılsaydı **bir buçuk kat şişik** bir bant mühürlenecekti.
B144'ün Çember'de yakaladığı kusurun aynısı, iki dosya ötede.*

✅ **B66'nın tetikleyicisi bu turda doldu:** *"üçüncü seremoni sayfası yazılınca damıtılır"*
diyordu — üçüncü gövde artık var (`kakao-seremonisi-online` · `kakao-seremonisi-yuzyuze` ·
`hasat-ve-sukran`). Borç kapanmadı, **beklediği koşul geldi.**

#### `bes-kadim-kaynak` uyarısının kapsamı daraltıldı

Arşivci turu 14 gövdenin deploy'u için KARAR 409 uyarısı vermişti (Notion ve kod aynı
commit'te gitmezse section render dışı kalır). **Kaan daralttı ve haklı:** bu turda yazılan
14 gövdenin **hiçbirinde `## section:` marker'ına dokunulmadı** — yalnız marker'ların
**altındaki metin** değişti. Marker sözleşmesi bozulmadı; **14 gövdenin deploy'u o arızayı
üretmez.**

`bes-kadim-kaynak` (**B159**) ayrı bir iştir ve **gerçekten atomik gitmeli.**

*Ders: bir kuralın kapsamı daraltılmadan tekrarlanırsa gereksiz kilit üretir. KARAR 409
marker **adına** bakar, marker altındaki metne değil.*

#### `00-durum.md` — bilerek bekletiliyor

Arşivci turunun değerlendirmesi kabul edildi: dosya **195/200**, yazmak bir **tahliye turu**
gerektiriyor, ve Sayfalar DB turundan sonra dosya zaten yeniden yazılacak. **İki kez tahliye
etmek gereksiz risk.**

**Karar (Kaan, 23 Ağustos): `00-durum.md` Sayfalar DB turu bitene kadar bekliyor.** O turda
tahliye + yazım birlikte koşulacak; tahliye adayı dosyanın kendi işaret ettiği **11 Ağustos**
bloğu (B47 · B58). Not `03-sira.md`'ye düşüldü — bir sonraki tur *"durum neden bayat"* diye
zaman kaybetmesin.

⚠ **Bu, B131'i kapatmaz.** Borç açık kalır; kapanışı tahliye turudur.

---

## 23 AĞUSTOS 2026 — KISALTMA YENİDEN ADLANDIRMASI + ZEMİN ENVANTERİ (CC, sıfır kod)

Aynı günün üçüncü doküman turu. **Üç KARAR mühürlendi (552–554), üç borç doğdu
(B169–B171), bir dosya açıldı (`31-zemin.md`).** Kod dosyasına dokunulmadı.

*(Yazım gece yarısını geçti — commit 24 Ağustos tarihini taşır, tur 23 Ağustos'undur.)*

### İki brief, çünkü birincisi üç yerden bayattı

r1 brief'i `1b4b998`'e bakıyordu; ölçüm anında HEAD **`8cdd595`**, arada **5 commit** vardı.
ADIM 0 üç çelişki raporladı ve **hiçbiri yazmadan önce çözüldü** — r2 brief'i r1'i ezdi:

| çelişki | r1 ne diyordu | gerçek | r2 kararı |
|---|---|---|---|
| kapsam | beş dosya | `20-ref-icerik-dili.md`'de de kart referansı var | dosya eklendi, **altı** oldu |
| doğrulama | *"grep 0 dönmeli"* | kapanmış borçlar + KVKK `P1` hep kalacak | kriter **3 `İ` + 1 `P` satırı** |
| 0.3 | *"B140/B141 tam metni iki dosyada"* | `03-sira.md` yalnız toplu anma tutar | aksiyon yok |

*Ders: `N → 0` biçimindeki grep kriteri, aranan dizenin **korunması gereken** metinde de
geçip geçmediği kontrol edilmeden yazılmaz (CLAUDE.md §3). r1 bunu üç kalemde yaptı.*

### İki ölçüm aracı sessizce yalan söyledi

**(a) `grep` bu kabukta `ugrep`'e sarılı bir shell function.** Tırnaksız dosya listesiyle
çağrıldığında newline-birleşik diziyi **tek dosya adı** sanıyor ve `No such file or
directory` veriyor. İlk tarama `2>/dev/null` taşıdığı için uyarı görünmedi; sonuç
**0 eşleşme** olarak okundu.

**(b) `git grep -P -nE` — `-E`, `-P`'yi eziyor.** `\b` POSIX ERE'de bir metakarakter
değildir, yani `\bC[0-9]{1,2}\b` sessizce hiçbir şey eşleştirmez. Sanity testi bunu
yakaladı: bilinen `\bB1[0-9]{2}\b` deseni de **0** döndü — oysa dosyada 146 satır var.

⚠ **İkisi de "0 eşleşme" olarak raporlanır.** Bir taramanın boş dönmesi, taramanın
çalıştığının kanıtı değildir. **Bilinen-pozitif bir sanity deseni olmadan sıfır beyan
edilmez.**

### Homograflar — mutabakat yakaladı, tarama değil

Yeniden adlandırmadan sonra eski↔yeni jeton mutabakatı koşuldu: her `İn` için `Cn`,
her `Pn` için `Vn` sayısı eşleşmeli (eksi bilinçli atlananlar). **Beş jeton tutmadı.**

`İ1` eskiden 14, `C1` yeniden 14 — oysa bir tanesi kapanmış borçta atlandığı için **13**
olmalıydı. Fazlalık, **önceden var olan** ve ADIM 0'ın kaçırdığı jetonlardı:

- `20-ref-marka.md:391` — `C1` = **VSCO fotoğraf preseti** (`A6/C1, sıcak shift`)
- `03-sira.md:66` — `V6` `V7` `V8` = **Midjourney motor sürümü** (`Midjourney V8.x`)
- `01-kararlar.tsv:405` — `V2` = **sayfa sürümü** (`Gündönümü V2 sadeleştirme`)

⚠ İlk ikisi kart/prompt jetonlarıyla **aynı dosyada** yaşıyor: `20-ref-marka.md` hem
`C1` presetini (391) hem `C1–C24` kartlarını (538); `03-sira.md` hem `V6/V7/V8` motorunu
(66) hem `V1–V9` promptlarını (350). Metinlere **dokunulmadı** — anlam değiştirmek
brief'in mandası değildi. `30-sosyal.md` eşleme notuna disambiguasyon bloğu eklendi,
karar **B171**'e bırakıldı.

*Ders: yeniden adlandırma turunun gerçek doğrulaması "yeni desen kaç kez geçiyor" değil,
**eski sayı = yeni sayı + atlanan sayı** mutabakatıdır. Ön tarama kaçırsa bile mutabakat
yakalar; tersi doğru değil.*

### Ölçüm — 165 jeton dönüştü, 4 satır bilerek durdu

| dosya | `İ`→`C` | `P`→`V` |
|---|---|---|
| `30-sosyal.md` | 78 | 48 |
| `02-borclar.md` *(yalnız açık kalemler)* | 11 | 11 |
| `03-sira.md` | 6 | 2 |
| `20-ref-marka.md` | 2 | 2 |
| `20-ref-program.md` | 4 | — |
| `20-ref-icerik-dili.md` | 1 | — |
| **toplam** | **102** | **63** |

**Bilerek dokunulmayan dört satır** — üçü tarihsel kayıt, biri farklı kavram:

- `02-borclar.md:30` (`İ22`) — dosya ön-bölümü, değişiklik günlüğü
- `02-borclar.md:249` (`İ19`) — **B04 ✅ KAPANDI**
- `02-borclar.md:1714` (`İ1`) — **B86 ✅ KAPANDI**
- `20-ref-protokoller.md:65` (`P1`) — *"KVKK TR **P1**"*, KARAR 100'ün iş adı; **prompt değil**

*İlke: kapanmış borç kalemi ve değişiklik günlüğü satırı, canlı bir dosyanın içinde yaşayan
**tarihsel kayıttır** — kronoloji ve arşivle aynı statüdedir. O gün "İ22" deniyordu; öyle kalır.*

`90-kronoloji/` · `_arsiv/` · `_uretilen/` · `01-kararlar.tsv` · `20-ref-bot.md` ·
`tools/` — hepsinde **sıfır** değişiklik.

#### KARAR 552 — Zemin dosya adı karta değil zemine bağlanır (AKTIF) {#k552}

Şema `ocak-zNN-slug-YYYY-AA-GG.png`. **Kart numarası ve MJ iş kimliği dosya adına girmez;**
eşleme `docs/31-zemin.md`'de yaşar. Basılan kart ayrı şema: `ocak-cNN-kM-YYYY-AA-GG.png`
(karusel değilse `-kM` yok).

Gerekçe: **zemin kalıcı bir varlık, kart ataması ise değişebilen bir karardır.** Aynı seansta
iki kez değişti — bir zemin C1'den C22'ye taşındı, bir aday şekil taramasıyla elendi. Adı
karara bağlamak **her karar değişiminde yeniden adlandırma borcu doğurur.**

`docs/31-zemin.md` bu turda açıldı: yedi zemin (z01–z07), MJ iş kimliği + ek, üreten prompt,
atandığı kart, ve piksel ölçümleri (boyut · üstKaranlık · ton · ortalama luma). Ölçümler
Claude.ai'de tam çözünürlüklü kaynak dosya üzerinden alındı, **ekran görüntüsünden değil** —
tanımlar dosyanın kendisinde yazılı. **z01 ve z02 mühürlüdür** (KARAR 542 · KARAR 546).

#### KARAR 553 — Kısaltma yeniden adlandırması: `İn` → `Cn`, `Pn` → `Vn` (AKTIF) {#k553}

`İn` → `Cn` (içerik/content) · `Pn` → `Vn` (görsel/visual). **Numaralar korunur, yalnız harf
değişir.** Gerekçe: `İ` Türkçe-özel bir harf — dosya adında ve grep'te sorun çıkarıyor; `P`
ise prompt mu görsel mi belirsizdi.

**Uygulama sınırı:** canlı, **ileriye bakan** dosyalar. Kronoloji, arşiv, türetilen çıktı,
mühürlü ledger satırları ve **kapanmış borç kalemleri** dokunulmaz.

⚠ `C-n` (tireli) KARAR 219'un kayıt hattı disiplinidir; `Cn` (tiresiz) içerik kartıdır.
**İkisi ayrı kavramdır, birbirine dönüştürülmez.** Üç homograf daha var — **B171**.

#### KARAR 554 — Bir karta varlık atamadan önce canlı sıra ve borç okunur (AKTIF) {#k554}

`03-sira.md` ve `02-borclar.md` salt-okunur; **kalem kapanmışsa yeni atama yapılmaz.**

Gerekçe: bu seansta **C1'e ikinci kez zemin atanacaktı.** Zemin aynı sabah mühürlenmişti
(KARAR 546) ve üç canlı dosyada yazılıydı. `30-sosyal.md` tek başına okundu; sıra ve borç
okunmadı. **KARAR 102'nin (gerçeklik spec'i ezer) atama işlerine uzantısıdır** — ledger ve
sıra dosyası da "spec"tir, gerçek olan mühürlenmiş atamadır.

### Doğan üç borç

**B169** kart derleyicisi (`tools/ocak-kart-derleyici.html` `kartAdi()`) KARAR 552 şemasıyla
hizalı değil — koda dokunur, ayrı tur. **B170** yedi zeminin yedeği yok (B141'in genişlemesi;
notu aynen duruyor — yedek, `--sref` çapasının ölmesini çözmez). **B171** `C`/`V` homograf
çakışması, yukarıda.

### `00-durum.md` — yine bekletildi

Dosya **195/200**. Kaan'ın aynı gün verdiği karar duruyor: **Sayfalar DB turu bitene kadar
bekliyor**, o turda tahliye + yazım birlikte koşulacak. Bu tur da ona uydu — yazılsaydı tavan
zorlanacaktı (KARAR 457). **B131 açık kalır.**

---

## 24 AĞUSTOS 2026 — SIFIR DOLGU + KARUSEL ZEMİN KURALI (CC, sıfır kod)

**Üç KARAR mühürlendi (555–557), bir borç kapandı (B171), iki borç doğdu (B172–B173).**
Kod dosyasına dokunulmadı. Taban `6fde0b0`.

### Brief'in kendi kapısı — r2'nin dersinin kaydı

r2 sert bir kapı koymuştu: *"eşleşme varsa DUR."* **Ölçütü yazdı, yöntemi yazmadı.**
CC `git grep -P -nE` yazdı, `-E` `-P`'yi ezdi, `\b` POSIX ERE'de metakarakter olmadığı
için tarama sessizce `0` döndü — ve o sıfır *"çakışma yok"* diye okundu. Üç homograf
kapıdan geçti. **Yakalayan şey tarama değil, dönüşüm sonrası mutabakat oldu.**

r3 brief'i her sert kapıyı **komutuyla birlikte** yazdı ve bir sanity deseni ekledi.
Bu turda sanity `\bB1[0-9]{2}\b` → **323 satır** döndü; PCRE'nin çalıştığı kanıtlandı,
sonra ölçüm okundu. Ders **KARAR 557** olarak mühürlendi.

### Homograf envanteri — `docs/` tamamı, üç kova

Canlı kapsamda **dördüncü homograf çıkmadı** — bilinen üçü teyit edildi:
`20-ref-marka.md:391` `C1` (VSCO preseti `A6/C1`) · `03-sira.md:68` `V6 V7 V8`
(Midjourney motor sürümü) · `01-kararlar.tsv:405` `V2` (sayfa sürümü).

⚠ **Kronoloji ve arşivde beş homograf sınıfı daha var, hepsi kapsam dışı:**

| yer | jeton | anlamı |
|---|---|---|
| `2026-05.md` · `00-devir.md` · `_arsiv/ocak-kronoloji-v1.md` | `C1`–`C5` | brief commit adımları (*"C1 (`d754da4`) — Registry"*) |
| `_arsiv/2026-08-23-mj-kayit.md:56,59` | `C2` | MJ ızgara koordinatı (`A2/B2/C2`) |
| `2026-08.md:3425,3454-3457` · `_arsiv/2026-08-23-devir.md` | `V7` `V8` | MJ motor sürümü |
| `2026-05.md:2240` · `_arsiv/ocak-kronoloji-v1.md:2330` | `V1` | tasarım sürümü (*"V1'de sade kalabilir"*) |
| `2026-07.md:505` | `V2` | sayfa sürümü |

**Dokunulmadı ve dokunulmayacak.** Bu tabloyu tarihsel kayıt için yazıyoruz: ileride
biri kronolojide `C3` görürse üçüncü kartı değil üçüncü commit'i aramalı.

### Ölçüm — 118 jeton dolduruldu, 15 satır bilerek dolgusuz kaldı

| dosya | `Cn`→`C0n` | `Vn`→`V0n` |
|---|---|---|
| `30-sosyal.md` | 34 | 48 |
| `02-borclar.md` *(yalnız açık kalemler)* | 5 | 11 |
| `03-sira.md` | 4 | 2 |
| `20-ref-marka.md` | 1 | 2 |
| `20-ref-program.md` | 2 | — |
| `20-ref-icerik-dili.md` | — | — |
| `31-zemin.md` | 4 | 5 |
| **toplam** | **50** | **68** |

*`20-ref-icerik-dili.md` sıfır çünkü tek jetonu `C22` — zaten iki haneli.*

**Dolgusuz bırakılan 15 satır, üç sınıf:** (1) iki homografın kendi metni ·
(2) yeni eşleme notunun homografı **örnek olarak** gösterdiği dört satır ·
(3) kapanan **B171**'in gövdesi — kapanmış kalem tarihsel kayıttır.

**Doğrulama:** `git grep -Pn '\bC[0-9]\b'` ve `\bV[0-9]\b` yedi dosyada koşuldu;
dönen her satır ya homograf ya homograf alıntısı — **kart/prompt jetonu 0.**
Ayrıca ters-dolgu testi (`C0n`→`Cn`, `V0n`→`Vn`) HEAD ile karşılaştırıldı: yalnız
iki kasıtlı yeniden yazım (eski eşleme notu · B171 başlığı) fark verdi, **başka
hiçbir satır değişmedi.**

#### KARAR 555 — Kart, prompt ve zemin jetonları sıfır dolguludur (AKTIF) {#k555}

`C01`–`C24` · `V01`–`V09` · `z01`–. Numara korunur, önüne sıfır gelir.

**Üç gerekçe.** (a) **Homograf ayrımı:** `V8` hem sekizinci prompt hem Midjourney motor
sürümüydü; `C1` hem birinci kart hem VSCO presetiydi. `--v 8.1` her promptun içinde
yazılı — bu çakışma günlük konuşmanın tam ortasında yaşıyordu. `V08`/`C01` grep'te ve
gözde ayrışır. (b) **Önek çakışması:** `C1` deseni `C10`–`C19` içinde eşleşir; 23 Ağustos
turunda bunun etrafından *yüksek-numaradan-alçağa inerek* dolaşmak gerekmişti. Dolgu bu
hata sınıfını **kalıcı kapatır.** (c) **Biçim tutarlılığı:** `z01`–`z07` zaten dolguluydu;
üç aile artık aynı biçimde okunur.

✅ **B171 bu kararla kapandı.** Homograflara dokunulmadı ve dokunulması gereksizleşti —
biçim ayrıştırıyor, niteleme (*"Midjourney **motor** V8.x"*) gerekmiyor.

#### KARAR 556 — Karusel zemini yüzey olur, nesne olmaz (AKTIF) {#k556}

Karusel iç kartları **tek üretimin crop'larıdır**; crop'un çalışması için zeminin karenin
her yerinde **sürmesi** gerekir. **Yüzey** (kül, kor, kıvılcım, duvar dokusu) sürer —
hangi pencere alınırsa alınsın malzeme vardır. **Nesne** (kapı, mum çemberi, pencere,
fincan) sürmez; kareye bir kez girer, crop'ların bir kısmı onu gösterir, **kalanı boş kalır.**

**Ampirik dayanak:** C02'nin altı kartı z03 (gözenekli kül) üstünde basıldı, altısında da
metin temiz okundu — 23 Ağustos.

**Uygulandığı yer — C24.** Zemini `V03` (eşik/kapı) idi, `V01` (söz kartı ana zemini)
oldu. Gerekçe kart gövdesine yazılmadı, burada yaşıyor: C24 kapı değil *"aynı yüzeyde
farklı konumlar"* anlatıyor (*"kimi uykuda, kimi merakta, kimi yolda"*) — sürekli kül
tarlası bunun karşılığı. Ayrıca **C01 külle açılıyor, C24 külle kapanıyor.**

**Uygulanmayan iki yer — B172 ve B173.** C09 (V04 mum çemberi, **8 crop**) ve C20
(V06 pencere/mum, 6 crop) aynı kuralı ihlal ediyor ama çözümleri belirlenmedi: zemin
yüzeye mi çevrilir, kart mı kısalır. **Metne dokunulmadı, borç açıldı.** C09 dokuzuncu
gün — V04 üretilmeden karara bağlanmalı.

#### KARAR 557 — Sert kapı ölçütü yöntemiyle birlikte yazılır (KALICI) {#k557}

Bir brief *"eşleşme varsa dur"* gibi **bloke edici** bir ölçüt koyuyorsa, ölçümün
**komutunu** ve bir **sanity testini** de yazar. Ölçüt yöntemsiz yazılırsa kapı kâğıttan
olur: yanlış komut sessizce `0` döndürür ve sıfır *"temiz"* diye okunur.

**KARAR 470'in (her sayı yöntemini taşır) brief yazımına uzantısıdır.** 470 raporlayana
bakıyordu, 557 **brief'i yazana** bakıyor.

*Vaka: r2'nin çakışma taraması `-E`'nin `-P`'yi ezmesi yüzünden 0 döndü; üç homograf
kapıdan geçti ve bunları tarama değil, dönüşüm sonrası mutabakat yakaladı. İki tur önce
aynı sınıftan bir tuzak daha vardı — bu kabukta `grep` bir `ugrep` shell function'ı ve
tırnaksız dosya listesini tek dosya adı sanıyor. **İkisi de "0 eşleşme" olarak raporlanır.***

---

## 24 AĞUSTOS 2026 — SAYFALAR METİN TURU + C LİSTESİ (1–5) + DEPLOY (claude.ai + Notion + CC)

**On KARAR mühürlendi (558–567), iki borç kapandı (B80 · B164), sekiz borç doğdu (B174–B181).**
Yüzey: claude.ai + Notion MCP; kod dosyasına dokunulmadı. **Deploy aynı gün alındı** —
`1b4b998` → **`9acbabb`**.

⚠ **Bu oturum boyunca `ocak-docs` MCP bayat bir snapshot servis etti.** `docs_karar(547)`
*"ledger'da bu numara yok"* döndü, `Çember Rehberi` araması **0** eşleşme verdi — yani
23 Ağustos'un ana patch'i (`28265fa`) ve ek patch'i (`d8f0c56`) oturumun ölçebildiği
korpusta **yoktu**, çünkü henüz push'lanmamışlardı. Patch bu yüzden numara taşımadan geldi.
*Bu, KARAR 479'un (MCP git deposunu okur) doğrudan sonucudur: **push edilmemiş commit
claude.ai için yok hükmündedir.** Bugünkü push o pencereyi kapattı.*

### Yazım envanteri — on dört yüzey, yirmi bir yazım

`/sen-neredesin` (Çember Rehberi) · `/acik-kapi` (yazım yok, iki kalem zaten uygulanmıştı) ·
`/seremoni` (yazım yok, kayıt DB'den çözüldü) · `/mini-retreat` (inkâr cümlesi + kayıt cevabı) ·
`/anadolu` (kalem zaten kapalıydı) · `/biz` (ortak bir dil + Rehber + dört alan) ·
`/advaita` (savunmacı cümle + rehberleri) · `/ekip` (beş gövde + Meta Açıklama + ham link) ·
`/hikaye` (yazım yok) · `/bulusmalar` (en derini) · `/cember` (kayıt istisnası) ·
`/sehir-aksami` (iki ham link) · `/adimiz` (köz) · `/iletisim` (rehberleri) ·
`home` (Hikâyeyi oku linki taşındı).

Etkinlikler: `kakao-seremonisi-online` + `hasat-ve-sukran` gövde (kayıt satırı) ·
üç `Kayıt Var` işareti kaldırıldı · dört başlık/slug/gövde · iki Kakao başlığı ·
`ates-seninle-geldi` tam çerçeve.

**"Çember Lideri" on yerde yaşıyordu.** Devirdeki tarama listesi üç yer işaret ediyordu;
gerçek dağılım: `/sen-neredesin` · `/biz` · `/advaita` · `/ekip` (beş gövde + bir property) ·
`/iletisim`. *KARAR 551'in korpus ayağı 23 Ağustos'ta yedi geçişte kapanmıştı; Notion ayağı
bugün on geçişte kapandı. **Aynı kararın iki yüzeyi, iki ayrı sayım.***

### Ölçüm notları

Gövde yazımlarında KARAR 550 protokolü koşuldu — `python3 len()` ile delta, yazım, geri okuma.
Beş gövdede beklenen uzunluk birebir tuttu:

| gövde | önce → sonra | delta |
|---|---|---|
| `kakao-seremonisi-online` | 3605 → **3571** | −34 |
| `hasat-ve-sukran` | 3672 → **3638** | −34 |
| `kokun-nerede-kaldi` → `ates-seninle-geldi` | 3036 → **3330** | **+294** |
| `hangi-tohumu-ekeceksin` | 3480 → **3472** | −8 |
| `dusundugum-hissettigim-yaptigim` | 3049 → **3044** | −5 |

⚠ **Üç bekçi bandı birden kaydı** — patch yalnız birini saymıştı, arşivci turu üçünü de
türetti (yukarıdaki beş deltadan, dünkü on dört rakamın üstüne):

| format | dünkü bant | bugünkü gerçek | kayan uç |
|---|---|---|---|
| **Seremoni** | `3.605–3.686` | **`3.571–3.686`** | alt uç −34 |
| **Açık Kapı** | `3.036–3.264` | **`3.044–3.330`** | **iki uç birden** |
| **Çember** | `3.480–3.771` | **`3.472–3.771`** | alt uç −8 |

*Açık Kapı bandının iki ucunun birden kayması `ates-seninle-geldi`nin +294'ünden geliyor:
tam çerçeve yeniden yazımı bandın tavanını kırdı, `dusundugum`un −5'i tabanını indirdi.
**Bir gövdenin yeniden yazımı bandı iki yönde birden bozabilir** — B144/B145/B88 buna göre
tazelendi.*

- **Kayıtlar DB'de yalnız iki satır, ikisi de test** → dört slug değişimi **bedelsizdi**.
  Redirect gerekmediğinin ölçülmüş dayanağı budur, varsayım değil.
- **Asistan bir kez sayfa ID'sini tahmin etti, 404 aldı.** Kural yeniden yazıldı: **ID yalnız
  sorgu sonucundan alınır.** ⚠ Var olan başka bir kayda denk gelseydi **404 gelmez, yanlış
  kayda yazılırdı** — ve hata sessiz olurdu. 404 bu turda şanstı.

### KARAR 558 — Seremonide ve Çemberde kayıt yoktur, istisnasız (KALICI) {#k558}

Bloğun cinsine göre kısmi kayıt doktrini (KARAR 547) **bu iki format için geçersizdir.**
Açık Kapı ve Atölye'de kısmi kayıt sürer.

**Gerekçe bir çelişkidir, bir tercih değil:** `/seremoni` ve `/cember` sayfaları kadına
**mutlak** *"kaydedilmez"* sözü veriyordu; oysa `hasat-ve-sukran` ve `kakao-seremonisi-online`
`Kayıt Var` işaretliydi ve iki gövde *"Tema ve seremoni kaydedilir"* diyordu.
**Söz ile mekanizma çelişiyordu; söz kazandı.**

*Söz kazandı — çünkü mekanizmayı değiştirmek bizim elimizde, verilmiş sözü geri almak değil.*

`←547` bu iki format için daraltır. Canlıda doğrulandı (`/cember`: *"Bu istisnasızdır:
çemberin hiçbir bölümü kaydedilmez — ne tema, ne paylaşım."*).

### KARAR 559 — Etkinlik slug konvansiyonu `{tema}-{YYYY-AA-GG}` (AKTIF) {#k559}

**Gün hassasiyeti zorunludur.** Gerekçe ölçüm: dört Çember'in dördü de aynı ay içinde —
**9 · 16 · 23 · 30 Eylül** — `{YYYY-AA}` biçimi dördünü tek slug'a çakıştırırdı.
Yolculuk durakları `yolculuk-{durak}-{kohort}`.

**Slug boş bırakılmaz**; `Yayınla` açılmadan önce zorunlu alandır. **Slug ile `Başlık`
ayrışmaz** — tek gerçek disiplini (Kaan, bu oturum).

### KARAR 560 — Dört ad ve iki başlık değişti (AKTIF) {#k560}

| eski slug | yeni başlık | yeni slug | format |
|---|---|---|---|
| `elin-neyle-dolu` | **Ellerin Dolu** | `ellerin-dolu-2026-09-09` | Çember |
| `ekmeden-once` | **Yer Açmak** | `yer-acmak-2026-09-23` | Çember |
| `hangi-tohumu-ekeceksin` | *(başlık aynı)* | `hangi-tohumu-ekeceksin-2026-09-30` | Çember |
| `kokun-nerede-kaldi` | **Ateş Seninle Geldi** | `ates-seninle-geldi-2026-09-03` | **Açık Kapı** |

Kakao ikizleri → **Kakao Seremonisi · Online** ve **Kakao Seremonisi · İzmir**
(başlıklar birebir aynıydı, takvimde ayırt edilemiyordu).

⚠ **Patch bu dördünü "Çember adları" diye başlıklandırdı — dördüncüsü Açık Kapı'dır.**
Arşivci turu ölçtü (canlı kayıt rotası, 24 Ağu): `ates-seninle-geldi-2026-09-03` →
`/acik-kapi/kayit` · diğer üçü → `/cember/kayit`. **Dördüncü Çember `neyi-bekliyorsun`dur**
(16 Eylül) ve **tarih eki almadı** — B175'in ilk kalemi. 23 Ağustos kronolojisi
`kokun-nerede-kaldi`yı zaten Açık Kapı olarak kaydetmişti; korpus haklıydı, patch başlığı
bayattı.

*Not: KARAR 559'un çakışma gerekçesi bundan etkilenmez — dört Çember gerçekten 9/16/23/30
Eylül'de ve `{YYYY-AA}` onları yine çakıştırırdı.*

### KARAR 561 — Başlık gövdenin cevapladığı soruyu sormaz (KALICI) {#k561}

Yazım ilkesi. **Dört sayfada aynı arıza:** kapak soru soruyor, gövde cevabı zaten veriyor.

| kapak | gövde |
|---|---|
| *"Elin neyle dolu?"* | *"ellerin dolu"* |
| *"Ekmeden önce"* | *"bu akşam ekmiyoruz"* |
| *"Kökün nerede kaldı?"* | *"seninle geldi"* |
| *"…en son ne zaman aynı yöne baktı?"* | sayfa geçmişe bakmıyor |

**Ad kararı gövdenin kendi vurgu cümlesinden alınır.** → `ocak-metin` ve `ocak-lint`'e girer.

### KARAR 562 — Metin kadına ne olduğunu ya da ne yapması gerektiğini söylemez (KALICI) {#k562}

Advaita'nın **beş ayrı yorumunun ortak grameri**: *"yargı" kelimesi* · *"kadın gelmeyi
istesin"* · *"çok eril, itici"* · *"buna karışamayız ki"* · *"sınav değil demek bile sınava
işaret ediyor"*.

Uygulaması: `hangi-tohumu-ekeceksin`in **kıtlık mantığı** (*"tarla bir tane taşır"* ·
*"bu kez bir tane seç"*) tümden çıkarıldı, yerine **çağrı çerçevesi** geldi.
→ `ocak-lint`'e sertlik/erillik kural sınıfıyla birlikte girer (**B166**).

*Beş yorum beş ayrı sayfada, beş ayrı cümle hakkındaydı; ortak grameri ancak yan yana
konunca göründü. **Tek tek bakan göz bu sınıfı göremez** — kural sınıfı bu yüzden gerekli.*

### KARAR 563 — Mühür ile denetim satırı aynı anda yazılır (KALICI) {#k563}

Bir yasak ya da yeniden adlandırma kararı, **`yasak-dizeler.tsv` satırı yazılmadan
mühürlenmiş sayılmaz.**

**Gerekçe ölçümdür:** KARAR 86 (köz yasağı, Mayıs) ve KARAR 398–410 ailesi (psikolog rolü
kaldırıldı, Temmuz) denetim satırı almadıkları için **aylarca canlıda kaldı** ve bu oturumda
**elle** bulundu. KARAR 507 bunu doğru yapmıştı.

*Denetimsiz karar, kararın kendisi kadar uzun yaşamaz — metin onu sessizce aşındırır.*

### KARAR 564 — Kaynak dosya elde varken özetten çalışılmaz (KALICI) {#k564}

Uzun sohbette eski tool sonuçları bağlamdan silinir; asistan **kendi özetinden** çalışmaya
başlar ve **sessizce** yanılır.

**Vaka:** bu oturumda C-5 için *"Advaita alternatif vermemiş"* denildi — **dosyada üç
alternatif duruyordu.** Kural: kaynak dosya diskteyse her kalem için **yeniden okunur**
(`grep`/`sed`), özetten karar verilmez.

### KARAR 565 — Notion SQL `LIKE` Türkçe çekimde kördür (AKTIF · ölçüm yöntemi) {#k565}

`%çocuk%` araması *"Çocuğu"* geçen metinde **0** döndü — `k→ğ` yumuşaması.
**Grep bir tarama aracıdır, okumanın yerine geçmez.**

⚠ Bu oturumdaki desen taramaları (`dakika` · `istesin` · `kaydedil`) **bu körlüğü taşıyor
olabilir** — sonuçları "temiz" değil "bu desen için temiz" diye okunur.

*KARAR 470'in "iki ölçüm çelişirse önce tanımlar karşılaştırılır" kuralının Türkçe ayağı:
burada tanım farkı **dilin kendisinden** geliyor, ölçenden değil.*

### KARAR 566 — `prerender = false` taze içerik demek değildir (KALICI) {#k566}

Yedi sayfa SSR (`/cember` · `/acik-kapi` · `/seremoni` · `/atolye` · `/mini-retreat` ·
`/sehir-aksami` · `/yolculuk`) — **ama yedisi de içeriği build-time content collection'dan
okuyor.**

Ölçüm (24 Ağu, CC):
- `getEntry('sayfalar', '/…')` yedi sayfanın **yedisinde** (`src/pages/*.astro`)
- loader `store.clear()` + `fetchSayfalar(notion, …)` — **yalnız build sırasında**
  (`src/content/config.ts:20-31`)
- **ISR/`revalidate` yok** (`astro.config.mjs:20`, `isr|revalidate|expiration` → 0 eşleşme)
- bu yedi sayfada **doğrudan `notion.` çağrısı 0**
- tek canlı Notion çağrısı `etkinlikGecmisMi(id)` (`davet-guvenlik-agi.ts:19`) ve **kapılı**
  (`if (!id) return false`) — yalnız `?etkinlik=` parametreli davet linkinde açılıyor,
  getirdiği şey **tarih**, içerik değil

**Sonuç: her metin değişikliği deploy ister — sayfa SSR olsun ya da olmasın.**
**Ters yüzü:** SSR sayfası `dist/`te statik karşılık üretmez, **yerel build onu doğrulayamaz.**
Ölçüm yolu kalıcı olarak: **SSR sayfası canlıda doğrulanır.**

### KARAR 567 — Başarısız olamayan doğrulama yazılmaz (KALICI) {#k567}

Bu turun deploy brief'i dokuz kontrolü `dist/` üzerinde tanımladı; **gerçek yol
`dist/client/`.** Dördü (`grep … | wc -l`, beklenen `0`) **sahte sıfır** verecekti — boş
dizinde `grep` her zaman `0` döner. **Dört "temiz" sonuç hiçbir şeye bakmamış olacaktı** ve
içerik doğrulanmış sayılacaktı.

Kural: bir kontrolün **başarısız olabildiği önce kanıtlanır** — yolun var olduğu `ls` ile
doğrulanır, ya da **beklenen-pozitif** bir eşleşme aynı komutla aranır.

`←557` (sert kapı ölçütü yöntemiyle birlikte yazılır) ile aynı aileden: 557 ölçütün
**komutunu** ister, 567 o komutun **yanılabilir** olmasını.

### Deploy — aynı gün, üç adımlı CC brief'iyle

salt-read → yerel build + dokuz `dist/` kontrolü → push + sekiz canlı kontrol.
Sonuç: `dpl_Dvoo9UkkF9HVZSXQqT4BJZ8bcpTJ` · target production · **READY** (50 sn) ·
region `iad1` · `www.ocak.biz` alias'ta · SHA **`9acbabb`** = local HEAD = origin/main.
**Sekiz canlı kontrolün sekizi de tuttu**, hiçbir madde düzeltme gerektirmedi.

Push edilen on commit'in **onu da `docs/`**, sıfır kod dosyası. Deploy git push'la tetiklendi —
**`tZR9LcwJq9` hook'una dokunulmadı** (ölü `astro-iskelet` dalına gider, **B64**).

**İki brief hatası CC tarafından yakalandı, ikisi de KARAR'a döndü** — 567 (sahte sıfır) ve
566 (`/cember` SSR olduğu için `dist/`ten ölçülemez). İkincisi asıl bulguyu getirdi.

**Redirect bilerek konmadı.** Kaan dört slug'ın hiçbir yerde yayımlanmadığını doğruladı;
Temmuz emsali (`/workshop` · `/istanbul`) canlı ve bookmark'lanmış adreslerdi, bunlar hiç
duyurulmadı. Eski üç slug canlıda **404** veriyor — beklenen davranış, ölçüldü.

### Kapananlar

**B80 ✅** `/adimiz` *"közüyle taşınır"* → *"kor alınır, taşınır"*. KARAR 86 nihayet uygulandı.
Kapanış şartı canlıda doğrulandı (24 Ağu): `/adimiz` gövdesinde `közüyle` **0**,
`kor alınır` **1**. Sosyal kart zaten temizdi (C10 "kor" diyordu); **site cümlesi üç ay
geride kalmıştı** — KARAR 563 tam bu vakadan doğdu.

**B164 ✅** `Kayıt Var` işaretlemesi doğrulandı: dokuz işaretli kayıt beklentiyle **birebir**
uyuştu (altı Açık Kapı + üç seremoni). ⚠ **Sonra KARAR 558 üçünü kaldırdı, altı kaldı.**
Yani borç "doğrulandı" diye kapandı ve **aynı turda doğrulanan değer değişti** — kapanış
ölçümün doğruluğunu tescil eder, değerin kalıcılığını değil.

### Arşivci turunun kendi ölçümleri — üç düzeltme

**1 · Patch iki borcu yanlış numarayla kapatıyordu.**

| patch'in dediği | gerçek | ne olurdu |
|---|---|---|
| *"borç 40"* (köz) | **B80** | B40 zaten kapalı (11 Ağu, `b72c539`) — **kapalı bir borç ikinci kez kapatılırdı** |
| *"borç 26'nın ekip ayağı"* | **B numarası yok** — Pilot borç 26 (`2026-07.md:38`, "Zincir kontrolleri") | B26 `.ocak-kayit-cta__buton`, **⏸ ERTELENDİ** — ertelenmiş bir CSS borcu yanlışlıkla kapatılırdı |

*Eski Pilot numaralandırması (borç 18–27) ile B numaralandırması **aynı sayı uzayını
paylaşıyor ve ayırt edici bir önek taşımıyor.** Bu turda iki kez çarptı.*

**2 · Ham Notion linki taraması yarım değil — tamamlandı, temiz.**
Patch *"etkinlik gövdeleri taranmadı"* diyordu. Arşivci turu **sitemap'in 48 rotasının
48'ini** canlıdan taradı (`href` içinde `pvs=` · `notion.so` · `notion.site` · `/p/`):
**0 eşleşme.** On beş etkinlik sayfası dahil, yedi SSR sayfası dahil.
**Borç açılmadı** — `/ekip` ×1 ve `/sehir-aksami` ×2 düzeltmesiyle sınıf kapandı.

**3 · `/yolculuk` `yolculuk-eksen` bölümü boş değil — çıktıda hiç yok.**
Patch *"section boş"* diyordu. Ölçüm (canlı, 24 Ağu): sayfanın on iki `data-section`'ı var
(`hero` · `intro` · `yolculuk-nedir` · `yolculuk-kime` · `online-yolculuk` · `buyuk-vurgu` ·
`son` · `mini-cta` · `iki-yolculuk` · `sonraki-bulusma` · `kayit-btn` · `siradaki-kapi`) ve
**`yolculuk-eksen` aralarında yok.** Boş bir kap değil, **hiç doğmamış bir kap.**
*"Kod var" ≠ "output var" — teşhis buna göre kurulur (**B176**).*

### `00-durum.md` TAHLİYESİ — 11 Ağustos bloğu indi (24 Ağustos, KARAR 457 · 61)

Dosya **195/200**'dü (**B131**). 23 Ağustos'ta Kaan *"Sayfalar DB turu bitene kadar bekliyor;
o turda tahliye + yazım birlikte koşulur"* demişti. Tur bitti, tahliye koşuldu.
İnen iki madde **olduğu gibi**, `ŞU AN NEREDEYİZ` bloğundan alındı — **silinmedi, taşındı:**

> - **B47 ✅ (11 Ağu)** — `05-harita.md` doğdu. Liste değil **otorite sırası**: ham gerçeklik →
>   kronoloji → gövde → indeks → ayna. **KARAR 482** mühürlendi (indeks, indekslediği gövdeyi
>   asla yenmez). B40 · B55 · B56 karar borcu olmaktan çıktı, mekanik yazıma indi. **B58 ✅.**
> - **B40 · B55 · B56 ✅ (11 Ağu)** — KARAR 482'nin ilk uygulaması, tek commit `b72c539`.
>   `61/88` → `61` (88 KIRPMA'nın kardeşi değil, patch modunun halefi). Sahip tablosu
>   gövdelerden yeniden türetildi, beş bayat kalem çıktı. Ledger düzeltildi, kronoloji
>   **şerh** aldı — iki dosya bilerek aynı işlemi görmedi. **KARAR 483** mühürlendi.

Yerine `00-durum.md`'de **tek satırlık işaretçi** kaldı. Tahliyenin ayrıntılı anlatısı
zaten bu dosyada, 11 Ağustos kayıtlarında yaşıyor; inen iki madde onların **özetiydi**.

⚠ **B131 kapanmadı.** Tahliye tavanı bir kez daha açtı, **kapağı kaldırmadı.** Bir sonraki
tahliye adayı, dosyanın kendi işaretine göre, `ŞU AN NEREDEYİZ`in ADIM 7 dalgaları.

**İkinci tahliye — `B01 ✅` maddesi de indi (aynı tur).** İlk tahliye dosyayı tam **200/200**'e
oturttu: tavanın altında ama **payı sıfır.** Bir sonraki yazanın önünde boşluk bırakmak için
kapanmış ve içinde canlı iş kalmamış bir madde daha alındı — **olduğu gibi:**

> - **B01 ✅ (10 Ağu)** — klon · remote · Vercel proje adı üçü de `ocak`. Repo içi canlı yol
>   referansları 13 → 3 satır (kalan üçü bilerek korundu). Tek commit `50294e6`, sıfır site
>   kodu. Vercel git bağlantısı `githubRepoId` üzerinden **kırılmadı**, MCP damgası ilerledi
>   — ikisi de ölçüldü. Yeni borç: **B58 · B59 · B60**.

⚠ **İnen maddenin bir cümlesi bugün hâlâ canlı bir tartışmaya bakıyor:** *"Vercel proje adı
üçü de `ocak`"*. 24 Ağustos ölçümü bunu **teyit etti** (deployment yanıtında
`project.name = "ocak"`) — ve aynı ölçüm `.vercel/repo.json`'ın *"ocak-site"* dediğini,
yani **yerel link dosyasının 27 Mayıs'tan beri yalan söylediğini** gösterdi (**B179**).
Bu yüzden cümle `00-durum.md`'de **kaybolmadı**: `KOD / DEPLOY GERÇEĞİ` tablosunun Vercel
satırında ölçülmüş hâliyle yaşamaya devam ediyor.

*Tahliye kuralı burada bir kez daha kendini gösterdi: inen blok özet, kalan işaretçi olmalı —
ve **inen blokta hâlâ canlı bir iddia varsa, o iddia inmez, tazelenir.***

**Üçüncü tahliye — `Marka işareti ✅` maddesi (aynı tur).** Tablo satırları ve sessiz kırılma
maddeleri yazıldıktan sonra dosya yeniden **203**'e çıktı. İnen madde, olduğu gibi:

> - **Marka işareti ✅ (18–19 Ağu)** — OCAK'ın ilk logosu doğdu, mühürlendi (**KARAR 522**)
>   ve siteye yerleşti. Cormorant Garamond Light "O", iç boşlukta radyal kor; harfler
>   outline (font bağımlılığı yok), dosyalarda **SVG filtresi yok**, dolgu kuralı
>   `nonzero`. Kor yarıçapı iç boşluğun %70'i. `public/` beş varlık, `Layout.astro`
>   üç icon link, `Nav.astro` düz wordmark → `<img>`; nav yüksekliği değişmedi.
>   Aynı turda **başlık kanonu** düzeldi (**KARAR 523**): sekiz rotada çift "OCAK"
>   vardı, marka tek sahibe indi. Instagram · WhatsApp · e-posta anteti hâlâ eski →
>   **B105**.

⚠ **İnen maddenin canlı ayağı `B105`'tir** ve o borç açık — Instagram · WhatsApp · e-posta
anteti hâlâ eski işareti taşıyor. Bu yüzden `00-durum.md`'de kalan işaretçi **B105'i adıyla
anıyor**; madde indi, borç inmedi.

*Bir turda üç tahliye: 195 → 189 → (yazım) → 200 → 197 → (tablo + sessiz kırılma) → 203 → 196.
**Tahliye ile yazım aynı turda koşulunca tavan bir kez değil, üç kez zorlanıyor** — çünkü her
yazım bloğu tavana yeniden bastırıyor. Sıra şu olmalı: **önce tahliye, sonra yazım, sonra
ölçüm** — ama gerçekte yazımın boyu ancak yazarken belli oluyor. **B131 bu yüzden kapanmıyor.***

---

## 24 AĞUSTOS 2026 — MJ GÖRSEL TURU: V03 KAPANIŞI, V10 TERKİ, YORGUNLUK DOKTRİNİ (claude.ai + Midjourney)

**Yüzey:** Claude.ai (ölçüm/karar) + Midjourney (üretim) + `ocak-kart-derleyici.html` (yerel).
**Kod deposu bu turda etkilenmedi** — MJ üretimi CDN'de, derleyici düzeltmesi çalışma
ağacında, sıfır kod commit'i. Bu kayıt turun **doküman ayağıdır** ve `465591a`'nın üstüne gelir.

⚠ **Patch *"Öncül HEAD: `9acbabb`"* diyordu; gerçek HEAD `465591a`'ydı** — araya aynı günün
Sayfalar turu iki commit soktu (`6790cb1` · `465591a`). CC'nin oturum açılışındaki git
snapshot'ı da aynı bayat hash'i gösteriyordu; dönem HEAD satırı önce **ondan devralınarak**
yazıldı ve `git log -2 --format='%h' | tail -1` doğrulaması (KARAR 474) tutmayınca düzeltildi.
*Ders — KARAR 470'in bir vakası daha: **oturum açılışındaki snapshot bir ölçüm değildir.**
Aynı sınıf, aynı gün üçüncü kez: MCP bayat korpus servis etti (B181), patch bayat HEAD yazdı,
harness bayat `git status` gösterdi. Üçünün ortak nedeni **push edilmemiş commit** (KARAR 479).
Bu, KARAR 474'ün doğrulama adımının neden atlanamaz olduğunun kanıtıdır: hata yazıldı ve
**yalnızca doğrulama komutu yakaladı.***

**Üretim hacmi:** V03 bir draft turu (18 kare, gövde patch'i sonrası) + altı Vary seti
(24 kare) · V10 üç draft turu (54 kare, **sıfır kazanan**). Toplam 96 kare, dört mühürlü
zemin — **kabul oranı %4.2**.

### V03 (eşik/kapı) kapandı

Gövde patch'i uygulandı, üç tur ölçüldü, dört zemin mühürlendi (**z08–z11**), dört karta
bağlandı (C03 · C07 · C12 · C17). V01 ile birlikte **iki prompt ailesi tamam.**

Değişim tek cümleyle: **kapı uzaklaştırıldı.** Kadraja *"seen from a distance, small in the
lower third of the frame, vast unlit stone wall filling the upper two thirds"* girdi,
*"surrounding wall dissolving into charcoal darkness"* çıktı — duvar artık kaybolmuyor,
kadrajı kuruyor.

| | patch öncesi | patch sonrası |
|---|---|---|
| `ustKaranlik` medyanı | %52.0 | **%94.8** |
| beş ölçütü geçen kare | 1/18 | **13/18** |
| ton medyanı | 24.0° | **12.7°** |
| palet dışı kare | 2 | **0** |

**Ton kendiliğinden düzeldi — ayrı bir ton turu harcanmadı.** Kapı küçülünce kehribar
aydınlık pikseller çoğunluk olmaktan çıktı, medyan taş duvarın kızılına kaydı. *Ders:
iki ölçüt aynı geometrik nedene bağlıysa biri düzelince öteki de düzelir; her ölçüt için
ayrı tur harcamadan önce nedenlerin ortak olup olmadığına bakılır.*

**z11'in dipnotu karta bağlıdır.** Kapının ardındaki kor yığını **görünür** — diğer üç kapı
zemininde ışık var, kaynağı yok. C17 *"ilk dokunuş"* kartında bu bilinçli tutuldu: portre
serisi uyku → merak → dokunuş yayında ilerlerken kaynağın görünür olması **dokunuş anına
denk gelir.** Başka bir karta verilse aynı özellik kusur olurdu.

### V10 (kilim yüzeyi) — açıldı ve üç turda terk edildi

Kayda geçmesi gereken bir **başarısızlık**. C09'un sekiz-crop'luk karuseline sürekli bir
yüzey arandı; kilim seçildi çünkü V04'ün kendi gövdesinde zaten geçiyor
(`hand-woven kilim texture at the light's edge`), Anadolu malzemesi ve çekim kılavuzunda
R4 olarak marka sözlüğünde.

| tur | gövde çekirdeği | `ustKaranlik` medyanı | `süreklilik` medyanı | geçen |
|---|---|---|---|---|
| 1 | `hand-woven Anatolian kilim` | %72.3 | %107 | 0/18 |
| 2 | `extreme close-up of flat-woven wool texture` | %81.6 | %107 | 0/18 |
| 3 | `flat-woven wool textile … at arm's length` | %73.5 | %110 | 0/18 |

- **Tur 1 kusuru — nesne adı sahneleme çağırıyor.** `kilim` bir *nesne* adıdır; model onu
  sahneledi: katladı, astı, yanına tahta merdiven koydu, büyüteç ve harf formu üretti, bir
  karede İskoç ekosesi verdi. V01'in `fine grey ash surface`'i bunu yapmıyor çünkü **yüzey adı.**
- **Tur 2 kusuru — `extreme close-up` ölçeği serbest bıraktı.** Nesne adı kalkınca model
  *"ne kadar yakın?"* sorusuna *"iplik lifi kadar"* cevabı verdi; set halat/urgan üretti.
  O ölçekte her ip ayrı bir silindir, ışık her birinin üstünde ayrı parlıyor — **süreklilik
  sapmasının kaynağı bu.**
- **Tur 3 kusuru — malzemenin fiziği.** Halat gitti (`--no rope, cord, yarn` +
  `no single thread readable` tuttu), yerine **kumaş kıvrımı** geldi. `--no folds, drapery`
  üç turdur listede, **üç turdur tutmuyor.**

**Ders (korpusa girdi, `30-sosyal.md` Bölüm 16): kumaş yüzey değildir, kumaş düşer.**
MJ bir bez gördüğünde ona ağırlık, kıvrım ve düşüş verir; `--no folds` bunu değiştirmez
çünkü **kıvrım bezin fiziğidir, kelimesi değil.** Kül düşmez, taş duvar düşmez — V01 ve V03
ilk turda bu yüzden tuttu. Sürekli zemin arayan bir karusel için **düşmeyen malzeme** seçilir.
**Prompt korpusa girmedi; ders girdi.**

⚠ **Terk eşiği üç tur olarak, tur 2 sonrası, önceden ilan edilmişti — ve uygulandı.** Bu
turun sessiz kazancı budur: eşik ilan edilmemiş olsaydı dördüncü tur meşru görünürdü.

### Yorgunluk belgesine üç düzeltme

Belge 23 Ağustos'ta **başka bir sohbette** yazıldı. Üç yeri ölçümle ya da canlı korpusla
çelişiyordu; korpusa **düzeltilmiş hâliyle** girdi.

**(a) z03 ile z04 ters sıralanmıştı.** Belge z03'e *"✓ orta"*, z04'e *"✗ söz kartı için değil,
negatif alan kirli"* diyordu. Yedi zeminin **üst yarısındaki** parlak nokta sayımı
(luma>110, bağlı bileşen ≥8px, tam çözünürlüklü kaynak dosya):

```
z01: 0   z07: 0   z04: 18   z05: 22   z06: 23   z02: 108   z03: 299
```

z03'ün metin bölgesinde z04'ün **on altı katı** parlak nokta var — yani belge tam tersini
söylüyordu. **Ama tek metrik iki farklı olguyu aynı sayıya indiriyor:** z03'ünkiler doku
beneği (gözenekli kül), z04'ünkiler serpilmiş kıvılcım; göz ikincisini *leke*, birincisini
*yüzey* olarak okur. Ampirik hakem yalnız bir tarafa düştü — C02'nin altı kartı z03 üstünde
basıldı, en uzunu on iki kelimeydi, **altısında da metin temiz okundu** → **z03 doğrulandı.**
**z04 hiç basılmadı** → ne doğrulandı ne çürütüldü, **B182** açıldı.

**(b) Notasyon ve URL eskimişti.** Belge baştan sona `P1–P9` diyordu; 24 Ağustos'ta
`V01–V09` oldu (KARAR 553 · 555). Belge ayrıca `--sref KURUCU-URL` yer tutucusunu
kullanmayı söylüyordu.

**(c) Öncelik sırası eksikti.** Belge V03 → V05 → V04 → V08 öneriyordu; **V07 listede yoktu**
ama C08 sekizinci gün ve V07 istiyor. İlk iki hafta ihtiyacına göre doğru sıra:
**V03 → V02 → V07 → V05 (Urla) → V08.**

### Kart–zemin atama tablosu (24 Ağustos akşamı)

| Kart | Zemin | Durum |
|---|---|---|
| C01 Lansman | z02 | ✅ |
| C02 Çember Miti (6 crop) | z03 | ✅ |
| C03 İmza (ALTIN) | **z08** | ✅ yeni |
| C04 Reels "bir mum, bir nefes" | gerçek çekim | ⚪ çekim bekliyor |
| C05 Eylül'de iki yol (6 crop) | z04 | ✅ (⚠ **B182** — tek kart testi yapılmadı) |
| C06 Adımız 1/3 (6 crop) | V02 | 🟡 dört aday, HD yok |
| C07 Portre: uyku | **z09** | ✅ yeni |
| C08 Pratik: telefon | V07 | ⚪ üretilmedi |
| C09 Yedi kapı (8 crop) | **V02** | 🟡 KARAR 571 — prompt ailesi seçildi, kare seçilmedi |
| C10 Adımız 2/3 (6 crop) | z05 | ✅ |
| C11 Anadolu evre 1 | V05 · Urla | ⚪ üretilmedi |
| C12 Portre: merak | **z10** | ✅ yeni |
| C13 Felsefe (ALTIN) | z06 | ✅ |
| C17 Portre: ilk dokunuş | **z11** | ✅ yeni |
| C22 Kül kartı | z07 | ✅ |
| C24 Kapanış (6 crop) | V01 rezervi | 🟡 rezerv seçilmedi |

**Kesintisiz yayın: C01–C03 (3 post).** C04 zinciri kırıyor ve MJ işi değil — bir mum, bir
telefon. **Kilidi en çok açan iş V02**: C06 · C09 · C16, üç kart, **yeni draft gerekmiyor**
(dört aday elde, eksik olan HD).

### KARAR KAYITLARI

- **KARAR 568 — KOR İMZA DEĞİL, MOTİFTİR; İMZA ATEŞİN TEK IŞIK KAYNAĞI OLMASIDIR (AKTIF):**
  Görsel yorgunluk riski **söz kartı zemininde değil, feed grid'inin bütününde** yaşar.
  Doğru cevap kor görselini azaltmak değil, **imzayı motiften ayırmak**: ışık sabit kalır
  (tek kaynaklı, ateşten), **aydınlattığı şey değişir** — kapı, çember, mekân, natürmort,
  pencere, bozkır. Kor bu ailenin yalnız **en yakın çekimidir**, ailenin kendisi değil.
  *Kaynak: 23 Ağustos yorgunluk değerlendirmesi, 24 Ağustos ölçümüyle düzeltilmiş hâli.*
  ⚠ **Pratik sonucu 569 ve 570'tir** — bu karar tek başına uygulanabilir bir kural değil,
  ikisinin çerçevesidir.

- **KARAR 569 — ARDIŞIKLIK KURALI DOKU SINIFI ROTASYONUDUR; KABA KURAL İPTAL (AKTIF):**
  Yorgunluk belgesi **iki** ardışıklık kuralı taşıyordu ve ikisi aynı çiftte **zıt cevap
  veriyordu.** Bağlayıcı olan **ince kural**: ardışık kartlarda **doku sınıfı** tekrarı yok
  (mat-kül · glow · partikül · dokulu-kor · duvar). **Kaba kural iptal edildi:**
  *"ardışık iki saf kor-zemin kare yasak."* *Gerekçe: yedi zemin dört ayrı doku sınıfına
  dağılıyor; C01→C02 geçişinde göz mat külden gözenekli kora geçiyor — **bu tekrar değil.**
  Kaba kural uygulanırsa C01 ile C02 arasına motif sokmak gerekir, oysa C02 "Çember Miti"
  C01'in doğrudan devamıdır ve **ayrılamaz.*** ⚠ İki kural bir arada yaşadığı sürece
  uygulayıcı hangisini tuttuğuna göre farklı feed kuruyordu; **çelişkiyi çözen şey ölçüm
  değil, hangisinin ne yasakladığının yazılmasıydı.**

- **KARAR 570 — GRİD ORANI VE MESAFE ROTASYONU (AKTIF):** Kor-zemin söz kartları feed'in
  **en fazla yarısı**; kalanı motifli V03–V09 + gerçek çekim. Üç çekim mesafesi dönüşümlü
  yaşar: **makro** (V01–V02) · **orta** (V04 · V07 · V09) · **geniş** (V05 · V08).
  **Grid testi:** her yayın öncesi **son dokuz kareye topluca** bakılır; altıdan fazlası
  makro kor ise sıradaki post **motifli veya gerçek çekim olmak ZORUNDA.**
  ⚠ Testin değeri tek kareye değil **kümeye** bakmasındadır — yorgunluk kare kare
  bakıldığında hiçbir zaman görünmez.

- **KARAR 571 — C09 ZEMİNİ V02 KÜL DOKUSUDUR (AKTIF):** V04 (mum çemberi) **KARAR 556'yı
  ihlal ediyordu** — sekiz crop, tek odaklı nesne. Ara çözüm olarak V10 (kilim yüzeyi)
  denendi ve terk edildi (yukarıda). C09 **V02'ye** bağlandı. *Gerekçe: kartın kendi başlığı
  "Yedi kapı, **BİR** ateş" — sekiz pencere, tek zemin. V02 zaten üretildi (dört aday),
  kül düşmez, süreklilik sorunu **yapısal olarak yok.** V04 C14 ve C18'de kalır; orada tek
  kart, nesne doğru çalışır.* **B172 bu kararla kapandı.** ⚠ **Kapanan şey prompt ailesi
  seçimidir** — somut V02 karesi (dört adaydan hangisi, HD var mı) hâlâ seçilmedi ve o iş
  C06'nınkiyle aynı iştir. ⚠ **B173 (C20, altı crop, V06) emsal aldı ama karar almadı:**
  C09'un gerekçesi kartın kendi başlığıydı, C20'nin başlığı aynı argümanı vermiyor.

- **KARAR 572 — ÖLÇÜT EŞİKLERİ ÖLÇEK-BAĞIMSIZ TANIMLANIR (AKTIF):** `isikKaynagi` (parlak
  bağlı bileşen sayısı) **küçük ızgara tile'ında kalibre edilmişti**; aynı kare dört kat
  büyük önizlemede **1–6 yerine 13–42** döndürüyor. **Kare değişmedi, ölçek değişti.**
  Piksel-*sayısı* eşikleri kare boyutuna oranlanmadan kullanılmaz; oran ve medyan döndüren
  ölçütler (`ustKaranlik`, ton) bu tuzağa düşmez. *KARAR 470'in — her sayı yöntemini taşır —
  uzantısı: **yöntem ölçeği de içerir.*** Korpus ayağı `31-zemin.md` Tanımlar bloğuna düştü.

### YENİ NUMARA AÇILMAYAN ADAYLAR — mükerrerlik denetimi

Patch numarasız geldi (KARAR 479'un sonucu: diğer turların commit'leri push'lanmamıştı,
MCP bayat snapshot servis ediyordu). Numaralar **canlı ledger'a karşı** atandı ve **altı
karar adayının biri mükerrer çıktı:**

- ⚠ **[K-b] *"Karusel zemini yüzey olur, nesne olmaz"* = KARAR 556.** Aynı gün, başka bir
  oturumda, **birebir aynı başlıkla** mühürlenmişti; metni `30-sosyal.md` Bölüm 16'da zaten
  yaşıyor, C02/z03 ampirik dayanağı dahil. **Yeni numara açılmadı.** Patch'in getirdiği tek
  yeni malzeme **karşıt dayanaktı** (V10 kilim, üç turda %107–110 süreklilik sapması) — o
  da 556'nın kaydına değil, bu turun V10 bölümüne ve Bölüm 16'ya yazıldı.
- **Bu, B181'in tarif ettiği çakışmanın ikinci vakasıdır.** Birincisi *"Çember Rehberliği"*
  ↔ *"Çember Rehberi"* idi. ⚠ **İkisi de zararsız atlatıldı çünkü numara atama anında
  ledger okundu** — ama ikisi de **aynı sınıftan** ve kural hâlâ yazılmadı (B181 açık).
- Borç tarafında da üç aday mükerrer çıktı: **[B-d]** = **B169** (`kartAdi()` şema hizası) ·
  **[B-e]** = **B173** (C20 karusel ihlali) · **[B-f]** = **B170** (zemin yedeği). Üçüne de
  yeni numara açılmadı; mevcut kalemler **güncellendi.** **[B-c]** = **B180**'in
  cevaplanmamış sorusuydu (aşağıda).

### ÜÇ ÇELİŞKİ — patch dosyanın gerçeğiyle tutmadı (CLAUDE.md §2)

- ⚠ **Patch §3 *"Parametreler değişmedi"* dedi; dosyada başka bir kuyruk var.** Patch'in
  ölçüm kuyruğu `--ar 4:5 --style raw --v 8.1 --stylize 130 --chaos 5 --sref <kurucu> --sw 60`
  + on beş öğeli `--no` listesi. `30-sosyal.md:779`'un gerçeği:
  `--ar 4:5 --style raw --stylize 130 --sref KURUCU-URL --sw 60 --no people, faces, hands, text, watermark`.
  **`--v 8.1` yok · `--chaos` yok · `--no` listesi beş öğeli · `--sref` yer tutucu.**
- ⚠ **Patch §6(b) *"B139 ile dokuz promptun da URL'i dolduruldu"* dedi; üç yüzey tersini
  söylüyor.** Ölçüm (`grep -c`, 24 Ağu): `KURUCU-URL` **12** · `--v 8.1` **2** · `--chaos` **3**.
  **B139 açık** ve kendi gövdesi *"Bölüm 16'nın V01–V09 tam set değişimi (ADIM 3d) bu borçtan
  **ayrıdır**"* diyor. `00-durum.md` YAYINI KİLİTLEYENLER 2. maddesi de *"kalan iş
  `KURUCU-URL` ara-değiştir"* diyor. **Patch yanlış.**
- **Ne yapıldı:** gövde **ölçüldüğü hâliyle yazıldı** (çapa dosyaya birebir tuttu), kuyruğa
  **dokunulmadı**, ve Bölüm 16'ya durumu iki kaynağıyla birlikte söyleyen bir `⚠ TEYITSIZ`
  notu düşüldü. **Uzlaştırma yapılmadı** — iki kaynak çeliştiğinde CC kendi başına
  uzlaştırmaz. Kalem **B184** olarak açıldı; ADIM 3d koşulunca not kalkar.
- ⚠ **Üçüncü, küçük çelişki:** patch §4 [K-e] *"C09 V02'ye bağlandı"* derken §7 tablosu aynı
  satıra *"zemin seçilmedi"* yazıyordu. **Uzlaştı:** seçilen **prompt ailesi**, seçilmeyen
  **somut kare.** KARAR 571 ve B172'nin kapanışı bu ayrımı adıyla taşıyor.

### `tools/ocak-kart-derleyici.html` — B180'in üç sorusundan ikisi kapandı

Patch §8 [B-c] derleyicinin üç düzeltmesini adıyla sayıyordu; bu, **B180'in
*"kim yazdı"*** sorusunun cevabıydı. Ölçümle doğrulandı:

- **Kim yazdı: 23–24 Ağustos MJ görsel oturumu (claude.ai).** `grep -c`, çalışma ağacı:
  `kapakIstisna` **5** · `KAPAK_ALAN` **2** · `kaynakOlcu` **2** · zoom `1.62` **2** ·
  `1.76` **1**. HEAD kopyasında **beşi de 0.** Üç düzeltme: `dagit` crop dağıtımı
  (`oy` negatife inmiyor, gezinme payı yatayda %18→%57 dikeyde %7→%23, kaynak 1968px'te
  büyütmesiz tavan 1.82) · **Kart 1 kapak olsun** onay kutusu (`ciz()` ince sarmalayıcıya
  döndü, **çizim kodu değişmedi**) · ölçü şeridinde `KAYNAK en × boy` + büyütme uyarısı.
- **Hangisi gerçek: `ocak-kart-derleyici.html`.** `ocak-kart-derleyici 2.html` **HEAD ile
  birebir aynıydı** (53.825 bayt, `diff` farksız) — commit'li sürümün macOS indirme dubleti,
  **sıfır bilgi.** Silindi; geri gerekirse `git show HEAD:…` bir komutla üretir.
- ⏳ **Kalan tek soru: commit'lenecek mi.** **Bu turda commit'lenmedi** — MJ patch'i doküman
  işidir, `tools/` kod dosyasıdır; ayrı konu = ayrı commit (CLAUDE.md §6). ⚠ **Değişiklik
  hâlâ korumasız:** `47 ekleme, 5 silme`, hiçbir commit'te değil, **tarayıcıda test edilmedi**
  (`node --check` temiz). O commit turu **B169**'u (şema hizası) ve **B143**'ü (font gömme)
  aynı anda kapatabilir — üçü aynı dosya, tek doğrulama.

### AÇILAN BORÇLAR

**B182** (z04 tek kart testi yapılmadı — C05 basılmadan) · **B183** (V03 gövdesi hâlâ figür
üretiyor; `invitation without a figure` → `empty threshold, no one present`) ·
**B184** (Bölüm 16 parametre kuyruğu ölçülen prompttan sapıyor, `KURUCU-URL` 12 yerde).

**GÜNCELLENEN BORÇLAR:** B169 (üç düzeltme `kartAdi()`'yi kapsamadı) · B170 (yedeksiz zemin
**7 → 11**) · B173 (emsal doğdu, karar doğmadı) · B180 (üç sorudan ikisi kapandı).
**KAPANAN:** **B172** ✅ (KARAR 571).
